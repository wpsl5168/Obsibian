---
title: "写作与排版规范"
date: 2026-04-08
tags: [规范, 写作, 知识库]
---

# 写作与排版规范 (Style Guide)

为了对标 Google AI、Anthropic (Claude) 等官方文档的专业水准，本知识库的所有内容生成必须严格遵循以下 Markdown 排版规范。小贝 (second-secretary) 在生成内容时需将此作为系统指令的一部分。

## 1. 结构与层级 (Hierarchy)
- **H1 (`#`)**：仅用于文章总标题，需与 Frontmatter 的 `title` 保持一致。
- **H2 (`##`)**：用于核心大节（如：核心概念、原理解析、对比分析等），前后必须留空行。
- **H3 (`###`)**：用于模块拆解。
- 尽量控制在 H3，避免过度嵌套（不得超过 H4）。

## 2. 核心概念与提示块 (Callouts / Admonitions)
借用 Obsidian 的 Callout 语法，使版面具有“官方文档”的视觉层次感：
- **定义/概念**：
  ```markdown
  > [!note] 核心定义：Agent (智能体)
  > 智能体是一个具备感知环境、自主决策并执行动作以达成特定目标的 AI 系统。
  ```
- **关键提示/最佳实践**：
  ```markdown
  > [!tip] 最佳实践
  > 在构建 RAG 时，优先使用 Hybrid Search（混合检索）而非单一的向量检索。
  ```
- **警告/避坑**：
  ```markdown
  > [!warning] 性能陷阱
  > 避免在 LLM 输出强绑定结构化 JSON 时使用过小的 Temperature，这可能导致推理能力下降。
  ```

## 3. 对比与数据呈现 (Tables)
在进行架构对比（如 Dense vs MoE）、产品对比（如 Cursor vs Windsurf）时，**必须使用格式对齐的 Markdown 表格**，要求：
- 表头清晰。
- 优缺点分明，必要时在单元格内使用 `✅` 和 `❌` 标识。

## 4. 代码与技术细节 (Code Blocks)
- 所有代码块必须标注**语言高亮标签**（如 `python`, `json`, `csharp`, `tsql`）。
- 关键概念、API 字段、类名在正文中必须使用包裹的 `inline code`（如：调用 `tools` 数组）。

## 5. 字体与阅读节奏 (Typography & Spacing)
- **加粗 (`**text**`)**：仅用于核心技术术语的首次出现，或段落的引导语，切忌大面积加粗。
- **中英文排版**：中英文之间、中文与数字之间**必须保留一个半角空格**（如：使用 MCP 协议接入 API）。
- **段落留白**：不同逻辑段落、列表、代码块之间，必须保留一个完整的空行。

## 6. 关联与导航 (Links)
- 强制要求使用 Obsidian 双向链接 `[[页面名称]]` 将相关概念串联。
- 文末统一设置 `## 关联阅读` 章节，提供拓扑导航。

