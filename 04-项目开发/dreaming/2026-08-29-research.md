# Dream B 日审｜2026-08-29

## 结论

- 审查：`components/FreeBuildTray.js`（第五轮 day 2）。目标文件与 `origin/main` 一致；ESLint 通过。
- 新发现：Technic 分类把“连接件/轴1×2”映射到普通砖 `1x1/1x2`，点击后实际放置普通 stud brick；`PART_DEFS` 已存在 `tech1x*` 与 `axle2`，属于产品语义断路，归 BUG-P1，仅报告。
- 自动修复：0。触控 32px、`scrollbar-hide` 死类、分类按钮缺 `aria-pressed` 均已有 OPEN PR #44 涉及同文件，按冲突闸不开新 PR；其中 #44 已覆盖前两项但未覆盖选中语义。
- 测试：`__tests__/freeBuildTray.test.js` 10/10 FAIL，仍是旧 emoji/旧分类/旧色值且未先打开抽屉，测试破产；没有新增失败。
- 队列：Dream B OPEN 6，低于熔断线 8；最近一次 Dream B merge 为 2026-07-02，已 58 天，PROC-P1 持续。

## 已修复

| ID | 级别 | 结果 |
|---|---|---|
| — | — | 无；P2 被同文件 OPEN PR #44 拦截 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 结论 / 建议 |
|---|---|---|---|
| DEP-1 | P0 排期 | `next@14.2.35` | 官方仅为 15.5.24 / 16.3.3 提供 08-25 Critical 修复；14.x 无修复线且落后两代。确定迁移周，优先评估 16.3.3 |
| BUG-1 | P1 新增 | `FreeBuildTray.js:88-91` | “连接件”→`1x1`、“轴1×2”→`1x2`，实为普通砖；需求需拍板映射为 `tech1x*` / `axle2`，并确认 renderer 是否要表现 Technic 孔/轴几何 |
| A11Y-1 | P1 延续 | `FreeBuildTray.js:172-208` | FAB 在抽屉打开时仅 scale/opacity/pointer-events 隐藏，抽屉关闭时内部按钮仅 translateY 隐藏；两边都仍留 tab order。拍板用 `inert` + `aria-hidden` 或条件渲染 |
| TEST-1 | P1 延续 | `__tests__/freeBuildTray.test.js` | 10/10 FAIL；重写为真实“打开→切分类→选颜色/零件→关闭”流程，使用 role/aria 选择器，并断言 closed 后不可聚焦 |
| CSS-1 | P1 延续 | `FreeBuildTray.js:225,263` | `scrollbar-hide` plugin 未装且全局无兜底；PR #44 已用原生 scrollbar style 修复，待合并 |
| PROC-1 | P1 | PR #43–#48 | 6 条 OPEN；最近 merge 已 58 天。先 batch review/merge/close |
| PR-COLL-1 | P1 | PR #44 | 同文件，`CLEAN/MERGEABLE`；阻断触控/CSS/a11y 后续修复，建议先合并或关闭后统一收口 |

## 可自动修但被闸拦截

| ID | 级别 | 位置 | 结果 |
|---|---|---|---|
| A11Y-2 | P2 | `FreeBuildTray.js:227-244` | 分类选中仅靠颜色/缩放，缺 `aria-pressed={activeCategory === idx}`；同文件 PR #44 存在，不新开 PR |
| TOUCH-1 | P2 | `FreeBuildTray.js:266-283` | 颜色按钮 32×32，低于 44px 最低线；PR #44 已改 44×44，待合并 |
| TOUCH-2 | P2 | `FreeBuildTray.js:248-256` | 关闭按钮 44×44，仅达最低线；PR #44 已改 56×56，待合并 |

## 回滚

| 文件 | 结果 |
|---|---|
| — | 无代码改动，无回滚 |

## 验证链

- 活代码：`pages/sandbox.js` import 并渲染 `FreeBuildTray`。
- 语义探针：`PART_DEFS` 同时存在普通 `1x1/1x2`、Technic `tech1x1/tech1x2` 与 `axle2`；当前 UI 只返回前两者。
- ESLint：PASS。
- Jest：10/10 FAIL（baseline 破产；非本次引入）。
- PR #44：`CLEAN/MERGEABLE`，修改同文件并覆盖触控/CSS 两项。

## 依赖动态

| 依赖 | 当前 | 上游 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.3 Active LTS / 15.5.24 Maintenance LTS | P0 迁移排期；08-25 Critical 修复无 14.x 线 |
| React / R3F | 18.3.1 / 8.18.0 | R3F 9.7.0（07-31） | v9 含 reconciler 修复，但需随 React 19 / Next 升级联合评估；无单独抢跑 |
| Three.js | r183 | 本次未发现新增 breaking 证据 | 3D 升级仍须 Visual Check |
| SWC native | 14.2.33 | 跟随 Next 14 主版本 | 主版本一致，无 binding 撕裂 |
| LDraw Parts | 项目本地库 | 官方 2026-07 有新 parts preview；beta 更新页最新列 2026-06（83 新 parts） | 无 breaking；素材继续只取官方库 |

### Next.js CVE 命中面复核

- Middleware / proxy：0；App Router：0；Cache Components / dynamicIO：0；Server Actions / RSC：0。
- `next/image`：3 个文件。两处远端 CDN 图片明确 `unoptimized`；唯一启用 optimizer 的首页缩略图是本地固定 `.png`。
- AVIF RCE `GHSA-2xp9-vwfh-vxw4`：版本命中；现有 JSX 未见攻击者可控 AVIF 进入 optimizer。`remotePatterns` 仍按 host 放行，直接构造 `/_next/image` 的可达性取决于固定 CDN 是否允许攻击者托管 AVIF，未证实；不能写成已可利用，也不能当无风险。
- Windows RCE `CVE-2026-75604`：当前 Linux、无 App Router，不适用。
- July 2026 批次：7 类探查仍为 0 个已证实命中。

## 竞品动态

| 对象 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio / Mecabricks | 本次未发现可信的新 breaking / 架构变化 | 不上报噪音 |
| LDraw.org | 2026-07 新 parts preview 持续更新 | 仅作为官方素材增量观察 |

## 信息源

- Next.js 官方 August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- GitHub Advisory GHSA-2xp9-vwfh-vxw4：https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4
- React Three Fiber v9.7.0：https://github.com/pmndrs/react-three-fiber/releases/tag/v9.7.0
- LDraw.org Library Updates：https://beta.ldraw.org/updates
- LDraw 2026-07 Preview：https://library.ldraw.org/updates/view2607
- 本地实测：ESLint、Jest、`PART_DEFS` 探针、GitHub PR API、Next CVE 命中面检索（2026-08-29）
