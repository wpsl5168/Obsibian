# Dream B 审查 2026-06-12 — lib/llmPrompt.js

## 元信息
- 轮换：今日 `lib/llmPrompt.js`（上次 2026-06-03）
- 文件规模：898 行，42.8 KB
- 角色：定义两套 LLM system prompt（v6 文本版 + DSL JSON 版）+ 重试错误回喂消息
- 修改分支：**未创建**（0 条 P2/P3 修复）
- PR：—

## 4 维度扫描

### Bug 隐患
- ❌ 初看疑似 line 122 `\`;` 提前闭合模板字符串、126-399 行成"裸 markdown"
- ✅ **复核结果**：源文件 line 123 实际是 `\`+`;`（反引号被反斜杠转义），模板字符串从 line 11 一直延伸到 line 399 才真正闭合（`buildSystemPrompt` 返回 18,873 字符，包含 MINIFIGURE PARTS / RED SPORTS CAR 等所有示例）。`node -e import(...)` 通过，`npx eslint` + `npx next lint --file lib/llmPrompt.js` 双重 0 warning
- ✅ 结论：误判，无 bug

### 架构
- 双 prompt 体系（buildSystemPrompt v6 文本 + buildDSLSystemPrompt v1 JSON）共存合理，给上游 pipeline 留切换余地
- 重试消息按错误类型分桶（unsupportedParts / formatErrors / matrixErrors / collisionErrors / floatErrors）→ 精细回喂，符合 LLM 自纠最佳实践
- 零件库表格、坐标系说明、参考样例在 prompt 内嵌入完整 → token 占用偏大（~19K chars ≈ 4-5K tokens），但属业务取舍非缺陷

### UI 规范 / 触控
- N/A：纯字符串构造文件，无 React 组件、无 DOM、无样式

### 测试覆盖
- 仓库无 `__tests__/llmPrompt.test.js` 或类似
- prompt 字符串属"软资产"，传统单元测试价值低；但当前缺少"prompt 体积上限断言 + 关键关键词存在断言"等回归守卫
- **不自动建测试**：测试策略涉及业务判断，仅记录（P1 待确认项）

## 已修复表
| # | 文件 | 级别 | 改动 | lint | jest |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

（今日 0 条自动修复）

## 待确认表（P0/P1，需老王决策）
| # | 级别 | 位置 | 现象 | 建议 |
|---|---|---|---|---|
| 1 | P1 | lib/llmPrompt.js 整体 | 无 prompt 回归测试。任意人改动可能让总长度爆/关键章节丢失，下游 LLM 行为静默漂移 | 加 `__tests__/llmPrompt.test.js`：断言长度 ∈ [15k,25k]、必含 "ANTI-FLOAT"/"OFFICIAL COLOR CODES"/"<LEGO>" 标签 |
| 2 | P1 | 117 行 | 文本版仅列 9 个颜色码（0/1/2/4/6/7/14/15/43），DSL 版列 16 个；两套 prompt 颜色覆盖不一致，会导致用同 LLM 不同分支时调色板缩水 | 将文本版色码扩到 DSL 版同样 16 个 |

## 回滚表
（无）

## 依赖动态
- three.js：r178 已发布（2026），重点是 WebGPU 工作流；本地 `three ^0.183.2` 已含。无紧迫升级压力
- @react-three/fiber：v9 稳定（GitHub #3471 显示 9.0.2 + Next 15.1.7 早期有兼容问题）；本地 9.122 OK，**升 Next 15+ 时务必同步验证 R3F**
- Next.js：14.2 → 15/16 路径明确（16.2 已发布 2026-06，主推 Turbopack + codemod 化迁移）。当前 14 仍受支持，无 breaking 紧急

## 竞品动态
- BrickLink Studio：2026-05-27 又一次小版本，仅 bug fix（part designer 贴花线条 / 提交流程边缘 bug）。无新功能威胁
- LDraw 官方库：2026-04 更新 +188 文件（99 新零件），2026-05 更新已发布；BrickHub `lib/ldrawParser.js` 应定期对照 `library.ldraw.org/updates` 抓增量 zip

## 信息源
- https://80.lv/articles/three-js-178-is-now-available
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- https://releasebot.io/updates/vercel/next-js
- https://www.bricklink.com/v3/studio/download.page
- https://library.ldraw.org/updates?latest=

## 自检
- 搜索次数：5/6
- 红线：未碰素材、未改 3D 渲染、未碰 UI
- lint+test 守卫：今日 0 改动 → 无需触发
