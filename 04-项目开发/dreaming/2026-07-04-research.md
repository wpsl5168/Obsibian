# Dream B · BrickHub 每日审查 · 2026-07-04

**审查文件**:`components/BrickRenderer.js` (1130 LOC / 44 KB / 3D 核心 + LDraw pipeline)
**队列**:OPEN=0（老王 07-03 已批量清账,ad44ca4/32d838b/fe0ae5c 三合一整合合入,rotation 07-03 已重置）
**耗时基线**:模型加载走 IDB cache + partsIndexDict + material patch,大模型 (≥400 mesh) 自动降级 shadow

## 一、已修复(PR #43,守卫全过)

| 类别 | 项目 | 位置 | 死因/清理逻辑 |
|---|---|---|---|
| P3 死常量 | `SCENE_BG = '#FFF5E6'` | 行 108 | 从未在 JSX 引用;Canvas 用 `background: 'transparent'`,底色由外层 CSS 提供 |
| P2 死函数姐妹批 | `looksLikeHtmlDocument` | 行 126-128 | HTML 侦测已 inline 至 `FileLoader.load` 网络回调(行 ~322)和 IDB 回读处(行 ~346)两处 `.test()` |
| P2 死函数姐妹批 | `normalizeDatRefPath` | 行 130-160 | 早期候选路径生成器,现走 `partsIndexDict`(`/ldraw/parts-index.json`)字典查询 |
| P2 死函数姐妹批 | `shouldPreferPrimitiveDir` | 行 162-169 | primitive 目录偏好判定,同批废弃 |

**姐妹批考古**:3 个死函数全部在 commit `43db9a34 fix(renderer): resolve LDrawLoader crash` 引入(2026-04 Phase 2 crash 修复期),后续被 inline + 全局 `missingPartUrlCache` (Set 白名单)取代。零内外引用(pages/components/lib/__tests__ 全 0)。

**净变化**:1 file, +4 / -45 LOC(注释保留死因说明防止再引入)。

**守卫**:eslint 干净 / jest LDrawRenderer 3 files (test/extended/report) 全过 / 无外部 import 命中。

## 二、待老王确认(不动手,P0/P1)

| ID | 级别 | 位置 | 现象 | 建议 |
|---|---|---|---|---|
| B1 | P1 逻辑 | 行 727 / 842 | `const localMissing = []` **从未 push**,导致 `setMissingParts(Array.from(new Set(localMissing)))` 永远拿到空 Set,UI 上"当次加载缺失清单"完全失效 | 是否恢复?若恢复需在 catch 路径(行 340-345 FileLoader err)推数据,或干脆删除 localMissing + 相关 setMissingParts UI |
| B2 | P1 内存 | 行 839 | `window._brickhubDebugGroup = groupResult.result` 每次 render 都覆盖赋值到 window,LDraw 大模型 group tree 永久驻留(GC 拿不到),多次切模型必泄漏 | 加 `process.env.NODE_ENV === 'development'` 门禁,或删除(生产从不需要 window 调试引用) |
| B3 | P1 测试 | AutoFit / SandboxAnimator / PerfProbe / highlight 材质缓存 | 4 个 hook 零 React render 测试;`__tests__/LDrawRenderer.*.test.js` 仅覆盖 `parseLDraw`(纯函数),3D 组件行为无回归网 | 补 `@testing-library/react` + `@react-three/fiber` test-utils 单测,重点 hint 动画/高亮清理路径 |

**说明**:B1/B2 涉及业务判断(是否保留 debug hook / UI 展示策略),Dream B 硬红线不改;B3 是测试排期,老王决定优先级。

## 三、审查未见异常(negative results,报表健康度)

- ✅ SandboxAnimator (行 386-458) 07-03 刚补的 cleanup useEffect (`return () => { group.traverse(reset userData) }`) 逻辑正确,unmount 会清 origPosY/targetPos/animating/alreadyPlaced 等所有 userData 标记,无泄漏
- ✅ AutoFit 3-frame 包围球稳定后再 fit 的策略(行 24-42)合理防御异步流式加载
- ✅ 大模型自动降级:`HEAVY_LOADER_MESHES=400` / `HEAVY_PROCEDURAL_BRICKS=80` 阈值触发关阴影(行 627-631)、`dpr=[1,1]`(行 1017),行为一致
- ✅ material patch (行 279-295) 兜底 `getMaterial` 缺 code → 灰色 fallback 防原生 crash;IDB HTML 错误页拦截(行 353-357)避免污染缓存
- ✅ LDrawLoader `parse.length >= 3` 兼容两版签名(行 731-736),LOADER_SCALE 1/20 + Math.PI X 轴翻转 + box.min.y 贴地一整套 fix 保留
- ✅ 触控:`touchAction:'none'` + `TOUCH.ROTATE/DOLLY_PAN` 手势映射到位

## 四、依赖动态(≤6 搜索,已用 5)

| 依赖 | 当前 | 上游状态 | 影响 |
|---|---|---|---|
| **next** | 14.2.35 | **16.2.6 stable** (2026-05-07 GA);14.x 早已 EOL,持续无 patch | **P1 强提醒** — 建议排期升 16.x(Turbopack 默认 / React 19.2 / Cache Components / middleware.ts→proxy.ts)。**CVE 复盘**:07-02 已核实 CVE-2026-44575 (middleware auth bypass) + CVE-2026-44578 (SSRF) 均**不适用 brickhub**(Pages Router,`find -name middleware.*` 无命中);CVE-2025-29927 14.2.25 已修。SSRF/EOL 仍应排期升级,**非紧急**。 |
| **@next/swc-linux-x64-gnu** | pinned `14.2.33` (03-15 已修 W27 撕裂,commit 4d4f70d) | next optional dep,当前匹配 | ✅ 已锁 patch 号,binding 不会 fallback WASM |
| **three** | 未在本次逐项扫描 | **r170 LDrawLoader material 初始化 regression** 已在社区确认 | ✅ 本仓库已 `__brickhubMaterialPatched` 兜底(行 279-295),patch 依然对号入座,无需动作 |
| **@react-three/fiber** | 未逐项扫描 | **v9 有 breaking**:`useLoader` API 变化 + StrictMode 双执行副作用暴露 | ⚠️ 未来升 R3F v9 前需评估 useLoader 用法(本仓库主要走 LDrawLoader.parse,非 useLoader,风险中低) |

**无紧急 breaking / 无新增高危 CVE**。

## 五、竞品动态

| 项目 | 动作 | 对 BrickHub 影响 |
|---|---|---|
| **LDraw 官方 Parts Library** | **2026-06 update 已发布**(+123 files,含 83 parts + 1 primitive) | **P2 提醒** — 本仓库 `public/ldraw/parts/` 可能落后一版,建议老王同步官方最新素材库(尤其"新出品套装"零件),避免 AI 生成模型引用新零件 ID 时缺料回退 |
| **Bricklink Studio** | 无重大新版本(仍以桌面 LDraw exporter 为主) | 无影响 |
| **Mecabricks** | Web 端渲染优化持续,photoreal render 预览 | 参考竞品:BrickHub 走"儿童友好 + 语音 + AI 生成"差异化,方向不冲突 |
| **社区 R3F LEGO** | `bhushan6/lego-builder` 小型开源 R3F 积木编辑器(实时协作) | 观察项,规模远小于 BrickHub,无迁移压力 |

## 六、拍板清单(老王 review 顺序)

1. **PR #43** — 死代码清理,可直接 merge(全绿,无风险)
2. **B1 (localMissing 断路)** — 决定"当次加载缺失清单 UI"是否恢复;若不恢复,下轮 Dream B 追加 P2 删除该状态位
3. **B2 (window._brickhubDebugGroup 泄漏)** — 决定加 NODE_ENV 门禁还是干脆删
4. **依赖 P1 强提醒** — Next 14→16 升级排期窗口(Q3 或 Q4);同批评估 R3F v9 迁移(次要)
5. **LDraw 2026-06 Parts Update** — 排期同步 `public/ldraw/parts/`(自动化脚本已存在?否则半日工)
6. **B3 (3D 组件测试)** — 补 hook 单测优先级,可延后

## 七、信息源

- Next.js 15/16 迁移指南 · jishulabs.com/blog/nextjs-15-16-features-migration-guide-2026
- Next.js 16.2.6 stable release notes · tech-insider.org/nextjs-tutorial-app-router-13-steps-2026
- three.js r170 LDrawLoader regression · discourse.threejs.org/t/75535
- @react-three/fiber v9 migration guide · r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 2026-06 Parts Update · forums.ldraw.org/portal.php
- Mecabricks · mecabricks.com

---
🤖 Dream B (auto) · 2026-07-04 · 轮换后指向 `components/FreeBuildTray.js`(下次候选)
