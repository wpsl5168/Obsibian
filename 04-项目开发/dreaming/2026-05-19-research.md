---
title: "BrickHub Dream B 2026-05-19"
date: 2026-05-19
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-19

## 今日审查：components/home/LDrawModal.js (61 LOC)

选取理由：rotation.json 中**未审过**的核心组件（候选池里仅剩 LDrawModal / LDrawEditorPanel / FloatingControls / MenuPanel / HistoryPanel / KnowledgePreviewPanel / PromptDock 七个 `components/home/` 文件未轮过）。LDrawModal 是 modal/弹窗组件，依 pitfall #28 是 a11y 高产挖矿点；选它做今日审查。

### ✅ 已自动修复 → [PR #24](https://github.com/wpsl5168/brickhub/pull/24)

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | ✕ 关闭按钮 `h-8 w-8` (32px) 违反 44px 最低触控 | L25 | `h-11 w-11` | P2 |
| 2 | 取消/渲染按钮 `py-2.5` 内容小时不足 44px | L42, L53 | `min-h-11` 兜底 | P2 |
| 3 | 缺 `role="dialog"` / `aria-modal` / `aria-labelledby` | L17-19 | 加上，`<h2>` 配 `id` | P2 |
| 4 | ✕ 图标按钮缺 `aria-label`（屏幕阅读器只读到字符"✕"） | L23 | `aria-label="关闭"` | P2 |
| 5 | `setTimeout(focus, 50)` 在 useEffect 中无清理 | L7-11 | 返回 `clearTimeout` | P3 |
| 6 | 按钮缺 `type="button"` | L23, L40, L46 | 加 `type="button"` 防表单提交副作用 | P3 |

守卫：`npx eslint` ✅ pass。该组件无对应测试。

### 🔴 待人工确认（P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | **死组件 / 未接线** | `components/home/LDrawModal.js`、`components/home/LDrawEditorPanel.js` | 全仓 grep `LDrawModal\|LDrawEditorPanel` 仅匹配定义自身，0 个挂载点；用户根本看不到这两个组件 | (a) 接线到 home 流程（手动输入 LDraw 入口），或 (b) 删除以减少维护负担。pitfall #29 模式 | P1 |
| 2 | **a11y 三件套缺两件**（pitfall #28） | LDrawModal.js | 弹窗无 Escape 关闭、无 backdrop 点击关闭、无焦点恢复 | 接线时一并补：(a) `useEffect` 监听 keydown→Escape; (b) 最外层 div 加 `onClick={onClose}`（内层已 `e.stopPropagation()` 就位）; (c) 关闭时把焦点还给触发元素 | P1 |

涉及交互行为变更与产品决策（接 vs 删），留 PR review 决定。

### ⚠️ 自动修复回滚

无。

## 依赖动态

| 依赖 | 当前 | 最新主要里程碑 | 重要变更 |
|------|------|---------------|---------|
| three | 0.183.2 | 已进入 WebGPU 主线（2026 大动作） | WebGPURenderer 已稳定，但 r170+ 主要受益方是 R3F v9。无强制 0.183→latest 的破坏性 |
| @react-three/fiber | 8.18.0 | **v9** 已发布（"A New Era"） | v9 同时支持 WebGL + WebGPU + 新调度器；升级需 React 19 + Three latest，**非紧急但 v8 将停止接收新特性** |
| @react-three/drei | 9.122.0 | 与 R3F v9 配套 v10 即将到来 | 与 R3F 升级同步，单独动 drei 风险高 |
| next | 14.2.0 | 15 已稳定一段时间 | App Router 用户大都平滑；BrickHub 用 Pages Router，升级前需评估 caching 行为变更 |
| react | 18.3.0 | 19 已 GA | 与 R3F v9 升级捆绑 |

**建议节奏**：暂不动 deps。R3F v8 → v9 + React 18 → 19 是连锁，需要专门 spike，不适合 dreaming 顺手做。Next 14 → 15 风险更可控，但收益不紧急。

## 竞品动态

今日 quota 已用于核心依赖扫描，竞品（Studio / Mecabricks）跳过 — 近三周已多次报告无重大变动。

## 信息来源

- [What's New in Three.js (2026)](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [Next.js 15 release notes](https://nextjs.org/blog/next-15)
- [R3F Releases — "A New Era"](https://github.com/pmndrs/react-three-fiber/releases)
- 修复 PR: https://github.com/wpsl5168/brickhub/pull/24

## 建议行动（供老王确认）

- [ ] Merge PR #24（6 项 a11y / 触控 / 清理修复，纯 P2/P3，无功能变更）
- [ ] 决定 LDrawModal & LDrawEditorPanel 命运：接线到 home 还是删除？两个文件 111 LOC 死代码积累中
- [ ] 长期 backlog：评估 R3F v8→v9 + React 18→19 升级窗口（不在 dreaming 范畴）
