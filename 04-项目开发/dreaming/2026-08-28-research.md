# Dream B 日审｜2026-08-28

## 结论

- 审查：`components/BrickRenderer.js`（上一轮 11 个核心文件已于 08-27 完成，今日重置轮换）。
- 代码：ESLint 通过；3 个 LDrawRenderer 测试通过，但仅覆盖转导出的 parser，不覆盖 React/R3F 渲染、缓存与动画链。今日无新增 P0/P1/P2/P3；未修改代码、未创建 PR。
- 队列：Dream B OPEN 6（#43–#48），低于熔断线 8；#43/#44 均命中本文件且 `CLEAN/MERGEABLE`，即使出现 P2 也应由同文件冲突闸拦截。最近一次 Dream B merge 为 2026-07-02，已超过 56 天，流程持续梗死。
- 依赖：`next@14.2.35` 仍落后官方 Active LTS `16.3.3` 两个大版本；08-25 官方 Critical 修复已发布。AVIF RCE 为“版本命中 + `next/image` 功能命中，但项目未启用 AVIF、攻击者可控输入未证实”；Windows RCE 对当前 Linux + 无 App Router 不适用。

## 已修复

| ID | 级别 | 结果 |
|---|---|---|
| — | — | 无；没有安全可自动修的 P2/P3 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 结论 / 建议 |
|---|---|---|---|
| DEP-1 | P0 排期 | `next@14.2.35` | 官方已发 `15.5.24` / `16.3.3` 修复两个 Critical；14.x 无修复线且落后两代。确定迁移周，优先评估直升 16.3.3；若先止血则升 15.5.24 |
| PROC-1 | P1 | PR #43–#48 | 6 条 OPEN，最近 Dream B merge 2026-07-02；全部 CLEAN。先 batch review/merge/close，避免下一轮继续被同文件 PR 拦截 |
| CACHE-1 | P1 延续 | `BrickRenderer.js:709-722` | cache hit 直接 return，未调用 `onRendered`；模型详情页依赖该回调解除透明态。需补重复打开同模型回归测试，修复须 3D Visual Check |
| CACHE-2 | P1 延续 | `BrickRenderer.js:215-229,1059` | LRU 复用同一 `Object3D`，多 Canvas 会抢 parent；淘汰也未 dispose。需改成资源共享、场景节点独立的缓存策略 |
| BUG-1 | P1 延续 | `BrickRenderer.js:393-394` | `if (!origPosY)` 把合法 0 当未初始化，悬浮基线漂移；改为 `=== undefined` 并做 3D Visual Check |
| BUG-2 | P1 延续 | `BrickRenderer.js:727,842` | `localMissing=[]` 零 mutation 却被消费，缺失零件清单永远为空；拍板恢复采集点或删除状态/UI 链 |
| TEST-1 | P1 延续 | `__tests__/LDrawRenderer*.test.js` | 3/3 PASS 只测 parser 转导；渲染/cache/动画无真实守卫，其中历史 report/extended 测试缺有效断言 |

> 今日代码面无新增问题；以上均为未落地的既有风险，避免重复制造 attention 噪音。

## 回滚

| 文件 | 结果 |
|---|---|
| — | 无代码改动，无回滚 |

## 依赖动态

| 依赖 | 当前 | 上游 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.3 Active LTS / 15.5.24 Maintenance LTS | P0 迁移排期；08-25 Critical 修复不提供 14.x 线 |
| React / R3F | 18.3.1 / 8.18.0 | R3F 9.7.0 stable，v10 alpha | R3F 9 需随 React 19 / Next 迁移联合评估；不单独抢跑 |
| Three.js | r183 | r186 | r184→r186 有迁移项；当前代码未命中 `SimplifyModifier` / `Source` 等新 breaking 点，升级需 3D Visual Check |
| SWC native | 14.2.33 | 跟随 Next 14 主版本 | 主版本一致，无 binding 撕裂 |
| LDraw Parts | 2026-07 | 294 新 parts / 414 新 files | 无 08 月新正式 Parts Update |

### Next.js CVE 命中面复核

- Middleware / proxy：0。
- Router：Pages Router；无 `app/`。
- Cache Components / dynamicIO：0。
- Server Actions / RSC：0。
- `next/image`：3 处（`pages/index.js`、`pages/model/[id].js`、`components/PartsTray.js`）。
- AVIF：`next.config.js` 未启用 `image/avif`；remotePatterns 为固定 CDN。`GHSA-2xp9-vwfh-vxw4` 版本和 Image Optimization 功能面命中，但攻击者可控 AVIF 可达性未证实，禁止写成“已证实可利用”。
- Windows RCE `CVE-2026-75604`：当前 Linux、无 App Router，不适用。
- July 2026 批次：按 7 类探查仍为 0 个已证实命中。

## 竞品动态

| 对象 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio | 2.26.7_1（2026-07-30）仍为最新，bugfix | 无 breaking / 无新增功能压力 |
| Mecabricks / GitHub LDraw | 本次检索未发现可核实的新正式 release | 不上报噪音 |
| LDraw.org | 论坛出现新设计工具功能预览；正式库仍为 2026-07 | 仅观察，不改变路线 |

## 飞行检查与队列

- Step 0：Dream B OPEN = 6，未熔断。
- 活代码：`pages/model/[id].js` 动态加载默认组件，`pages/sandbox.js` 使用 `Brick` export。
- 同文件 OPEN PR：#43、#44，均 `CLEAN/MERGEABLE`；本日无 P2/P3，因此未开分支。
- PR 队列：#43–#48 全部 CLEAN；最近 merge 2026-07-02，命中低 OPEN + 长期零合并的 PROC-P1。

## 信息源

- Next.js 官方 August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- Three.js Migration Guide：https://github.com/mrdoob/three.js/wiki/Migration-Guide
- React Three Fiber Releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw.org Parts Update：https://library.ldraw.org/updates?latest
- BrickLink Studio Early Access：https://www.bricklink.com/v3/studio/early-access.page
- 本地实测：ESLint、Jest、`npm list`、GitHub PR API、Next CVE 命中面 grep（2026-08-28）
