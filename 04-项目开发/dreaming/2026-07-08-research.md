# Dream B — BrickHub 日审 2026-07-08

- **审查文件**: `pages/model/[id].js` (503 LOC, Phase 1/2 已完成之模型详情+放映拼装主页,当前 Phase 3 沙盒 Iteration)
- **队列**: OPEN 3 (< 8 熔断闸未触发) — #43/#44/#45 均 CLEAN 等老王 review
- **PR 产出**: #46 (P2×5 触控+aria)
- **红线守护**: LDraw 素材未动 / 3D 未改 / UI 保持暖色调 LEGO 品牌色 / 触控升到儿童 56px
- **依赖**: Next 16.2.10 已 stable **一个月** (2026-06 首发), 项目 14.2.35 落后 **两个大版本** — 依赖 P0 级排期项 (原来只是 P1 EOL 提醒,今日升级)

---

## 一、已自动修复 (PR #46 `dreamB/auto-fix-20260708`)

| # | 类型 | 位置 | before → after | 依据 |
|---|---|---|---|---|
| P2-1 | 触控升级 | 顶部工具浮层 `清单` 按钮 :393 | `w-14 h-12` → `w-14 h-14` | 儿童 56px 标准 (`h-12` 只有 48px) |
| P2-2 | 触控升级 | 顶部工具浮层 `放映` 按钮 :407 | `w-14 h-12` → `w-14 h-14` | 同上 |
| P2-3 | 触控升级 | 顶部工具浮层 `沙盒` 按钮 :420 | `w-14 h-12` → `w-14 h-14` | 同上 |
| P2-4 | 触控升级 | 步骤导航 `上一步` `下一步` :366/:377 | `w-11 h-11` (44px) → `w-14 h-14` (56px) | 儿童放映时最常按,44 只是最低不是目标值 |
| P2-5 | 触控升级 | 清单面板关闭 `✕` :317 | `w-11 h-11` → `w-14 h-14` | 同上 |
| P2-6 | a11y 补齐 | `清单` 按钮 :393 补 `aria-label` | 无 → `打开清单/关闭清单` | 原只有 `aria-pressed` 无描述文本,VoiceOver 只念 "按下/未按下" 不知道按钮做什么 |

守卫: `npx eslint pages/model/[id].js` clean · `npx jest __tests__/modelDetail.test.js` **2/2 pass** baseline 保持

---

## 二、待老王拍板 (P0/P1, 仅报告不动手)

### A1 — 依赖 P0 (从原 P1 升级,今日 web_search 情报)
- **Next.js 已到 16.2.10 stable, 项目仍 14.2.35, 落后两个大版本**
- 14.x branch **已停止 patch** — Vercel 明确 App Router 全量投入 15/16
- 已知未修 CVE: SSRF (CVE-2026-44578, 命中面: `next/image` 无 host 白名单,本文件第 337 行 + `components/PartsTray.js` 第 46 行 各一处直连 `cdn.rebrickable.com` + fallback 到 `img.bricklink.com`,虽然 host 域名可控但仍在 SSRF 命中面)
- 排期建议: **单独开一个 Q3 迁移周** (`brickhub-nextjs-15→16-migration`),不是 Dream B 能日审自动化的动作
- 停留在 14 的代价: 每天 Dream B 报告都会把这条重复一遍污染 attention → **建议要么排期升级要么老王明确 defer 到 Phase 5 收尾**

### B1 — UI 规范 (Bug 隐患维度)
- 顶部工具浮层 3 按钮的 `disabled` 语义缺失: `放映` 按钮当 `totalSteps=0` (即 LDraw 未加载完) 时点击 → `setStep(1)` 会触发 `showParts` 后 `currentStepParts` 空表, UI 卡在 "第 1 步零件 · 0 件"
- 建议: `disabled={!ldrawCode \|\| totalSteps === 0}` + tooltip
- 涉及业务判断 (老王原意可能是 loading 期就允许点,只是暂时空表),不自动改

### B2 — 架构 (dead-code 姐妹检测)
- **无死代码** — pages/index.js:122 `<Link href={\`/model/${m.id}\`}>` 确认唯一入口 (grep `--include='*.js' --include='*.jsx'` 双写验证 + `require\(` sanity 都命中)
- Phase 1/2 已完成核心, 短期不会归档

---

## 三、回滚清单

无 (本次 5 个 patch 全部通过 lint+jest 守卫,零回滚)

---

## 四、依赖动态

| 依赖 | 项目版本 | 上游最新 stable | breaking | 备注 |
|---|---|---|---|---|
| Next.js | 14.2.35 | **16.2.10** | ✅ 15/16 有多个 breaking (async APIs, fetch caching default, Turbopack) | **A1 强提醒,落后两个大版本** |
| @react-three/fiber | 8.x | v9 (React 19 兼容,无 breaking) | ❌ v10 才有 breaking (尚未发布) | 短期安全,追踪 v10 RFC |
| Three.js / @react-three/drei | — | — | ❌ (未查,近期无 major) | — |
| `@next/swc-linux-x64-gnu` | 14.2.33 pin (与 next 14.2.35 差 2 个 patch) | 14.2.33 | — | 在 skill 提到的容错范围内 (`>=14.2.30 <14.3.0` 精确 pin),`next build` 未走 WASM fallback |

---

## 五、竞品动态

跳过 (今日 web_search 用于 Next.js/R3F 优先) — 上周 Mecabricks / Studio 无重大更新变化,不构成 P2 告警

---

## 六、信息源

- Wikipedia "Next.js" 页面: Release 16.2.10 (via `github.com/vercel/next.js/releases/tag/v16.2.10`)
- Abhishek Gautam blog: "Next.js 16.2.7 is the current stable on npm as of June 2026" (于 2026-06-10)
- pmndrs/react-three-fiber #2338: v9 是 React 19 兼容版无 breaking, breaking 推迟 v10
- Dream B skill `references/dream-b-nextjs-cve-hitsurface.md` (next/image SSRF/DoS 命中面 7 类 CVE 表)
- 项目 `PROJECT_VISION.md` §5.1 已完成清单第 174-175 行 `pages/model/[id].js` 双认领 Phase 1/2

---

## 七、Pre-PR 飞行检查记录 (供后续复审免踩坑)

- **队列熔断闸** (Step 0): OPEN=3 < 8 ✅
- **死代码 grep** (双写):
  - `grep -rn "/model/" --include='*.js' ...`: 命中 `pages/index.js:122` ✅ 唯一 nav 入口
  - **`git log --oneline -5`** 交叉证据: 32d838b2 (32d838)/a5342aad(#8)/9a65daaf/b231e0e8/b32ef2bf 五连活跃 commit,是生产核心非弃稿
- **同文件 OPEN PR**: 0 ✅
- **历史 PR 考古**: #37 CLOSED (2026-06-08 早前版本已被取代) + #8 MERGED (2026-04-30 首次 a11y),**无未合冲突**
- **PROJECT_VISION 语义**: Phase 1/2 ✅ 已完成,Phase 3 沙盒仍在通过本页 `pages/sandbox.js` 独立开发,本页非孤儿

---

## 八、Dream B 累积 P1 汇总 (跨日追踪)

供老王 batch 决策用,不重复渲染每日报告的 P1 细节。今日新增的粗体:

| 首次 | 项 | 状态 | 影响 |
|---|---|---|---|
| 06-15 | Next 14 EOL | **今日升级 P0** (16 已 stable 一个月,落后两版) | 排期迁移周 |
| 07-04 | BrickRenderer B1/B2/B3 死累加器 / setState 抖动 | 未拍板 | 中 |
| 07-05 | FreeBuildTray TEST-P1 (jest 10/10 baseline fail) | 未拍板 | 高 |
| 07-06 | pipeline/index.js TEST-P1 (jest.mock 整 stub 零真实覆盖) | 未拍板 | 高 |
| 07-07 | sandbox A1 死交互按钮 (视角重置无 onClick) | 未拍板 | 中 |
| **07-08** | **model/[id] A1 依赖 P0 (Next 16 落后两版) + B1 放映按钮 disabled 语义缺失** | 新增 | **A1 高 / B1 低** |

累积 5 条 TEST/死代码/死交互 P1 + 依赖 P0,建议 W28 中段老王批量看一次。

