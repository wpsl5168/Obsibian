# Dream B 日审 2026-06-25 — `lib/ldrawParser.js`

**轮换上次审查**: 2026-06-06(19 天)  
**LOC**: 618  
**ESLint**: ✅ clean  
**自动修**: 0 项(无 P2/P3 可改) → 不开 PR

---

## 已修复表

| 文件 | 修复项 | 等级 | 状态 |
|---|---|---|---|
| — | 无 | — | 0 PR (避免堆积) |

---

## 待确认表(P0 / P1)

| ID | 位置 | 等级 | 问题 | 建议 |
|---|---|---|---|---|
| LDP-T1 | `validateGeometry` 行 488–618(130 LOC 几何核心:碰撞 AABB + 悬空检测) | **P1** | **零测试覆盖** — `grep -rn "validateGeometry" __tests__/` 0 匹配,无 mock,无任何调用。该函数是 AI 生成结果的物理可行性守门员(防止上线给孩子的模型直接散架),ROI 极高 | 建一份 `__tests__/validateGeometry.test.js`,至少覆盖:① 单砖不悬空 ② 两砖正确叠层(yMin≈yMax) ③ AABB 体积重叠误判 ④ 旋转后 8 角点变换正确 ⑤ MAX_PER_TYPE=5 早退 ⑥ 跳过 disc/未知零件分支 |
| LDP-T2 | `parseLDraw` 行 410–429:`tok.length < 15` 时半推入 brick(行 416–427) | **P1** | 字段不足 15 但 ≥ 6 时仍 `bricks.push(...)` — **缺 `quat` 字段**(行 461 才设的)。若 `BrickRenderer` 消费这种 partial brick,运行时 `brick.quat[0]` 会 throw `Cannot read properties of undefined` | ① 要么 push 时给 `quat: [0,0,0,1]` 兜底(简单) ② 要么直接 `continue` 不推入(更激进但安全)— 涉及"是否要 best-effort 渲染坏数据"的产品判断 → 老王拍 |
| PROC-Q | dreamB OPEN PR 队列 | **P1** | OPEN=**36** / 近 30 天 MERGED=**0** / 全 CLEAN — 队列从 4-24 起持续梗死,Dream B 每天产 PR 但**老王 60 天未走过一次 merge**。继续抹奶油只会让队列爆掉到 ≥40 | ① 一次性 batch-merge 已绿且不冲突的(#42 / #41 / #40 / #39 / #38 等)② 或显式拍 close 全队列,重启 Dream B 自动修策略 ③ 在拍板前 Dream B **临时停止开 PR**(本次已 0 PR) |
| LDP-DEP | `package.json` Next.js 14.x | **P1 强提醒** | 2026-05 Vercel 一次性发 **13 个 CVE 修复**(middleware/proxy bypass、SSRF、DoS、cache poisoning、XSS),只补到 **15.5.18 / 16.2.6**;**14.x 全版本无补丁**(GitHub #93868 / #89551 确认)。继 6-18 报告后再加码 — auth bypass + SSRF + multiple DoS 已可被自动化扫描器命中 | 升 15.5.18(最小迁移代价)或 16.2.6(长寿)。注意:asyncparams/searchParams 是 breaking,要全仓 grep `params\.\|searchParams\.` |

---

## 回滚表

| 文件 | 原因 |
|---|---|
| — | 无(0 PR) |

---

## Bug-Hazards 表(已验证非 bug,避免下次轮再花时间)

| ID | 位置 | 表面现象 | 判定 |
|---|---|---|---|
| LDP-V1 | 行 460 `position: [x, -y, z]` vs 行 504-506 `validateGeometry` 用原 y | 两套坐标语义不一致 | 无 bug:`parseLDraw` 输出 world 坐标(Y-up,供 R3F);`validateGeometry` 在 LDraw 原生坐标(Y-down)做几何 — 注释 488-487 已明示 |
| LDP-V2 | 行 393 `if (/\.dat$/i.test(raw.trim())) return '1x1'` | 未知 `.dat` 静默回退占位 | 无 bug:注释"Full-library compatibility"。是有意 fallback,渲染兼容性 > 警告 |
| LDP-V3 | 行 442 `mat` 看似未用,只取了 `quat` | 死变量 | 无 bug:`new THREE.Quaternion().setFromRotationMatrix(mat)` 必须显式传 mat,变量只是中间构造手段 |
| LDP-V4 | 行 596 `brickInfos.some(...)` O(N²) | 大模型卡顿 | 无 bug:`MAX_PER_TYPE=5` 早退,实际只跑前 5 块不及格的,N 大时也不会 OOM |
| LDP-V5 | 行 467-470 `Math.min(...arr)` 已被循环替换(commit f538999b) | 历史栈溢出 | 已修复:见 git log,无需重复审查 |

---

## 依赖动态

| 依赖 | 当前 | 最新 | 风险 | 操作 |
|---|---|---|---|---|
| **Next.js** | 14.x | 15.5.18 / 16.2.6 | 🔴 **High** — 2026-05 13 CVE 14.x 全无补丁(已 6-18 报过,再次升级) | **P1 强提醒**,见上表 LDP-DEP |
| Three.js | r? (项目内) | r171+ | 🟢 无 breaking — WebGPU 生产就绪是新增能力,WebGL 仍兼容 | 无需操作,可后续选择性切 WebGPURenderer |
| LDraw parts library | 静态别名表 | 2026-05 update +305 文件 / +200 part | 🟡 增益机会 — 表内 `PART_ALIASES`(行 312-384)未覆盖新零件,AI 生成新零件会触发 LDP-V2 fallback 1x1 占位 | 可考虑写脚本从官方 `parts.lst` 同步,但不紧急 |

## 竞品动态

| 项目 | 状态 |
|---|---|
| 本次未轮 — 节约搜索配额给依赖告警(已用 3/6) | — |

---

## 信息源

- https://github.com/vercel/next.js/discussions/93868 — Next.js 14 EOL + 用户求补丁
- https://github.com/vercel/next.js/discussions/89551 — CVE-2026-23864(DoS)
- https://vercel.com/changelog/next-js-may-2026-security-release — 2026-05 13 CVE 一次性
- https://www.utsubo.com/blog/threejs-2026-what-changed — Three.js r171+ WebGPU
- https://forums.ldraw.org/thread-29527.html — LDraw 2026-05 parts update

---

## 本次特别记录

- **Pre-PR 飞行检查 3 项全过**(死代码:否,parseLDraw 32次真实测试;同文件 OPEN PR 冲突:0)→ **本可以开 PR**
- **但 0 P2/P3 可自动修**(ESLint 全绿,无明显规范违规,无未用变量),所以无 PR 是"实际没东西修"而非"被守卫拦下"
- 与 6-18 报告呼应:Next.js 14.x CVE 暴露已从"未来风险"变"已坐实暴露面",建议本周内安排升级窗口
- PROC-Q 是连续第 N 次报队列梗死,Dream B 已在本次主动选择不开 PR — 但这只是被动避让,**根因是老王对队列的处置决策悬而未决**
