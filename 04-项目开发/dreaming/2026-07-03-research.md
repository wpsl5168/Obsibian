# BrickHub Dream B — 2026-07-03(Fri, W27)

## 今日选文件

**`components/VoiceInput.js`**(96 行,2984 bytes)

- 轮换起点:rotation.json 已重置(2026-07-03),04-24→07-01 覆盖全核心文件一圈完成后重来,VoiceInput 是重置后第 1 个
- 上次触碰:`3b2a2698` (2026-06-10, PR #39 MERGED) — a11y aria-label/pressed/live + JSDoc

## 队列健康(Step 0)

- **dreamB OPEN PR:0**(7-3 早上老王「持续推进」授权后已从 19 → 1,唯一剩 #5 是 copilot 作者的首页复刻 DRAFT,非 Dream B 产物)
- 熔断阈值 8,**不熔断**,正常进 Step 1-6
- 队列清账周期显著缩短(W26 末 36 OPEN → W27 中 1 OPEN),整合收口方法论有效

## 扫描结果(4 维度)

### 1. Bug 隐患

**核实无 bug**。三重验证:

| 检查 | 结果 |
|---|---|
| `npx eslint components/VoiceInput.js` | ✔ No errors |
| 关键疑点:`useEffect deps: [onError, onResult]` 是否会重建 SR 实例? | 需父组件 useCallback,这是 React hook 官方推荐;改子组件 `deps: []` 会引入 stale closure,**违反规则**,不修 |
| 触控尺寸 `h-14 w-14`(56px) | ✔ 过儿童 56px 红线,sm 断点 h-16(64px)更大 |

### 2. 架构

**关键发现:VoiceInput 是孤儿组件**

`grep -rn "VoiceInput"` 全仓命中:
- `components/home/PromptDock.js:1` `import VoiceInput from '../VoiceInput'` — **唯一 JS 引用**
- 其余全是文档(PROJECT_VISION / .github/copilot-instructions / agent md)

而 `components/home/*` 已在 2026-06-30 Dream B 确认为**死代码目录**:

| home/ 子文件 | pages/lib/tests 引用数 |
|---|---|
| BrickScene / FloatingControls / HistoryPanel / KnowledgePreviewPanel / LDrawEditorPanel / LDrawModal / MenuPanel / ProcessDebugPanel / PromptDock / StatusBanner | **全部 0** |

**结论**:VoiceInput 传递闭包依赖 `home/*` 死代码 → **当前生产未启用**。PROJECT_VISION 规划 Phase 4 才会在 `pages/ai-studio.js` 挂载(Phase 4 状态 ⬜ 未开始)。

### 3. UI 规范

- 品牌色:`#e31b23`(红) / `#ffe14d`(黄) / `#7f1d1d`(暗红) / `#8a1117` / `#d4b11b` / `#fff5bf` / `#eadfb8` — ✔ LEGO 品牌色 + 暖色调
- 无深色底 ✔
- 触控 56px(儿童红线)/ sm 64px ✔
- aria-label(动态)/ aria-pressed / role=status / aria-live=polite / aria-hidden(emoji)✔ 全齐
- `title` tooltip 补齐了非 screen-reader 场景 ✔
- Tailwind 任意值(`border-[3px]` / `rounded-[1.4rem]` / `shadow-[...]`) — 都用方括号 JIT 语法,**不是** `border-3` 那种非标准死类,健康(参考 skill `dream-b-ui-css-deadpattern-checks`)

### 4. 测试覆盖

- `__tests__/` 下**零 VoiceInput 测试** + `SpeechRecognition` mock 也没有
- **但不列 P1**:VoiceInput 高度依赖浏览器 `SpeechRecognition` Web API,测试成本高、ROI 低,不同于 `validateGeometry` 那种纯函数
- Phase 4 启用时应写 integration test(mock `window.SpeechRecognition`),现在写属于超前投入

## Pre-PR 飞行检查

| 闸门 | 结果 | 动作 |
|---|---|---|
| Step 0 队列熔断 | 0 OPEN < 8 | 通过 |
| 死代码 grep | 唯一 caller `home/PromptDock.js` 是死代码 | ⚠️ **不开 PR**(在孤儿组件上继续抹奶油无价值) |
| 死代码姐妹批 | 已确认 `components/home/*` 全 0 引用(见上表) | 上报 P1(下文) |
| 历史 PR 考古 | `#39 MERGED`(2026-06-10 a11y) / `#26 CLOSED`(2026-05-23 a11y) — Dream B 已 2 次在 VoiceInput 上开 PR | **累计抹奶油 4 次**(git log a11y 相关 commit) |
| 同文件 OPEN PR 冲突 | 0 | 通过 |
| 队列健康 | 1 OPEN(非 dreamB)、30d 无梗死 | 通过 |

**结论**:即便技术上通过,VoiceInput 无 P2/P3 可修,不开 PR。

## 自动修复清单

**0 项**(见上,无可修)。今日不开分支、不开 PR。

## 待老王拍板

### P1(单条,优先级中,复述提醒)

**`components/home/*` 死代码目录归档**(继承自 2026-06-30 报告,尚未落地)

- 10 个文件、pages/lib/tests 引用全 0
- Dream B 在这些"死洞"里累计开过 11 条无意义 a11y PR(#22 #23 #24 #25 #27 #28 #29 #30 #31 #32 #33 — 后 3 条已在 7-3 收口 close)
- **建议一键**:
  ```bash
  cd ~/workspace/brickhub
  git mv components/home components/_archive/home-deprecated-20260703
  git rm -r components/_archive/home-deprecated-20260703  # 或保留 archive/ 作 phase4 参考
  git commit -m "chore: archive components/home/* (dead code, 0 refs since 2026-04-09)"
  ```
- 归档后 `VoiceInput.js` 变真正的"待 Phase 4 启用"孤儿(仅文档引用),Dream B 应把它从每日轮换名单挪到"Phase 4 冻结区",Phase 4 启动前不再扫

### P0

**无**。

## 依赖动态

| 依赖 | 当前 | 状态 | 处置 |
|---|---|---|---|
| next | 14.2.35(package.json ^14.2.0) | 🔴 EOL 已 8 个月(2025-10-26),不打补丁 | **持续 P1**(上周报告已列,不重复渲染) |
| @next/swc-linux-x64-gnu | 14.2.33 | 🟢 与 next 14.x 大版本一致,不撕裂,无 WASM fallback(7-1 已止血) | 无动作 |
| @react-three/fiber | ^8.18.0 | 🟢 v9 breaking,但当前 v8 稳定,react 18 兼容 | 无动作(v9 需要升 react 19) |
| three | ^0.183.2 | 🟢 与 r3f v8 兼容 | 无动作 |
| react | ^18.3.0 | 🟢 稳定 | 无动作 |

### CVE 核实(不重复渲染)

- **CVE-2026-44578 SSRF**:仅 self-hosted Node server,brickhub 走 Vercel 不适用;self-host 需 WAF。同 07-01/07-02 结论,不重复
- **CVE-2026-44575 middleware bypass**:brickhub 无 `middleware.*` 文件 + pages router → **不适用**(已核 `find . -name middleware.*` 无命中)
- **CVE-2025-29927 x-middleware-subrequest**:14.2.25 已修,14.2.35 已含
- 无新 CVE 或 patch 发布

### 一句话

**无新 breaking / 无新 EOL 临近 / 无新 CVE 暴露**。Next 14 → 15 升级仍建议 W28-W29 排期(EOL 已 8 月,窗口收窄)。

## 竞品动态

- BrickLink Studio:沿用 2.0,无重大动作
- Mecabricks:未见新版本
- 无威胁性动作

## 信息源

- https://vercel.com/changelog/next-js-may-2026-security-release
- https://releasebot.io/updates/vercel/next-js
- https://www.netlify.com/changelog/2026-05-08-react-nextjs-security-vulnerabilities
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- 本地:`brickhub/PROJECT_VISION.md` Phase 4 规划、`.github/copilot-instructions.md`

## 方法论沉淀

**"孤儿组件"新分类**(下次遇到需触发)

VoiceInput 情况揭示的模式:某组件本身健康 + 有历史 a11y PR 记录 + **但唯一消费者已确认为死代码** → 不应继续常规轮换扫描,应挪到"Phase X 冻结区",避免连续抹奶油。

建议在 skill `dreaming` Dream B 端加一条 pre-flight:
> **孤儿检测**:所选文件若 (a) 生产 pages/lib 无引用,(b) 唯一 caller 属死代码目录,(c) 项目文档标记 "Phase X 待启用" → 不扫描,记入"冻结区",通知老王。

留待下次 Dream A 周报时正式沉淀进 skill。

## 备注:2026-07-03 早上收口二段

7-3 早上老王「持续推进」授权已把队列 19 → 1(见 `2026-07-03-integration-closeout.md`)。本次 Dream B 是**清账后第一次日审**,验证:
- 熔断闸解除 ✔(0 OPEN < 8)
- rotation 重置生效 ✔(VoiceInput 是重置后第 1 个)
- 无幽灵 PR 干扰 ✔

方法论——"整合而非逐条合" + "DIRTY PR 等价 apply 而非 rebase"——都收进 skill `dream-b-pr-queue-cleanup.md`,下次队列再堆时可直接复用。
