# Dream B 2026-07-26 — lib/pipeline/utils.js

第三轮 restart day 8。轮到 `lib/pipeline/utils.js`（第二轮 07-17 复审过，本轮复发第 2 次）。

## 队列状态（Step 0）
- OPEN=5（#43-47 全 CLEAN），未触熔断（阈值 ≥8）
- 最近 MERGED：#39（2026-07-02，24 天前）→ **PROC-P1 队列梗死第 10 天**
- 30d MERGED=0 & 14d MERGED=0 双阈值触发 PROC-P1

## 扫描结果

| 维度 | 结论 |
|---|---|
| Lint | ✅ clean |
| 死代码 | ✅ 无（`pages/api/generate` → `lib/pipeline/index` → `./utils` 真实链路） |
| 死内部符号 | ✅ 8 export 内 4 helper（isUnitAxis/dot3/det3/isValidOrthogonalMatrixTokens）在同文件 `isValidOrthogonalMatrixTokens` 内被使用（正交矩阵校验闭包），非死符号 |
| 触控/UI | N/A（纯函数模块） |
| 测试覆盖 | ❌ TEST-P1 复发第 3 次 |

## P0/P1 待确认（本轮无新增，全部延续）

| ID | 级别 | 事项 |
|---|---|---|
| DEP-P0 | P0 | Next 14.2.35 → 16 第 19 天，16.3 已 stable 14 天，落后 2 大版本，建议排期迁移周或明确 defer 到 Phase 5 |
| CVE-P1 | P1 | `next/image` × 3 命中 May 2026 Image Optimization SSRF/DoS 批次，Next 14.x 无补丁 |
| PROC-P1 | P1 | 队列梗死第 10 天，5 条 CLEAN PR 累计 24 天零合并，Dream B 每日 0 修复报告即将进入第 25 天 |
| TEST-P1 | P1 | `lib/pipeline/utils.js` 8 export（含 `normalizeLDrawMatrices`/`deduplicateLDraw`/`isValidOrthogonalMatrixTokens` 等矩阵校验+去重守门员）零测试；此模块是 LLM 生成结果落盘前最后一道数据净化关，缺守卫风险高，建议补 `__tests__/pipelineUtils.test.js`（第二轮 07-17 已标注，复发第 2 次） |

## 依赖动态
- Next.js 14.2.35 EOL 持续、16.3 stable 14 天 → DEP-P0 延续
- 无新 breaking

## 竞品动态
无需扫描（延续 W28 观察窗口）。

## 拍板清单（老王一键收口）
```bash
# 队列清账（batch merge 或 close）：
cd ~/workspace/brickhub && export PATH=~/bin:$PATH
gh pr list --state open --search "head:dreamB" --json number,title
# 建议：全 CLEAN 无冲突，可 batch merge #43-47（每条独立、非同文件）
# 补测试：为 lib/pipeline/utils.js 建 __tests__/pipelineUtils.test.js
# Next 迁移：给 Phase 4/5 排期 Next 16 迁移周
```

## 轮换
- 已追加 `2026-07-26: lib/pipeline/utils.js`
- 第三轮 day 8，下轮 → `lib/knowledgeBase.js`（第二轮末位）
