# BrickHub Dream B 2026-06-14

**审查文件**: `components/PartsTray.js` (65 LOC)
**分支/PR**: `dreamB/auto-fix-20260614` → [PR #42](https://github.com/wpsl5168/brickhub/pull/42)
**轮换状态**: 距上次轮换 PartsTray = 28 天 (上次 2026-05-16)

---

## ✅ 已自动修复

| 级别 | 行 | 问题 | 修复 | 守卫 |
|---|---|---|---|---|
| P2 | 23 | `scrollbarWidth: 'thick'` 是非法 CSS 值（`scrollbar-width` 合法值仅 `auto/thin/none`），浏览器静默忽略；与同行 `scrollbar-hide` 意图相悖 | 改为 `'none'` | eslint ✔ / modelDetail.test.js 2/2 ✔ |
| P3 | 23 | `scrollbar-hide"  style=` 双空格 | 单空格 | 同上 |

commit: `3b7bdd70 fix(PartsTray): scrollbarWidth thick→none, dedup space`

## 🔴 待人工拍板（P1，未自动改 — 涉及全项目设计 token / 插件配置）

| # | 位置 | 问题 | 影响 | 建议 |
|---|---|---|---|---|
| 1 | PartsTray.js:23, FreeBuildTray.js:225/263, pages/model/[id].js:320 | `scrollbar-hide` Tailwind 类在 `tailwind.config.js` 无 plugin、`styles/globals.css` 无自定义实现 → **dead class，0 渲染效果** | Chrome/Safari 上仍显示横向滚动条，UI 杂乱 | 安装官方 `tailwind-scrollbar-hide` plugin（4 处一次性生效），或在 globals.css 补 `.scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; } .scrollbar-hide::-webkit-scrollbar { display: none; }` |
| 2 | PartsTray.js:54, pages/model/[id].js:368/378/392/403/414 (5 处) | `border-3` 非 Tailwind 默认 borderWidth 刻度（默认仅 `border-1/2/4/8`） → **类不存在，边框不渲染** | UI 边框缺失（视觉对设计稿） | 在 `tailwind.config.js` `theme.extend.borderWidth` 加 `'3': '3px'`，或全局改用 `border-[3px]` 任意值 |

> 这两项都是「全项目级 token / plugin」决策，按红线 Dream B 不能自动改 — 留给老王在「UI 系统统一巡检」时一次性处理。

## 🟡 待确认（P2 灰区，不修）

| # | 位置 | 观察 | 备注 |
|---|---|---|---|
| 1 | PartsTray.js:44 | `https://cdn.rebrickable.com/media/parts/ldraw/71/${cleanName}.png` 颜色码硬编码 `71`（浅蓝灰）| 有 `handleImageFallback` 兜底，先不动；若未来要颜色化预览图改取真实色 |
| 2 | PartsTray.js:54 | `font-bold` 与 `font-black` 同时出现在 className | Tailwind 后者覆盖前者，无 bug 但冗余，待 UI 重构时清理 |

## ⏪ 回滚

无。

## 📦 依赖动态（≤6 search）

| 库 | 当前 | 上游 | breaking 评估 |
|---|---|---|---|
| @react-three/fiber | ^8.18.0 | v9 已发布 | v9 为 React 19 兼容更新，**无 API breaking**，仅 StrictMode 下高亮原有 bug；后续 v10 才是真正 breaking。**项目仍 React 18 + R3F 8，可继续观望** |
| next | ^14.2.0 | v15 GA / v16 docs 已出 | v15 引入 Async Request APIs (`headers/cookies/params` 均改 async)、GET Route Handlers 与 Client Router Cache 默认 uncached。**项目 pages router + v14 暂稳，但升 v15 时是大改** |
| @react-three/drei | ^9.122.0 | 与 R3F v8 兼容线持续维护 | 无 breaking |
| tailwindcss | n/a（未在 package.json 主依赖列出） | v4.3+ 自带 `scrollbar-*` utilities | **关联 P1#1**：升级 Tailwind 至 4.3+ 是另一条解法，但工程量大；先用 `tailwind-scrollbar-hide` plugin 最便宜 |

## 🏁 竞品动态

| 竞品 | 动作 | 启示 |
|---|---|---|
| Bricklink Studio | 推出 **Sculpture** 功能：3D 模型自动转砖块艺术（baseline + 编辑） | 与 BrickHub「AI 自然语言生成 LDraw」是同方向的下游能力；可作为 v2 路线参考 |
| Bricklink Studio | Render 模块支持照片级渲染+动画导出 | 长期对比 BrickRenderer 仅 R3F 实时；photoreal 路线暂不投入，差异化在「交互编辑」即可 |
| Mecabricks | 仍以浏览器原生 LDraw 编辑 + 渲染为主，无明显新动作 | — |

## 🔎 信息源

1. https://github.com/pmndrs/react-three-fiber/issues/2338 — R3F v9 RFC
2. https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
3. https://nextjs.org/blog/next-15
4. https://nextjs.org/docs/app/guides/upgrading/version-16
5. https://npmjs.com/package/tailwind-scrollbar-hide
6. https://design2tailwind.com/blog/tailwindcss-hide-scrollbar
7. https://www.instagram.com/reel/DQesKaaFKrY — Bricklink Studio Sculpture

## 📌 下次轮换候选

`components/Toolbar.js`（cron 清单里有但仓库无；跳过）→ `components/BrickRenderer.js`（最近一次 06-04，已 10 天）/ `lib/instancingPipeline.js`（06-01，已 13 天）。
