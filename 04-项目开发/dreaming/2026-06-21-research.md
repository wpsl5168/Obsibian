# BrickHub Dream B 2026-06-21

**轮换文件**:`components/home/PromptDock.js`(57 行)
**审查结论**:🚨 **整个 `components/home/` 目录(10 个组件)疑似死代码 + Dream B 流程梗死** — 跳过自动修(死代码上抹奶油无意义),直接 ARCH/PROC 报告,等老王拍板。

---

## 🔴 ARCH-P1:`components/home/*` 整目录零引用(死代码嫌疑)

### 验证(三重 grep,确凿)

```
$ grep -rn "PromptDock" --include="*.{js,jsx,ts,tsx,json,md}" .
./components/home/PromptDock.js:3:export default function PromptDock({  # 仅自身定义

# 对 home/ 目录下全部 10 个组件批量验证:
for f in components/home/*.js; do
  name=$(basename "$f" .js)
  count=$(grep -rln "from.*home/$name\|home/$name'" --include="*.{js,jsx}" \
    pages/ components/ lib/ 2>/dev/null | grep -v "^$f$" | wc -l)
  echo "$name: $count"
done

BrickScene: 0
FloatingControls: 0
HistoryPanel: 0
KnowledgePreviewPanel: 0
LDrawEditorPanel: 0
LDrawModal: 0
MenuPanel: 0
ProcessDebugPanel: 0
PromptDock: 0
StatusBanner: 0
```

| 检测维度 | 结果 |
|---|---|
| 静态 import (`from .*home/...`) | **0 处**(全 0)|
| 动态 import (`next/dynamic` 字符串拼接) | **未命中** — `next/dynamic` 在 `pages/model/[id].js`、`components/home/BrickScene.js` 出现两次,目标都不指向 `home/*` |
| 字符串 / Markdown / JSON 引用 | 0 |
| 测试文件覆盖 (`__tests__/`) | 0(`__tests__` 下 13 个测试无一覆盖 `home/`)|

### 来由考古

```
$ git log --all --diff-filter=A --pretty=format:"%h %ai %s" -- components/home/
632d814c 2026-04-09 10:06:54 +0800 refactor: reorganize home flow and remove shared password
```

→ 2026-04-09 一次"home 流改造"创建,之后整个目录从未被任何 `pages/*` 拉起来。当前 `pages/index.js`(Home 函数)是 gallery 模式,纯卡片网格 + Tailwind hex 调色板,**完全没用** home/ 任何组件;`pages/sandbox.js` 用的是 `components/FreeBuildTray`、`components/PartsTray`、`components/BrickRenderer` 等顶层组件。

### 影响(为什么必须先报)

| # | 项 | 说明 |
|---|---|---|
| 1 | **代码维护成本沉没** | Dream B 过去 ≥11 次 PR(#22/23/24/25/27/28/29/30/31/32/33)全在 home/* 死文件上抹 a11y 奶油 |
| 2 | **PR 队列被死代码改动撑爆** | 死改动通不过 review(老王看不出价值),累计 30 个 OPEN PR(详见下) |
| 3 | **rotation.json 浪费选片名额** | 过去 60 天 rotation 中 home/* 占据 11 次轮次 |
| 4 | **bundle 体积** | dead-code-elimination 应该能清掉,但破坏阅读心智地图 |

### 三档候选处置(老王拍板)

| 选项 | 操作 | 风险 |
|---|---|---|
| A. 整目录归档 | `git mv components/home/ components/_archive/home-deprecated-20260409/` + README | 0 风险,可快速 revert |
| B. 整目录删除 | `git rm -r components/home/` | 同 A,有 git 历史可追,推荐 |
| C. 重接首页 | 把 `pages/index.js` 改成 home 流(用 PromptDock + StatusBanner 等组合) | 高(需重新设计 UX/交互/状态管理) |

**Dream B 建议**:**选 B**(归档/删除)。如果真要重做首页,从 phase1 弃稿恢复成本反而高于重写。

---

## 🔴 PROC-P1:PR 队列严重梗死

```
OPEN_total = 30      # gh pr list --state open
MERGED 近30天 = 0    # gh pr list --state merged --search "merged:>2026-05-22"
```

按 dreaming skill 阈值:**OPEN > 10 + 近 30 天 MERGED=0 = 流程梗死**;OPEN=30 已踩 "OPEN > 20 队列过载" 第二阈值。

### 队列内容(全部 dreamB/auto-fix-*)

最早一条 #13(2026-05-06),最新 #42(2026-06-14),全程 `mergeStateStatus=CLEAN` — **不是冲突卡住,是老王没空 review/merge**。

### 建议(老王拍板)

| 选项 | 操作 |
|---|---|
| A | 一次性 close 全部 30 个,Dream B 暂停自动 PR 模式 1-2 周直到老王清完 |
| B | 让 Dream B 把 P2/P3 改动**直接 push 到 main**(skip PR),回归"快速合入"模式 |
| C | Dream B PR 一周后未 review 自动 close(防积压) |

→ ARCH-P1(死代码)和 PROC-P1(队列梗死)**强耦合**:死代码改动占据队列大头,清死代码 + 暂停 a11y 抹粉 PR 双管齐下才能解套。

---

## ✅ 自动修复

无 — 跳过(Pre-PR 飞行检查触发死代码护栏 + 同期已有 30 个 OPEN PR,不再增量)。

## ⏸ 未能自动修复 / 待确认

| 级别 | 项 | 文件 | 处理 |
|---|---|---|---|
| ARCH-P1 | `components/home/*` 整目录零引用死代码 | `components/home/{BrickScene,FloatingControls,HistoryPanel,KnowledgePreviewPanel,LDrawEditorPanel,LDrawModal,MenuPanel,ProcessDebugPanel,PromptDock,StatusBanner}.js` | 推荐选项 B(`git rm -r`) |
| PROC-P1 | PR 队列 30 条积压,近 30 天 0 merged | `wpsl5168/brickhub` | 推荐选项 A(批量 close + 暂停 auto-PR 1-2 周) |

---

## 📦 依赖动态(≤ 6 次搜索,本次用 2 次)

| dep | 当前 | 最新 | 状态 |
|---|---|---|---|
| three | ^0.183.2 | r183(对齐) | ✅ 已对齐;r170(2025-08)WebGPU modules→addons、r171(2025-09)零配置 WebGPU、r183 引入 RenderPipeline — **均向后兼容,无 breaking** |
| @react-three/fiber | (查 lockfile)v8.x | **v9 已发布** | ⚠️ v9 在 React Strict Mode 下有 breaking,`useLoader` API 改动;升级前需做 strict-mode 兼容测试。**P2 告知**,无紧急性(v8 仍在维护) |
| next | ^14.2.0 | 15/16 主线 | 🔴 **保持 06-18 P1 强提醒(EOL + 高危 CVE 无补丁)** — Next.js 14 patches not planned,2026 累积多个 High CVE,Vercel/Netlify 明示需升 15/16 |

## 🏗 竞品动态

本次未独立搜索(总配额留给依赖)。**无 breaking 触发**。

## 📚 信息源

1. <https://www.utsubo.com/blog/threejs-2026-what-changed>(Three.js 2026 changes)
2. <https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide>(R3F v9 migration guide)
3. <https://threejsroadmap.com/blog/the-complete-guide-to-threejs-post-processing-in-2026>(r183 RenderPipeline)

## 🧪 验证命令(老王复核用)

```bash
cd ~/workspace/brickhub
# 1. 死代码验证
for f in components/home/*.js; do
  name=$(basename "$f" .js)
  echo -n "$name: "
  grep -rln "from.*home/$name\|home/$name'" --include="*.{js,jsx}" pages components lib 2>/dev/null | grep -v "components/home/" | wc -l
done

# 2. PR 队列
gh pr list --state open --json number | jq length
gh pr list --state merged --search "merged:>2026-05-22" --json number | jq length

# 3. 删除预演(干 run)
git rm -r --dry-run components/home/
npx next lint && npm run build  # 确认 build 不依赖
```

---

**结论一句话**:今天没做修(对的 — 死代码不抹奶油 + 队列已挤爆),但抓到了**两个累积流程问题**让老王主动决策:home/ 死目录如何处置、30 个 PR 怎么收口。
