# Dream B · BrickHub 日审 2026-06-15

- **审查文件**: `lib/pipeline/utils.js`(129 行,纯函数模块:LDraw 去重/抽取/正交矩阵校验/offline fallback)
- **轮换**:自 2026-05-27 起 19 天未轮,本轮拉回
- **整体评分**: 🟢 干净。eslint 通过,无 P0/P1 真 bug,无可自动修 P2/P3 项

## 1. 4 维度扫描结果

### Bug 隐患
| ID | 位置 | 内容 | 判定 |
|---|---|---|---|
| B-1 | L43 `Number.parseInt(t,10)` | `"1.0"` 会被解析为 1 | **无 bug**:下游 `isUnitAxis` 用 `Number.isInteger` + 严格枚举 (-1/0/1) 二次拦截 |
| B-2 | L1-11 `deduplicateLDraw` | 仅去重 `1 ` 起头行(part 引用),其他行(注释/meta) 全部保留 | **设计意图**,非 bug |
| B-3 | L13-23 `extractLDraw` | 过滤只留 type 0/1 行,丢弃 LDraw type 2-5(line/triangle/quad/cond) | **业务意图**:LLM 生成场景只产出 type 0/1,ldrawParser 也只消费这两类 |
| B-4 | L66-86 `normalizeLDrawMatrices` | 非正交矩阵静默替换为单位矩阵 | **OK**:`matrixFixCount` 已经被 `lib/pipeline/index.js:155` 通过 `hooks.onEvent` 上报 |

### 架构
- 纯函数模块,无副作用 / 无 React hook / 无外部 IO → 干净
- `buildOfflineFallbackLDraw` 三个 category 分支模板硬编码,有重复但量小(各 7-10 行),抽取 helper 收益不大,**不修**

### UI 规范
- N/A(纯逻辑模块,无 JSX/Tailwind)

### 测试覆盖
- ❌ **零单元测试** — 5 个纯函数(`deduplicateLDraw` / `extractLDraw` / `isUnitAxis` / `dot3` / `det3` / `isValidOrthogonalMatrixTokens` / `normalizeLDrawMatrices` / `buildOfflineFallbackLDraw`)在 `__tests__/` 下零覆盖
- 纯函数 + 无依赖 + 输入输出明确 → **测试性价比极高**
- 列为 P1 仅报告(要不要补、覆盖目标涉及质量策略,不自动写)

## 2. 已修复表
本轮**无 P2/P3 可自动修项**,跳过分支创建。

## 3. 待确认表(仅报告,不可动手)
| 级别 | 项目 | 建议 |
|---|---|---|
| **P1** | `lib/pipeline/utils.js` 整模块零单元测试 | 优先补 `isValidOrthogonalMatrixTokens`(L41-64,业务关键:挡住 LLM 给出的歪矩阵) + `extractLDraw`(L13,挡住 LLM 包 ``` 或 `<LDRAW>` tag) + `deduplicateLDraw`。每个 fn 写 3-5 个用例,预计 60-80 行测试代码 |
| **P1**(依赖) | `next@^14.2.0` 距 EOL 2026-10-21 仅 4 个月 | 升 15 / 16,前者用 codemod 平滑升,后者需评估 Turbopack 默认与 App Router 迁移成本(项目目前看像 Pages Router) |

## 4. 回滚表
无(本轮未触发自动修)。

## 5. 依赖动态
| 包 | 当前 | 最新关键事件 | 影响 |
|---|---|---|---|
| `three` | `^0.183.2` | r183 引入 RenderPipeline,与本项目锁定版本一致 | ✅ 已对齐,无 breaking |
| `@react-three/fiber` | `^8.18.0` | v9 已发布,主打 React 19 兼容,无破坏性变更;v10 才会引入 breaking | ⚪ 升级窗口宽,不急 |
| `next` | `^14.2.0` | **15 LTS 支持 2026-10-21 EOL**;16.2.7 已是当前 stable | 🟡 **P1**,需排期升级 |

## 6. 竞品动态
本日未触发(无 P0/P1 引发深查),配额留作下日。

## 7. 信息源
- Three.js 2026 综述:https://www.utsubo.com/blog/threejs-2026-what-changed
- R3F v9 release notes:https://github.com/pmndrs/react-three-fiber/issues/2338
- R3F v9 迁移指南:https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- Next.js EOL 时间表:https://eosl.date/eol/product/nextjs
- Next.js 15→16 升级:https://nextjs.org/docs/app/guides/upgrading/version-15
