---
title: "BrickHub Dream B 2026-05-27"
date: 2026-05-27
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-27

## 今日审查：`lib/pipeline/utils.js`

129 LOC，LDraw 文本处理工具集（extract/dedup/normalize/fallback），被 `lib/pipeline/index.js` 5 处调用。无直接单测，依赖 `generateIntegration` + `parseLDraw` 套件做集成验证。基线 37/37 pass。

### ✅ 已自动修复（PR #29）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | `deduplicateLDraw(code)` 传 null/undefined 即 `Cannot read properties of null (reading 'split')` 崩 | utils.js:1-3 | `(code ?? '').split(...)` 兜底，对齐 `normalizeLDrawMatrices` 已有写法 | P2 |
| 2 | `extractLDraw(text)` 同上 null 即崩 | utils.js:13-22 | `const src = text ?? ''` 统一兜底 | P2 |

**守卫**：`eslint utils.js` clean；`jest generateIntegration parseLDraw` 37/37 pass（无回归）。PR：https://github.com/wpsl5168/brickhub/pull/29

### 🔴 待人工确认（P1）

| # | 问题 | 位置 | 根因 | 建议方案 |
|---|------|------|------|---------|
| 1 | fenced-block 正则会误抽非 LDraw 代码块 | utils.js:17 ``` /```(?:ldraw)?\s*([\s\S]*?)```/``` | `(?:ldraw)?` 可选 + `\s*` 吞空白 → ` ```python\nprint(1)\n``` ` 也会被当成 LDraw 抽出，下游 normalize 时静默被过滤为空。理论上模型几乎不会产 python 块但 RAG/上下文回声里可能出现 | 改成必须要 ldraw 语言标签 ``` /```ldraw\s*\n([\s\S]*?)```/ ```，或保留宽松匹配但加 "内容须含 `^[01]\s` 行" 二次校验。涉及 prompt 链路输出契约，让老王拍板 |
| 2 | `normalizeLDrawMatrices` 静默把非轴对齐合法旋转重置为单位矩阵 | utils.js:60-78 | `isUnitAxis` 只接受整数 ±1/0，但 LDraw 规范允许任意正交旋转矩阵（如 45° Y 轴：cos/-sin/sin/cos 这种非整数值）。当前实现把所有非"正方"矩阵替换成 identity，意味着模型若正确输出旋转部件，会被悄悄"摆正"。`matrixFixCount` 会上报但没人监控 | 两种走向：(a) 真相是只支持轴向矩阵（性能/教学简化）→ 在函数 JSDoc 里写清"axis-aligned only"，并把 fix 数加入 telemetry 告警阈值；(b) 真想支持任意正交矩阵 → 改 `isUnitAxis` 为浮点正交检查（保留 dot=1 ± ε 等 invariant）。属业务定义题，绝不自动改 |

### ⚠️ 自动修复失败回滚

无。

## 依赖动态

| 依赖 | 当前 | 最新（兼容上限内） | 重要变更 |
|------|------|---------------------|---------|
| next | ^14.2.0 | 14.x 线最高 14.2.x；CVE 全栈受影响 | 🔥 **CVE-2026-23870 / -23869 / -45109 (HIGH 7.5)** DoS via App Router Server Functions，影响 13.x/14.x/15.x/16.x。**BrickHub 是 Pages Router**，**不直接受影响**，但补丁版 15.5.16/16.2.5+ 已发布。若未来迁 App Router 必须先升级 |
| three | ^0.183.2 | r183（r3f v8 peer 上限附近） | r183-r190 是 WebGPU 强化大行情，但 r3f v8 锁三件套（react 18 / r3f v8 / three ≤r183 左右），单独升 three 会触发 peer 警告。要联动升必须先迁 r3f v9 + react 19 |
| @react-three/fiber | ^8.18.0 | v8 线已稳定（v9 需 react 19） | v9 大版本带 WebGPU canvas、React 19 typed JSX。BrickHub 在 react 18 → 跟 v8 不动是正解 |
| @react-three/drei | ^9.122.0 | v9 仍在维护 | 跟 r3f v8 配套 |

**判断**：今日无紧迫升级。Next.js CVE 因 BrickHub 不使用 App Router 不直接受影响，但建议下次重大依赖周期顺手升 14.x 内 patch（14.2 最末一版）；R3F/three/react 是连体栈，要升一起升（react 19 + r3f v9 + three r190+ + WebGPU 迁移），属技术债季度规划题。

## 竞品动态

今日无重要竞品搜索（搜索预算用于安全 advisory 复核，已达 ≤6 上限）。

## 信息来源

- https://github.com/vercel/next.js/security/advisories/GHSA-q4gf-8mx6-v5v3 (CVE-2026-23869)
- https://advisories.gitlab.com/npm/next/GHSA-8h8q-6873-q5fj (CVE-2026-23870)
- https://nvd.nist.gov/vuln/detail/CVE-2026-45109
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- https://github.com/wpsl5168/brickhub/pull/29
