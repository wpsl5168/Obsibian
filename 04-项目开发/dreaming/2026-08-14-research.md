# Dream B — 2026-08-14

## 结论

审查 `lib/pipeline/utils.js`。无 P2/P3 自动修复，未创建分支/PR。新增 1 条 P1：矩阵 token 用 `parseInt` 解析，`1.5`、`1x` 会被截断成合法单位轴，畸形矩阵绕过归一化。Dream B 仍有 5 条 OPEN PR，距最近合并 42 天，流程继续梗死。

## 已修复

| ID | 位置 | 结果 |
|---|---|---|
| — | — | 0 项；无 P2/P3 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 发现与建议 |
|---|---|---|---|
| BUG-NEW-01 | P1 | `lib/pipeline/utils.js:42-45` | `Number.parseInt` 接受非整数字符串前缀；实测 `1.5`、`1x` 均被判为合法矩阵 token，`normalizeLDrawMatrices` 因此不修复畸形矩阵。建议改为严格 `Number(token)` + `Number.isInteger`，并补 malformed token 回归测试；涉及 AI 输出容错契约，不自动改。 |
| TEST-01 | P1 | `__tests__/` | 8 个 export 均无真实测试；矩阵校验/归一化、LDraw 提取和去重没有回归网，本次 bug 正由此漏出。建议新增 `pipelineUtils.test.js`，重点覆盖小数、尾随字符、NaN、正反射矩阵及 type-1 行。 |
| DEP-01 | P0 排期 | `package.json` | Next `14.2.35`，latest `16.3.1`，落后 2 个主版本且 14.x 无当前安全补丁线；需指定迁移周或明确 defer。 |
| DEP-02 | P1 | `next/image` ×3 | CVE-2026-64644 条件部分命中：自托管 + 默认 Image Optimization + remotePatterns 已具备，但当前固定可信 CDN `.png`，恶意 SVG 可达性未证实；不夸大为已可利用。 |
| PROC-01 | P1 | Dream B PR 队列 | Dream B OPEN 5（总 OPEN 6），最近合并 2026-07-02，已 42 天。需 batch review + merge/close。 |

## 回滚

| 文件 | 原因 | 结果 |
|---|---|---|
| — | 无改动 | 无回滚 |

## 验证链路

- `npx eslint lib/pipeline/utils.js`：通过。
- 真实 caller：`lib/pipeline/index.js` 调用 `extractLDraw`、`normalizeLDrawMatrices`、`deduplicateLDraw`、`buildOfflineFallbackLDraw`；模块非死代码。
- 测试检索：8 个 export 在 `__tests__/` 均为 0 引用。
- 最小复现：`isValidOrthogonalMatrixTokens(['1.5','0','0','0','1','0','0','0','1']) === true`；`'1x'` 同样为 `true`；畸形 type-1 行归一化计数仍为 0。
- Next CVE 命中面：Pages Router；无 middleware/proxy、App Router、Server Actions、Cache Components、RSC、动态 rewrite/redirect hostname、特定 `fetch(new Request...)`；`next/image` 3 处。
- SWC `14.2.33` 与 Next `14.2.35` 主版本一致，无 binding 撕裂。

## 依赖动态

| 依赖 | 项目 | 最新 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.1 | P0 迁移排期；官方 July 2026 安全修复线为 15.5.21/16.2.11，14.x 无补丁线 |
| @react-three/fiber | 8.18.0 | 9.7.0 | v9 面向 React 19 且有 migration breaking；与 Next 16/React 19 合并评估 |
| three | 0.183.2 | 0.185.1 | 落后 2 revisions；未检出需立即处理的 breaking |
| @next/swc-linux-x64-gnu | 14.2.33 | 随 Next | 主版本对齐，无 WASM fallback 撕裂 |

## 竞品 / 素材动态

| 项目 | 动态 | 判定 |
|---|---|---|
| LDraw Official Parts Library | 最新仍为 2026-07：414 个新文件、294 个新零件 | 后续只同步官方库，禁止 AI 编造 part ID |
| BrickLink Studio / Mecabricks | 未检出可信的新 breaking 动态 | 无动作 |
| GitHub LDraw 工具 | 未检出影响 BrickHub 的新 breaking release | 无动作 |

## 拍板清单

1. 修复矩阵 token 严格解析，并补 `pipelineUtils.test.js`。
2. 清理 PR #43–#47：Dream B 已 42 天零合并。
3. 给 Next 14→16 指定迁移周，R3F 8→9 / React 19 一并评估。

## 信息源

- Next.js 官方 July 2026 Security Release：https://nextjs.org/blog/july-2026-security-release
- Next.js 16.3 官方发布：https://nextjs.org/blog/next-16-3
- React Three Fiber v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- Three.js Releases：https://github.com/mrdoob/three.js/releases
- LDraw Official Library Update：https://library.ldraw.org/updates?latest=
- BrickLink Studio Update：https://www.bricklink.com/v3/announcement/studio_update.page
- npm registry：`npm view next/three/@react-three/fiber version`
