# Dream B 2026-07-15 审查:lib/pipeline/index.js

## 概览
- **文件**:`lib/pipeline/index.js` (231 LOC,AI 生成主管线;DSL/LDraw 双路 + telemetry + fallback)
- **PR 队列**:OPEN=5 (#43/44/45/46/47 全 CLEAN,均 Dream B 自动修复,全部未合)
- **自动修复**:0(lint clean;exports 全部有真实调用;无 P2/P3 命中)
- **P0/P1**:0/2(依赖 P0 延续 + TEST-P1 延续 + 新增 Next security release 窗口 P1)
- **熔断**:OPEN=5 < 8,未熔断

## Pre-PR 飞行检查
- **死代码 grep**(双写):`generateLDrawPipeline`/`getTelemetrySnapshot` 均被 `pages/api/generate.js:3` 真实拉起;`bumpTelemetry`/`noteFailureReason` 仅模块内自用(内部符号,非死符号,同文件多次调用)。**非死代码**
- **同文件 OPEN PR 检测**:5 条 OPEN PR 中无一命中 `lib/pipeline/index.js`。**无冲突**
- **历史 PR 考古**:`gh pr list --state all --search "pipeline/index"` 无直接命中;ad44ca00 整合 commit 已收编相关健壮性修复
- **PR 队列健康**:30 天 MERGED=0(最近合并 07-02 #35/36/39),OPEN=5;**PROC-P1 队列梗死延续**(轻度,未过 10 条阈值,但已 13 天零合并)

## 待确认(P0/P1,仅报告不动手)

| ID | 级别 | 位置 | 判定 | 建议 |
|---|---|---|---|---|
| DEP-P0 | P0 | package.json | Next 14.2.35 落后两大版本(16.3 已 stable ≥30 天,16.2/16.3 相继发布) | 排期迁移周或明确 defer 到 Phase X;第 8 天延续,老王仍未拍板 timeline |
| DEP-P1-CVE | P1 | package.json | Next 14.x EOL 无补丁,累积多个 High CVE(SSRF/DoS 命中面);官方计划 **2026-07-20 security release**(距今 5 天),14.x 大概率仍不覆盖 | 关注 07-20 上游公告,若再爆 High + 14.x 不 patch → 升 P0 强推迁移 |
| TEST-P1 | P1 | `__tests__/generateIntegration.test.js:15,29` | 整模块 `jest.mock('../lib/pipeline/index.js', () => ({...}))` stub — `generateLDrawPipeline`/`getTelemetrySnapshot` 零真实覆盖;`bumpTelemetry`/`noteFailureReason`/`getValidationSignature` 亦零覆盖;pipeline 主管线(dsl 编译+fallback+geo 校验+telemetry)是 AI 生成核心链路却无端到端测试 | 补 `__tests__/pipelineIndex.test.js`:真实调用 `generateLDrawPipeline` 用 mocked llm(仅 mock `./llm` 底层)覆盖 DSL/LDraw fallback/offline-fallback/validation-stall 4 条路径。第 3 次延续(07-06/07-09/07-15) |

## 回滚
- 无(0 修复无 PR)

## 依赖动态
- **Next.js 14.2.35** — EOL 已过 4 个月(patches not planned);官方博客明示 **2026-07-20 security release**,预计仅 patch 16.x/15.x
- **Next.js 16.3** — 已 stable(Turbopack 默认,Cache Components);16.4 preview 出 Instant Navigations
- **@react-three/fiber / three / LDraw parts** — 无 breaking 变更

## 竞品动态
- 本轮无搜索预算(依赖占用 1 次;总 ≤6 未跑竞品,轮到明日看)

## 拍板清单(待老王)
1. **DEP-P0 Next14→16 迁移**:延续第 8 天,已到必须给 timeline 或明确 defer 决定的窗口
2. **PR 队列**:OPEN=5 (#43/44/45/46/47) 全 CLEAN 13 天零合并;考虑批量 review + merge 释放队列(否则 Dream B 下轮 restart 到 BrickRenderer/PartsTray/sandbox/model 会再触发同文件冲突)
3. **07-20 上游 security release** 观察窗口 — 若 14.x 再爆 High CVE 无 patch → DEP 升 P0 强推
4. **TEST-P1 pipelineIndex.test.js**:第 3 次延续,AI 生成核心链路无端到端测试

## 信息源
- Next.js 16 blog:https://nextjs.org/blog/next-16
- Next.js blog index(security release 公告):https://nextjs.org/blog
- Next.js 16.2 blog:https://nextjs.org/blog/next-16-2
