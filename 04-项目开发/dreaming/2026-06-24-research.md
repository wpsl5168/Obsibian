# Dream B 日审 — 2026-06-24

**审查文件**: `components/FreeBuildTray.js` (13 KB, 351 行 — 自由建造模式的 FAB+三态抽屉)
**轮换距上次**: 19 天 (上次 2026-06-05 = #35)
**结论**: 0 自动修复 PR(被 PR 队列梗死 + 同文件冲突双重 gate 拦截);1 条 PROC-P1 流程告警 + 3 条 UI/A11Y 问题报告

---

## 🚨 重大流程告警 — PROC-P1(必报)

### 现象
- OPEN dreamB PRs: **30**(阈值 >20 = 队列过载)
- 近 30 天(2026-05-25 起) MERGED: **0**
- 触发 skill `dream-b-pre-pr-pre-flight.md` 中 PROC-P1 双重命中

### 影响
1. **PR 队列累积**:从 #13 (2026-05-06) 到 #42 (2026-06-14),全部 `mergeStateStatus=CLEAN` 但 0 合并,Dream B 每天在抹奶油但没人吃
2. **回归风险**:30 个未合 PR 之间相互冲突的可能性随时间指数上升,后期 rebase 成本暴涨
3. **本日决策依据**:同文件 (`components/FreeBuildTray.js`) 已存在 OPEN PR
   - #35 (2026-06-05) — FreeBuildTray a11y
   - #36 (2026-06-07) — sandbox a11y(同时改了 FreeBuildTray.js)
   - #19 (2026-05-14)、#18 (2026-05-13) — 也都改过同文件
   → 即使开新 PR 也会与 #35/#36 冲突,且 17~50 天没被 review

### 建议(待老王拍板)
1. **本周内集中清账**:挑出最早 10 个 PR 决策 merge / close,把队列拉到 ≤ 10
2. 若长期无暇 review → 暂停 Dream B 自动修复模式,改为「仅 Obsidian 报告,不开 PR」直到队列消化
3. 评估是否撤销 `dreamB/auto-fix-2026-05-09 ~ 20260514` 这批 6 周前的早期 PR(代码可能已被后续 main 改写)

---

## 已自动修复表

| ID | 类型 | 说明 |
|---|---|---|
| — | — | **0 修复**:Pre-PR pre-flight 拦截 |

**拦截原因**:
- **Gate 1 (PR-COLL)**: 同文件已有 OPEN PR #35 / #36(>30 天 + >17 天未合)
- **Gate 2 (PROC)**: 总 OPEN=30 (>20),近 30d MERGED=0 → 队列梗死

---

## 待确认表(交老王判定;均非红线,优先级排序)

| ID | 等级 | 位置 | 问题 | 建议改法(暂不动) |
|---|---|---|---|---|
| UI-1 | **P1** | line 225 / 263 | `scrollbar-hide` class 被使用,但 `package.json` 无 `tailwind-scrollbar-hide` 插件,`tailwind.config.js` 也无 plugin 引用 → 该类被 Tailwind purge 后变为**未生成 CSS**,移动端会出现 native 滚动条破坏 FAB 抽屉的 polish 感 | 二选一:① `npm i -D tailwind-scrollbar-hide` 并在 `tailwind.config.js` plugins 加入;② 改为内联 `style={{ scrollbarWidth: 'none', msOverflowStyle: 'none' }}` + `::-webkit-scrollbar { display: none }` global CSS |
| A11Y-1 | P2 | line 172-196 (FAB) | 按钮控制抽屉三态(closed/peek/open) 但缺 `aria-expanded` / `aria-controls`,屏幕阅读器无法感知抽屉是否展开 | `aria-expanded={!isClosed}` `aria-controls="freebuild-drawer"`,并在 drawer div 加 `id="freebuild-drawer"` |
| TOUCH-1 | P2(规范) | line 248-256 (X 关闭按钮) | `w-10 h-10` = 40×40px,**低于 44px 最低触控阈值**;儿童红线 56px 也不满足 | 改为 `w-11 h-11`(44px,踩线)或 `w-12 h-12`(48px) |
| TOUCH-2 | P2(规范) | line 273-282 (颜色圆点) | `width: 32, height: 32` = 32×32px,**远低于 44px 最低**且远低于 56px 儿童红线 | 外包一个 44×44 透明 padding 容器(保留视觉 32px 圆点居中),或直接放大到 44 |
| A11Y-2 | P3 | line 227-244 (category 按钮) | 已有 `aria-label` 但缺 `aria-pressed={activeCategory === idx}` 表示当前激活 tab(下方零件按钮 309 行有,色块 270 行有,唯独 category tab 漏了) | `aria-pressed={activeCategory === idx}` |

**为何不开 PR**:全部 P1/P2/P3 一并卡在 pre-flight。改了等于在 30 PR 的堆上再加 1;且 UI-1 涉及 deps 安装,严格属于 P1(配置变更),按规则就不能自动改。

---

## 自动验证(Pre-PR Pre-Flight)

| 检查 | 命令 | 结果 |
|---|---|---|
| 文件 liveness | `grep -rn FreeBuildTray pages components lib __tests__` | ✅ 活跃(`pages/sandbox.js:9` + `__tests__/freeBuildTray.test.js`) |
| 同文件 OPEN PR | `gh pr list --search "head:dreamB"` | ❌ **冲突** PR #35 / #36 已改同文件 |
| 队列健康 | OPEN=30 / 30d MERGED=0 | ❌ **PROC-P1 梗死** |
| eslint baseline | `npx eslint components/FreeBuildTray.js` | ✅ clean |

---

## 依赖动态(总搜索:本日 0 次)

跳过依赖搜索,本日预算节省到流程整顿。最近 7 日依赖结论(沿用 2026-06-18 报告):
- **Next.js 14.x EOL + 高危 CVE 未修补 → P1** 强提醒已挂账,建议升 15 或 16
- Three.js / R3F / LDraw parts 无 breaking 进展

## 竞品动态
跳过(本日预算节省)

---

## 信息源 / 验证日志
- `~/.hermes/skills/dreaming/SKILL.md` Pre-PR pre-flight 节
- `~/.hermes/skills/dreaming/references/dream-b-pre-pr-pre-flight.md`
- `~/.hermes/skills/dreaming/references/dream-b-ui-css-deadpattern-checks.md`(scrollbar-hide pattern 已在此条目中预警过)
- `gh pr list` 输出已落 `/tmp/open.json` `/tmp/merged.json`
- 文件路径:`/home/wpsl5168/workspace/brickhub/components/FreeBuildTray.js` git HEAD

---

## 下一步(留给老王)
1. 决策 PR 队列:批量 close 还是补 review?
2. UI-1 scrollbar-hide:装插件 OR 改 CSS-in-JS?(15 min 工作量)
3. 评估是否把 Dream B 改为「仅报告模式」直至队列 ≤ 10
