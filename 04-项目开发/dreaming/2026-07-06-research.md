# Dream B BrickHub 日审 — 2026-07-06

**审查文件**:`lib/pipeline/index.js`(232 LOC,AI 生成主管线:DSL→LDraw fallback + validation retry + offline fallback,`pages/api/generate.js` 唯一 caller,生产核心)
**队列状态**:OPEN dreamB PR = 2(#43 BrickRenderer 死代码清理 + #44 FreeBuildTray 触控)< 8,未熔断
**自动修复**:0(P2/P3 无 issue,lint clean,jest 6/6 pass)
**开 PR**:❌ 无

---

## 4 维度扫描

### ✅ Bug 隐患:0(表面可疑先跑三重验证,均通过)

| 位置 | 表面现象 | 判定 |
|---|---|---|
| L173 `stalledValidation` 短路 | `errors.length > 0 && signature === previousSignature` 首轮 `previousValidationSignature=''` 时若 signature 也是 `""` 会误命中 | **无 bug**,因 signature 由 `getValidationSignature(errors)` 计算,errors 非空时至少产 `{"unknown":1}` → signature 永不为 `""`;`previousValidationSignature` 只在 L212 循环末尾赋值,首轮不可能等于当轮 signature |
| L145 `useDSLPath=false` 后未在下一循环重置 | 一旦降级 LDraw 就永久不再回 DSL | **设计如此**,已在 `pipelineStats.mode='ldraw-fallback'` 显式记录,配合 `maxRetries=1` 只做单次降级;非 bug |
| L207 类型推断 `error?.type \|\| /.../.test(msg)` | 优先级模糊 | JS 短路正常,`type` 存在则直接取;不影响 |

**eslint**:0 warning / 0 error
**jest `__tests__/generateIntegration.test.js`**:6 pass 0 fail 0.951s

### ⚠️ 架构:pipeline 核心生产链路健康,但被测试破产遮蔽(见下 TEST-P1)

- `generateLDrawPipeline` 承担 DSL 编译 → LDraw 降级 → 几何校验 → offline fallback 完整决策链
- 4 个 export(`generateLDrawPipeline / getTelemetrySnapshot / bumpTelemetry / noteFailureReason`)在 `pages/api/generate.js:3` 真实 import,**非死代码**
- 通过 `hooks.onEvent` 与 SSE 前端解耦,结构合理
- 无内部符号/常量残留(`awk` 顶层符号扫已跑过,均活)

### ✅ UI 规范:N/A(pipeline 是纯后端逻辑,无 JSX)

### 🔴 测试覆盖:TEST-P1 测试破产(高优,与 06-23 knowledgeBase 同性质)

**证据链**:
```bash
grep -rln "generateLDrawPipeline\|bumpTelemetry" __tests__/
# → 仅 __tests__/generateIntegration.test.js

head -25 __tests__/generateIntegration.test.js
# → jest.mock('../lib/pipeline/index.js', () => ({
#     generateLDrawPipeline: jest.fn(async () => ({ ldraw: '...', errors: [], ... }))
#   }))
```

- 整个 pipeline/index.js 200+ LOC 被 `jest.mock` 换成假函数 → **零真实覆盖**
- 未跑到的关键路径:
  - `dsl_compile_failed` 降级(L125-136)
  - `missing_lego_tag` 降级(L137-148)
  - `validation_stalled` 侦测 + `autoFallbackOnValidation` offline 兜底(L173-193)
  - `upstream_error` catch → offline fallback(L217-229)
  - Retry 循环(L93,`maxRetries` 边界)
- **影响**:儿童 AI 生成链路的守门员逻辑改动无回归网,任何一次 LLM prompt/DSL schema/几何校验规则调整都可能静默破坏 fallback 路径

**修法二选一**(由老王拍板):
- **推荐 (a)**:单独建 `__tests__/pipeline.test.js`,用 mock provider (`jest.mock('./llm', ...)`)覆盖 DSL/LDraw/offline 三条路径 + 各自 retry 场景,保留 generateIntegration 现有 HTTP mock 层
- **备选 (b)**:在 generateIntegration 里去掉 `jest.mock('./pipeline/index.js')`,mock 更底层的 `callProviderWithFallback` — 会大幅增加 fixture 维护成本,不推荐

---

## 依赖动态

| 依赖 | 当前 | 状态 | 备注 |
|---|---|---|---|
| next | 14.2.35(package.json `^14.2.0`) | 🔴 EOL + High CVE 无补丁 | 详见"依赖暴露"独立段 |
| @next/swc-linux-x64-gnu | 14.2.33 | ✅ | 与 next 14.2.35 同 minor,patch 差 2(binding registry 常规延迟),binding 一致性验证通过 |
| three | 0.183.2 | ✅ | r182 deprecate `PCFSoftShadowMap`,brickhub 未使用(grep 0 命中);无 breaking |
| @react-three/fiber | 8.18.0 | ✅ | v9 无 runtime breaking(仅 TS 相关,配合 React 19),暂无升级压力 |
| @react-three/drei | 9.122.0 | ✅ | 与 fiber v8 兼容 |

### 🔴 依赖暴露:Next.js 14.2 EOL + Vercel 5 月安全 release(持续 P1 强提醒)

- 2026-05 Vercel 官方 changelog:一次性修 **13 条**跨 DoS/middleware bypass/SSRF/cache poisoning/XSS 的 advisory,**14.x 全版本受影响**,官方 patch 只到 15.5.18 / 16.2.6
- **适用性核实**(遵循 skill 07-02 pitfall):
  - ❌ Middleware/proxy bypass — brickhub 无 `middleware.js`/`proxy.js`,Pages Router 不适用
  - ❌ DoS via RSC / Cache Components / PPR — 无 App Router,不适用
  - ⚠️ Image Optimization API DoS — 3 处 `next/image` 使用,**适用但强度低**(DoS 非 RCE/auth bypass)
  - ⚠️ SSRF — pages/api 有 upstream LLM 代理,理论适用面
- **非紧急,但需排期**:与 07-01 SSRF 判定一致,升级窗口 3-6 个月;不做 WAF 特设,直接排 Next 15 迁移

## 竞品动态

- Bricklink Studio:2.24.3 稳定(6 月中),无新动作
- Mecabricks:无重大更新
- 无 GitHub 热门 LDraw 项目趋势变化

## 信息源

- pmndrs/react-three-fiber issue #2338 / #3410(v9 migration)
- three.js discourse — r181→r182 shadowmap 变更
- Vercel changelog — Next.js May 2026 security release
- HeroDevs — Next.js EOL 时间线

---

## 拍板清单

1. **TEST-P1 pipeline 测试破产** — 建独立 `__tests__/pipeline.test.js` 覆盖 4 条 fallback 路径(dsl_compile_failed / missing_lego_tag / validation_stalled / upstream_error)+ retry 边界,预计工作量 2-3h。**优先级高于 UI a11y 补丁**,守门员逻辑无回归网风险最大
2. **Next.js 14→15 迁移排期** — 与 07-01 报告一致,SSRF+Image DoS 适用面确认后建议 30 天窗口启动,方案在 07-01 报告已列
3.(可延)本轮踩坑:`grep --include="*.{js,jsx,ts,tsx,md}"` 引号内 brace expansion 被 shell 抑制 → 0 命中假死代码判定,已计划回补进 dreaming skill(见 skill patch 计划)

---

## Meta

- Pre-PR 飞行检查:全过(0 P2/P3 → 未开 PR)
- 死代码验证踩坑:首轮 grep 引号 `"*.{js,jsx,ts,tsx,md}"` 未展开 → 假死代码;第二轮 `grep -n "^import" pages/api/generate.js` 直接查证 → `pages/api/generate.js:3` 真实 import,链路活
- 未熔断:OPEN dreamB=2 < 8
- 用时:约 6 min,搜索 3 次(未过 6 上限)
