# Dream B — 2026-09-06

## 结论

审查 `lib/knowledgeBase.js`。发现 1 个新增 BUG-P1：分类器无命中时强制回退 `vehicle`，使「dragon / 机器人 / 猫」等请求被汽车知识污染。无 P2/P3，不改业务策略、不创建 PR。Dream B 队列 6 条 OPEN，全部 CLEAN，但已 66 天零合并，流程继续梗死。

## 审查与验证

- 目标：`lib/knowledgeBase.js`（第五轮）
- 四维：Bug 隐患 / 架构 / UI 规范 / 测试覆盖
- ESLint：通过
- 调用链：`pages/api/generate.js`、`pages/api/retrieve.js` 真实引用，非死代码
- 现有测试：`__tests__/generateIntegration.test.js` 6/6 通过，但 `jest.mock('../lib/knowledgeBase')` 整模块替身，生产实现仍零真实覆盖
- 边界探针：已实跑 `detectQueryCategory`、`searchKnowledge(topK)` 与 prompt formatter

## 已修复

| 项目 | 结果 |
|---|---|
| P2/P3 | 0；无代码改动、无新 PR |

## 待确认（P0/P1）

| 等级 | 发现 | 证据与建议 |
|---|---|---|
| BUG-P1（新增） | 无类别命中时默认 `vehicle`，无关请求被强灌汽车知识 | `detectQueryCategory('dragon'/'机器人'/'猫') => {key:'vehicle',score:0}`；随后 Top 4 以 vehicle 为主且得分 7。需业务拍板：`score=0` 返回 `unknown/null` 并取消 category bonus，或扩完整分类词表；必须补 unknown-category 回归测试 |
| BUG-P1（延续） | 子串双向匹配被单字母 token 污染 | 实测 `a race car => ship`、`i want a castle => aircraft`。建议英文 exact/boundary、中文 substring 分轨 |
| API-P1（延续） | 非法 `topK` 静默返回空结果 | 实测 `topK='abc'` 得 0 条且 API 仍可返回 HTTP 200；建议 API 层 400 或回退默认值 |
| TEST-P1（延续） | 7 个 export + 内部 ranking/cache helper 零真实测试 | 集成测试整模块 mock；新增 unknown-category、单字母、非法 topK、缓存失效测试 |
| PROC-P1（延续） | Dream B 队列梗死 | 6 条 Dream B OPEN PR 全 CLEAN；最近合并为 #39（2026-07-02），已 66 天零合并。先 batch review/merge/close |
| DEP-P0（延续） | Next 14.2.35 落后 Next 16.3.4 两个大版本且 14 已停止新安全补丁 | 16.3 stable 已超过 30 天；必须给迁移周或明确 defer 节点。R3F 8→9 与 React 18→19 应并入同一迁移验证 |
| DEP-P0（延续） | August Critical AVIF RCE 暴露 | `GHSA-2xp9-vwfh-vxw4`：版本命中（14.2.35）+ `next/image` 3 处；未配置 AVIF、remotePatterns 仅固定 CDN，可达性未证实。Windows RCE 不适用（Linux、无 app/） |

## 回滚

| 项目 | 结果 |
|---|---|
| 自动修复 | 0；无需回滚 |

## PR 队列

- Dream B OPEN：6（#43–#48），均 `CLEAN`
- 仓库 OPEN 总数：7
- 最近 merged：#39，2026-07-02
- 判断：`OPEN≥3 且 14 天无合并`，命中 PROC-P1；当前为 66 天

## 依赖动态

- Next：本机 `14.2.35`，npm latest `16.3.4`；SWC `14.2.33`，主版本一致，无 binding 撕裂。
- Next CVE 命中面：7 类已扫。App Router / middleware / proxy / Cache Components / Server Actions / RSC / 特定 Request fetch 均 0；`next/image` 3 处。August AVIF RCE 为版本+功能命中、可达性未证实。
- Three.js：本机 `0.183.2`；未发现今日新增 breaking。R3F v9 仍是 React 19 compatibility release，Strict Mode 有 breaking，项目当前 R3F 8 + React 18，不宜单独跳版本。
- LDraw：本次搜索未发现 9 月新官方 Parts Update；无 breaking。

## 竞品动态

- BrickLink Studio：2.26.8_1（2026-08-27），仅 bug fixes，无需跟进。
- Mecabricks：未检索到可信产品级新版本或 breaking 动态。
- GitHub LDraw 项目：未发现影响 BrickHub 的新 breaking；Three LDraw/WebGPU 兼容讨论不影响当前 WebGL 路线。

## 信息源

- Next.js August 2026 Security Release：https://nextjs.org/blog
- R3F v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- BrickLink Studio 下载与 release notes：https://www.bricklink.com/v3/studio/download.page
- LDraw 官方论坛：https://forums.ldraw.org/
- GitHub ldrawloader：https://github.com/pixeljetstream/ldrawloader
- 本地证据：`package.json`、`next.config.js`、`lib/knowledgeBase.js`、`pages/api/{generate,retrieve}.js`、`__tests__/generateIntegration.test.js`
