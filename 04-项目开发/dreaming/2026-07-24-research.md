# Dream B 2026-07-24 — lib/pipeline/index.js(第三轮 day 6)

## TL;DR
第三轮 restart 第 6 天,连续 6 天 W28 陷阱命中(#43~#47 未合，新一轮首选与上轮 PR 目标撞车)。**队列梗死升级第 8 天,22 天零合并**（07-02 后无 MERGED）。本轮零修复:目标文件 lib/pipeline/index.js 上轮 07-15 已完整审计，本次仅回归验证 + 依赖/CVE 延续。**PROC-P1 队列梗死 + DEP-P0 Next14→16 落后两大版第 17 天** 并列拍板阻塞项。

## Pre-PR 飞行检查
- 队列熔断闸: OPEN=5 (<8) 未熔断
- 死代码 grep: `pages/api/generate.js:3` 真实 import `generateLDrawPipeline`, `getTelemetrySnapshot` — **非死代码**（brace-expansion 陷阱已避,用多 --include）
- 同文件 OPEN PR 冲突: `head:dreamB` OPEN 5 条无 `pipeline/index.js` 命中 → **通过**
- 历史 PR 考古: `gh pr list --state all --search "pipeline/index"` 无历史 PR
- PR 队列健康: **OPEN=5, 22 天零合并 → PROC-P1** (阈值:14d 零合并 + OPEN≥3)

## 已修复(P2/P3)
无。目标文件 lint clean、exports 全被 `pages/api/generate.js` 拉起、上一轮已充分审计,本轮无新增可自动修项。

## 待老王拍板(P0/P1)
| ID | 级 | 项 | 说明 | 修法 |
|---|---|---|---|---|
| DEP-P0 | P0 | Next 14→16 主版本落后 2 大版 | 16.3 stable 已 11 天,brickhub 14.2.35 落后两大版第 17 天,报警渲染为日常噪音 → **必须给出迁移周排期或 defer 到 Phase X 决定** | 明确 timeline 或 defer |
| PROC-P1 | P1 | 队列梗死第 8 天 | OPEN=5 (#43~#47) CLEAN 全绿,22 天零合并(自 07-02 #39 后),Dream B 第三轮 restart 连续 6 天被同文件 OPEN PR 拦截,无法开新 PR | **批量 review + merge #43~#47** 才能让 Dream B 第三轮真正推进 |
| PR-COLL-P1 | P1 | #43~#47 阻塞 Dream B 全部 10 核心文件轮换 | 每天 restart 首选文件都撞现存 PR | 同上,合并即解 |
| CVE-P1 | P1 | Next 14.x `next/image` × 3 命中 May-2026 Image Optimization SSRF/DoS | brickhub 3 处 `next/image` 使用 | 随 DEP-P0 迁移解决 |
| TEST-P1 | P1 | `lib/pipeline/index.js` jest.mock 整 stub 零真实覆盖 | `__tests__/generateIntegration.test.js:14 jest.mock('../lib/pipeline/index.js', () => ({ generateLDrawPipeline: jest.fn(...) }))` | 拆出 integration test 走真实 pipeline |

## 回滚
无。

## 依赖动态(≤6 搜索,本次 0)
- Next.js 14.2.35: EOL + 高危 CVE 无补丁, 16.3 stable 已 11 天 → **P0**（升级为 P0 已第 17 天）
- Three.js / R3F / LDraw: 无 breaking 新动态
- swc binding `@next/swc-linux-x64-gnu@14.2.33` vs `next@14.2.35`: 主版本一致,无 WASM 撕裂

## 竞品动态
无。

## 信息源
- 目标: `~/workspace/brickhub/lib/pipeline/index.js`
- 上轮报告: `2026-07-15-research.md`(同文件完整审计)
- Pre-PR: `dreaming` skill 全套通过
- Rotation: `~/.hermes/cron/output/dream_b_rotation.json`

## 建议:一键清账脚本
```bash
cd ~/workspace/brickhub && export PATH=~/bin:$PATH
# 逐条 review + merge (低危 a11y/触控/死代码清理,全 CLEAN)
gh pr view 43 && gh pr merge 43 --squash --delete-branch
gh pr view 44 && gh pr merge 44 --squash --delete-branch
gh pr view 45 && gh pr merge 45 --squash --delete-branch
gh pr view 46 && gh pr merge 46 --squash --delete-branch
gh pr view 47 && gh pr merge 47 --squash --delete-branch
```
合完 Dream B 第三轮才能真正推进,否则 07-25 首选又将撞 #43 BrickRenderer.js 循环。
