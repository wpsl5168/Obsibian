# Dream B 2026-08-09 — `pages/model/[id].js`

## 结论

- 自动修复：0；未开 PR。同文件已有 OPEN PR #46，Pre-PR 冲突闸拦截。
- P0：1（Next.js 14→16 迁移需排期）；P1：4；P2：1（因 PR 冲突未修）。
- lint 通过；`__tests__/modelDetail.test.js` 2/2 通过。
- Dream B 队列 5 条 OPEN，全部 CLEAN；距最近 merge 38 天，流程持续梗死。

## 已修复

| 项目 | 结果 |
|---|---|
| 自动修复 | 无；同文件 PR #46 拦截 |

## 待确认（P0/P1）

| 级别 | 位置 | 发现 | 建议 |
|---|---|---|---|
| DEP-P0 | `package.json` | Next 14.2.35 已 EOL，当前稳定线已到 16.x，落后 2 个大版本超过 30 天 | 明确迁移周；优先评估受支持的 16.2.x/16.3 stable |
| PR-COLL-P1 | PR #46 | 该 PR 已改本文件触控 44/48→56 与清单 aria，CLEAN 32 天；阻塞今日 P2 | 老王先 review/merge；若弃用则 `gh pr close 46 --comment "被后续统一修复取代"` |
| PROC-P1 | PR #43–#47 | 5 条 Dream B PR OPEN，最近 merge 为 07-02，已 38 天 | batch review + merge/close，解除轮换阻塞 |
| A11Y-P1 | 301–318 | 清单面板隐藏时仅 `pointer-events-none`，内部关闭按钮仍可 Tab 聚焦，形成不可见焦点 | 业务拍板：关闭时 `inert`/条件渲染；不可只改 CSS |
| LIFE-P1 | 55–58 | `useCallback` 内 700ms timer 无卸载清理；快速切模型/卸载后仍可能 setState | 用 `useRef` 保存 timer，并在 `useEffect` cleanup；禁在 `useCallback` return 假 cleanup |

## 未能自动修复 / 回滚

| 级别 | 位置 | 项目 | 原因 |
|---|---|---|---|
| P2 | 325–354 | 可点击零件行是 `<div onClick>`，无 `role/button`、`tabIndex`、键盘 handler、focus ring | 同文件 OPEN PR #46，按 Pre-PR 红线不另开冲突 PR；未改代码 |

## 验证

- `npx eslint 'pages/model/[id].js'`：通过。
- `npx jest __tests__/modelDetail.test.js --runInBand`：2/2 通过。
- 页面生产入口：Next.js file route；测试真实 import 页面，非整模块 mock。
- PR 考古：OPEN #46；CLOSED #37；MERGED #8。

## 依赖动态

- Next.js：项目实际 14.2.35；SWC 14.2.33，同为 14.x，无 binding 主版本撕裂。
- July 2026 官方安全修复仅在 15.5.21 / 16.2.11；14.x 无修复线。
- CVE 命中面复核：无 middleware/proxy、App Router、Cache Components、Server Actions、RSC、动态 rewrite/redirect、特殊 `fetch(new Request...)`；`next/image` 3 处。
- CVE-2026-64644：项目自托管 + Image Optimization + remote patterns 条件部分命中；当前 URL 固定 Rebrickable/BrickLink `.png`，恶意 SVG 可达性未证实。维持 P1，禁夸大为已可利用。
- Three.js：项目 r183，官方 r185；小版本差距，未见必须立即处理的 breaking。
- R3F：项目 8.18，最新稳定 9.7；大版本迁移需结合 React/Next 升级，不单独抢跑。
- LDraw：2026-07 官方库新增 294 个零件；素材继续只用官方 Parts Library。

## 竞品动态

- BrickLink Studio / Mecabricks：本轮未发现可验证的 breaking 或必须跟进动作。
- GitHub LDraw：`ldr_tools_blender` 继续以 Rust 处理核心强化导入性能，属长期架构参考；无当日动作。

## 信息源

- https://nextjs.org/blog/july-2026-security-release
- https://vercel.com/changelog/next-js-may-2026-security-release
- https://github.com/mrdoob/three.js/releases
- https://github.com/pmndrs/react-three-fiber/releases
- https://library.ldraw.org/library/updates/
- 本地：`package.json`、`pages/model/[id].js`、`__tests__/modelDetail.test.js`、GitHub PR #43–#47
