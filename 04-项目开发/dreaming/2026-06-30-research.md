# Dream B 2026-06-30 BrickHub 质检报告

**今日审查文件**: `lib/dslCompiler.js` (722 LOC, 21 天未轮)
**结论**: 🔴 **整个 `lib/dslCompiler.js` 是死代码** — 与同期发现的 `lib/llmPrompt.js`、`lib/generateClient.js`、`lib/sampleLdraw.js` 一并构成 **lib/ 死代码姐妹批**。**不开 PR**(skill「Pre-PR 飞行检查」死代码检测+同文件 OPEN PR 冲突双重红线触发)。改报 **ARCH-P1 + PROC-P1**,请老王拍板归档/删除 + 关闭历史 PR #17/#38。

---

## ⚠️ 核心发现:lib/dslCompiler.js 是死代码,Dream B 在其上已抹了 2 次奶油

### 引用计数(grep 实测)

| 文件 | LOC | exports | 业务引用数 | 测试覆盖 | 首创 | 最后被业务动 |
|---|---|---|---|---|---|---|
| `lib/dslCompiler.js` | 722 | 2 (`extractDSL`, `compileDSL`) | **0** | **0**(无 jest.mock) | 2026-04-09 (RAG/双模生成) | **从未** — 最后 commit `133beb2b` 是 Dream B 自己的清理(2026-04-27) |
| `lib/llmPrompt.js` | 898 | 4 | **0** | **0** | 2026-04-09 (Phase 1 LLM pipeline) | 2026-04-10(最初 phase 1 收口) |
| `lib/generateClient.js` | 45 | 1 | **0** | **0** | 2026-04-09 (reorganize home flow) | 2026-04-09 |
| `lib/sampleLdraw.js` | 5 | 1 | **0** | **0** | 2026-04-09 | 2026-04-09 |

合计 **1670 LOC 死代码**,4/9 个 `lib/*.js` 文件无意义占位。

### 生产路径(对照实证)

`pages/api/generate.js:3`:
```
import { generateLDrawPipeline, getTelemetrySnapshot } from '../../lib/pipeline/index.js'
```

→ 生产 API 走 `lib/pipeline/{index,llm,utils}.js`,**完全绕过** `lib/dslCompiler.js` 与 `lib/llmPrompt.js`。
后者是 4 月初 Phase 1 的旧版实现,被 RAG/pipeline 重构替换后**遗留在仓库未删**。

### Dream B 历史污染(强证据)

OPEN 状态、从未 merge、都是抹在死代码上:

| PR # | 日期 | 标题 | 状态 |
|---|---|---|---|
| **#17** | 2026-05-11 | dslCompiler NaN 链式污染 | OPEN — 死代码上的 P2 修复,无意义 |
| **#38** | 2026-06-09 | dslCompiler.js null-guard + Next.js 14 安全告警 | OPEN — 死代码上的 P2 修复,无意义 |

→ Dream B 在过去 60 天里轮换到 `dslCompiler.js` **4 次**(2026-04-27 / 05-11 / 06-09 / 06-30),每次都在死代码上发现"问题"并开 PR。这正是 Dream skill ⚠️ 节点名的「不要在死代码上抹奶油」反模式。

### 30 秒验证(三重)

```bash
# 1. import + 调用 NaN 路径 — 确实有真实的 NaN 写入 bug
node /tmp/dsl-check.mjs
# Test 1 (color='red'): ldrawCode 写出 "1 NaN  0 0 0 ..." — color 不做 isFinite 检查
# Test 2 (y_ldu='abc'): ldrawCode 写出 "1 4  0 NaN 0 ..." — y_ldu 不做 isFinite 检查
# Test 3 (layer='foo'): warnings 命中 — layer 已被 isFinite 防御(PR #17/#38 修过的部分)

# 2. 业务引用 grep
grep -rln "dslCompiler\|extractDSL\|compileDSL" --include="*.{js,jsx,ts,tsx}" pages components lib __tests__ scripts
# → 仅 lib/dslCompiler.js 自身

# 3. 同目录姐妹扩张
for f in lib/*.js; do
  name=$(basename "$f" .js)
  cnt=$(grep -rln "lib/$name'\|from.*lib/$name" --include="*.{js,jsx,ts,tsx}" pages components lib __tests__ | grep -v "^$f$" | wc -l)
  echo "$name: $cnt"
done
# dslCompiler: 0  generateClient: 0  llmPrompt: 0  sampleLdraw: 0
# instancingPipeline: 1  knowledgeBase: 3  ldrawIdbCache: 1  ldrawParser: 6  runtimeConfig: 3
```

bug 是真 bug(`color`/`y_ldu` 无 isFinite 守卫),但**修在死代码上等同空气**。

---

## 待确认 (P0/P1/P2 仅报告,Dream B 不动手)

| ID | 等级 | 位置 | 现象 | 推荐处置 |
|---|---|---|---|---|
| **ARCH-P1** | 死代码批量 | `lib/dslCompiler.js` (722) + `lib/llmPrompt.js` (898) + `lib/generateClient.js` (45) + `lib/sampleLdraw.js` (5),合计 1670 LOC | 0 业务引用,0 真实测试。生产 pipeline 在 `lib/pipeline/*.js`,这批是 4 月初 Phase 1 遗留 | **一次性归档**:`git mv lib/{dslCompiler,llmPrompt,generateClient,sampleLdraw}.js lib/_archive/legacy-phase1-deprecated-20260630/` 或直接 `git rm`。**Dream B 必须从轮换表 `dream_b_rotation.json` 把这 4 个文件移除**,否则下次 7 天后又会被轮到 |
| **PROC-P1** | OPEN PR 累积 | PR #17 (2026-05-11)、#38 (2026-06-09) | 两条都改在 `dslCompiler.js` 死代码上 — 即便 lint+test 全过,merge 也无业务价值,只增加未来 git blame 噪音 | **建议 close + reject**(`gh pr close 17 38 --comment "lib/dslCompiler.js 已确认为死代码,归档处置见 Dream B 2026-06-30 报告"`),与归档动作同步 |
| **PROC-P1** | PR 队列梗死 | OPEN 总数 = **36**,近 30 天 MERGED = **0**,最近 merged 是 #42 (2026-06-14) 之前 | 队列流程梗死 ≥ 16 天,Dream B 自 #42 后停产 → 老王 attention 没消化此队列 | 老王 batch-merge 或 batch-close。**Dream B 已固化:OPEN > 30 时强提醒**,本次满足 |
| **DSL-P2**(失效) | 同 ARCH-P1 文件 | `compileDSL` 对 `color`/`y_ldu` 不做 `Number.isFinite` 检查,会把 `NaN` 写入 LDraw 输出 | 本可一行加 fallback 自动修,但**死代码不修** | 若老王决定保留,再开 PR;若归档,本条作废 |
| **TEST-P1**(伴随) | 同上 | `lib/dslCompiler.js`、`lib/llmPrompt.js` 在 `__tests__/` 全无引用,zero coverage(非 mock,**真零**) | 若归档则 N/A;若保留必须先补测试再继续修 |

## 已自动修复

**0 条**。Dream B 飞行检查在「死代码 + 同文件 OPEN PR」双重红线触发后立即停手,**未创建分支**,未跑 lint/test,未开 PR。

## 回滚

无。

## 依赖动态

⚠️ **本次 web_search 后端(Tavily)返回 HTTP 432**(配额/付费限制),3 次尝试中 0 次成功。
对照已知信息:
- `next@^14.2.0` — Dream B 2026-06-15 报告已记录"距 EOL 约 4 月 → P1";2026-06-18 报告升级到"EOL + 高危 CVE 无补丁 → P1 强提醒"。**本次无新数据**,提醒仍有效:升级 Next.js 15/16 至今未排期
- `three`(未在 dependencies,通过 `@react-three/fiber@^8.18.0` 传递引入)+ `@react-three/drei@^9.122.0` — 上次报告未发现 breaking
- `react@^18.3.0` — 仍为 18 LTS,无 EOL 临近

→ 简报本次**不重复提 Next.js 14 EOL**(避免 7 天内重复轰炸),但若老王本周不动作,周六 Dream B 自动再次升报。

## 竞品动态

web_search 失败,跳过。无新发现。

## 信息源

- 仓库:`~/workspace/brickhub/` HEAD 14 天 commit count = 0(老王本人未推新代码)
- PR queue:36 OPEN(`gh pr list --state open --limit 50 --jq 'length'`)
- Dream B 历史 PR #5/#8-#42(连续编号,无业务作者 PR 间插)
- Pipeline 实证:`pages/api/generate.js:3` 引用 `lib/pipeline/index.js`,grep 确认 4 文件零业务引用
- Skill:`dreaming` ⚠️「Pre-PR 飞行检查」+ 「死代码姐妹扩张」+ 「PR 队列健康检测」三项强约束全部触发

## 给老王的拍板清单(优先级排序)

1. **(2 分钟)关闭无意义历史 PR**:
   ```bash
   cd ~/workspace/brickhub && PATH=~/bin:$PATH \
     gh pr close 17 38 --comment "lib/dslCompiler.js 确认为死代码(Phase 1 遗留),已归档处置,见 Dream B 2026-06-30 报告"
   ```
2. **(5 分钟)归档死代码 4 文件**:
   ```bash
   cd ~/workspace/brickhub && \
     git checkout -b chore/archive-legacy-phase1 && \
     mkdir -p lib/_archive/legacy-phase1-deprecated-20260630 && \
     git mv lib/dslCompiler.js lib/llmPrompt.js lib/generateClient.js lib/sampleLdraw.js \
       lib/_archive/legacy-phase1-deprecated-20260630/ && \
     git commit -m "chore: archive lib/ Phase 1 legacy files (no business references, see Dream B 2026-06-30)" && \
     git push -u origin chore/archive-legacy-phase1 && \
     gh pr create --base main --title "归档 lib/ Phase 1 遗留死代码(4 文件 1670 LOC)" --body "见 obsidian-vault/04-项目开发/dreaming/2026-06-30-research.md"
   ```
3. **(2 分钟)清理 Dream B 轮换表**:同步删 `~/.hermes/cron/output/dream_b_rotation.json` 里的 `lib/dslCompiler.js`、`lib/llmPrompt.js`(2 文件未在表中:`generateClient.js`、`sampleLdraw.js`,不需操作)
4. **(15-30 分钟,看老王心情)Batch 处理 OPEN PR 队列**:逐个看 #8-#42 决定 merge / close,把队列压回 < 10
5. **(可拍可不拍)`lib/pipeline/{index,llm,utils}.js` 测试覆盖审计** — Dream B 下次轮到时会逐文件做 export 粒度筛
