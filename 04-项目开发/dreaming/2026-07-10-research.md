# Dream B 2026-07-10 · lib/pipeline/utils.js

## 概览
- 文件：`lib/pipeline/utils.js`（130 LOC，纯函数工具集：LDraw 去重/提取/矩阵校验+归一化/离线兜底）
- 队列：OPEN 5（< 8 未熔断）；30d MERGED 15 健康
- 结果：**0 P2/P3 修复**（lint clean、单一 caller `lib/pipeline/index.js` 全 6 处真实调用）；1 条 TEST-P1 + 依赖 P0/P1 延续

## 已修复
无。

## 待老王拍板

| ID | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| T1 | TEST-P1 | `__tests__/*` × `lib/pipeline/utils.js` | 8 个 export 全 0 测试覆盖（`grep` 全 hits=0）；仅 5 个被 `lib/pipeline/index.js` 间接引用（deduplicateLDraw / extractLDraw / normalizeLDrawMatrices / buildOfflineFallbackLDraw + isValidOrthogonalMatrixTokens 内部），3 个几何原语（isUnitAxis / dot3 / det3）零外部引用 | 补 `__tests__/pipelineUtils.test.js`：①`extractLDraw` 3 分支（<LDRAW>/```ldraw/纯行 filter）②`normalizeLDrawMatrices` 非正交矩阵回退到单位阵 ③`deduplicateLDraw` 相同 part+pos 去重 ④`buildOfflineFallbackLDraw` 三 category 分支。ROI 极高（纯函数，输入输出清晰） |
| A1 | 依赖 P0 | `package.json` next@14.2.35 | Next 16.2.10 stable 已 1 个月，落后两大版本，Vercel/Netlify 明示需升级；14.x 无补丁 | 排期迁移周或明示 defer 到 Phase X |
| A2 | 依赖 P1 | Next 14.x EOL + `next/image` × 3 命中 | 5 月 Image Optimization SSRF/DoS CVE 命中面延续（`pages/`, `components/` 3 处引用） | 与 A1 合并处置 |

## 回滚
无（未开 PR）。

## 依赖动态

| 库 | 当前 | 最新 | 状态 |
|---|---|---|---|
| next | 14.2.35 | 16.2.10 | **P0**：落后 2 大版本 stable ≥ 30d |
| @next/swc-linux-x64-gnu | 14.2.33 | — | 与 next 主版本一致 ✓ |
| @react-three/fiber | 8.18.0 | 8.x | 无 breaking |
| three | (r155+) | — | 无 breaking |

## 竞品动态
本轮未查（依赖 P0 优先，避免 6 次搜索预算浪费）。

## 信息源
- 本机 `~/workspace/brickhub/lib/pipeline/utils.js` @ HEAD
- `~/workspace/brickhub/lib/pipeline/index.js` L5,L150,L153,L161,L182,L219 caller verify
- `gh pr list --state open --search "head:dreamB"` OPEN=5, MERGED30=15
- `next/package.json` 14.2.35 vs 上游 16.2.10 stable（07-08 W28 首次 P0 触发延续第 3 天）
