# BrickHub Dream B — 2026-06-13 审查报告

**审查文件**: `lib/pipeline/llm.js` (178 行 / 上次轮 2026-05-26,17 天未轮)
**PR**: [#41 Dream B 自动修复 2026-06-13](https://github.com/wpsl5168/brickhub/pull/41) (分支 `dreamB/auto-fix-20260613`)
**分级汇总**: P0 0 / P1 4 / P2 1(已修) / P3 0

---

## 1. 自动修复(已合入 PR #41)

| # | 文件 | 行 | 问题 | 修复 | 守卫 |
|---|---|---|---|---|---|
| 1 | `lib/pipeline/llm.js` | 165-175 | `streamToText` web 分支 `reader.read()` 循环未包 `try/finally`,reader 抛错时不会 `releaseLock`,流锁会一直挂在 body 上,后续若想 re-acquire / 再 cancel 会拿到 InvalidStateError | 包 try/finally,finally 内 `try { reader.releaseLock() } catch {}`(套二次 try 防 releaseLock 自身在已 cancel 流上抛错) | `npx eslint` ✅ + `node -e "import(…)" 7 exports` ✅ |

行为不变,只保证异常路径下 reader 正确释放。Node `.on('data')` 分支原本就由 `'end'`/`'error'` 事件自然清理,无需改。

---

## 2. 待老王拍板(P1,涉及业务/架构,绝不自动改)

| # | 严重度 | 问题 | 影响 | 建议修法 |
|---|---|---|---|---|
| 1 | **P1 高** | **Anthropic `claude-3-5-sonnet-20241022` 6-15 后可能停服**(2 天后) | 调用 `callAnthropic` 直接 404,fallback 会跳到下一 provider,但若 Anthropic 是首选会拖慢 | 改成 `claude-3-5-sonnet-latest` 或升级到 `claude-sonnet-4-5`/`claude-opus-4-5`;同步更新 `anthropic-version` header (`'2023-06-01'` 太老,新模型可能要 `'2024-10-22'` 或更新);`anthropic-beta: 'messages-2023-12-15'` 已无意义,可移除 |
| 2 | P1 中 | **4 个 `callXxx` 均无 HTTP 超时**(`https.request` 不设 `timeout`) | 远端服务端 hang 时,连接会无限挂,前端 SSE 也跟着挂死 | `req.setTimeout(30000, () => req.destroy(new Error('upstream timeout')))`,或 `AbortController` + `signal` 走 30s/60s |
| 3 | P1 中 | **model ID + max_tokens + temperature 全部硬编码**(分散在 4 个函数,5 处) | 改一次要碰 5 个地方;无法按用户角色/场景切换模型 | 抽 `PROVIDER_CONFIG = { github: {model, ...}, ... }` 集中,callXxx 只接 messages |
| 4 | P1 中 | **`callProviderWithFallback` 失败无退避**(failover 立即打下一 provider) | 上游 rate limit 时会立刻把整个 fallback 链刷光 | 失败时 `await sleep(exp(attempt))` 或检测 429/503 才退避 |
| 5 | P1 低 | **`lib/pipeline/llm.js` 零单测覆盖**(`__tests__` 无 pipeline/llm 相关) | 任何 streaming 解析改动都裸奔 | mock `https.request` + 喂 SSE 字节流,断言 onChunk 回调次数 + final text;Anthropic `content_block_delta` 路径单独测 |

**优先级**: #1 必须 6-15 前处理,否则线上 GitHub Models 之外的 fallback 几乎瘫;#2 + #4 可同 PR;#3 + #5 可延后。

---

## 3. 回滚记录

无。1 项 P2 修复一次过 lint + import 验证。

---

## 4. 依赖动态

| 包 | 现状 | breaking? | 说明 |
|---|---|---|---|
| **@anthropic-ai/SDK / model `claude-3-5-sonnet-20241022`** | **2026-06-15 停服** | **是,2 天后** | 见第 2 节 #1。Anthropic 官方 deprecation policy + Bedrock 同步 EOS(soracom 公告) |
| Next.js | 我们在 14,15 已 stable(March 2026),16 引入 async Request APIs breaking | 远期 | 升级路径:14 → 15 (Turbopack stable / React 19) → 16 (await cookies/headers/params) |
| @react-three/fiber | v9 = React 19 兼容,无 breaking;v10 才会有 breaking | 否 | RFC #2338 明确 v9 not breaking |
| Three.js | r165+ 与 R3F v9 配套 | 否 | — |
| LDraw 官方 parts | 无 changelog 监控配置 | — | 维持手动同步策略 |

---

## 5. 竞品动态

本轮跳过(本周搜索预算已用 3/6,留给 Anthropic + Next.js + R3F 这种 breaking 信号扫描)。

---

## 6. 信息源

- `https://www.anthropic.com/research/deprecation-updates-opus-3` — Anthropic 模型退役协议
- `https://developers.make.com/.../anthropic-claude-model-deprecations-on-june-15-2026` — 6-15 停服公告
- `https://changelog.soracom.io/...-claude-3.5-sonnet-...-end-of-support-on-march-1-2026.-1Qd4Os` — Bedrock 侧同期 EOS
- `https://github.com/pmndrs/react-three-fiber/issues/2338` — R3F v9 RFC
- `https://nextjs.org/docs/app/guides/upgrading/version-16` — Next.js 16 async Request APIs
- BrickHub commit: `ee30dbd0` on `dreamB/auto-fix-20260613`

---

## 7. 本期备忘(给下次 Dream B)

- `lib/pipeline/llm.js` 已轮换 5 次(4-26 / 5-05 / 5-26 / 6-12 llmPrompt / 6-13 llm),下次至少 7 天后再选
- Web ReadableStream reader 是高复发踩坑(无 try/finally 几乎成本能写法),其它 `getReader()` 调用点扫一遍:`hooks/useBrickHubHome.js` / `lib/generateClient.js`(下轮优先)
- Anthropic 模型 ID 硬编码这事如果 6-13 / 6-14 两次审查老王都没拍板,Dream B 6-15 当天要在简报里降为 P0 重复提醒
