# Dream B 2026-07-13 BrickHub 质检报告

- **审查文件**: `lib/ldrawParser.js` (618 LOC — LDraw 色表 + 零件字典 + `parseLDraw` + `validateGeometry` 几何守门员)
- **队列状态**: OPEN=5 (< 8 未熔断)
- **lint**: ✅ 0 warning
- **jest**: `parseLDraw.test.js` + LDrawRenderer×3 + officialLdrawRender 共 50 处真实 `parseLDraw` 调用,覆盖 OK

## 自动修复
无。lint 全清、命名规范、无未用变量、无死累加器/死内部符号,无 P2/P3 可动。

## 待确认(仅报告)

| ID | 等级 | 位置 | 说明 |
|---|---|---|---|
| TEST-P1 | P1 | `validateGeometry` L488-618 (130 LOC) | 同文件多 export 按函数粒度筛第三道命中 — `parseLDraw` 覆盖 OK 但 `validateGeometry` 在 `__tests__/` 零 grep 匹配。此函数是 AI 生成模型的**物理可行性守门员**(碰撞 AABB + 悬空检测,含旋转矩阵变换 + 舍入误差容差 5/8 LDU),生产在 `lib/pipeline/index.js:163` 和 `scripts/evaluate.js:57` 拉起。零测试 → 8 LDU 容差调整/AABB 边界条件/边缘相切阈值变更时无回归网。建议排期补 `__tests__/validateGeometry.test.js`,fixtures 至少含:①合法叠层(pass);②轻微 overlap 在 COLL_EPSILON 内(pass);③明显碰撞(fail);④单点悬空(fail);⑤地基层多砖(pass) |
| DEP-P0 | **P0** | package.json `next@14.2.35` | Next 16.3 已发布(2026-07-12,GitHub releases),16.2 stable > 30 天,brickhub 落后**两大版本**(14→15→16),P0 排期第 6 天延续。建议明确 timeline 或 defer 到 Phase X |
| CVE-P1 | P1 | `next/image` 3 处命中 | Image Optimization SSRF/DoS CVE 批次持续无补丁(14.x EOL 4 月),已探查确认命中面,升级为唯一止损 |

## 依赖动态
- Next.js 16.3 释出(2026-07-12),重点 Turbopack 内存治理 + 构建持久化缓存,无 breaking(相对 16.x)。**对 brickhub 意义**:如迁移直接跳 16.3 更值,不必先落 16.2
- three / @react-three/fiber 无新动态
- LDraw parts 库无新动态

## 竞品动态
略(轮换到 6 天暂缓,余额留给依赖核实)。

## 信息源
- Next.js Releases GitHub(16.3.0 2026-07-12)
- Next.js Blog 16.3 Turbopack whats-new

## 结论
lib/ldrawParser.js 代码质量 ✅,可动手项 0。核心痛点是 `validateGeometry` 零测试(TEST-P1)+ 依赖 Next14→16 (DEP-P0 已连续 6 天)。

## 拍板清单
1. 是否本周排 `validateGeometry.test.js`(半天工作量,Fixtures 已在报告列出)?
2. Next 14→16 迁移窗口:本周开分支起步 / defer 到 Phase 5 / 保持 14 但显式接受 CVE 风险 —— 请拍板
