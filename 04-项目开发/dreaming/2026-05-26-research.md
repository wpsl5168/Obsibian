---
title: "BrickHub Dream B 2026-05-26"
date: 2026-05-26
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-26

## 今日审查：`lib/pipeline/llm.js` (177 LOC, lint clean)

服务端 LLM 上游路由模块（4-provider fallback: github/openai/zhipu/anthropic + SSE 流处理）。文件 lint clean，无单元测试覆盖，只被 `pages/api/generate.js` 引用。本轮**0 条 P2/P3 自动修复**（无安全可改项；下方所有 finding 都触及业务/安全/架构判断，必须人工拍板）。

### ✅ 已自动修复
| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| — | 无 | — | — | — |

合理性：文件是 LLM 关键路径（参考 Pitfall #19 prompt 类白名单原则），且 lint clean，没有命名/格式/未用变量可动；所有"看起来像 P2"的项一深究都触及 provider 切换行为或安全边界，归到 P1。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | **API key 可能泄露到客户端错误链路** | L35, L54, L73, L94 | `Buffer.concat(chunks).toString('utf8').slice(0, 200)` 把上游响应体前 200 字节拼进 throw message。如果 upstream 用 401/403 回 `{"error":{"message":"Invalid API key sk-...xxx"}}`，key 片段会进 `failures.join(' \| ')` (L121) 再被 `pages/api/generate.js` 透传给前端。 | 错误日志只保留 statusCode + 自定义错误码；body 仅 server 端 `console.error`，不进 throw message；或先正则脱敏 `Bearer\s+\S+`/`sk-\S+`。 | **P1** |
| 2 | **HTTP 错误响应未释放 socket** | L33-36, L52-55, L71-74, L91-94 | 4 处 `for await (const chunk of response) chunks.push(chunk)` 读完 body 没调 `response.destroy()`，但 stream 已 drain 算 OK；真正风险是 try 块外抛错时 socket 未关。Node 18+ undici 通常自动回收，但显式 destroy 更稳。 | 在 throw 前显式 `response.destroy()`。 | P1 (低危) |
| 3 | **Anthropic `anthropic-beta` header 过时** | L85 | `'anthropic-beta': 'messages-2023-12-15'` 是 2023 年内测标识，且 model 已升到 `claude-3-5-sonnet-20241022`，Messages API 早已 GA。带过时 beta header 可能触发兼容性警告或被未来版本拒绝。 | 直接删除 `anthropic-beta` header（GA 接口不需要）。 | **P1** |
| 4 | **缺超时设置** | L7-14 (httpsPost) | `https.request` 没设 timeout，上游卡死会导致前端请求挂死直到 Next.js api route 默认超时（通常 60s 或 platform 限制）。 | 加 `req.setTimeout(30_000, () => req.destroy(new Error('upstream timeout')))`。 | **P1** |
| 5 | **fallback 顺序由调用方传入，无优先级/熔断/冷却** | L99-122 callProviderWithFallback | 任意 provider 失败就立刻试下一家，无 backoff、无失败计数。一个被 ban 的 provider 每次请求都浪费一次 RTT。 | 加内存级 failure cache（30s TTL），命中跳过；或在 `pages/api/generate.js` 层做。 | P1 (架构) |
| 6 | **`max_tokens: 4096` / `temperature: 0.7` hardcoded 在 4 处** | L27, L46, L65, L86 | 4 个 provider 各自重复同一组超参，DRY 违反，未来调温度要改 4 处。 | 抽 `DEFAULT_LLM_PARAMS` 常量。**P2 候选但触及 LLM 行为白名单**，仍归 P1 让老王拍板。 | P1 |
| 7 | **callProviderWithFallback 嵌套三目运算符** | L102-110 | provider switch 用 3 层 `?:`，可读性差；新增 provider 要重写整个表达式。 | 改 `const handlers = {github: callGitHubModels, openai: callOpenAI, ...}; await handlers[provider.name](messages, provider.key)`。视为架构改动归 P1。 | P1 |

### ⚠️ 自动修复失败回滚
无（未尝试任何自动修）。

---

## 依赖动态

| 依赖 | 当前 | 最新可达 | 状态 / 变更 |
|------|------|---------|-------------|
| **next** | `^14.2.0` | 15.5.18 / 16.2.6 | 🔥 **2026-05 安全发布仍未升**（与 5/25 报告同 finding）。11 个 advisory 含 SSRF (High)、DoS (High)、缓存中毒 (Moderate)、XSS (Moderate)。14.x 全版本受影响，无 WAF mitigation，**必须升 15.5.18 或 16.2.6**。此外 16.1.7 修了 CVE-2026-27979。**仍是本周第一优先级。** |
| **@react-three/fiber** | `^8.18.0` | 9.x stable | r3f v9 已 stable（绑 React 19）。升级路径需要 React 18→19 联动，**整套一起升**（next 15+/16, react 19, r3f 9, drei 兼容版本）。 |
| **three** | `^0.183.2` | r172+ | r3f v8 peer 锁 three ≤r158，当前 ^0.183 已超出 peer 范围（可能已有 peer warning）。**核查**：建议先固定到 r3f v8 peer 范围内最高版本（约 r158），再随 r3f v9 升级一并跳到 r172。 |
| **@react-three/drei** | `^9.122.0` | 9.latest | drei 9 配 r3f v8，升 r3f v9 需同步升 drei 至兼容版本。 |
| **tailwindcss** | `^3.4.0` | 3.4.x / 4.x | 无 advisory，4.x 主要架构改动（CSS-first config），不急。 |

**推荐升级路径**（两条供老王选）：
- **A (保守 / 本周可做)**：only next → 15.5.18（在 14→15 跨大版本但只修 security）。保留 R3F/React 不动。先验证 BrickHub 是否兼容 App Router 变动（若仍用 Pages Router 兼容性更好）。
- **B (激进 / 需 1-2 周)**：next 16.2.6 + react 19 + r3f v9 + drei 兼容版 + three r172 整套升。性能 + 安全 + 最新生态一次到位，但需要重测 3D 渲染管线 + 触控交互。

## 竞品动态
- **LDraw 库**：2026-04 更新发布，新增 188 文件 / 99 parts。BrickHub 的 LDraw 镜像如果是定期 sync，应已包含；如手动管理需更新。2026-01 也曾增 146 文件。
- **Mecabricks**：官方论坛有用户讨论 AI brick recognition tool，社区诉求中，无官方动作。我们的 AI-driven build 仍有先发位。
- **BrickLink Studio**：下载页无 2026 重大更新公告，传统桌面客户端为主，不构成 web/移动端竞争威胁。

## 信息来源
- [Next.js May 2026 Security Release - Vercel](https://vercel.com/changelog/next-js-may-2026-security-release)
- [Netlify Next.js + React Security](https://www.netlify.com/changelog/2026-05-08-react-nextjs-security-vulnerabilities)
- [r3f v9 Migration Guide](https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide)
- [Best of JS: react-three-fiber](https://bestofjs.org/projects/react-three-fiber)
- [LDraw.org Library Updates](https://library.ldraw.org/updates)

## 累积观察（跨日提醒）
- `components/home/` 目录 11 文件 + `hooks/useBrickHubHome.js` 仍为 **orphan**（无 import 入口），近 10 天反复报到。
- BrickRenderer 与几个 UI 组件已有多个 P1 a11y/dead-code/无 handler 项堆积未消化，建议老王本周抽 1-2h 集中清理。
