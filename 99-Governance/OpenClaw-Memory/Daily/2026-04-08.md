daily Obsidian backup completed 2026-04-08 14:34
OpenClaw system backup completed 2026-04-08 14:34
- **知识库重构与规范化 (14:30 - 15:30)**:
  - 针对小贝整理知识库质量不高（缺乏双链、无 Frontmatter）的问题，开发了 `audit_second_secretary.py` 审计打分脚本。
  - 重新规划了 Obsidian 知识库目录结构：01-新闻速递、02-AI知识星球、03-日常灵感、04-项目开发、05-日常笔记，并以根目录 `README.md` 作为全局索引。
  - 将小贝的模型从默认切换为 `claude-3.5-sonnet`，提升逻辑和结构化能力。
  - 补充了 `SOUL.md` 的严苛 KPI（拒绝纯搬运，强制双链/元数据，结合 SQL Server/C# 背景进行深度类比科普）。
  - 执行了全量文件清洗、迁移和合并，旧结构全部删除，已推送到 GitHub 仓库。
## 2026-04-08 (晚上更新)
- **BrickHub 项目部署**:
  - 克隆了新项目 `brickhub` 到本地工作区 (`~/brickhub`)，这是一个基于 Next.js 的前端项目（含 LLM API `/api/generate`）。
  - 配置了 `.env.local`，使用提供的 `GITHUB_TOKEN` 作为大模型调用的 API 凭证。
  - 移除了 Next.js 的纯静态导出限制，使用 `PM2` 在 `3005` 端口守护运行 Node 服务。
  - **网络排错与域名配置**: 在将服务部署到 `news.brickhub.cc` 过程中遇到 HTTPS 访问异常。排查发现 iptables 有 DROP 80 端口的规则，且系统内 Nginx 与 Caddy 存在端口冲突。最终处理：停用 Nginx，完全由 Caddy 掌管 80/443 端口，并在 Caddyfile 中配置 `news.brickhub.cc` 通过 `reverse_proxy` 代理至本地 `127.0.0.1:3005`。