---
title: "BrickHub Dream B 2026-04-26"
date: 2026-04-26
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B

## 今日审查：lib/llmPrompt.js（898行，4个导出 prompt 构建器）

**说明**：此文件属"LLM输出关键路径"——即便看似 P2/P3 的小改动（去 BOM、统一示例格式）都可能改变模型生成行为。本次**全部按"仅报告不动手"处理**，待老王确认后人工调整。

### ✅ 已自动修复
| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| — | 无 | — | — | — |

> 0 项自动修复 → 未创建 dreamB 分支。

### 🔴 待人工确认（P0/P1/敏感P2）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|----------|------|
| 1 | **legacy `buildSystemPrompt` 自相矛盾**：第 51-54 行明令 "DO NOT output raw LDraw text lines. Use the DSL JSON format ONLY"，紧接着第 122 行写 "Now, please generate the JSON DSL inside `<DSL>` tags"；但**第 126-160 行的 VERIFIED EXAMPLE / FORBIDDEN 段整段是 raw `<LDRAW>` 文本格式**，与上述指令直接冲突。更严重的是模板字符串闭合在 L123 (`\`;`)，**后续 124-399 的所有内容根本不在 prompt 里，是死代码**——但作者意图显然是当作示例传给 LLM。 | `lib/llmPrompt.js:51-399` | DSL 版迁移时旧的 LDRAW 示例未删，模板提前关闭 | 二选一：①若已废弃 → 删除 124-399 全部冗余文本；②若需保留 → 改成 DSL JSON 示例后重新拼回模板字符串。**强烈建议先看 fallback 路径调用情况再决定**。 | **P1** |
| 2 | UTF-8 BOM 字符在文件首字节（第1行第1列） | `lib/llmPrompt.js:1` | Windows 编辑器保存所致 | 去 BOM。**单独 PR**，不与本批混。 | P2（敏感） |
| 3 | `pipeline/index.js:96-101` 同时使用 DSL 和 legacy 两条提示链，本仓里没看到 fallback 触发开关说明 | `lib/pipeline/index.js:96-101` | 双路径维护成本高 | 补注释或灰度开关；若 legacy 已弃用应删除分支 | P1 |
| 4 | prompt 文本里硬编码大量魔术常量（24 LDU/8 LDU/坐标 offset 表），与 `dslCompiler.js` 没共享来源 | 全文 | 双源真相 | 建议从 `dslCompiler.js` 的常量表自动生成 prompt 段，避免漂移 | P1 |
| 5 | `buildUserMessage` (L406+) 的错误回喂消息中英混写（"请为我设计：" + 全英规则） | L406 | 早期版本残留 | 统一成英文（更利于 LLM token 利用），需评估对现有用户语料的影响 | P2（敏感） |

### ⚠️ 自动修复失败回滚
| # | 问题 | 失败原因 |
|---|------|---------|
| — | 无 | — |

## 依赖动态

| 依赖 | 当前 | 最新 | 变更/影响 |
|------|------|------|------|
| three | ^0.183.2 | r183 系列已上 RenderPipeline（取代 EffectComposer）| 后期渲染管线变化，影响后处理升级路径 |
| @react-three/fiber | ^8.18.0 | v9.5.0（React 19 + Activity 支持）| **重大版本差距**，目前在 v8 |
| @react-three/drei | ^9.122.0 | 与 R3F v9 配套需升级 | 跟随 R3F 升级 |
| next | ^14.2.0 | v16.x（v15 引入 async request APIs，v16 收紧）| **重大版本差距** |
| **@next/swc-linux-x64-gnu** | **^16.2.3** | 16.x | ⚠️ **版本错配警告**：next 主包 14，但 swc 二进制是 16。可能引发难追踪的构建异常，建议锁回 14.x 对应版本 |
| LDraw library | — | 2026-03 update（+134 文件，+76 parts） | 若用 `scripts/build-ldraw-index.js`，考虑同步 |
| BrickLink Studio | — | 2026-03-25 发布，Series 11 新色板 | 若测试素材来自 Studio 导出，注意新色码 |

## 竞品动态
- **BrickLink Designer Program Series 11** 4/20 开放投稿至 5/4，社区热度高。
- GitHub LDraw 生态本周无显著新项目（pybricks/ldraw、michaelgale/ldraw-py 等老仓为主）。

## 技术发现
- Three.js 2026 主线方向：WebGPU 全浏览器 production-ready + RenderPipeline 节点化后处理。
- R3F v9 的 React 19 Activity 特性可优化 sandbox 多模型切换时的卸载/恢复成本。

## 建议行动（供老王确认）
- [ ] **(高优)** 修 `package.json` 里 next vs @next/swc 版本错配（next 升 16 或 swc 锁回 14.x）。
- [ ] **(高优)** llmPrompt.js 死代码段 124-399 决策——删除还是重新拼回模板字符串。
- [ ] **(中)** 评估 R3F v8 → v9 升级窗口（伴随 React 18 → 19）。
- [ ] **(中)** 同步 LDraw 2026-03 库（若用了官方零件库索引）。
- [ ] **(低)** llmPrompt.js 去 BOM；统一回喂消息语言。

## 信息来源
- [Three.js 2026 改动综述](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [R3F v9 Migration Guide](https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide)
- [Next.js v16 升级指南](https://nextjs.org/docs/app/guides/upgrading/version-16)
- [LDraw 2026-03 Parts Update](https://library.ldraw.org/)
- [BrickLink Studio 下载页](https://www.bricklink.com/v3/studio/download.page)
- [BrickLink Designer Series 11](https://www.bricklink.com/v3/designer-program/series-11/main.page)
