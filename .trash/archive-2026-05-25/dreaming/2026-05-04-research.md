---
title: "BrickHub Dream B 2026-05-04"
date: 2026-05-04
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-04

## 今日审查：lib/pipeline/index.js（230行）

模块职责：LDraw 生成主管线，DSL 优先 + LDraw 兜底 + 离线兜底，含遥测、重试、几何校验。

### ✅ 已自动修复（PR #12）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | `retrieval.themes.map(...)` 未做 null 防御 | `index.js:88` | `(retrieval.themes || []).map(...)` | P2 |

**守卫**：eslint ✅，jest `__tests__/generateIntegration.test.js` 6/6 通过。
**PR**：https://github.com/wpsl5168/brickhub/pull/12

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议 | 级别 |
|---|------|------|------|------|------|
| 1 | 重试预算与"模式切换"语义混淆 | `index.js:125-148` | `dsl_compile_failed` / `missing_lego_tag` 走 `retryCount++` 后切到 LDraw 模式并 `continue`，但语义上这是"换路径"而非"重试"。当 `maxRetries=1` 时 LDraw 兜底实际只跑 1 次，没有真正的二次机会。 | 区分 `retryCount`（同模式重试）与 `pathSwitches`（模式切换不消耗 retry budget）；或在文档/参数命名中明确语义。涉及业务策略，留给老王。 | P1 |
| 2 | DSL 兜底切到 LDraw 后 `lastErrors` 注入了"未检测到 <LEGO> 标签"中文消息，作为 LDraw 模式的 user 上下文 | `index.js:145` | LDraw 路径的 prompt 拿到的是上一轮 DSL 失败的诊断文本，可能反向污染 LDraw 输出风格。 | 切模式时清空 `lastErrors` 或单独传 `pathSwitchReason`。 | P1 |

### ⚠️ 自动修复失败回滚

无。

### 其他观察（信息项，未修）

- `extractLDraw` 兜底逻辑（`pipeline/utils.js:13-23`）会把任意以 `0`/`1` 开头的行抓出来，包含 LLM 解释性"0 这个模型由..."注释行——属于设计选择，可接受。
- `streamToText` 异常分支用 `catch { /* ignore */ }` 静默吞 JSON parse 错误（`llm.js:147`）——SSE 数据里偶发 keepalive 行确实需要忽略，但建议在 dev 模式下打日志方便调试，留给老王决定。

## 依赖动态

| 依赖 | 当前 | 最新 | 重要变更 |
|------|------|------|---------|
| three | ^0.183.2 | 0.184.0 (~17天前) | r183 引入 `RenderPipeline` 节点式后处理，r184 后续打磨；当前未用 EffectComposer 影响小 |
| @react-three/fiber | ^8.18.0 | 9.5.0 | v9 兼容 React 19.2，含 Activity 特性；本项目还在 React 18，不急升 |
| next | ^14.2.0 | 15.x | Next 15 要求 React 19 RC、async request APIs、移除 squoosh 改 sharp；升级需联动 R3F v9 + React 19 |
| Bricklink Studio | — | 2026-04-27 | 修复 POV 渲染 flexible parts 收缩问题，无新功能 |

## 竞品动态

- **Bricklink Studio** 2026-04-27 发版，仍是 bugfix 节奏，无 AI 生成方向动作。
- **R3F + React 19 / Activity** 生态已就绪，未来"AI 生成 → R3F 高频局部重渲染"的场景可以受益于 Activity 暂停子树。

## 建议行动（供老王确认）

- [ ] Review PR #12（一行 null 防御）
- [ ] 评估 `lib/pipeline/index.js` 重试 vs 模式切换语义（P1 #1，建议加单元测试覆盖 maxRetries=1 时 LDraw 兜底是否真有重试）
- [ ] 评估是否在 DSL→LDraw 切换时清空 `lastErrors`（P1 #2）
- [ ] three.js 0.183 → 0.184 小版本可顺手升

## 信息来源

- https://www.npmjs.com/package/three
- https://github.com/pmndrs/react-three-fiber/blob/master/packages/fiber/CHANGELOG.md
- https://nextjs.org/blog/next-15
- https://www.bricklink.com/v3/studio/download.page
