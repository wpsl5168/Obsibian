# assets（配图资源库）

规则（强约束）：
- **禁止使用 AI 生图模型生成技术配图。**
- 所有图片必须来自：官方架构图/权威概念图/高质量博客插图。
- 图片必须**原封不动**下载保存，不做 AI 重绘。
- 统一放在 `assets/` 下，建议按 topic 建子目录：
  - `assets/claude-code/`
  - `assets/mcp/`
  - `assets/openclaw/`
- 在 Markdown 里使用相对路径引用，并在图旁标注来源 URL + 访问日期。
