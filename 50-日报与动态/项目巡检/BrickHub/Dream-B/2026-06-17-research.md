# Dream B 日审 · 2026-06-17 · lib/instancingPipeline.js

## TL;DR
- **轮换文件**:`lib/instancingPipeline.js`(192 行,5 导出函数,纯函数模块,零副作用)
- **lint**:clean(eslint 通过)
- **test**:6/6 PASS(`__tests__/instancingPipeline.test.js`,1.2s)
- **自动修复**:0(无 P2/P3 触发,代码质量已达标)
- **P0**:0
- **P1**:2(孤儿模块 + Next.js 14 EOL 临近)
- **依赖动态**:Three.js r183 已发,RenderPipeline 替代 EffectComposer(项目未用 EffectComposer,无影响)
- **竞品动态**:无关键变动

## 已修复(自动)
*无 — 该文件代码质量已达 Production-grade*

| ID | 文件 | 修复 | commit |
|---|---|---|---|
| — | — | — | — |

## 待确认(仅报告,需老王拍板)

| ID | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| D-617-1 | **P1** | `lib/instancingPipeline.js` 全文件 | **孤儿模块**:5 个导出函数(`groupBricksForInstancing` / `buildInstanceTimeline` / `sampleInstanceTransform` / `summarizeInstancing` / `easeInOutCubic`)在生产代码零调用,仅 `__tests__/instancingPipeline.test.js` 引用。验证命令:`grep -rn 'instancingPipeline\|buildInstanceTimeline\|...' --include='*.js' \| grep -v __tests__ \| grep -v node_modules` 仅命中文件自身。当前 BrickRenderer 渲染走 @react-three/fiber + drei InstancedMesh 路径,未消费这套自定义批次时间线。**业务判断点**:(a) 这是为"渐进显示动画"准备的待集成基础设施,后续接入 BrickRenderer → **保留**;(b) 历史方案已废弃 → **删除文件 + 测试**,清理 192 行 + 116 行死代码。`PROJECT_VISION.md:192` 提及该测试但未说明集成状态。 | 老王回复方向后:保留则在 BrickRenderer 中调用并写入 design 文档;废弃则 `rm lib/instancingPipeline.js __tests__/instancingPipeline.test.js` |
| D-617-2 | **P1** | 依赖排期(全项目) | **Next.js 14.2 EOL 2026-10-21**,距今约 4 个月。生命周期窗口收窄,届时停止安全补丁。Next.js 15 已稳定,React 19 + R3F v9 也已配齐。 | Q3 排期升级至 Next.js 15;先建一个 `chore/upgrade-next-15` 分支跑 codemod (`npx @next/codemod@latest`),评估 R3F + drei + Three.js 联动 |

## 回滚
*无自动修复 → 无回滚*

## 依赖动态(本日扫描)

| 包 | 当前 | 最新 / 动态 | 影响 |
|---|---|---|---|
| three (via R3F 8.18) | 隐式 | r183 引入 RenderPipeline 替代 EffectComposer | 项目未使用 EffectComposer,**无 breaking** |
| @react-three/fiber | ^8.18.0 | v9 已发,React 19 兼容更新,**Strict Mode 下有 highlight breaking**;v10 才会做真正破坏式变更 | 项目仍 React 18.3 + R3F 8.18,**暂不动**;升级需配 React 19 |
| next | ^14.2.0 | **EOL 2026-10-21** | **P1 排期提醒**(见 D-617-2) |
| @react-three/drei | ^9.122.0 | 与 R3F 8 兼容 | 无 breaking |

## 竞品动态(本日扫描)

| 项目 | 动态 | 启示 |
|---|---|---|
| Three.js 生态 | WebGPU 默认化趋势(2026);r183 RenderPipeline | BrickHub 未来若做后处理(辉光/AO),可直接走 RenderPipeline 而非 EffectComposer |
| Bricklink Studio / Mecabricks | 无重大版本动作(本月) | — |

## 4 维度扫描详情(`lib/instancingPipeline.js`)

### Bug 隐患
- `mulberry32` 实现(L21-29)是标准 PRNG 变体(变量重命名 t→r 的等价改写),**非 bug**
- `safeArray3` / `safeQuat` / `normalizeNum` 防御性 sanitize,可拦截 NaN / undefined / 错位数组
- `sampleInstanceTransform` 中 `progress` clamp 到 `[0,1]` 后过 `easeInOutCubic`,**无除零/NaN 风险**
- `summarizeInstancing(groups, timeline)` 两参均访问 `.instances`,数据结构一致,**无误用**
- `globalIndex` 全局递增控制批次延迟分配,逻辑符合"渐进显示"意图

### 架构
- 纯函数模块,无 React/Three 直接耦合,易测试 ✓
- 但**未被 BrickRenderer 消费**(见 D-617-1),需澄清意图

### UI 规范
- 不适用(纯逻辑模块,无 UI 渲染)

### 测试覆盖
- 6 个 test 覆盖 5/5 导出函数 ✓
- 含确定性测试(fixed seed)、边界(clamp [0,1])、分组规则(signature)、`maxPerGroup` 分片
- 覆盖率达标,无需补充

## 信息源
- Three.js r183 RenderPipeline:https://threejsroadmap.com/blog/the-complete-guide-to-threejs-post-processing-in-2026
- R3F v9 migration:https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- R3F v9 RFC:https://github.com/pmndrs/react-three-fiber/issues/2338
- Next.js 14 EOL 历史推算:Dream B 2026-06-15 已记录(distance 计算)

## 轮换状态
- 本周已轮:`pages/sandbox.js`(06-07) / `pages/model/[id].js`(06-08) / `pages/index.js`(06-08) / `lib/dslCompiler.js`(06-09) / `components/VoiceInput.js`(06-10) / `lib/pipeline/index.js`(06-11) / `lib/llmPrompt.js`(06-12) / `lib/pipeline/llm.js`(06-13) / `components/PartsTray.js`(06-14) / `lib/pipeline/utils.js`(06-15) / `components/home/BrickScene.js`(06-16) / **`lib/instancingPipeline.js`(06-17,本日)**
- 下次候选(最久未轮):`lib/knowledgeBase.js`(上次 06-02) / `components/home/LDrawEditorPanel.js`(05-28) / `components/home/HistoryPanel.js`(05-30)等
