# Dream B · BrickHub 每日审查 · 2026-07-05

**审查文件**:`components/FreeBuildTray.js` (351 LOC / 13.2 KB / 自由搭建模式零件+颜色抽屉)
**队列**:OPEN=1 (#43 昨日 BrickRenderer 死代码清理,仍待老王 review) → **不熔断,正常扫描**
**PR**:#44 已开(3 项 P2 自动修,单文件单提交,守卫全过)

---

## 一、Pre-PR 飞行检查(全过)

| 检查 | 结果 |
|---|---|
| 死代码 grep | 命中真 caller `pages/sandbox.js:9,409`;非孤儿 |
| 死代码姐妹扩张 | 不触发(非死代码) |
| 历史 PR 考古 | #35 MERGED (06-05 a11y) / #6 CLOSED (04-28 早期) — 无 dangling OPEN |
| 同文件 OPEN PR | 0 |
| PR 队列健康 | OPEN=1,30d MERGED = 07-03 三合一整合 + #43;健康 |

**结论**:可安全开新 PR。

---

## 二、已修复(PR #44,守卫全过)

| # | 分级 | 位置 | 变更 | 依据 |
|---|---|---|---|---|
| U1 | P2 触控 | 行 274-275 KID_COLORS button | width/height `32→44px` | 违反最低 44px 触控红线;不加到 56 是因抽屉 `maxHeight: 35vh` 硬约束,颜色条 + 网格 + 分类横条挤 3 层,44px 是可接受折衷。若老王要 56px 需先把 `35vh` 提到 `48vh`,已列 B2 |
| U2 | P2 触控 | 行 250 关闭 X 按钮 | `w-11 h-11 → w-14 h-14` (44→56px);SVG 内容 20→22px 平衡 | 达儿童 56px 红线;与分类圆形按钮同尺寸 |
| U3 | P2 CSS 失效 | 行 225 分类横条 / 行 263 颜色条 | 移除 `scrollbar-hide` Tailwind 类,改 inline `style: { scrollbarWidth: 'none', msOverflowStyle: 'none', WebkitOverflowScrolling: 'touch' }` | 仓库未装 `tailwind-scrollbar-hide` plugin(`grep tailwind.config.js package.json` 双证零匹配),Tailwind purge 阶段直接丢弃类;桌面 macOS/Chrome + Safari 会显示裸滚动条,违反设计规范;iOS 保留 momentum scroll |

**净变化**:1 file, +22 / -8 LOC。

**守卫**:
- `npx eslint components/FreeBuildTray.js` ✅ 干净
- `npx jest __tests__/freeBuildTray.test.js` ❌ 10/10 **baseline 保持不变** — 测试 fixture 与组件早期就脱节(测试期望 `🧱 砖块` 等 emoji tab + `FFCF00` 色号,组件已改 SVG icons + `FFCD00` + 新分类命名 `砖块/薄板/特殊/科技/装饰`)。**非本 PR 引入**,详见 TEST-P1

---

## 三、待老王确认(不动手,P0/P1)

| ID | 级别 | 位置 | 现象 | 建议 |
|---|---|---|---|---|
| **TEST-P1** | P1 测试破产 | `__tests__/freeBuildTray.test.js` | 10/10 全 fail(基线状态,自组件重构后从未通过)。fixture 硬编码 `screen.getByText('🧱 砖块')` / `screen.getByText('🟦 薄板')` / `.toMatch(/FFCF00/)`,组件已改 SVG icons(`cat.icon`)+ hex `#FFCD00`。**当前最坏第三态**:测试文件在但零信号,PR 合并守卫失效 | 二选一:(a) rewrite 测试用 `aria-label={cat.label}` 定位(`getByLabelText('砖块')`)+ 匹配 `FFCD00`;(b) `git rm` 明确弃测(会失 UI 契约保护)。**推荐 (a)**,恢复回归网 |
| **B1** | P1 触控完整性 | 行 300-306 draggable button | 拖拽仅 `draggable={true}` + `onDragStart` dataTransfer;**触屏零回退**。iPad/Android 儿童核心场景:选完零件按住往画布拖 → 直接失败无反应 | 二选一:(a) 引入 `react-dnd` 或 `@dnd-kit/core` + `TouchBackend`(体量较大);(b) 最小改:点击 setPending → Canvas 单击落位。**推荐 (b)**,与 BrickRenderer 已有的 pointer 逻辑集成成本低 |
| **B2** | P1 空间/触控冲突 | 行 213 `maxHeight: isOpen ? '35vh' : '80px'` + 行 292 网格 `calc(35vh - 160px)` | 展开态 35vh 硬顶。iPhone SE 812pt: 35vh ≈ 284pt,减 header/color/gap 后网格仅剩 100pt 高,五列布局单元格 <60pt。修完颜色 44px 后颜色条独占 60pt,进一步挤压 | 提到 `40vh` 或改自适应 `min(35vh, 260px)` 并把网格 `maxHeight` 改成 `flex-1 min-height: 200px`。会牵动 sandbox.js Canvas 高度算式,需 老王/一并回归 |
| **DEP-P1** | P1 已知项(续) | `next@^14.2.0` (14.2.35) | **2026-05 Vercel Coordinated Release**新增 13 CVE(含 HeroDevs 披露 CVE-2026-44573/44577/44572 EOL 高危,官方明示 14.x 不再补丁);社区 issue #93868 讨论 backport 被拒。W27 起持续跟踪,本轮不改动 | 30 天窗口规划升 15.x/16.x;短期若不升需 Cloudflare WAF 前置拦截 middleware 相关 header。参见 W27 报告 DEP-P1 |

**说明**:B1/B2 涉及交互契约(拖拽策略、抽屉高度)+ 影响 sandbox.js/[id].js 联动,Dream B 红线不改;TEST-P1 是「测试文件本身」重写,虽是 P2 级工作量但影响 CI 回归网 → 归 P1。

---

## 四、依赖动态

| 项 | 状态 | 结论 |
|---|---|---|
| **Next.js 14.2.35** | Vercel 05-13 一次性发布 13 CVE fixes 全在 15.5.16/16.2.5,14.x 不打;HeroDevs 提出 EOL patch 服务 | **P1 强提醒**(不变) |
| **@react-three/fiber 8.18.0** | R3F CHANGELOG "fix javascript interpreting negative renderpriority as positive"(major breaking) — BrickHub 全仓 `grep renderPriority` **零业务命中**(仅 drei 内部 GizmoHelper 用) → **不受影响** | 无 breaking |
| **three 0.183.2** | Three.js 2026 主线转 WebGPU + TSL,但 WebGL 老路径向后兼容;当前项目走 WebGL renderer 稳 | 无 breaking |
| **@react-three/drei 9.122.0** | 无重大 breaking | 无 breaking |

## 五、竞品动态

| 项 | 观察 | 结论 |
|---|---|---|
| Bricklink Studio | 官网仍 v2.24 系列(2026-07-05 server time active);Mecabricks 无 07 月更新 | 无威胁 |

## 六、回滚

无。3 项修复全部 eslint 通过,jest 基线保持,无新回归。

## 七、信息源

1. Vercel Changelog "Next.js May 2026 security release" https://vercel.com/changelog/next-js-may-2026-security-release
2. HeroDevs "CVE-2026-44573/44577/44572 EOL Next.js" https://www.herodevs.com/blog-posts/cve-2026-44573-cve-2026-44577-cve-2026-44572-three-next-js-vulnerabilities-affecting-eol-versions
3. Next.js GH discussion #93868 (backport request rejected)
4. pmndrs/react-three-fiber CHANGELOG(renderpriority fix)
5. Utsubo "Three.js 2026: WebGPU + TSL"
6. Bricklink Studio download page(server time 2026-07-05 T06:00-0400,页面版本无变化)

## 八、内部信号

- 07-03 整合三合一(ad44ca4/32d838b/fe0ae5c)后队列健康:1 OPEN(#43 待 review)+ 今日新增 #44
- 每日轮换本轮进度:07-03 VoiceInput / 07-04 BrickRenderer / **07-05 FreeBuildTray**
- 下轮建议:`components/PartsTray.js`(66 LOC 与 FreeBuildTray 是姐妹组件,用在 `pages/model/[id].js`)或 `lib/pipeline/index.js`(核心 API pipeline,231 LOC 未轮)
