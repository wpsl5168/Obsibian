# Dream B — 2026-09-03

## 结论

- 今日审查：`lib/pipeline/index.js`；0 项自动修复，未创建分支/PR。
- 新增 `BUG-P1`：管道把 `compileDSL`、parser/validator、业务 hook 等内部异常与上游异常放在同一个 `catch`，统一伪装成 `upstream_unavailable` 并返回本地兜底；真实代码回归会被错误记成供应商故障，监控和排障方向失真。
- `DEP-P0` 延续：项目 `next@14.2.35`，官方当前 `16.3.4`，落后 2 个大版本且 14.x 无安全补丁线；需明确迁移周或 defer 节点。
- Dream B 队列 6 条 OPEN、全部 CLEAN；距最近 merge 63 天，`PROC-P1` 持续。

## 已修复

| ID | 文件 | 结果 |
|---|---|---|
| — | — | 0 项；无 P2/P3，不开 PR |

## 待确认（P0/P1）

| ID | 等级 | 位置 | 发现 | 建议 |
|---|---|---|---|---|
| BUG-01 | P1 | `lib/pipeline/index.js:104-229` | `try/catch` 同时包住 provider 调用、DSL compile、矩阵归一化、parser/geometry validator 和 hooks。临时 Jest 探针令 `compileDSL` 抛 `TypeError('compiler invariant broken')`，实际返回 `pipeline.fallbackReason='upstream_unavailable'`，测试 1/1 通过，证实内部 bug 被伪装成上游宕机 | 将 provider/network 异常与内部 processing 异常拆成窄 catch；内部 invariant 错误应记录 `pipeline_internal_error` 并上报，是否继续兜底由业务拍板 |
| BUG-02 | P1（延续） | `lib/pipeline/index.js:93-147,230` | `maxRetries<0` 或 DSL 首轮失败后循环可退出到函数末尾，返回 `undefined`；API 随后访问 `result.pipeline` 崩溃 | 入口统一校验为非负整数，并在循环外提供显式 fallback/throw；补边界测试 |
| TEST-01 | P1（延续） | `__tests__/generateIntegration.test.js:13-30` | 集成测试整体 mock `lib/pipeline/index.js`；6/6 通过只覆盖 API 协议，管道 4 个 export 零真实覆盖 | 新建 pipeline 单测，至少覆盖 compile 异常、provider 异常、validation fallback、retry 边界和 hook 异常 |
| DEP-01 | P0（延续） | `package.json` | `next@14.2.35` → 当前 `16.3.4`，落后两大版本；14.x 已无修复线 | 排期迁移 Next 16 + React 19/R3F 9 联合验证；若暂缓，写明 Phase 节点 |
| SEC-01 | P1（延续） | `next/image` 3 处 | August Critical `GHSA-2xp9-vwfh-vxw4`：版本与 Image Optimization 功能面命中；当前未配置 AVIF、remotePatterns 为固定 CDN，可达性未证实。Windows RCE `CVE-2026-75604` 因 Linux + 无 App Router 不适用 | 禁描述为“已证实可利用”；通过升级到安全线消除版本暴露 |
| PROC-01 | P1（延续） | Dream B PR 队列 | OPEN 6，#43-#48 全部 CLEAN；最近 merge 为 #39（2026-07-02），已 63 天 | batch review：合并仍有效项，关闭被替代/低价值项 |

## 回滚

| 文件 | 原因 | 结果 |
|---|---|---|
| — | 无代码修改 | 无回滚 |

## 验证链

- `npx eslint lib/pipeline/index.js`：通过。
- `npx jest __tests__/generateIntegration.test.js --runInBand`：6/6 通过，但管道被整体 mock。
- 临时隔离 Jest 探针：1/1 通过，证实 `compileDSL` 内部异常被标记为 `upstream_unavailable`；探针文件已删除。
- 生产引用：`pages/api/generate.js` 真实 import `generateLDrawPipeline/getTelemetrySnapshot`，不是死代码。
- SWC：`next 14.2.35` / `@next/swc-linux-x64-gnu 14.2.33`，主版本一致，无 binding 撕裂。
- CVE 命中面：无 middleware/proxy、无 App Router、无 Cache Components、无 Server Actions/RSC/rewrites/特殊 Request fetch；`next/image` 3 处；AVIF 配置 0。

## 依赖动态

| 项目 | 当前 | 上游 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 16.3.4 | P0：落后 2 大版本；August Critical 官方安全线为 16.3.3/15.5.24，14.x 无补丁 |
| Three.js | 0.183.2 | 0.185.1 | 无已确认 breaking；暂不单独追版本 |
| React Three Fiber | 8.18.0 | 9.7.0 | v9 有 Strict Mode、texture colorSpace、TS JSX 类型 breaking；应与 React 19/Next 16 联合迁移，不单升 |
| LDraw Parts | 2026-07 观察基线 | 2026-08 | 新增 202 files / 144 parts / 2 primitives；建议下一次官方零件库同步纳入，不使用 AI 编造 part ID |

## 竞品动态

- BrickLink Studio：搜索未发现新的结构性 breaking，公开结果仍以安全升级/补丁为主。
- Mecabricks：未发现影响 BrickHub 路线的新产品级动作。
- GitHub LDraw 生态：未发现需要立即跟进的热门 loader breaking/release。

## 拍板清单

1. 指定 Next 16 迁移周，或明确 defer 到哪个 Phase 收尾。
2. 清理 #43-#48：有效项 batch-merge，其余 close；当前全部 CLEAN。
3. 同意后再拆分 pipeline catch 边界并补真实单测；该项涉及故障语义，不自动改。

## 信息源

- Next.js 官方 August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- Next.js 官方 Blog / 最新版：https://nextjs.org/blog
- R3F v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 官方 2026-08 Parts Update：https://www.ldraw.org/
- LDraw Library Updates：https://library.ldraw.org/updates
- BrickLink Studio Update：https://www.bricklink.com/v3/announcement/studio_update.page
