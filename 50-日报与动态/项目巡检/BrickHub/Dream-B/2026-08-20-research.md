# Dream B — BrickHub 每日质检与预研（2026-08-20）

## 结论

- 今日审查：`pages/model/[id].js`
- 自动修复：0；目标文件已被 OPEN PR #46、#47 覆盖，Pre-PR 冲突闸拦截，未建分支、未改代码。
- 新增：P1 × 3（MPD 放映解析、异步加载事务性、测试缺口），P2 × 1（嵌套交互控件）。
- 队列：Dream B OPEN 5 / 全部 OPEN 6；最近合并 2026-07-02，流程已梗死 49 天。

## 已修复

| ID | 位置 | 结果 |
|---|---|---|
| — | — | 0 项；同文件 OPEN PR 冲突，未自动修改 |

## 待确认（P0/P1）

| ID | 等级 | 位置 | 证据 | 建议 |
|---|---|---|---|---|
| BUG-01 | P1 | `pages/model/[id].js:201-235` | `stepsPartsList` 扫描整个 MPD，只识别 `STEP` 和 type-1 行，不处理 `0 FILE` 子文件边界。样本 `4489-1-AT-AT-Mini.mpd_Packed.mpd` 元数据 82 件，但当前算法把内嵌 `.dat` primitive 计入步骤，解析出 1230 条引用、末步 1152 条 | 放映步骤解析复用正式 MPD parser，限定主模型 section；补 Packed MPD fixture |
| BUG-02 | P1 | `pages/model/[id].js:177-199` | `fetch` 未检查 `response.ok`，404 HTML 会被当 LDraw 文本；无 AbortController/请求代次守卫，路由快速切换时迟到旧请求可覆盖新模型；切换时未清空 `errors/ldrawCode` | 加 HTTP 状态检查、请求取消/代次守卫，并在新加载开始时清空旧错误和模型文本 |
| TEST-01 | P1 | `__tests__/modelDetail.test.js` | 当前仅 2 个简单 `.ldr` 用例且 mock `BrickRenderer`；Jest 2/2 通过，但未覆盖 Packed MPD、非 2xx、乱序响应和错误恢复 | 新增上述 4 组回归测试，直接走生产解析/加载路径 |
| PROC-01 | P1 | PR 队列 | 5 条 Dream B PR 均 CLEAN；最近一次 merge 为 2026-07-02，已 49 天零合并 | batch review 后 merge/close #43–#47；#46/#47 正阻塞本文件后续自动修 |

## P2/P3 与 Pre-PR 结果

| ID | 等级 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| A11Y-01 | P2 | `pages/model/[id].js:263-268` | `<Link>`（最终 `<a>`）内嵌 `<button>`，形成无效嵌套和两个顺序聚焦目标 | 应移除内部 button，直接样式化 Link；因 OPEN PR #46/#47 均修改本文件，未自动修 |

Pre-PR 检查：

- 目标页有生产入口：`pages/index.js:122` 链接到 `/model/{id}`，非死代码。
- 顶层内部符号单次引用扫描：0 个死符号。
- 同文件 OPEN PR：#46 CLEAN、#47 CLEAN，冲突闸命中。
- 历史同文件 PR：OPEN #46/#47；CLOSED #20/#37；MERGED #8。
- 队列健康：全仓 OPEN 6，Dream B OPEN 5；最近合并 2026-07-02。

## 回滚

| 文件 | 原因 | 结果 |
|---|---|---|
| — | 未进入修改阶段 | 无回滚 |

## 验证

- `npx eslint 'pages/model/[id].js'`：通过。
- `npx jest __tests__/modelDetail.test.js --runInBand`：1 suite / 2 tests 全通过。
- 仓库原有未跟踪目录/文件未触碰：`docs/architecture/`、`public/downloads/`、`scripts/test_vlm_lego.py`、`test_images/`。

## 依赖动态

| 项目 | 当前 | 上游/风险 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 官方 16.3 已于 2026-08-03 发布；16 主版本已稳定超过 30 天，项目落后 2 个主版本 | DEP-P0：必须给迁移周或明确 defer；14.x 无新安全补丁线 |
| Next CVE 命中面 | Pages Router；`next/image` ×3；无 App Router/middleware/proxy/Server Actions/cache/rewrites | July 2026 批次逐类探查 0 个已证实命中；May Image Optimization 类仍有功能命中面，14.x 无补丁分支 | P1 持续暴露；禁止再误报 CVE-2026-64644 命中 14.2.35 |
| SWC | next 14.2.35 / swc 14.2.33 | 同主版本 | 无撕裂 |
| R3F | 8.18.0 | 上游 latest 9.7.0，含 reconciler 修复；v9 与 React 19 迁移绑定评估 | 无今日 breaking；纳入 Next/React 迁移验证，不单独贸然升级 |
| Three.js | 0.183.2 | 项目已较新 | 无 breaking 新发现 |

## 竞品 / LDraw 动态

- 今日未发现 BrickLink Studio、Mecabricks 或 LDraw 官方新的 breaking 动作。
- 搜索只确认既有 2026 parts update 信息；不重复上报旧动态。

## 信息源

- Next.js 16.3（官方，2026-08-03）：https://nextjs.org/blog/next-16-3
- React Three Fiber Releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw Parts Update：https://forums.ldraw.org/thread-29359.html
- 本地依赖：`package.json`、`node_modules/next/package.json`、`node_modules/@next/swc-linux-x64-gnu/package.json`
- 本地 CVE 探查：`dreaming/references/dream-b-nextjs-cve-hitsurface.md`
