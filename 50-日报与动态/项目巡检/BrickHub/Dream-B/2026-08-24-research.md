# Dream B — 2026-08-24

## 结论

- 审查：`lib/pipeline/llm.js`。
- 自动修复：0；无 P2/P3，未创建分支/PR。
- 新增 BUG-P1：`streamToText()` 把 `onChunk` 放在 JSON 解析的宽泛 `try/catch` 内，下游事件回调抛错会被静默吞掉；最小探针实测回调执行 1 次并抛错，但函数仍返回 `"x"`。生产调用链是 `pages/api/generate.js -> hooks.onEvent -> res.write()`，可能把下游投递异常伪装成生成成功。
- Next.js 08-26 Critical 安全发布仍待 advisory；当前不可推断 BrickHub 是否命中。`next@14.2.35` 相对 stable `16.3.2` 落后 2 个主版本，维持 DEP-P0。
- Dream B OPEN PR 5 条，均 CLEAN；最近 merge 为 2026-07-02，已 53 天，维持 PROC-P1。

## 队列状态

| 指标 | 结果 | 判定 |
|---|---:|---|
| Dream B OPEN | 5 | 未触发 ≥8 熔断 |
| 全仓 OPEN | 6 | — |
| 最近 merge | PR #39 / 2026-07-02 | 53 天零合并，PROC-P1 |
| 目标文件 OPEN PR | 0 | 无同文件冲突 |
| OPEN 状态 | #43–#47 均 CLEAN | 可集中 review + merge/close |

## 四维扫描

| 维度 | 结果 | 证据 |
|---|---|---|
| Bug 隐患 | 新增 BUG-P1 | `processLine()` 的宽泛 catch 同时吞掉 JSON parse 与 `onChunk` 异常；最小运行探针实证 |
| 架构 | LLM-P1 延续 | 无 request timeout/abort；未知 provider 静默落 Anthropic；provider/model 硬编码；7 个 export 中 5 个仅模块内部使用 |
| UI 规范 | 不适用 | 纯服务端 pipeline 模块，无 UI/3D 修改 |
| 测试覆盖 | TEST-P1 延续 | `__tests__` 无 `lib/pipeline/llm.js` 真实引用；API 集成测试 mock 整个 pipeline |

## 已修复

| 项目 | 结果 |
|---|---|
| P2/P3 | 0；无代码改动 |

## 待确认（P0/P1）

| 等级 | ID | 事项 | 建议 |
|---|---|---|---|
| P0 | DEP-P0 | `next@14.2.35` → stable `16.3.2`，落后 2 个主版本 | 明确迁移周；08-26 后直接评估 patched `16.3.3`，避免先升已知将被替代版本 |
| P1 | BUG-P1 | `streamToText` 静默吞掉 `onChunk`/`hooks.onEvent` 异常，生成成功与下游投递成功不可区分 | 只捕获 `JSON.parse`；回调异常应 reject/中止 upstream，并补 callback-throw 回归测试。该语义会改变失败策略，故不自动改 |
| P1 | DEP-P1 | 官方预告 08-26 发布 1 个 Critical 修复，影响版本/功能面尚未披露 | 发布当日核对 affected versions、CVE 描述、BrickHub 命中面 |
| P1 | TEST-P1 | 7 个 export 零真实单测，现有集成测试绕过本模块 | 新建 `pipelineLlm.test.js`，覆盖 provider 路由、fallback、SSE 分片、callback/error/abort |
| P1 | LLM-P1 | timeout/abort、未知 provider、模型硬编码、export 面过宽等已知债务仍在 | 合并成一次 provider adapter 重构，不拆零碎 PR |
| P1 | PROC-P1 | 5 条 Dream B PR CLEAN，53 天零合并 | batch review 后 merge/close #43–#47 |

## 回滚

| 项目 | 结果 |
|---|---|
| 回滚 | 0；未修改项目代码 |

## 依赖动态

| 依赖 | 项目版本 | 当前版本/动态 | 判定 |
|---|---:|---|---|
| Next.js | 14.2.35 | stable 16.3.2；官方预告 08-26 发布 16.3.3/15.5.24 修复 1 个 Critical | DEP-P0 + DEP-P1 延续 |
| SWC native | 14.2.33 | 与 Next 同为 14.x | 无主版本撕裂 |
| Three.js | 0.183.2 | npm latest 0.185.1 | 未检出需立即处理的 breaking |
| R3F | 8.18.0 | latest 9.7.0（2026-07-31） | 主版本迁移需与 React 19 / Next 16 联动 |
| LDraw | 2026-07 | 既有 294 parts 更新 | 无新增 breaking |

### Next.js CVE 命中面复核

- middleware/proxy：0。
- App Router / Cache Components / Server Actions / RSC：0。
- `next/image`：3 处；July `CVE-2026-64644` affected versions 不含 14.2.35，禁止误报。
- `rewrites()` / `redirects()` / `fetch(new Request(...), differentInit)`：0。
- 08-26 Critical：advisory 未发布，仅观察，不预判命中。

## 竞品动态

| 项目 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio / Mecabricks | 未检出可信的新 breaking/关键发布 | 不进简报 |

## 验证记录

- `npx eslint lib/pipeline/llm.js`：通过。
- callback 异常探针：`onChunk` 调用 1 次并抛 `downstream failed`，`streamToText` 仍 resolve `"x"`，确认静默吞错。
- 生产链路：`lib/pipeline/index.js:107-110` 把 chunk 交给 `hooks.onEvent`；`pages/api/generate.js:79-82` 最终调用 `res.write()`。
- `next@14.2.35` / `@next/swc-linux-x64-gnu@14.2.33`：主版本一致。
- web_search/web_extract 后端本轮持续 HTTP 432，browser 无 Chrome；改用 GitHub API、npm registry、官方公告既有直链与本地 CVE 命中面实测交叉验证。
- 工作区原有未跟踪目录/文件未触碰。

## 信息源

- Next.js 官方：Upcoming Next.js August Security Release（2026-08-20）  
  https://nextjs.org/blog/upcoming-nextjs-security-release-august-2026
- Next.js GitHub Releases：stable `v16.3.2`（2026-08-21）  
  https://github.com/vercel/next.js/releases/tag/v16.3.2
- React Three Fiber GitHub Releases：`v9.7.0`（2026-07-31）  
  https://github.com/pmndrs/react-three-fiber/releases/tag/v9.7.0
- LDraw.org Library Updates  
  https://library.ldraw.org/updates?latest=
- npm registry：`next` / `three` / `@react-three/fiber` latest（2026-08-24 实查）
