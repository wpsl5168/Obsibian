# Dream B 2026-07-07 — pages/sandbox.js

- **文件**: `pages/sandbox.js` (417 LOC, 上次轮换 2026-06-07 #36 MERGED)
- **PR**: [#45](https://github.com/wpsl5168/brickhub/pull/45) `dreamB/auto-fix-20260707` (CLEAN)
- **守卫**: `npx eslint pages/sandbox.js` clean / `npx jest __tests__/sandbox.test.js` **22/22 pass**
- **队列态**: OPEN=3 (未熔断), 近 30d MERGED=15 (流程健康)
- **历史 PR 考古**: #36 MERGED / #19 CLOSED / #7 CLOSED — 无死代码嫌疑, 是活跃迭代页

## Pre-PR 飞行检查

| 检查项 | 结果 |
|---|---|
| 熔断闸 (OPEN ≥ 8) | ✅ OPEN=3 |
| 死代码 grep | ✅ `SandboxPage` 页级组件, Next.js 路由自动挂载 |
| 死内部符号扫 | ✅ 19 个顶层符号全部 ≥2 次引用 (最少 `BrickContextMenu/GhostBrick/SandboxBrick/SandboxScene/sandboxReducer/loadFromLS/saveToLS/buildOccupancy/initSandboxState/DEFAULT_DEF_KEY` 各 2, 无死符号) |
| 死代码姐妹扩张 | 免 (非死代码) |
| 孤儿组件检测 | ✅ 生产入口, `PROJECT_VISION.md` Phase 3 已开 |
| 同文件 OPEN PR 冲突 | ✅ 0 命中 |
| 历史 PR 考古 | 3 条历史 (#36/#19/#7) 均已 MERGED/CLOSED, 无遗留 |
| PR 队列健康 | ✅ OPEN=3, 30d MERGED=15 |

## 已自动修复 (P2/P3 → PR #45)

| ID | 位置 | 问题 | 修法 |
|---|---|---|---|
| P2-1 | L329 返回首页 button | `w-11 h-11` (44px) 未到儿童 56px | → `w-14 h-14` (56px) + svg 20→24 |
| P2-2 | L343/347/357/367 右侧工具条 (视角重置/撤销/重做/清空) | `w-12 h-12` (48px) 未到 56px | → `w-14 h-14` × 4 |
| P2-3 | L343 视角重置 button | 缺 `aria-label` (仅有 `title`) | 补 `aria-label="重置视角"` |

**兜底说明** (符合红线, 2026-07-05 W27 固化容器约束例外):
- `BrickContextMenu` L127/129/131 三按钮 (旋转/删除/关闭) 维持 `w-11 h-11` (44px)。理由: R3F `<Html>` 3D overlay 悬浮在所选积木正上方 (`position={[0, brickH+1.2, 0]}`), 若升 56px 会遮挡视野, 红线允许 44px 兜底 (成人可精准点击, 儿童场景选中态短暂)。

## 待老王拍板 (P0/P1 — 涉及业务/架构判断, 未动手)

| ID | 等级 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| **A1** | P1 (功能缺陷) | L343 "视角重置" 按钮 | **无 onClick handler**, 是死按钮 — 用户点了什么都不发生 (仅 hover/active CSS 有反应, 无功能) | 二选一: (a) 拿 `OrbitControlsRef` 调 `.reset()` (需给 `<OrbitControls>` 加 ref) + 重置到默认 `[12,10,12]/fov:48`; (b) 若功能未 ready, `disabled` + 加 tooltip "开发中" 或直接删按钮避免误导 |
| **B1** | P1 (Bug 隐患) | L269-270 pointerdown/up 全局监听 | `(e.touches && e.touches.length > 1)` — **PointerEvent 没有 `touches` 属性** (那是 TouchEvent 专属), `e.touches` 恒 `undefined`, `&&` 短路直接假 → 触屏多指旋转手势**判定失效**, `isRightDragging` 永远不会因触屏被设 true | 用 `pointerType === 'touch'` 判定 + 数原生 activePointer (pointercapture) 或直接单挂 `touchstart/touchend` 两个监听并存, 别混用一个 handler |
| **B2** | P1 (性能隐患) | L263-266 useEffect `saveToLS(parts)` | 每次 parts 变都写 localStorage, 密集连续 PLACE (儿童快速拖砖) 时序列化 + IO 抖动, 大件时 JSON.stringify 阻塞主线程 | 加 debounce 300ms (`useDebouncedCallback` from `use-debounce` 或 setTimeout ref), 或改成 `requestIdleCallback` 兜底 |

## 依赖/竞品

| 项 | 现状 | breaking / EOL / CVE | 处置 |
|---|---|---|---|
| **Next.js** | 14.2.35 | 🔴 **P1 强提醒 (延续 W27)** — 5 月 2026 官方释出 12-13 条新 CVE (SSRF/DoS/cache poisoning/XSS), 修复分支只出到 **15.5.18 / 16.2.6**, 14.x **all versions 受影响且不再打补丁**。**brickhub 命中面**: 3 处 `next/image` (pages/index, pages/model/[id], components/PartsTray) → Image Optimization SSRF/DoS 命中; middleware/proxy/Cache Components 均未用 → 相关 CVE 不适用 | 排期升 next 15.5.18 (最小改动) 或 16.2.6 (顺势升 React 19); **非紧急** (儿童产品无鉴权流水, 站点公开无付费), 但**在 14.x 时钟里越拖越危险** |
| swc binding | @next/swc-linux-x64-gnu@14.2.33 vs next@14.2.35 | ✅ 大版本对齐 14.x, 无 WASM fallback 撕裂 | 保持 |
| @react-three/fiber | 未查具体版本 (仓库 Phase 3 稳定) | ℹ️ v9 是 React 19 兼容更新, **无 breaking**; v10 才有 breaking (未 GA) | 观望 |
| Bricklink Studio / Mecabricks | (本轮未搜, 3D 部分本文件不动) | — | — |
| LDraw parts 官方库 | 稳定 | — | — |

**信息源**:
- Vercel changelog: https://vercel.com/changelog/next-js-may-2026-security-release
- Netlify security release: https://www.netlify.com/changelog/2026-05-08-react-nextjs-security-vulnerabilities
- HeroDevs Next.js EOL timeline: https://www.herodevs.com/blog-posts/nextjs-eol-dates-version-support-timeline
- R3F v9 RFC: https://github.com/pmndrs/react-three-fiber/issues/2338

## 未回滚

无。所有 P2/P3 修改均通过 lint + jest 守卫。

## 呼应设计文档

- `BUILDER_DESIGN_SPEC.md` 儿童触控 ≥56px — P2-1/P2-2 已修齐主工具条
- `DESIGN.md` LEGO 品牌色暖调 — 本轮无色彩改动, `warm-bg` + `#C91A09` 乐高红符合
- `PROJECT_VISION.md` Phase 3 (自由拼搭沙盒) — sandbox.js 是 Phase 3 主入口, A1 死按钮属 Phase 3 未完成品

## 拍板清单 (给老王)

1. **A1 视角重置死按钮** — 决定是补 onClick 还是 disabled/删除 (最快 5min)
2. **B1 触屏多指判定失效** — 影响 iPad 端用户体验 (右键拖拽旋转手势失效), 建议下轮 Dream B 定向修
3. **B2 localStorage 无 debounce** — 密集连续操作性能, 中等优先级
4. **Next.js 14 EOL + CVE 命中 Image Optimization** — 排期升 15.5.18 (最小改动), 不升的话 `next/image` 就是 SSRF/DoS 攻击面 (虽非紧急, 但排期越拖越危险)
