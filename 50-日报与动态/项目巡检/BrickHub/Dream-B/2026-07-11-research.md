# Dream B 2026-07-11 审查报告

## 概览
- 文件：`lib/knowledgeBase.js`（301 LOC, 知识库检索+prompt 格式化，AI 生成链路上游）
- 队列：OPEN PR = 4（< 8 熔断阈值，正常审查；#43/44/45/46 均 CLEAN 待老王 batch merge）
- 结果：**0 P2/P3 自动修**（代码整洁）；**1 P0（依赖）+ 2 P1（测试破产+CVE 命中面）延续**

## Pre-PR 飞行检查
| 项 | 结果 |
|---|---|
| 队列熔断 (Step 0) | OPEN=4 < 8，正常 |
| 死代码 grep (跨文件 export) | ✅ 3 真实 caller：`scripts/evaluate.js` / `pages/api/generate.js` / `pages/api/retrieve.js`，非死代码 |
| 死内部符号扫 | 15+ 内部 helper 均被 export 链路调用（loadJsonOnce→loadTaxonomy/loadOmrMetadata/…→mergedExamples→rankExamples→searchKnowledge） |
| 同文件 OPEN PR | 无冲突（现存 4 条 PR 均不涉此文件） |
| lint | ✅ `npx eslint lib/knowledgeBase.js` 无输出 |

## 已修复
（无 P2/P3 issue → 未开 PR，未创建分支）

## 待确认 P0/P1

| 级 | ID | 位置/类型 | 现象 | 建议 |
|---|---|---|---|---|
| **P0** | DEP-P0 | 全项目 | Next 16.2 stable ≥ 30 天（2026-06-10 首发已一月+，backport patch 亦已就位），brickhub 仍锁 14.2.35 **落后两大版本**（14→15→16） | **老王必须给排期或明确 defer 到 Phase X**：迁移周（预估 2-3 天：codemod + App Router 兼容层评估 + 依赖回归）或写明推迟原因 |
| **P1** | TEST-P1 | `__tests__/generateIntegration.test.js:32` | `jest.mock('../lib/knowledgeBase', () => ({...}))` 整模块 stub 成假函数 → 15+ 内部 helper（`tokenize`/`scoreTokenOverlap`/`normalizeText`/`rankThemes`/`rankExamples`/`mergedExamples`/`detectQueryCategory`/`getExampleChunks`/`findCorpusPreview`/`getKnowledgePreviewLDraw`/`getKnowledgeStats`/`getOmrMergeCacheToken`/`loadJsonOnce`/`loadTaxonomy`/`loadOmrMetadata`/`loadCreativeCorpus`/`loadGeneratedOmrMetadata`）**零真实覆盖**；`searchKnowledge` / `formatKnowledgeForPrompt` / `getKnowledgeExampleById` / `formatVariantPromptContext` 也从未真跑过 | 新建 `__tests__/knowledgeBase.test.js` 覆盖：(a) `detectQueryCategory` 中英词典命中；(b) `searchKnowledge` topK 排序+category boost；(c) `formatKnowledgeForPrompt` 空/非空 retrieval 输出契约；(d) `mergedExamples` mtime cache 失效逻辑 |
| **P1** | CVE-P1 | 依赖面（延续 07-07/08/09/10） | Next 14.x 无补丁 + 2026 May 批次 Image Optimization SSRF/DoS CVE（`next/image` × 3 命中面），无 middleware 命中（Pages Router） | 迁移到 Next 16.x（与 DEP-P0 合并解决） |

## 回滚
无（未修）

## 依赖动态
| dep | 当前 | latest stable | 差 | 判定 |
|---|---|---|---|---|
| next | 14.2.35 | 16.2.x（已 stable ≥ 30 天） | **主版 -2** | **P0**（见上）|
| three / @react-three/fiber | -（未查询本轮，节省配额） | - | - | 上轮无 breaking，本轮延续 |

搜索用量：1 / 6

## 竞品动态
本轮未消耗（0 P2/P3 修复日节省配额，Next 16 排期比竞品更急）

## 信息源
- Next 16.2 公告：https://nextjs.org/blog/next-16-2（首发 2026-06 上旬，最新 16.2 backport 已发多轮 patch）
- InfoQ 报道：https://www.infoq.com/news/2026/06/nextjs-6-2
- Vercel release bot：https://releasebot.io/updates/vercel/next-js（多轮 security-focused backport 均只回补 15/16，14.x 明示 not planned）

## 拍板清单
1. **DEP-P0**：Next 14→16 迁移周排期（本周/月/Phase X 收尾）或明确 defer 决定 —— 已连续 4 天 P0（07-08 起），无 timeline 会持续升级
2. **TEST-P1**：`knowledgeBase.test.js` 是否补齐（AI 生成链路上游，测试 ROI 极高）
3. **PR 队列**：#43/44/45/46 四条 CLEAN 已积压 3-7 天，建议 batch merge 或 close，参考 `references/dream-b-pr-queue-cleanup.md` §Step 8 三件套

---
*Dream B 2026-07-11 · lib/knowledgeBase.js · 0 auto-fix · 1 P0 + 2 P1*
