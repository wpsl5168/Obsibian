---
title: "BrickHub Dream B 2026-05-14"
created: 2026-05-14
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-05-14
---

# BrickHub 每日 Dream B — 2026-05-14

## 今日审查：`pages/sandbox.js` (413行)

上次审查 04-29，距今 15 天，rotation 中最早的未近审条目。文件 lint clean，22/22 测试全过 (baseline 22 pass)。

### ✅ 已自动修复 (PR #19)

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 6 个 icon-only 按钮缺 `aria-label`（屏幕阅读器读不出按钮含义） | 返回首页/重置视角/撤销/重做/清空/旋转/删除/关闭菜单 | 添加中文 aria-label | P2 |
| 2 | 关闭按钮 `w-7` (28px) 低于触控最低 44px | L131 BrickContextMenu close | `w-7` → `w-11` (44px)，符合 a11y 与儿童触控规范 | P2 |

PR: https://github.com/wpsl5168/brickhub/pull/19

守卫：eslint clean + jest 22/22（baseline 22/22，无回归）

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | 「重置视角」按钮无 `onClick` — 死按钮 | L342 | 仅放了 SVG 图标和 className，没绑定回调 | 需要把 cameraRef/OrbitControls 暴露 reset 方法（可用 controls.reset()），onClick 调用之；或临时 disabled+变灰 | P1 |
| 2 | `e.touches` 在 PointerEvent 上不存在 | L269-270 `down/up` handler 里检查 `e.touches.length` | PointerEvent 没有 `touches` 属性（那是 TouchEvent 才有的）。当前 `e.touches && e.touches.length > 1` 永远 false，多点触控的右键拖拽分支永远走不到 | 改用 PointerEvent.pointerType === 'touch' + 自维护 active pointer set，或同时监听 touchstart/touchend | P1 |
| 3 | 重做后 `setSelectedId` 未重置，可能选中已不存在的 id | L316-319 vs L102-108 (REDO) | DELETE 后 selectedId 设 null，但 UNDO/REDO 不会同步清空 selectedId | 在 useEffect 监听 parts 变化，若 selectedId 不在 parts 中则 setSelectedId(null) | P1 |

### ⚠️ 自动修复失败回滚

无。

## 依赖动态

| 依赖 | 当前 | 最新 | 重要变更 |
|------|------|------|---------|
| three | ^0.183.2 | (滚动发布，2026 主线推 WebGPU) | WebGPU 全浏览器可用是今年趋势，渐进迁移即可，无 breaking |
| @react-three/fiber | ^8.18.0 | 9.6.1 (15 天前) + alpha 系列 | **Major：8.x → 9.x** 含 React 19 适配等。当前 8.18 仍维护，可观望，升前先看 Drei 11 兼容矩阵 |
| @react-three/drei | ^9.122.0 | (与 R3F 9 配 Drei 11 alpha) | 同上，等 9.x 稳定再升 |
| next | ^14.2.0 | 15.x | Next 15 已正式发布，含异步 Request API、turbopack 进展。BrickHub 仍在 14.2，目前无紧迫升级理由（无安全告警），但 15 有 perf+DX 收益，下一次空闲窗口可评估 |

**建议**：本季度先维持现状，待 R3F 9.x 稳定 + 兼容 Drei 11 后做一次同步升级（R3F+Drei+React），Next 15 可独立做。

## 竞品动态

- **Bricklink Studio**：2026-04-27 发新版（修复 flexible parts 在 POV 渲染时缩水问题）。仍是 LDraw 生态官方编辑器，BrickHub 不直接竞争而是消费 LDraw 资产。

## 信息来源

- https://www.npmjs.com/package/@react-three/fiber
- https://github.com/pmndrs/react-three-fiber/releases
- https://nextjs.org/docs/app/guides/upgrading/version-15
- https://www.bricklink.com/v3/studio/download.page
- https://www.utsubo.com/blog/threejs-2026-what-changed
