# Dream B 2026-07-18 审查:lib/knowledgeBase.js

## 审查概要
- 文件: `lib/knowledgeBase.js` (301 LOC, 7 exports)
- 队列: OPEN=5 (<8 未熔断)
- 最近 merge: #39 @ 2026-07-02 (**16 天零合并**,PROC-P1 队列梗死第 4 天)
- 自动修复: **0** (lint clean; 无死代码/死累加器/死交互按钮/死内部符号)
- Pre-PR 飞行检查: 未进入(无 P2/P3 触发)

## 已修复 (P2/P3)
无。

## 待老王确认 (P0/P1)

| 级别 | ID | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| P0 | DEP-P0 | package.json | Next 14.2.35 落后 Next 16 主版本 2 档,14.x EOL 已 4+ 月无补丁;16.3 stable ≥ 38 天;累计 SSRF/DoS/中间件绕过等高危 CVE 无 backport。**排期第 11 天** | 明确迁移周或 defer 到 Phase 5 收尾,不能再挂 P0 |
| P1 | TEST-P1 | `__tests__/generateIntegration.test.js:32` | `jest.mock('../lib/knowledgeBase', ...)` 整模块 stub,7 exports 全零真实覆盖(`getKnowledgeExampleById`/`getKnowledgePreviewLDraw`/`detectQueryCategory`/`searchKnowledge`/`formatKnowledgeForPrompt`/`formatVariantPromptContext`/`getKnowledgeStats`) + 15+ 内部 helper(`tokenize`/`scoreTokenOverlap`/`rankExamples` etc.) 零跑过。**复发第 6 次** | 新建 `__tests__/knowledgeBase.unit.test.js`:①`detectQueryCategory` 3 类 fixture ②`searchKnowledge` 排序稳定性 ③`formatKnowledgeForPrompt` 无检索命中的降级路径 |
| P1 | CVE-P1 | `next/image` × 3 处 | May 2026 批次 Image Optimization SSRF/DoS CVE 命中面延续未修 | 随 Next 16 迁移一次性消化 |
| P1 | PROC-P1 | GitHub PR 队列 | 5 条 OPEN(#43-47)全 CLEAN,自 2026-07-02 起 **16 天零合并**;新一轮首选文件已开始命中同文件冲突(07-14 BrickRenderer) | 批量 review + merge,不清则 Dream B 下一轮持续零修复 |

## 回滚清单
无(未开分支)。

## 依赖动态
- Next.js: 14.2.35 → 16.3.x stable,主版本落后 2 档(P0 排期第 11 天)
- swc binding: `@next/swc-linux-x64-gnu@14.2.33` 与 next 14.2.35 主版本一致,无 WASM 撕裂
- Three.js / R3F: 无 breaking 新窗口
- LDraw parts: 无新增官方包

## 竞品动态
无跟踪窗口(节省搜索配额,聚焦 Next EOL 排期决策)。

## 信息源
- `gh pr list --state open/merged` 队列快照
- `node -e require('next/package.json').version`
- 本地 grep + eslint

## 拍板清单
1. **Next 16 迁移** — 明确"排期周"或"defer 到 Phase 5"(P0 第 11 天,不能再挂)
2. **PR 队列批量清账** — `gh pr merge 43 44 45 46 47 --squash --auto`(或按需 close),不清 Dream B 后续必卡同文件冲突
3. **knowledgeBase 单测补齐** — 新建 unit test 摘掉 jest.mock 整 stub,恢复检索管线守卫
