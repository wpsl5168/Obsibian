# Dream B 质检与预研 — 2026-08-16

## 结论

- 审查：`lib/ldrawParser.js`
- 自动修复：0；未创建分支/PR。新增问题均涉及输入契约、LDraw 变换语义或校验策略，按 P1 仅报告。
- 新增：P1 × 3；P0 × 0。
- 基线：`npx eslint lib/ldrawParser.js` 通过；`__tests__/parseLDraw.test.js` 31/31 通过，但未覆盖新增边界。
- 队列：Dream B OPEN 5（#43–#47 全 CLEAN），距最近 merge 45 天，PROC-P1 持续。

## 已修复

| ID | 级别 | 结果 |
|---|---:|---|
| — | — | 0 项；P0/P1 红线未改代码 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 发现 | 建议 |
|---|---:|---|---|---|
| BUG-P1-1 | P1 | `lib/ldrawParser.js:431-444,466-470,504-507,521-542` | 完整 type-1 行用 `parseFloat` 且未校验 `Number.isFinite`。实测 `NaN` 坐标进入 bricks、矩阵 `NaN` 生成全 NaN quaternion、`20oops` 被接受为 20；均 `errors=[]`。`validateGeometry` 又会静默跳过/漏检，形成“解析成功但渲染数据污染”。 | 拍板契约：非法数值整行报错并丢弃（推荐），或明确容错；坐标与 9 个矩阵值统一 finite + 完整数字语法校验，并补边界测试。 |
| ARCH-P1-1 | P1 | `lib/ldrawParser.js:435-444` vs `520-542` | parser 把 LDraw 3×3 变换压成 quaternion，缩放/镜像信息丢失；validator 却按完整矩阵算 AABB。实测 X×2 矩阵产生非单位 quaternion，而校验按双倍宽度报碰撞，渲染与守门员语义分裂。 | 明确产品是否支持缩放/镜像 type-1；支持则保留 matrix/scale，禁止仅 quaternion；不支持则解析期明确拒绝非正交矩阵。 |
| GEO-P1-1 | P1 | `lib/ldrawParser.js:392-393,509-513` | 任意未知 `.dat` 被 `resolvePartId` 回退成 `1x1`。因此 validator 注释所称“跳过未知零件”实际不成立，复杂官方件/子模型会以虚构 1×1 AABB 参与碰撞与悬空判断，制造假阳性。 | 分离“渲染降级”和“几何校验解析”：未知官方件可显示占位，但 validator 应标记 unknown/跳过，不能伪装 1×1。 |
| TEST-P1 | P1 | `__tests__/parseLDraw.test.js` | `validateGeometry` 130 LOC 仍零直接测试；31 个 parse 测试未覆盖 NaN/Infinity/尾随垃圾数字、镜像/缩放矩阵和 unknown `.dat` 校验语义。 | 补 `ldrawParser.geometry.test.js`，先锁定上述契约。 |
| PROC-P1 | P1 | PR #43–#47 | 5 条 Dream B PR 全 CLEAN，45 天零合并；未触发 OPEN≥8 熔断，但流程已梗死。 | batch review 后 merge/close，避免继续占用队列。 |
| DEP-P0 | P0（决策项） | `next@14.2.35` | 官方 Next 16.3 已于 2026-08-03 stable；项目落后两大版本，14.x 无当前安全补丁线。 | 排迁移周；目标至少 15.5.21，优先评估 16.3.1 + React 19/R3F 9 联动。 |
| CVE-P1 | P1 | Next Image Optimization | July 2026 九类逐面扫描：App Router、middleware/proxy、Server Actions、cache、特殊 fetch、rewrites/redirects 均 0；`next/image` 3 处。CVE-2026-64644 条件部分命中：自托管 + remotePatterns 存在，但当前 URL 固定可信 CDN 且 `.png`，未证实恶意 SVG 可达。 | 不夸大为已可利用；升级压力来自 14.x EOL/无补丁。 |

### BUG-P1-1 最小实证

```text
NaN position → bricks[0].position=[NaN,0,0], errors=[]
NaN matrix   → bricks[0].quat=[NaN,NaN,NaN,NaN], errors=[]
20oops       → position.x=1（parseFloat=20，再乘 LDU）, errors=[]
Infinity     → 非有限 position 进入 bricks, errors=[]
```

## 回滚

| 项目 | 结果 |
|---|---|
| 代码回滚 | 无；未修改项目代码 |

## 依赖动态

| 依赖 | 项目 | 当前上游 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.1 | DEP-P0：stable 已 13 天，落后 2 大版本；官方 16.3 宣布 2026-08-03 发布 |
| Three.js | 0.183.2 | 0.185.1 | 落后 2 个 minor；本轮未发现明确 breaking，暂不单独升级 |
| @react-three/fiber | 8.18.0 | 9.7.0 | v9 面向 React 19，含 StrictMode 等 breaking；应与 Next/React 迁移一起验证 |
| @next/swc-linux-x64-gnu | 14.2.33 | Next 14.2.35 | 主版本一致，无 binding 撕裂 |

## 竞品 / LDraw 动态

| 项目 | 动态 | 判定 |
|---|---|---|
| LDraw 新零件 | 官方论坛新增 2026-08-01 批次，08-05 更新，含普通件与 textiles | 信息项；素材仍只取官方 Parts Library/Tracker，禁直接把 raw mesh 当正式 part |
| BrickLink Studio / Mecabricks | 本轮未检出可信 breaking 或产品级新动作 | 无需简报 |
| GitHub LDraw | 未检出影响当前架构的新增 breaking | 无需简报 |

## 信息源

- Next.js 16.3（官方，2026-08-03）：https://nextjs.org/blog/next-16-3
- Next.js July 2026 Security Release（官方）：https://nextjs.org/blog/july-2026-security-release
- R3F v9 Migration Guide（官方）：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 2026 New Parts（官方社区）：https://forums.ldraw.org/thread-29267.html
- 本地实证：eslint、Jest 31/31、Node 边界探针、Next CVE 7 类命中面 grep、GitHub PR 队列。
