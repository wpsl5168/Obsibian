# Dream B 2026-07-16 审查:lib/pipeline/llm.js

## 概况
- 文件:`lib/pipeline/llm.js` (181 LOC, 7 exports)
- 上次改动:febe1246 (feat phase 1) — 距今 >30 天,自 07-09 首审后**内容零变化**
- 唯一 caller:`lib/pipeline/index.js:4` `import { callProviderWithFallback, streamToText }`
- OPEN PR: 5(#43/44/45/46/47 全 CLEAN)| 30d MERGED:1(#42, 07-02) | 距最近 merge:14 天
- 队列 < 8 未熔断;lint clean;jest 无对应测试文件

## 已修复
_无 P2/P3 可自动修_

## 待确认(P0/P1 仅报告)

### DEP-P0 Next.js 14.2.35 落后两大版本(第 9 天延续)
- Next 16.3.x stable 已 >30 天,brickhub 仍 14.2.35;16 主版本已 stable >一个月
- 建议:老王给出迁移周排期 or 明确 defer 到 Phase 5
- 前置隐患:pages router 迁移 App Router 工作量、`next/image` API 调整、CSS/Font 载入变化

### DEP-P1 Next 14.x 无补丁 + CVE 命中面(延续)
- `next/image` 3 处仍暴露 May 2026 Image Optimization SSRF/DoS 批次(pages/model/[id].js / PartsTray.js / home/*)
- middleware 类 CVE (44575/44578/29927) 不适用(无 middleware.ts + pages router)

### LLM-P1×4 provider 层健壮性(第 3 次延续,07-09/07-15 已列)
1. **无超时**:`httpsPost` 未设 `req.setTimeout(...)`,provider 挂起时永不 fallback 到下一家 → API 路由会僵死到 Node 默认 keepalive 才断
2. **streamToText 无中断信号**:客户端断开 / 上游 500 中途终止,`upstreamRes.on('data')` 循环无 AbortController 通道,已消费 chunk 无法回滚
3. **model 硬编码**:`gpt-4o` / `glm-4-plus` / `claude-3-5-sonnet-20241022` 写死,无环境变量覆盖 → 换模型必改代码
4. **provider chain 无健康度记忆**:`callProviderWithFallback` 每次从头试,连续失败的 provider 不 backoff,吃满配额
- 涉及 API 网络重构,不自动改;建议一次性重构 `httpsPost` → `fetch + AbortSignal.timeout(30_000)`

### TEST-P1 零真实覆盖(延续)
- `__tests__/` 下无 `llm.test.js` 或 mock 引用
- 7 export 全部零跑过(fetch/stream 类天生难测,建议 `msw` 或 `nock` mock https)
- ROI 高:provider fallback 是 AI 生成链路的核心防线,零测试等于零守卫

### PROC-P1 队列梗死延续(第 2 天)
- OPEN 5 条距最近 merge 14 天(阈值 14),阻塞 Dream B 下一轮修复
- 建议:老王 batch-review + merge 或 close 非必要 PR;详见「PR 队列清账」清单

## 回滚
_无_

## 依赖动态
- **Next.js**:16.3.x stable > 30 天,14.x EOL Feb 2026 后无补丁 — DEP-P0
- **@react-three/fiber / Three.js / LDraw**:未查(≤6 次限额留给关键)
- **swc binding**:14.2.33 与 next 14.2.35 主版本一致,无撕裂

## 竞品动态
- 未搜索(节约配额,与 07-15 报告一致)

## 拍板清单
1. **DEP-P0 Next 16 迁移**:请老王给出周排期 or defer 到 Phase 5 明示
2. **PROC-P1 队列清账**:`gh pr list --state open --search "head:dreamB"` 5 条 batch merge/close(见 `references/dream-b-pr-queue-cleanup.md`)
3. **LLM-P1 provider 健壮性重构**:超时 + AbortController + model env 化(一次性 PR,业务改动请老王拍)

## 信息源
- 本地 lint(eslint clean)
- git log(lib/pipeline/llm.js 自 febe1246 无改动)
- Dream B 2026-07-09/15 报告 continuity
