# Dream B — BrickHub 每日质检与预研（2026-08-26）

## 结论

- 今日审查：`lib/knowledgeBase.js`。0 项自动修复，未创建分支/PR。
- 新增 **DEP-P0**：Next.js 2026-08-25 发布两个 Critical RCE 修复；项目 `next@14.2.35` 落在 AVIF Image Optimization RCE 受影响版本范围，且 14.x 无补丁线。当前代码未配置 AVIF、remotePatterns 仅两个固定 CDN，故暂未证实攻击者可控 AVIF 可达，但不能把 EOL 版本视为已修复。
- `CVE-2026-75604`（Windows + Pages/App Router 混用）不适用：当前运行/开发环境为 Linux，仓库无 `app/`。
- 流程仍梗死：5 条 Dream B PR 均 CLEAN，距最近 merge 55 天。

## 队列状态

| 指标 | 结果 | 判定 |
|---|---:|---|
| Dream B OPEN PR | 5 | 未触发 ≥8 熔断 |
| 全仓 OPEN PR | 6 | — |
| 最近 merge | 2026-07-02 | 55 天零合并，PROC-P1 |
| OPEN Dream B PR | #43–#47 | 均 CLEAN |

## 四维度审查

| 维度 | 结果 | 分级 |
|---|---|---|
| Bug 隐患 | `detectQueryCategory` 的双向 substring 仍使 `race car`、`carpet` 误判 `vehicle`；`topK='abc'` 仍静默返回 0 examples | BUG-P1 / API-P1，延续项，仅报告 |
| 架构 | 2 个生产 caller（`pages/api/generate.js`、`pages/api/retrieve.js`）；无死模块、无顶层死符号 | OK |
| UI 规范 | 纯服务端知识检索模块，无 UI/触控面 | N/A |
| 测试覆盖 | 唯一集成测试整模块 `jest.mock`；7 个 export 无真实单测。`generateIntegration` 6/6 通过但不执行本模块 | TEST-P1，延续项 |

### 实测探针

- `race car → vehicle(score=1)`；`carpet → vehicle(score=1)`；语义误判仍存在。
- `topK='abc' → examples=0`；非法数字参数仍形成“成功返回空结果”。
- `npx eslint lib/knowledgeBase.js`：通过。
- `npx jest __tests__/generateIntegration.test.js --runInBand`：6/6 通过；因整模块 mock，不构成真实覆盖。

## 已修复

| ID | 修复 | PR |
|---|---|---|
| — | 无 P2/P3；未开分支 | — |

## 待确认（P0/P1）

| ID | 级别 | 事项 | 建议 |
|---|---|---|---|
| DEP-0826 | P0 | `next@14.2.35` 命中 GHSA-2xp9-vwfh-vxw4 版本范围；官方仅提供 `15.5.24` / `16.3.3` 修复 | 明确迁移周，目标 `16.3.3+`；迁移前确认生产托管面与 AVIF 输入可控性 |
| PROC-01 | P1 | #43–#47 CLEAN，55 天零合并 | batch review 后 merge/close，解除 Dream B 长期阻塞 |
| BUG-0815A | P1 | 英文短 token 双向 substring 导致分类误判 | 英文 exact/boundary、中文 substring 分治 |
| API-0815B | P1 | 非法 `topK` 被 `NaN` 传入 `slice`，HTTP 200 + 空结果 | API 边界返回 400 或显式回退默认值 |
| TEST-01 | P1 | `knowledgeBase` 被整模块 mock，7 export 零真实覆盖 | 新增 public-function 边界测试，覆盖单字母 token 与非法 topK |

## 回滚 / 未能自动修复

| 项目 | 结果 |
|---|---|
| — | 无改动，无回滚 |

## 依赖动态

| 依赖 | 项目版本 | 最新 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.3 | DEP-P0；落后 2 大版本且 14.x EOL |
| @react-three/fiber | 8.18.0 | 9.7.0 | React 19 联动迁移，非今日 breaking |
| Three.js | r183 | r185 | 落后 2 revisions，未发现今日 breaking |
| SWC native | 14.2.33 | 随 Next | 与 Next 同主版本，无撕裂 |

### 2026-08 Critical 安全命中面

| Advisory | 适用条件 | BrickHub 结果 |
|---|---|---|
| GHSA-2xp9-vwfh-vxw4（AVIF Image Optimization RCE） | Next `>=10 <15.5.24` 或 `<16.3.3` + 优化攻击者可控 AVIF | **版本命中**；`next/image` 3 处；未配置 `image/avif`，remotePatterns 仅 `cdn.rebrickable.com` / `img.bricklink.com`，可达性未证实 |
| CVE-2026-75604 / GHSA-p293-qw3h-jr36（Windows RCE） | Windows + Pages Router/App Router 混用 + 无 Cache Components | 不适用：Linux，且无 `app/` |

补充：CVE 探查 7 类重跑——无 middleware/proxy、无 App Router、无 Cache Components/Server Actions、无 RSC、无 rewrites/redirects；`next/image` 3 处。`npm audit` 尚未收录 08-25 Critical，但已列出 Next 14 的 14 项旧漏洞（1 moderate、13 high），不能以 audit 未报 Critical 作为安全结论。

## 竞品 / LDraw 动态

| 对象 | 动态 | 判定 |
|---|---|---|
| LDraw Parts Library | 最新仍为 2026-07：414 新文件、294 新零件 | 无 breaking；素材继续只用官方库 |
| BrickLink Studio / Mecabricks | 未核到影响 BrickHub 的 breaking 动作 | 无 breaking |
| GitHub LDraw | `datakurre/brick-mcp` 提供模型读写/改色/加件 MCP 工作流 | 观察项，非迁移触发 |

## 拍板清单

1. 为 Next 14 → `16.3.3+` 指定迁移周；Critical advisory 已发布，不再维持“待公告”。
2. 批量 review #43–#47，merge 或 close，解除 55 天流程梗死。

## 信息源

- Next.js 官方 2026-08 Security Release：https://nextjs.org/blog/august-2026-security-release
- GitHub Advisory GHSA-2xp9-vwfh-vxw4：https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4
- Vercel 影响说明：https://vercel.com/changelog/nextjs-august-2026-security-release
- Netlify 影响说明：https://www.netlify.com/changelog/2026-08-25-nextjs-security-vulnerabilities/
- LDraw 官方库：https://library.ldraw.org
- React Three Fiber：https://github.com/pmndrs/react-three-fiber
- GitHub brick-mcp：https://github.com/datakurre/brick-mcp
