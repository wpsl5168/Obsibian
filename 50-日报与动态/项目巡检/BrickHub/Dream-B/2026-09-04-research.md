# Dream B 日审 — 2026-09-04

## 结论

- 审查文件：`lib/pipeline/llm.js`
- 自动修复：0；未创建分支/PR。
- 新增：LLM-P1 ×1。`callProviderWithFallback()` 只覆盖建立 HTTP 响应阶段；流读取在返回后由 `streamToText()` 单独执行，若 SSE 中途断流，流程直接进入离线兜底，不会尝试 provider chain 的下一家。
- 延续：Next.js 14 迁移 DEP-P0；Dream B 队列 PROC-P1（6 OPEN，距上次 merge 64 天）。

## 四维扫描

### 已修复

| ID | 位置 | 结果 |
|---|---|---|
| — | — | 无 P2/P3，不改代码 |

### 待确认（P0/P1）

| ID | 等级 | 位置 | 发现 | 建议 |
|---|---|---|---|---|
| LLM-1 | P1（新增） | `lib/pipeline/llm.js:99-122` + `lib/pipeline/index.js:104-111` | provider fallback 在拿到 2xx response 后即结束；`streamToText` 中途 `error` 由 pipeline 外层 catch 接管并切本地模型，剩余 provider 不再尝试 | 将“请求+完整流消费”合并为单次 provider attempt，失败后再推进 chain；同时区分 handshake/stream/parse/callback 错误 |
| LLM-2 | P1（延续） | `llm.js:3-18,124-181` | 请求与流读取无 timeout/AbortSignal，半开连接可长期占用请求 | 统一 deadline + AbortController，并记录 provider/阶段/耗时 |
| LLM-3 | P1（延续） | `llm.js:102-110` | 未知 provider 静默落入 Anthropic | 显式 provider allowlist；未知值 fail-fast |
| LLM-4 | P1（延续） | `llm.js:129-148` | 宽泛 catch 会吞 `onChunk` 异常及 JSON 解析异常，观测与消费错误混在一起 | JSON parse 可容错；callback 异常应独立上抛或结构化记录 |
| TEST-1 | P1（延续） | `__tests__/` | 7 个 export 无真实测试命中 | 补 handshake 非 2xx、首家中途断流→次家接管、timeout、未知 provider、callback 抛错测试 |
| DEP-1 | P0（延续） | `next@14.2.35` | 上游 stable `16.3.4`，落后两个大版本且 14.x 无当前安全修复线 | 明确迁移周；最低先到 `15.5.25`，目标 `16.3.4` |
| PROC-1 | P1（延续） | GitHub PR 队列 | Dream B OPEN 6；最近 merge 为 2026-07-02，已 64 天 | batch review/merge/close，优先清 #43–#48 |

### 回滚

| 项目 | 结果 |
|---|---|
| 自动修复回滚 | 无；未修改 BrickHub |

## 验证链路

- `npx eslint lib/pipeline/llm.js`：通过。
- 生产调用：`lib/pipeline/index.js` 仅导入 `callProviderWithFallback`、`streamToText`；其余 provider helpers 仅模块内部使用。
- 测试检索：`__tests__/` 对 7 个 export 均无命中，属零真实覆盖。
- SWC：Next `14.2.35` / `@next/swc-linux-x64-gnu 14.2.33`，主版本一致，无 binding 撕裂。
- 工作区已有未跟踪目录/文件，且当前处于旧 Dream B 分支；本次未碰 BrickHub 工作树，避免污染。

## Next.js CVE 命中面

| 类别 | 结果 |
|---|---|
| Middleware / Proxy | 0，不适用 |
| App Router | 无 `app/`，不适用 |
| Cache Components / Server Actions / RSC | 0，不适用 |
| Image Optimization | `next/image` 3 处：`pages/index.js`、`pages/model/[id].js`、`components/PartsTray.js` |
| August AVIF RCE | 版本与 Image Optimization 功能面命中；现有配置未证实攻击者可控 AVIF 可达，不能写成已可利用 |
| Windows 双 Router RCE | Linux + 无 App Router，不适用 |

官方 2026-08-25 安全公告要求升 `16.3.3` 或 `15.5.24`；当前 stable 已到 `16.3.4`，恢复 AVIF Image Optimization。Next 14 无对应修复线，维持 DEP-P0。

## 依赖动态

| 项目 | 当前 | 最新/动态 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 16.3.4 | DEP-P0，迁移排期必须拍板 |
| Three.js | ^0.183.2 | r185 / npm 0.185.1 | r185 含 deprecated code removal；未发现本文件相关 breaking |
| R3F | ^8.18.0 | 9.7.0；v10 alpha.4 | v9 随 React 19；v10 仍 alpha，不追 alpha |
| LDraw | 2026-08 update | +202 files / 144 parts / 2 primitives | 官方增量，非 breaking |

## 竞品动态

| 项目 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio | 2.26.8_1（2026-08-31），修复 LXFML 映射、nested submodel null bricks、copy/mirror | bugfix，无 breaking |
| Mecabricks / GitHub LDraw | 未检出可信重大产品或 breaking 动态 | 不进简报 |

## 信息源

- Next.js August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- Next.js releases：https://github.com/vercel/next.js/releases
- Three.js releases：https://github.com/mrdoob/three.js/releases
- React Three Fiber releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw 2026-08 Parts Update：https://forums.ldraw.org/thread-29635.html
- BrickLink Studio Early Access：https://www.bricklink.com/v3/studio/early-access.page
