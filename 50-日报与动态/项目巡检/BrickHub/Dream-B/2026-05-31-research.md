---
title: "BrickHub预研 2026-05-31"
date: 2026-05-31
tags: [BrickHub, dreaming, research]
---

# BrickHub 预研报告 — 2026-05-31

## 今日审查文件
`components/home/MenuPanel.js` (26 LOC) — rotation 首次轮到。

## 自动修复（P2/P3）

| # | 文件 | 类型 | 改动 | Lint | 状态 |
|---|------|------|------|------|------|
| 1 | components/home/MenuPanel.js | P2 触控 | "清空历史"按钮 `py-2 text-sm` ≈36px < 44px 红线，加 `min-h-11` 兜底 | ✅ | committed |

分支：`dreamB/auto-fix-20260531`
PR：待 push（见末尾）

## P0/P1 报告（仅观察，不动手）

### 🔴 P0 — 无

### 🟠 P1 — components/home/ 目录全 orphan（已升级到目录级报告，#29b）

继上次 5-30 HistoryPanel 之后，今日 MenuPanel.js 同样确认无任何外部引用：
```
grep -rln "MenuPanel" --include="*.js" → 仅 components/home/MenuPanel.js 自身
```

**累计死组件清单**（components/home/ 11 文件 + hooks/useBrickHubHome.js，跨 2026-05-17 至今）：
- StatusBanner / ProcessDebugPanel / LDrawModal / PromptDock / KnowledgePreviewPanel / LDrawEditorPanel / FloatingControls / HistoryPanel / MenuPanel / BrickScene / KnowledgePreviewPanel
- 关联 hook：`hooks/useBrickHubHome.js`（仅自身引用）

**两次目录级 grep 都返空**：
```bash
grep -rln "from.*home/\|home/[A-Z]" pages/ components/ --include="*.js" | grep -v "components/home/"
# → 无输出
grep -rln "useBrickHubHome" --include="*.js" | grep -v "hooks/"
# → 无输出
```

**决策请求**（同 5-20、5-30 报告，持续未处理）：
1. 接线 — 这批组件是某 home page 重构的一半成品，需补 `pages/home.js` 或在 `pages/index.js` 引入
2. 删除 — 整个 components/home/ + hooks/useBrickHubHome.js 全删
3. 标记 archive — 移到 `components/_archive/` 保留代码备查

建议优先级：**1 周内拍板**，每天 Dream B 审计这批 orphan 是无效消耗（已是连续第 9 个 component）。

### 🟠 P1 — Next.js 14.2.0 仍在 May 2026 13-CVE 漏洞影响范围（持续未处理）

历史项重申（首次报告 2026-05-26 lib/pipeline/llm.js 时附带）：
- May 2026 Vercel 13 CVE 紧急安全发布（含 CVE-2026-23869/23870 DoS、CVE-2026-44578 WebSocket SSRF）
- 14.x 全版本受影响，**无 WAF mitigation**
- 必须升 14.x → 15.5.16+/16.2.5+ 或更高补丁

BrickHub 当前 `next: ^14.2.0`。建议保守路径：跟随 Next.js 14.x 是否有 backport 补丁；激进路径：升 15.5.x（评估 R3F + React 18→19 联动）。

## 依赖动态

| 依赖 | 当前 | 最新 | 备注 |
|------|------|------|------|
| Next.js | ^14.2.0 | 15.5.16+ / 16.2.5+ | 🔥 May 2026 13 CVE，必须升 |
| @react-three/fiber | ^8.18.0 | v9.5.0 | v9 需 React 19，先停在 v8 |
| @react-three/drei | ^9.122.0 | 同步 r3f v9 升级 | 一起升 |
| three | ^0.183.2 | r172/r173 | 已超 r3f v8 peer 上限（历史报告项，需 r3f v9 时一并对齐） |

## 竞品动态

无重大变化。Bricklink Studio / Mecabricks 本日无新发布。

## 信息来源
- https://github.com/vercel/next.js/security/advisories/GHSA-q4gf-8mx6-v5v3
- https://hadrian.io/blog/next-js-websocket-ssrf-unauthenticated-access-to-internal-resources-cve-2026-44578-2
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- https://github.com/mrdoob/three.js/issues/30263
