# Dream B 日报 — 2026-06-20

**审查文件**: `components/home/LDrawModal.js` (61 行, modal 弹层)
**结论**: 🔴 **死代码 + PR 队列梗死** — 跳过自动修复 (Pre-PR 飞行检查全挂)

---

## 🛑 Pre-PR 飞行检查结果(2026-06-18 固化协议)

| 检查项 | 命令 | 结果 | 决策 |
|---|---|---|---|
| 死代码检测 | `grep -rEn "LDrawModal\|home/LDrawModal\|ldraw-modal" --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" --include="*.md" --include="*.json"` | **仅自身定义 1 行**(`components/home/LDrawModal.js:3`),全仓零 import/dynamic-import/路由引用 | **不开 PR** |
| 同文件 OPEN PR 冲突 | `gh pr list --state open --search "head:dreamB" --json files` | 无命中(LDrawModal.js 上次 PR #24 2026-05-19 已 OPEN,无冲突也无新点可加) | 略 |
| PR 队列健康 | `gh pr list --state open` / `--state merged --search "merged:>=2026-05-20"` | **OPEN=30, 近 30 天 MERGED=0** | **PROC-P1 流程梗死** |

→ 不在死代码上抹奶油 (ARCH-P1),先报告让老王拍板。

---

## 🔍 4 维度扫描发现

### 维度 1 — Bug 隐患
- 无显式 bug,代码结构清晰
- `setTimeout(() => textareaRef.current?.focus(), 50)` 在 useEffect 内未清理 — 弱告警 (50ms,unmount 期间命中概率极低,P3 级别),但因死代码不修

### 维度 2 — 架构
- **🔴 ARCH-P1 死代码**: `LDrawModal` 全仓零引用。git log 显示该组件最近一次修改是 PR #24 (2026-05-19 a11y 修复),更早是 2026 年初 `a76677eb` BrickRenderer 重写时被遗留。
  - **可能场景**: ① 早期 home 页"手动粘贴 LDraw"入口已废弃 (现走 `LDrawEditorPanel` + `PromptDock`) ② 计划接入但未连线
  - **拍板项**: 删除 / 重接到 home 页 / 留作 future,需老王确认
  - **影响**: 死代码占编译产物 ~2KB,a11y 修复 (PR #24) 实际没意义

### 维度 3 — UI 规范 (假设需要保留时)
- ❌ **关闭按钮 h-8 w-8 = 32px**,违反"最低 44px"红线,儿童触控目标 56px 更不达标 (P2 级)
- ❌ **0 个 aria 属性 / role="dialog" / aria-modal** — 屏幕阅读器无法识别为模态对话框,焦点陷阱缺失 (P1 级 a11y)
- ❌ ESC 键不关闭 modal (无 keydown 监听) (P2 级)
- ❌ 按钮主色 `bg-blue-500` 不在 LEGO 品牌色系内 — 应用 LEGO 红 `#D01012` / 黄 `#FFD500` / 蓝 `#006CB7` (P2 级)
- ❌ 背景 `bg-black/50` 偏冷 — 红线"禁深色,要暖色调"应改 `bg-orange-900/40` 或 `bg-amber-950/40` (P2 级)

### 维度 4 — 测试覆盖
- ❌ `__tests__/` 无 `LDrawModal.test.js` — 但既然是死代码,测试 ROI 为负

---

## ✅ 自动修复 (本次:0 项)

PR 队列已 30 个 OPEN + 30 天 0 MERGED,且目标文件是死代码 — 双重红线触发,不开 PR。

---

## ⏳ 待确认 (P0/P1,需老王拍板)

| ID | 级别 | 描述 | 建议 |
|---|---|---|---|
| ARCH-P1 | P1 | `components/home/LDrawModal.js` 全仓零引用,确认是死代码还是待接入功能 | 三选一: ① `git rm` 删除 + 同步删 PR #24 + LDrawModal 相关测试期望 ② 在 home 页接回(配 `useState` + 触发按钮) ③ 加 `// @deprecated` 注释 + README 备注 |
| PROC-P1 | P1 | **PR 队列梗死: OPEN=30, 近 30 天 MERGED=0** | Dream B 自动修 PR 已堆积一个多月无人合,继续每天产出只会加深噪声。建议: ① 暂停 Dream B 自动修(改为只报告) ② 老王集中清账一批 ③ 设 PR 合并 SLA(7 天未合则 auto-close) |
| SEC-P1 | P1 | **Next.js 14 累积高危 CVE 无补丁** (持续报警,本周新增 May 2026 一次性 13 个 advisory) | 升级 Next.js 15.5.x 或 16.x。补丁不会回流到 14.x |

---

## 🔁 回滚

无,本次未做任何代码修改。

---

## 📦 依赖动态

| 包 | 当前 | 最新/动态 | 影响 |
|---|---|---|---|
| **next** | 14.x | 15.5.16 / 16.2.5 (2026-05 安全发布) | 🔴 **P1 EOL + 高危 CVE**: May 2026 一次性 13 条 advisory (8 High: auth bypass / proxy bypass / SSRF / DoS / 路由参数注入 等),三条 EOL-only CVE: 44573/44577/44572 (CVSS 7.5)。Vercel/Netlify 明示"14.x patches not planned,需升 15/16"。WAF 仅缓解 DoS,不解决 auth/SSRF |
| three | (rev ~r150 区间) | r170 (2025-08) | 无 breaking — WebGPU 模块移到 addons,GLTFLoader 增强;本项目仅用 LDrawLoader + WebGL 渲染,不受影响 |
| @react-three/fiber | v8.x | v9 (React 19 兼容) | 无 breaking — v9 唯一 breaking 是 TypeScript 类型,本项目纯 JS。但本项目 React 18,跨大版本暂不必跟进 |
| @react-three/drei | (随 R3F) | 持续更新 | 无 breaking |

## 🎯 竞品动态

本次预算未触发竞品扫描(总搜索 3/6,已用 Next.js/three.js/R3F)。下次轮换 Bricklink Studio + Mecabricks。

---

## 📚 信息源

1. Vercel changelog: Next.js May 2026 security release — https://vercel.com/changelog/next-js-may-2026-security-release
2. GitHub: Next.js Discussion #89551 CVE-2026-23864 mitigation guidance — https://github.com/vercel/next.js/discussions/89551
3. HeroDevs: CVE-2026-44573/44577/44572 EOL-only — https://www.herodevs.com/blog-posts/cve-2026-44573-cve-2026-44577-cve-2026-44572-three-next-js-vulnerabilities-affecting-eol-versions
4. Netlify changelog: Next.js & React DoS — https://www.netlify.com/changelog/2026-04-08-react-nextjs-dos-vulnerability
5. utsubo.com: What's New in Three.js 2026 — https://www.utsubo.com/blog/threejs-2026-what-changed
6. R3F v9 migration: https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide

---

## 💭 元注 — Dream B 自身需要拍板

PR 队列 30 个堆积 + 0 合并,本机制已进入"自动产出比人工消化快"的失效状态。建议老王本周内做出一个决定:
- 选项 A: 抽 1-2 小时 batch review/merge,清账后 Dream B 继续
- 选项 B: 切换 Dream B 模式 — 关闭"自动开 PR",改为"仅 Obsidian 报告 + 简报列待修项",由老王手动启动批改
- 选项 C: 缩窄 P2 自动修边界(只改 lint-fix 级别,不改 a11y/UI 规范),减少 noise

我推荐 B,降低 PR 队列噪声,把 attention 留给真的 P0/P1。
