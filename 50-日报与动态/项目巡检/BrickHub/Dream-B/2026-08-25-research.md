# Dream B 2026-08-25 — `lib/pipeline/utils.js`

## 结论

- 自动修复：0；未创建分支/PR。
- 新增 **BUG-P1 ×1**：矩阵“纠正”逻辑会把合法的任意角旋转和缩放改成单位矩阵，模型姿态/比例静默失真。
- 延续 **BUG-P1 ×1**：`parseInt` 接受 `1x` 等尾随垃圾；本次探针再次实证。
- 延续 **TEST-P1 ×1**：8 个 public exports 在 `__tests__/` 中零真实引用。
- 流程 **PROC-P1**：5 条 Dream B PR 均 CLEAN，但距最近合并已 54 天；队列未达熔断阈值。

## 审查范围

- Bug 隐患：输入提取、去重、矩阵校验/归一化、fallback 生成。
- 架构：生产 caller、export 面、LDraw Type 1 语义。
- UI：纯函数模块，不适用。
- 测试：按 export 粒度检索 `__tests__/`，零命中。

## 已修复

| ID | 级别 | 结果 |
|---|---|---|
| — | — | 无 P2/P3；未改项目代码 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 证据 | 建议 |
|---|---|---|---|---|
| BUG-1 | P1 | `utils.js:42-86` | 合法 45° Z 旋转矩阵 `0.707107/-0.707107` 经 `normalizeLDrawMatrices` 后被替换为 identity，`matrixFixCount=1`；合法 scale=2 同样被抹平。LDraw 官方规范明确 a..i 同时表示 rotation **and scaling**，并非只能取 `-1/0/1` | 业务拍板：校验应改为“9 项均 finite + determinant 非零/容差”，保留合法旋转/缩放；仅对非有限、奇异或缺项矩阵 fallback。需与 parser/renderer/validator 三链一起补回归测试 |
| BUG-2 | P1 | `utils.js:44` | `Number.parseInt('1x',10)===1`，畸形矩阵被判合法、`matrixFixCount=0`；2026-08-14 已首次报告 | 改 `Number()` + `Number.isFinite`，但需与 BUG-1 的合法矩阵契约一并设计，禁止单点自动修 |
| TEST-1 | P1 | `__tests__/` | `deduplicateLDraw/extractLDraw/isUnitAxis/dot3/det3/isValidOrthogonalMatrixTokens/normalizeLDrawMatrices/buildOfflineFallbackLDraw` 均无真实引用 | 新建表驱动测试：任意角旋转、缩放/镜像、奇异矩阵、NaN/Infinity/尾随垃圾、普通 code fence、fallback 官方 part ID |
| PROC-1 | P1 | PR #43–#47 | Dream B OPEN=5，均 `mergeStateStatus=CLEAN`；最近 merged 为 #39（2026-07-02），已 54 天 | batch review 后 merge/close，避免同文件冲突持续拦截 |
| DEP-1 | P0 | `next@14.2.35` | 官方 Next 16.3 已于 2026-08-03 stable，项目落后 2 个 major 且 14.x 无补丁线 | 排迁移周；目标至少等待 2026-08-26 安全版 `16.3.3` 发布后评估 |
| DEP-2 | P1 | Next 安全公告 | 官方预告 2026-08-26 修复 1 个 Critical，届时发布 `16.3.3/15.5.24`；当前尚无 advisory，**不能断言 BrickHub 命中** | 08-26 发布后按 CVE 编号、版本范围、功能面三对齐复核 |

## 回滚 / 未能自动修复

| 项目 | 状态 | 原因 |
|---|---|---|
| 无 | — | P0/P1 红线，仅报告 |

## 验证链路

- `npx eslint lib/pipeline/utils.js`：通过。
- 生产引用：`lib/pipeline/index.js` 真实 import 4 个函数，非死代码。
- 最小探针：45° rotation 与 scale=2 均被替换为 identity；`1x` 被静默接受。
- CVE 功能面：Pages Router；无 app/middleware/proxy/Server Actions/Cache Components/RSC/rewrites/redirects/特殊 `fetch(new Request...)`；July 2026 批次 0 个已证实命中。`next/image` 有 3 处，但不能据此套用已排除版本线的 CVE。
- SWC：Next 14.2.35 / SWC 14.2.33，major 对齐。

## 依赖动态

| 项目 | 当前 | 上游 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 16.3.2；08-26 计划 16.3.3 | P0 迁移排期；Critical 适用性待 advisory |
| React Three Fiber | 8.18.0 | 9.7.0 | v9 随 React 19，StrictMode/GL callback/texture color management 有迁移项；无当日新 breaking |
| Three.js | 0.183.2 | — | 本日未发现需立即动作的 breaking |
| LDraw Parts | 2026-07 | 414 new files / 294 new parts | 无新一批；素材仍只取官方库 |

## 竞品动态

| 项目 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio / Mecabricks / GitHub LDraw | 本次检索未发现影响 BrickHub 路线的 breaking/重大新动作 | 无动作 |

## 信息源

- LDraw File Format Specification（Type 1 matrix = rotation and scaling）：https://ldraw.org/article/218.html
- Next.js 16.3（2026-08-03）：https://nextjs.org/blog/next-16-3
- Upcoming Next.js August Security Release：https://nextjs.org/blog/upcoming-nextjs-security-release-august-2026
- R3F v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 2026-07 Parts Update：https://library.ldraw.org/updates?latest
