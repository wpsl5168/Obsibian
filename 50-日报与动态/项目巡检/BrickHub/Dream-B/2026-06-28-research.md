# Dream B BrickHub 日审 — 2026-06-28

**轮换文件**:`pages/model/[id].js`(上次 2026-06-08,距今 20 天)
**结论**:🛑 **不开 PR**;Pre-PR 飞行检查 3 道全 STOP 信号(同文件 OPEN PR 冲突 ×2 + PR 队列梗死 + 死代码姐妹扩张已固化)。今日只产报告,**不在死代码上抹奶油、不与未合 PR 重复劳动、不再撑爆队列**。
**文件本身**:`eslint` clean / `npx jest __tests__/modelDetail.test.js` 2/2 PASS / `node` 三重验证通过 → 文件层面无真 bug。

---

## 1. Pre-PR 飞行检查结果(强约束 — 决定是否开 PR)

| 检查项 | 结果 | 证据 |
|---|---|---|
| **同文件 OPEN PR 冲突** | 🛑 命中 | `gh pr list ... --jq '...select(.files[].path == "pages/model/[id].js")'` 返回 **PR #37**(2026-06-08, additions=25/deletions=9, mergeStateStatus=CLEAN, mergeable=MERGEABLE)与 **PR #20**(2026-05-15, CLEAN, MERGEABLE)。两条均同文件 a11y/边框,**今天再开就是相互覆盖**。 |
| **PR 队列健康** | 🛑 队列梗死 | OPEN 总数 = **30**,近 30 天 MERGED = **0**。`mergeStateStatus=CLEAN/MERGEABLE` 但没人合 → 不是技术阻塞,是**老王 review attention 已被淹没**。继续推 PR 只会让水位更高。 |
| **死代码姐妹扩张(components/home/\*)** | 🛑 全 0 | 同目录 10 个文件一次性扫:`BrickScene/FloatingControls/HistoryPanel/KnowledgePreviewPanel/LDrawEditorPanel/LDrawModal/MenuPanel/ProcessDebugPanel/PromptDock/StatusBanner` — 全部 import 计数 = **0**(在 `pages/components/lib` 里零引用)。Git log 显示 2026-04-09 一次"refactor ldraw pipeline"提交后再无任何 page/test 拉起。**今日审查文件 `pages/model/[id].js` 本身不在死代码内**,但本次复查印证 06-21 固化的「死代码姐妹扩张」结论:`components/home/` 整目录可归档。 |

→ **决定**:跳过 step 3(分支/PR 创建)。0 P2/P3 修复。

---

## 2. 文件本身扫描(`pages/model/[id].js`,495 行)

### 4 维度

| 维度 | 发现 | 等级 |
|---|---|---|
| Bug 隐患 | 无 — `eslint` 0 warn 0 err / `npx jest modelDetail.test.js` 2/2 PASS | ✅ |
| 架构 | `partsList`/`stepsPartsList` 两个 useMemo 的 LDraw 解析逻辑**在文件内重复实现**,本应抽到 `lib/ldrawParser.js`(已存在且有 `parseLDraw` export)。**ARCH-P1** — 涉及业务判断(parsing 路径调用关系),仅报告 | 🟡 P1 |
| UI 规范 | `border-3` 用在 6 处(行 368/378/392/403/414),`scrollbar-hide` 用在行 320。**`tailwind.config.js` 仍是默认配置**(`extend.fontFamily` only,`plugins: []`),既未注册 `border-3` 也未安装 `tailwind-scrollbar-hide` plugin → 这两个 utility 在 Tailwind v3.4 默认刻度下**渲染失效**。**UI-P1 全局**(非本文件单点),`shadow-soft` 通过 `styles/globals.css` 注册了所以 OK | 🟡 P1 全局 |
| 测试覆盖 | `__tests__/modelDetail.test.js` 已存在(2 个测试,smoke 级) | ✅ |
| 触控尺寸 | step 导航 `w-9 h-9`(36px)/ 模式切换 `w-14 h-12`(56×48px)。**步骤按钮 36px 不达老王红线 44px**,儿童 56px 更不达标 | 🟡 P1 |
| 暗色/品牌色 | 全部 `#FFD500`/`#FFCF00`/`#FFF5E6`/`#006CB7`/`#E3000B` — 合规 ✅ | ✅ |

### Bug-Hazards 早筛(防止下次同文件 reviewer 再花成本)

| ID | 位置 | 表面现象 | 判定 |
|---|---|---|---|
| BH-1 | 行 109-174 partsList useMemo | 双重 traverse + Set 拷贝 `new Set([...visited, f])` 每层都拷,O(N²) | 判定:**无 bug** — 模型 < 几千零件量级,实测渲染未观测卡顿;但 ARCH-P1 抽抽到 lib 时一并优化 |
| BH-2 | 行 199 `useEffect deps=[model]` | model 是 `gallery.find()` 返回的引用,gallery 重设会换新引用导致重 fetch | 判定:**无 bug** — `gallery` 由 `setGallery(json)` 一次性写入,只 set 一次,model 引用稳定 |
| BH-3 | 行 60-63 `handlePlacePart` 不用 `useCallback` | 拖放回调每次渲染换新引用 | 判定:**无 bug** — 传给 native `onDrop` 不影响 React 重渲;若传给子组件 memo 才有影响 |

---

## 3. 已修复表

无。今日 0 P2/P3 修复(Pre-PR 飞行检查 STOP)。

---

## 4. 待确认(仅报告,不动手)

| ID | 等级 | 位置 | 描述 | 建议 |
|---|---|---|---|---|
| ARCH-P1 | 🟡 P1 | `pages/model/[id].js` 行 102-235 | 内联两次 LDraw parsing,与 `lib/ldrawParser.js` 重复 | 抽到 lib,统一调用;改动跨文件需老王拍板 |
| UI-P1 | 🟡 P1 全局 | `tailwind.config.js` + 7 文件 | `border-3` 7 处 / `scrollbar-hide` 6 处 / `scrollbarWidth:'thick'` 1 处 — Tailwind 默认配置下渲染失效。历史 PR(#21/#16 等)修过单点但**根因 config 未扩展依旧**,继续吞代码 | 二选一:(a) `tailwind.config.js` 加 `borderWidth: { 3: '3px' }` + 安装 `tailwind-scrollbar-hide` plugin;(b) 全局替换为 `border-2`/原生 `[scrollbar-width:none]` |
| TOUCH-P1 | 🟡 P1 | 行 365-381 step 导航按钮 | `w-9 h-9` = 36px < 44px(最低)< 56px(儿童红线) | 升 `w-14 h-14` 或 `w-12 h-12`(48px 兜底) |
| DEADCODE-ARCH-P1 复查 | 🟡 P1 | `components/home/*` 10 文件 | 06-21 已固化的「目录级死代码」结论今日复查仍成立:同目录零外部引用,且 11 条历史 PR(#22-#33 含 home/* 路径的)全是抹奶油在死文件 | `git mv components/home components/_archive/home-deprecated-20260628/`,需老王单次拍板;同步关闭 #22-#33 这一批无意义 PR |
| PR-COLL-P1 | 🟡 P1 | 流程 | PR #37 和 #20 同时改 `pages/model/[id].js` 未合并 | 老王先合 #37 再裁判 #20(或反之),才解锁本文件未来 review |
| PROC-P1 | 🟡 P1 | 流程梗死 | OPEN=30 / 30天MERGED=0 | 优先级 #1:老王挑一个下午做一次清账(merge / close-with-reason / 转 issue 三选一),让 Dream B 重新有 ROI |

P0:无。

---

## 5. 回滚表

无(未创建分支,无回滚)。

---

## 6. 依赖动态

| 项 | 状态 |
|---|---|
| Next.js / @react-three/fiber / three.js / LDraw | ⚠️ **今日空白** — `web_search` 后端 Tavily 连续返回 HTTP 432(限流/配额),3 次全失败 |
| 历史结论延续 | next@14.2 距 EOL 4 月(06-15 固化)+ 14.x patches not planned + 累积多个 High CVE(auth bypass/SSRF/DoS/XSS,06-18 固化)→ **🔴 P1 强提醒(延续上日)**:升级到 Next 15+ 是计划性事项,本日未推进、依旧暴露中 |

下次执行优先把 web_search 配额复检放到 Step 1 前置,失败立即报。

---

## 7. 竞品动态

同上,今日空白。

---

## 8. 信息源

- `gh pr list --state open` / `gh pr view 37,20` / `git log -- components/home/`
- `grep -rn "border-3" --include="*.js"` / `grep -rn "scrollbar-hide"` / `read_file tailwind.config.js`
- `eslint pages/model/[id].js` / `npx jest __tests__/modelDetail.test.js`
- 同目录死代码扫:本 skill `references/dream-b-pre-pr-pre-flight.md` 死代码姐妹扩张 one-liner
- 历史固化:dreaming skill 中「死代码姐妹扩张(2026-06-21)」「Pre-PR 飞行检查(2026-06-18)」「Dream B UI/CSS dead-pattern(`border-3` / `scrollbar-hide`)」
- 外部:**0 次成功**(Tavily 432 ×3,跳过其余 ≤3 次额度)
