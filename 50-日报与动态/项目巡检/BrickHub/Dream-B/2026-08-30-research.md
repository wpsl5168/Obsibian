# Dream B — 2026-08-30

## 结论

- 审查：`pages/sandbox.js`（第五轮 day 3）。
- 自动修复：0。发现 3 个 P2，但同文件已被 OPEN PR `#45/#46/#47` 同时覆盖，触发 **PR-COLL-CLUSTER-P1**，未改代码、未开 PR。
- 新增：1 个 A11Y-P1、3 个 P2。
- 队列：DreamB OPEN 6；最近合并 `#39` 为 2026-07-02，已 59 天无合并，触发 PROC-P1。
- 依赖：`next@14.2.35` 落后最新 `16.3.3` 两个主版本且 14.x 已无安全修复线，DEP-P0 延续；August Critical AVIF RCE 为“版本命中 + `next/image` 功能命中 + 可达性未证实”。

## 已修复

| ID | 结果 | 说明 |
|---|---|---|
| — | 0 项 | Pre-PR 同文件 3 条 OPEN PR 冲突，禁止叠加新 PR |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 证据 / 影响 | 建议 |
|---|---|---|---|---|
| DEP-P0 | P0 | `package.json:33-36` | 实装 `next@14.2.35` / React 18；官方当前 Active LTS `16.3.3`，落后 2 个主版本且 14.x 无 August 修复线 | 明确迁移周：Next 16 + React 19 + R3F 9 联合评估；不能继续只记提醒 |
| A11Y-01 | P1 | `pages/sandbox.js:140-164,193-197` | 3D 积木只有 R3F `<group onClick>` 可选，Canvas 外无等价 DOM 控件；选中后才挂载操作菜单。键盘/读屏无法选择积木，也无法进入旋转/删除链路 | 设计场景对象列表或 DOM 等价操作层；属于交互架构，不自动改 |
| BUG-FOOTPRINT | P1（延续） | `pages/sandbox.js:61-71` | `buildOccupancy` 仅登记中心格，忽略 2×4 footprint 与旋转，导致穿插/错误落地 | 重写 footprint 占用模型并补旋转用例 |
| TEST-01 | P1（延续） | `__tests__/sandbox.test.js:12-49` | 测试复制了一份 reducer，而非 import 生产实现；22/22 绿不守护 `pages/sandbox.js` | 抽 reducer/occupancy 到纯函数模块后真实 import 测试 |
| UI-RESET | P1（延续） | `pages/sandbox.js:342-345` | “重置视角”按钮仍无 `onClick`，有 active 反馈但点击无动作 | 接 `OrbitControls.reset()` 或明确 disabled/移除 |
| INPUT-01 | P1（延续） | `pages/sandbox.js:268-280` | 监听 PointerEvent 却读取 TouchEvent 的 `touches`，双指分支不会按预期工作 | 用 `pointerId` 集合跟踪多指状态 |
| DATA-01 | P1（延续） | `pages/sandbox.js:19-23,263-266` | localStorage 载入无 schema/边界校验，且每次 parts 变化同步序列化写主线程 | 增 schema 校验、版本迁移与节流/idle 写入 |
| PR-COLL-CLUSTER | P1 | PR `#45/#46/#47` | 三条均命中 `pages/sandbox.js`，实测全部 `MERGEABLE/CLEAN`；继续开 PR 会叠加冲突 | 集中 review/merge/close，清账前暂停该文件自动修 |
| PROC-P1 | P1 | DreamB 队列 | OPEN 6；最近合并 59 天前，满足“OPEN≥3 且 14 天零合并” | batch review + merge/close |
| SEC-P1 | P1 | `next@14.2.35` + `next/image`×3 | `GHSA-2xp9-vwfh-vxw4`：版本范围命中；Image Optimization 功能面命中；未配置 AVIF，remotePatterns 仅固定 Rebrickable/BrickLink CDN，可达性未证实。Linux + 无 App Router，`CVE-2026-75604` 不适用 | 不夸大为已可利用；仍应升级到 16.3.3 消除长期暴露 |

## 可自动修但被 Pre-PR 拦截（P2）

| ID | 位置 | 问题 | 最小修法 |
|---|---|---|---|
| HTML-01 | `pages/sandbox.js:328-333` | `<Link><button>` 嵌套交互元素，产生 `<a><button>` 双重语义/焦点 | 直接把 Link 样式化为 44px 按钮并保留 `aria-label` |
| HIST-01 | `pages/sandbox.js:81-93,367-374` | 空场景仍可 CLEAR，重复清空写入无效 history；实测第一次 Undo 仍为空，第二次才恢复 | `disabled={parts.length===0}`，reducer 对 no-op CLEAR 返回原 state |
| STATE-01 | `pages/sandbox.js:282-310` | Undo/Redo 不清理 `selectedId`；撤销刚放置且已选中的砖后，残留 ID 使 ghost 隐藏、`handleGround` 提前返回 | Undo/Redo 同步清选中态，或 effect 校验 selectedId 是否仍存在 |

## 回滚 / 守卫

| 项目 | 结果 |
|---|---|
| 代码修改 | 无，未触碰项目代码 |
| ESLint | `npx eslint pages/sandbox.js`：通过 |
| Jest | `npx jest __tests__/sandbox.test.js --runInBand`：1 suite / 22 tests 全通过；但属于复制实现假覆盖 |
| 空 CLEAR 探针 | history 长度序列 `[0,1,0]`；Undo1=0 块、Undo2=1 块，问题复现 |
| Git 工作区 | 当前仍在旧分支 `dreamB/auto-fix-20260827`；原有未跟踪目录/文件未动 |

## Pre-PR 飞行检查

| 闸门 | 结果 |
|---|---|
| 队列熔断 | OPEN 6 < 8，未熔断 |
| 死代码 | `pages/sandbox.js` 是 Next 路由页；内部主要符号引用计数均 ≥2，无新增死符号 |
| 姐妹扩张 / 历史考古 | 未命中死代码，不触发 |
| 同文件 OPEN PR | 命中 `#45/#46/#47`，3 条均 MERGEABLE/CLEAN；PR-COLL-CLUSTER-P1，停止自动修 |
| 队列健康 | 6 OPEN + 59 天零合并，PROC-P1 |

### 拍板清单

1. 先清 `#45/#46/#47`；三条都可合，但需按实际 diff 决定 merge 或 close，避免继续阻塞 sandbox。
2. 给 Next 16 / React 19 / R3F 9 联合迁移一个明确周次；R3F 9 官方说明 StrictMode、Canvas `gl` callback、texture color management 等存在 breaking change。
3. 迁移前将 reducer/occupancy 抽成可测试模块，先恢复真实测试信号。

## 依赖动态

| 依赖 | 项目版本 | 当前版本 / 动态 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 16.3.3 Active LTS；15.5.24 Maintenance LTS；08-25 两个 Critical 修复 | DEP-P0 + SEC-P1；14.x 无修复线 |
| R3F | 8.18.x | npm latest 9.7.0；v9 面向 React 19，含 StrictMode / Canvas gl / texture color management breaking | 随 Next/React 迁移评估，不能单独盲升 |
| Three.js | 0.183.x | npm latest 0.185.1 | 落后 2 minor，未发现本轮必须立即处理的 breaking |
| SWC native | 14.2.33 | Next 14.2.35，同主版本 | 无主版本撕裂 |
| LDraw Parts | 2026-07 | 官方库最新 2026-07：新增 414 files / 294 parts / 6 primitives，总计 17052 shapes | 无 breaking；素材继续只取官方库 |

### Next.js CVE 命中面复核

- middleware/proxy：0。
- App Router：0（仅 Pages Router）。
- Cache Components / Server Actions / RSC：0。
- `next/image`：3 处。
- rewrites/redirects：0。
- August AVIF RCE：版本+功能命中，可达性未证实。
- Windows mixed-router RCE：Linux + 无 App Router，不适用。
- July 2026 批次：按版本/功能探查无已证实命中，维持此前纠偏结论。

## 竞品动态

| 对象 | 本轮结果 |
|---|---|
| BrickLink Studio / Mecabricks | 未检出可信官方 breaking 或产品级新动作；搜索结果以旧资料/聚合页为主，不上报噪音 |
| LDraw 生态 | 官方 2026-07 更新仍为最新；Next Release tracker 有候选文件流转，无格式 breaking |

## 信息源

- Next.js 官方 August 2026 Security Release：<https://nextjs.org/blog/august-2026-security-release>
- R3F 官方 v9 Migration Guide：<https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide>
- LDraw 官方 Parts Library：<https://library.ldraw.org/>
- LDraw Next Release tracker：<https://library.ldraw.org/tracker/next-release>
- 本地源码：`pages/sandbox.js`、`__tests__/sandbox.test.js`、`package.json`、`next.config.js`
- GitHub PR API：`#45/#46/#47` mergeable 状态、DreamB OPEN 计数、最近 merged 时间
