# Dream B — 2026-08-13

## 结论

审查 `lib/pipeline/llm.js`。无 P2/P3 自动修复，未创建分支/PR。新增 1 条 P1：未知 provider 名会静默落入 Anthropic 分支，配置拼写错误被伪装成 Anthropic 鉴权失败。Dream B 队列仍有 5 条 CLEAN/MERGEABLE PR，距最近合并 42 天，流程继续梗死。

## 已修复

| ID | 位置 | 结果 |
|---|---|---|
| — | — | 0 项；无 P2/P3 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 发现与建议 |
|---|---|---|---|
| LLM-NEW-01 | P1 | `lib/pipeline/llm.js:104-110` | provider 分派用最终 `else` 兜底 Anthropic；实测 `typo-provider` 被请求到 Anthropic 并返回 `typo-provider: Anthropic API error 401`。建议改为显式 provider map/switch，未知名称直接报配置错误；涉及 provider 契约，不自动改。 |
| LLM-01 | P1 | `lib/pipeline/llm.js:3-18` | `https.request` 无 timeout/abort；上游半开连接可长期占住生成请求。延续项。 |
| LLM-02 | P1 | `lib/pipeline/llm.js:124-181` | 流读取无中断信号，Node stream 若只 `close` 不触发 `end/error` 存在悬挂风险。延续项。 |
| TEST-01 | P1 | `__tests__/` | 7 个 export 零真实测试；生产只调用 `callProviderWithFallback/streamToText`，provider 分派、SSE 分片、失败降级均无回归网。 |
| DEP-01 | P0 排期 | `package.json` | Next `14.2.35`，npm latest `16.3.0`，落后 2 个主版本且 14.x 无新安全补丁；需给出迁移周或明确 defer。 |
| DEP-02 | P1 | `next/image` ×3 | 官方 CVE-2026-64644 条件部分命中：自托管 + 默认 Image Optimization + remotePatterns 已具备，但当前固定可信 CDN `.png`，恶意 SVG 可达性未证实；禁夸大为已可利用。 |
| PROC-01 | P1 | Dream B PR 队列 | OPEN 5，均 CLEAN/MERGEABLE；最近合并 2026-07-02，已 42 天。需 batch review + merge/close。 |

## 回滚

| 文件 | 原因 | 结果 |
|---|---|---|
| — | 无改动 | 无回滚 |

## 验证链路

- `npx eslint lib/pipeline/llm.js`：通过。
- 真实 caller：`lib/pipeline/index.js` 调用 `callProviderWithFallback` 与 `streamToText`；模块非死代码。
- 同文件 OPEN PR：0。
- 最小复现：`callProviderWithFallback([], [{name:'typo-provider',key:'invalid'}])` 实际返回 `typo-provider: Anthropic API error 401`，证实未知 provider 被误路由。
- Next CVE 命中面：Pages Router；无 `app/`、middleware/proxy、Server Actions、Cache Components、RSC；`next/image` 3 处；SWC `14.2.33` 与 Next `14.2.35` 主版本一致。

## 依赖动态

| 依赖 | 项目 | 最新 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.0 | P0 迁移排期；官方 2026-07 安全修复仅 15.5.21/16.2.11，14.x 无补丁线 |
| @react-three/fiber | 8.18.0 | 9.7.0 | v9 面向 React 19 且有 migration breaking；宜与 Next 16/React 19 合并评估，不单独冒进 |
| three | 0.183.2 | 0.185.1 | 落后 2 revisions，未发现需立即处理的 breaking |
| @next/swc-linux-x64-gnu | 14.2.33 | 随 Next | 主版本对齐，无 WASM fallback 撕裂 |

## 竞品 / 素材动态

| 项目 | 动态 | 判定 |
|---|---|---|
| LDraw Official Parts Library | 最新仍为 2026-07：414 个新文件、294 个新零件 | 可纳入后续官方零件库同步；禁止 AI 编造 part ID |
| BrickLink Studio | 2.26.7_1（2026-07-30）仅 bug fixes | 无 breaking / 无需跟进 |
| Mecabricks | 本轮未检出可信的新 breaking 动态 | 无动作 |

## 拍板清单

1. 清理 PR #43–#47：全部 CLEAN/MERGEABLE，Dream B 已 42 天零合并。
2. 给 Next 14→16 指定迁移周，R3F 8→9 / React 19 一并做兼容评估。
3. provider 路由契约改为显式白名单，未知名称 fail-fast；随后补分派与 SSE 测试。

## 信息源

- Next.js 官方 July 2026 Security Release：https://nextjs.org/blog/july-2026-security-release
- React Three Fiber Releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw Official Library Update：https://library.ldraw.org/updates?latest=
- BrickLink Studio Download / Changelog：https://www.bricklink.com/v3/studio/download.page
- npm registry：`npm view next/three/@react-three/fiber version`
