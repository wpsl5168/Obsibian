# Dream B — 2026-09-05

## 结论

审查 `lib/pipeline/utils.js`。发现 1 条新增 BUG-P1：`deduplicateLDraw()` 跨 MPD 子模型全局去重，会删除不同 `0 FILE` 局部坐标系中的合法同位零件。无 P2/P3，未改代码、未开 PR。

队列未熔断（DreamB OPEN 6 < 8），但 6 条 DreamB PR 均 CLEAN，最近一次合并仍为 2026-07-02，流程持续梗死。

## 已修复

| ID | 内容 | 结果 |
|---|---|---|
| — | 无 P2/P3 | 未创建分支/PR |

## 待确认（P0/P1）

| ID | 等级 | 位置 | 证据 | 建议 |
|---|---|---|---|---|
| BUG-MPD-DEDUP | P1 | `lib/pipeline/utils.js:1-10` | 最小探针输入两个 `0 FILE`，各含相同局部坐标 Type-1 行；去重后 Type-1 从 2 条变 1 条。仓库 `BrickRenderer.js` 与 `pages/model/[id].js` 均已显式支持/解析 MPD。 | 去重 key 加当前 `0 FILE` scope；无 MPD 时保持现行为。先补跨子模型同坐标回归测试。 |
| BUG-MATRIX-PARSE | P1（延续） | `lib/pipeline/utils.js:44` | `parseInt` 会把 `1x`、`1.5` 截断成整数，使畸形 token 绕过校验。 | 契约明确后改为完整数值解析并补非法 token 边界测试。 |
| ARCH-MATRIX | P1（延续） | `lib/pipeline/utils.js:42-86` | 仅允许 signed permutation matrix；合法 45° 旋转、缩放会被静默改为 identity。 | 与 renderer/parser 的完整 3×3 matrix 语义统一后再改，不能自动修。 |
| TEST-UTILS | P1（延续） | `lib/pipeline/utils.js` | 6 个 export 在 `__tests__/` 零真实引用；矩阵归一化与去重均无守卫。 | 新建 `pipelineUtils.test.js`，覆盖 MPD scope、十进制旋转、镜像、非法 token。 |
| PROC-QUEUE | P1（延续） | GitHub PR 队列 | DreamB OPEN 6、全部 CLEAN；最近 merged 为 #39（2026-07-02）。 | batch review/merge 或 close #43-#48，恢复自动修复吞吐。 |

代码 P0：0。新增代码 P1：1。

## 回滚

| 文件 | 原因 | 结果 |
|---|---|---|
| — | 无自动修复 | 无回滚 |

## 验证链路

- `npx eslint lib/pipeline/utils.js`：通过。
- 生产引用：`lib/pipeline/index.js` 真实调用 `extractLDraw`、`normalizeLDrawMatrices`、`deduplicateLDraw`、`buildOfflineFallbackLDraw`，目标不是死代码。
- MPD 最小探针：`type1_before 2 after 1`，稳定复现跨 `0 FILE` 误删。
- SWC：Next `14.2.35` / `@next/swc-linux-x64-gnu 14.2.33`，主版本一致，无 binding 撕裂。

## 依赖动态

| 依赖 | 项目 | 当前上游 | 判定 |
|---|---|---|---|
| Next.js | 14.2.35 | 16.3.4 | **DEP-P0 延续**：落后两大版本且 16.x stable 已超过 30 天，需明确迁移周或 defer 节点。 |
| Next.js Security | `next/image` 3 处 | 16.3.3/15.5.24 修复 August Critical | AVIF RCE：版本命中、功能命中；未配置 AVIF，remotePatterns 仅固定 Rebrickable/BrickLink CDN，可达性未证实。Windows RCE：Linux + 无 App Router，不适用。 |
| R3F | 8.18.0 + React 18 | 9.7.0 + React 19 | v9 为 React 19 compatibility release，Strict Mode 有 breaking；应与 Next 16/React 19 同一迁移批次评估。 |
| Three.js | 0.183.2 | 0.185.1 | 落后 2 个月度版本；Three 的月度版本可能含 breaking，当前无单独升级必要。 |
| LDraw | 2026-08 | 2026-08 | 官方更新新增 202 files，其中 144 parts、2 primitives；应仅同步官方库，禁编造 part ID。 |

### Next.js CVE 命中面复核

- 0 命中：middleware/proxy、App Router、Cache Components、Server Actions、RSC package、rewrites/redirects、`fetch(new Request(...))`。
- 3 命中：`next/image`。
- August Critical 的修复线为 15.5.24 / 16.3.3；项目 14.x 无该批修复线。

## 竞品动态

- BrickLink Studio / Mecabricks：本次检索未发现 breaking 或需立即跟进的产品动作。
- GitHub LDraw 生态：`datakurre/brick-mcp` 展示 AI 助手直接创建/检查/编辑 `.io` 与 `.ldr` 的 MCP 方向，可作为后续交互预研，不构成本期变更。

## 信息源

- Next.js August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- React Three Fiber v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 2026-08 Parts Update：https://forums.ldraw.org/thread-29635.html
- LDraw Library：https://library.ldraw.org
- brick-mcp：https://github.com/datakurre/brick-mcp
- 本地实测：eslint、Node 最小探针、`npm ls`、GitHub CLI PR 队列。

## 拍板清单

1. 排 Next 14 → 16 + React 18 → 19 + R3F 8 → 9 联合迁移周，或明确 defer 到哪个 Phase。
2. 清理 CLEAN 队列 #43-#48；不清账会继续阻塞同文件自动修。
3. BUG-MPD-DEDUP 与 matrix 语义一起进入 pipeline utils 测试/修复批次，避免局部补丁扩大格式分裂。
