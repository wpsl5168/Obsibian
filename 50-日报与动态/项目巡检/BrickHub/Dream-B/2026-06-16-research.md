# BrickHub Dream B 日审 — 2026-06-16

> 审查文件：`components/home/BrickScene.js`（轮换：2026-05-10 之后 37 天未轮）
> 触发：cron job `Dream B: BrickHub质检+预研`
> 审查耗时：~15 min,搜索 5/6

---

## 1. 执行摘要

🟡 **本次审查文件 BrickScene.js 自身代码无 bug,但发现一个更大的架构信号:整个 `components/home/` 目录(11 个组件,56KB) + `hooks/useBrickHubHome.js`(8KB) 是孤儿死代码**,自 2026-04-09 的 `632d814c "refactor: reorganize home flow and remove shared password"` 重构以来 64 天无任何 import 引用。

- 自动修复:0 项(没有 P2/P3,不创分支)
- 待确认:1 项 P1(孤儿目录批量删除/复活,涉及业务判断)
- 依赖告警:🚨 Next.js 14.2.35 已于 **2025-10-26 EOL**(EOL 后 7 个月,无安全补丁)

---

## 2. 已自动修复

| # | 文件 | 描述 | commit |
|---|---|---|---|
| — | — | 本次无 P2/P3 修复 | — |

---

## 3. 待确认(P0/P1,不自动改)

| ID | 等级 | 类型 | 位置 | 描述 | 建议修法 |
|---|---|---|---|---|---|
| D260616-1 | P1 | 架构/死代码 | `components/home/*.js`(11 文件) + `hooks/useBrickHubHome.js` | 全目录 + hook 自 2026-04-09 重构后零引用。`grep -rn "components/home\|useBrickHubHome"` 排除自身后仅 0 命中。`pages/index.js` 已重写为 174 行的画廊页,完全不挂 `BrickScene`/`PromptDock`/`StatusBanner` 等。本次轮到的 `BrickScene.js`(23 行 dynamic wrapper)是这堆死代码中的一份。 | **两条路二选一**: ① 若 phase2 暂不复用 → `git rm -r components/home/ hooks/useBrickHubHome.js`(节省 64KB + ESLint 时间 + Next.js bundle 扫描成本); ② 若计划在 phase2 接回生成流 → 在 `docs/architecture/` 加 README 标注"暂存待 phase2 接入,勿在 PR 中合并删除",并把目录改名 `_unused-phase2/` 让人一眼看出。两条都需老王拍板。 |

**P1 触发自动列出依据(per skill)**:`useBrickHubHome.js` 是纯 React hook、`BrickScene.js` 是纯 wrapper,均无外部 IO,但 `__tests__/` 下零覆盖;且代码量加起来 64KB,值得花一次老王 attention 拍板"删或留"。

**Bug-Hazards(三重验证 verdict)**:

| ID | 位置 | 表面现象 | 判定 |
|---|---|---|---|
| H260616-1 | `components/home/BrickScene.js:4` `dynamic(() => import('../BrickRenderer'))` | 拼写为 `../BrickRenderer` 而非 `../BrickRenderer.js`,看似可能 SSR/打包路径解析失败 | **无 bug** — Next.js 14 webpack alias 自动解析 `.js`/`.jsx`/`.ts`/`.tsx`;且文件根本不被 import,死代码无运行风险。`npx eslint components/home/BrickScene.js` 干净。 |
| H260616-2 | `components/common/ErrorBoundary.js:17` 渲染时 `bg-gray-100 ... text-red-500` | 违反"UI 禁深色,LEGO 暖色调"红线? | **无 bug**(场景兼容):这是 fallback,极少触发;但若孤儿目录决定保留,可在后续轮 `ErrorBoundary` 时改成 `bg-[#fff8dd] text-[#7f1d1d]` 与 BrickScene loading state 配色一致。本轮不动。 |

---

## 4. 未能自动修复(回滚)

| 文件 | 失败原因 | 回滚命令 |
|---|---|---|
| — | 本次 0 回滚 | — |

---

## 5. 依赖动态

| 依赖 | 当前 | 最新/LTS | 状态 | 行动 |
|---|---|---|---|---|
| **next** | 14.2.35 | 16.2.7 (Active LTS) / 15.5.19 (Maintenance LTS until 2026-10-21) | 🚨 **EOL since 2025-10-26**(7 个月前,无安全补丁) | **P1:必须在 2026 年内升 ≥15.x**。14→15 主要 breaking:`headers()`/`cookies()`/`params` 异步化、`fetch` 默认不缓存。建议 codemod `npx @next/codemod@latest upgrade latest` |
| **three** | 0.183.2 | r184 (latest) / r180 主力 | OK,差 1-2 个 minor | 暂不动,等老王主动升时再补 |
| **@react-three/fiber** | 8.18.0 | 9.x(React 19 兼容,**无 breaking**;v10 才有 breaking) | OK | 在升 React 19 时再连带升 |
| **@react-three/drei** | 9.122.0 | 跟随 R3F | OK | 同上 |
| **react** | 18.3.0 | 19.x | 暂稳 | 与 next 升级捆绑做 |

---

## 6. 竞品动态

| 项目 | 关键观察 | 对 BrickHub 影响 |
|---|---|---|
| Bricklink Studio 2.x | 2026-05-27 发布,纯桌面 CAD(Mac/Win),无 web 版 | BrickHub web-first + AI 生成路线仍差异化清晰,无威胁 |
| Mecabricks | 仍为 web 编辑器,目标专业玩家 | BrickHub 儿童友好 UX + 中文 + AI 生成是核心差异,正面无冲突 |
| LDraw Parts | **2026-05 更新**:+305 文件(200 新 part + 27 新色) | 老王下次手动跑 `scripts/setup-ldraw.js` 时建议带 `--refresh-index` 同步,可能新增儿童常用 part |

---

## 7. 信息源

- Next.js EOL: https://eosl.date/eol/product/nextjs · https://nextjs.org/support-policy · https://github.com/vercel/next.js/discussions/85289
- R3F v9: https://github.com/pmndrs/react-three-fiber/issues/2338 · https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- Three.js r180/r184: https://github.com/mrdoob/three.js/releases · https://sourceforge.net/projects/three-js.mirror/files/r180
- Bricklink Studio: https://www.bricklink.com/v3/studio/download.page
- LDraw 2026-05 update: https://forums.ldraw.org/thread-29527.html

---

## 8. 本次 Dream B 自反思 / pitfalls

- **轮换文件命中"全孤儿目录"**:rotation.json 里 `components/home/*.js` 共 10 条(BrickScene + 9 sibling),都在 64 天前已死。下次轮换器选文件时,如果发现选中的是死代码,应跳过到下一最久未轮且活的文件,**避免反复审死代码占 attention**;或者本次报告就一次性把"死代码族群"做成 P1 写明,后续就不再单独审了。本次按"诚实选了它就审它,同时把家族里的事一起处理"原则处理。
- **死代码族群是 Dream A 周报应捕获的信号**:Dream A 下周二可以扫一遍 `components/home/` 是否还在,如果到时已被 P1 处置(删/标记),从 rotation.json 里剔除这 10 条死链接,把腾出的轮位让给真实业务文件(如 `pages/api/generate.js`、`pages/api/retrieve.js`、`lib/runtimeConfig.js`、`store/history.js` 等当前未入轮但活跃的文件)。
