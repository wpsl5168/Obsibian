# Dream B 2026-08-05 — BrickHub 日审

## 结论

- 今日审查：`lib/ldrawParser.js`
- 自动修复：0；未创建分支/PR。
- 新发现 1 个功能缺陷：6-token 容错格式未执行 LDraw→Three.js 坐标换算，模型会被放大 20 倍且 Y 层序反转。
- 延续项：`validateGeometry` 130 LOC 几何守门员仍零测试；Dream B 5 条 PR 已 34 天零合并；Next 14 已退出支持且命中 `next/image` 暴露面。

## 验证链路

- `npx eslint lib/ldrawParser.js`：通过。
- `npx jest __tests__/parseLDraw.test.js --runInBand`：1 suite / 31 tests 全通过。
- 直接探针：两条 6-token 简写输入坐标 `(20,40,60)`、`(40,20,80)`，实际输出 `[[20,20,60],[40,0,80]]`；按完整 type-1 同一语义应为归一化后的 `[[1,0,3],[2,1,4]]`。
- CVE 命中面：7 类扫描仅 Image Optimization 命中，`next/image` 共 3 处；无 middleware、App Router、Cache Components、Server Actions、RSC。
- SWC：Next `14.2.35` / native binding `14.2.33`，主版本一致，无撕裂。

## 已修复

| 项目 | 结果 |
|---|---|
| P2/P3 | 无 |

## 待确认（P0/P1）

| 级别 | 发现 | 建议 |
|---|---|---|
| P1 新增 | `parseLDraw` 410–427 行的 6-token 容错分支直接保存 raw x/y/z；未乘 `LDU`、未翻转 Y，也没有 `quat`。现有测试只用全零坐标，未覆盖该错误。 | 明确 6-token 是否为正式 DSL。若保留，复用完整分支坐标/默认单位矩阵逻辑并补非零坐标回归测试；若非正式，删除“生成砖块”容错，避免静默错模。 |
| TEST-P1 延续 | `validateGeometry` 488–618 行承担碰撞/悬空判定，`__tests__` 对该 export 仍 0 引用。 | 增加旋转 AABB、边缘相切、支撑容差、悬空、错误上限测试。 |
| PROC-P1 延续 | Dream B OPEN PR 5 条（#43–#47）均 CLEAN；最近合并 2026-07-02，已 34 天。 | 批量 review + merge/close，解除新一轮同文件冲突。 |
| DEP-P0 延续 | 项目 `next@14.2.35`；官方支持策略仅 15.x/16.x，npm latest 为 16.3.0，已落后 2 个大版本。 | 排迁移周；低风险桥接 15.5.21，或同步 React 19 + R3F 9 直升 Next 16.x。 |
| DEP-P1 延续 | Next 14 无安全维护；项目 3 处 `next/image` 命中 Image Optimization SSRF/DoS 类暴露面。 | 不再留在 14.x；升级后复跑 7 类命中面与图片链路。 |

## 回滚

| 项目 | 结果 |
|---|---|
| 回滚 | 无；未改 BrickHub 代码。仓库原有未跟踪目录/文件保持不动。 |

## 依赖动态

| 依赖 | 动态 | 判定 |
|---|---|---|
| Next.js | 官方 2026-07 安全发布要求升 16.2.11 或 15.5.21，修复 4 HIGH + 5 MEDIUM；npm latest 16.3.0。 | P0 排期 + P1 暴露延续。 |
| `@react-three/fiber` | 项目 8.18.0；最新 9.7.0，v9 面向 React 19 且 migration guide 明示 Strict Mode 等 breaking。 | 不单独跃迁；与 Next/React 升级联动。 |
| Three.js | npm latest 0.185.1；本轮未发现必须立即处理的 breaking。 | 观察。 |
| LDraw | 官方 Parts Update 2026-07：414 个新文件、294 个新零件。 | 新动态；同步官方库索引，不手工编造 part ID。 |

## 竞品动态

| 项目 | 结论 |
|---|---|
| BrickLink Studio / Mecabricks | 本轮未找到可验证的产品 breaking 或必须跟进能力；BrickLink 公告页抓取失败，不据此下结论。 |

## 拍板清单

1. 6-token 简写格式：保留并修正确坐标，还是删除容错生成？
2. 给 Next 迁移一个明确周次；建议与 React 19 / R3F 9 联动评估。
3. 清理 #43–#47，Dream B 已连续 34 天被队列拖住。

## 信息源

- Next.js Blog：https://nextjs.org/blog
- Next.js Support Policy：https://nextjs.org/support-policy
- R3F Releases：https://github.com/pmndrs/react-three-fiber/releases
- R3F v9 Migration：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- LDraw 2026-07 Update：https://library.ldraw.org/updates?latest=
- 本地代码：`lib/ldrawParser.js`、`__tests__/parseLDraw.test.js`、`package.json`
