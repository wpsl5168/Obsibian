# Dream B — BrickHub 日审（2026-08-19）

- 审查文件：`pages/sandbox.js`
- 模式：正常（Dream B OPEN PR 5 < 8）
- 守卫：`npx eslint pages/sandbox.js` 通过；`npx jest __tests__/sandbox.test.js --runInBand` 22/22 通过
- 结论：今日无新增 P0/P1/P2/P3；历史缺口仍在，未自动修改、未开 PR。

## 已修复

| 项目 | 结果 |
|---|---|
| 自动修复 | 0 |
| 新 PR | 无 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 结论 / 建议 |
|---|---|---|---|
| DEP-P0 | P0 排期 | `next@14.2.35` | Next 14 已 EOL，当前 stable `16.3.1`，落后 2 个大版本且超过 30 天；需明确迁移周或 defer 节点。R3F 8→9 与 React 19/Next 16 联动评估。 |
| PROC-P1 | P1 | PR 队列 | Dream B OPEN 5，均 CLEAN；最近合并仍为 #39（2026-07-02），已 48 天零合并。`pages/sandbox.js` 同时被 #45/#46/#47 占用，继续阻塞自动修。 |
| BUG-P1 | P1 | `buildOccupancy` L63-72 | 只登记积木中心格，忽略 2×4 footprint 与旋转，占用/落地可能穿插；需按旋转后的 stud footprint 登记全部格。此项已于 2026-08-08 首报。 |
| TEST-P1 | P1 | `__tests__/sandbox.test.js` | 测试复制 reducer/occupancy 实现而非 import 生产代码；22/22 绿不能守住生产漂移，`buildOccupancy` 缺陷已被同构 fixture 固化。建议抽纯函数模块后真实 import。 |
| UI-P1 | P1 | L343-345 | “重置视角”仍无 `onClick`，属于死交互按钮；接入 `OrbitControls.reset()` 或 disabled/删除。 |
| TOUCH-P1 | P1 | L268-280 | PointerEvent 不含 `touches`，多指状态判断恒失效；需用 active pointer 计数或独立 TouchEvent。 |
| DATA-P1 | P1 | L19-23 | `loadFromLS` 仅处理 JSON 语法错误，不校验数组/part schema；合法 `{}`、`null`、`[null]` 可使初始化崩溃。此项 2026-06-07 已首报，2026-06-27 曾误判为无 bug；本次最小探针复核确认。 |

## 回滚

| 项目 | 结果 |
|---|---|
| 回滚 | 0（未改代码） |

## Pre-PR / 队列

- Dream B OPEN：5；仓库 OPEN 总数：6。
- 同文件冲突：#45、#46、#47 均修改 `pages/sandbox.js`，状态 CLEAN。
- 最近合并：#39，`2026-07-02T01:53:40Z`。
- 处置：无 P2/P3，且同文件冲突；未创建分支/PR。队列待 batch-merge/close。

## 依赖动态

| 依赖 | 项目版本 | 最新 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.1 | DEP-P0：14 已 EOL，落后 2 大版本；SWC 14.2.33 与 Next 同主版本，无 binding 撕裂。 |
| Three.js | 0.183.2（range） | 0.185.1 | 落后 2 个 release；未发现与当前路径直接相关的 breaking，升级前按官方 migration guide 回归 3D。 |
| @react-three/fiber | 8.18.0 | 9.7.0 | v9 面向 React 19且有 StrictMode breaking；应并入 Next 16/React 19 迁移，不单独跳升。 |

### Next.js CVE 命中面复核

- Pages Router；无 `app/`、middleware/proxy、Cache Components、Server Actions、RSC、rewrites/redirects、`fetch(new Request(...))` 特定形态。
- `next/image` 仍 3 处：`pages/index.js`、`pages/model/[id].js`、`components/PartsTray.js`。
- July 2026 批次按 advisory 版本与功能面复核：0 个已证实命中；不得再把 CVE-2026-64644 套到 14.2.35。
- 风险来源仍是 Next 14 EOL、未来漏洞无官方 14.x backport，而非已排除的 July CVE。

## 竞品 / LDraw 动态

- LDraw “Parts In Next Release” 当前显示 9 个候选；仍是 next-release / tracker 状态，不得当官方零件库直接入库。
- BrickLink Studio / Mecabricks 未发现需要立即跟进的 breaking 或产品动作。

## 信息源

- Next.js 官方 Blog：https://nextjs.org/blog
- Next.js 生命周期：https://www.herodevs.com/blog-posts/nextjs-eol-dates-version-support-timeline
- Three.js Releases：https://github.com/mrdoob/three.js/releases
- R3F v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- R3F Releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw Parts Next Release：https://library.ldraw.org/tracker/next-release
- 本地证据：`package.json`、node_modules 版本、GitHub PR 查询、eslint/Jest、Dream B Next.js CVE 探查表

## 拍板清单

1. 清理 #43–#47（batch-merge/close），解除连续 48 天流程梗死与 sandbox 同文件阻塞。
2. 给 Next 14→16 / React 18→19 / R3F 8→9 指定迁移周或明确 defer 节点。
3. sandbox 优先修 footprint occupancy + 抽出真实可测纯函数；死按钮、触屏手势、LS schema 同批收口。
