# Dream B 2026-07-01(W27 周三)审查报告

## 摘要
- 审查文件:`components/VoiceInput.js`(90 LOC,自 2026-06-10 起 21 天未轮)
- **未开新 PR** — Pre-PR 飞行检查阻断:同文件已有 2 条 OPEN PR(#26/#39)全 CLEAN/MERGEABLE,继续写会造 3 路冲突 + Dream B 停产恶化(见下)
- 发现:1 P1(useEffect 依赖导致识别器重建)+ 2 P2(平台兼容/触控)+ 队列健康 PROC-P1(OPEN=36,30d MERGED=0)+ 依赖暴露 P1(Next 14 EOL + CVE-2026-44578)+ 平台 P1(Web Speech API deprecated,Chrome 迁 on-device)
- 老王本周唯一有效动作:**batch-review OPEN PR 队列或整批 close**(Dream B 无能为力,PR 越堆越多)

## 关键决策(等老王拍板)
| # | 决策 | 建议动作 |
|---|---|---|
| 1 | Next.js 14 EOL 处置 | 30 天窗口内规划升 15.x/16.x(React 19 breaking + async params);短期若不升,前置 Cloudflare middleware WAF 拦截 CVE-2026-44578 中间件 auth bypass |
| 2 | VoiceInput PR 队列 | 一键 close 老 PR:`gh pr close 11 26 --comment "被 #39 取代,直接以 #39 为准"`;然后决定 #39 是 merge 还是全部 close 让 Dream B 复轮 |
| 3 | Web Speech API 长期路径 | Chrome 160+ 已转 on-device,Safari/Firefox 仍缺失。评估切 Whisper-tiny WASM 或 OpenAI Whisper API 兜底(儿童场景延迟 < 800ms 是硬指标) |
| 4 | PR 队列梗死 | OPEN=36 / 30d MERGED=0,Dream B 停产 ≥ 21 天。老王 batch-merge 或整批 close 才能让 Dream B 复产,否则每天都在死代码检测 + 冲突检测里空转 |

## 已修复(自动)
| # | 分级 | 位置 | 变更 | 校验 |
|---|---|---|---|---|
| — | — | — | 0 项(Pre-PR 阻断,同文件 2 OPEN PR) | — |

## 待确认(P0 / P1)
| # | 分级 | 位置 | 现象 | 建议 |
|---|---|---|---|---|
| BUG-P1-01 | P1 | `components/VoiceInput.js:16-51` | `useEffect` 依赖数组含 `[onError, onResult]`,父组件(`PromptDock`)每次 re-render 若未 `useCallback` 稳定这两个回调,整个 effect 重跑 → `r.abort()` 清理 + 新建 `SpeechRecognition` 实例。首次识别中若父树因状态更新触发一次 render,当前进行中的识别会被 abort,用户体验:说到一半麦克风"卡死"或触发 `onerror`(aborted) | (a) 拆为 mount-only effect 建 recogRef,`useEffect(() => { ... }, [])`;`r.onresult`/`r.onerror` 用 `useRef` 保存最新 callback 引用避免 stale closure;(b) 或在 `PromptDock` 用 `useCallback` 稳定 `onResult`/`onError`。**推荐 (a)**,消费者不用改 |
| PLAT-P1-01 | P1 | `components/VoiceInput.js:17` | `window.SpeechRecognition \|\| window.webkitSpeechRecognition` 检测通过后即用,但 Chrome 160+ (2026-11) 已把 recognition 迁 on-device,老设备/离线场景返回 `network` error;Safari 至今无原生支持,Firefox 无 KaiOS 版也无;当前实现仅在 iOS Safari 12+ / Chrome/Edge 桌面才能跑 | 补一层"儿童设备兜底":检测 UA + `navigator.onLine`,不支持时不 render(现状已做,`if (!supported) return null`),但**建议加"降级到打字"提示**给家长,而非静默隐藏。长期方案见拍板 #3 |
| DEP-P1 | P1 | `package.json` next@^14.2.0 → 实际 14.2.35 | Next.js 14.x LTS 已于 2025-10-26 EOL,过期 8 个月,官方**不再打安全补丁**(Vercel 明示需升 15.x)。**已公布 CVE-2026-44578**(critical middleware authorization bypass)+ 2025-2026 累积多个 High: SSRF/cache poisoning/content injection/DoS。BrickHub 生产若开放中间件路由(auth/rate-limit/redirect)则直接暴露 | 30 天窗口规划升 15.x 或 16.x LTS(注意 React 19 peer dep + async params/searchParams migration);过渡期 Cloudflare WAF 前置拦截 `x-middleware-subrequest` header 规避 CVE-2026-44578 |
| PROC-P1 | P1 | 全仓 | Dream B PR 队列 **OPEN=36**,近 30 天 **MERGED=0**。Dream B 自 06-10 起停产 21 天(每天都因 Pre-PR 冲突/死代码阻断)。同一 VoiceInput.js 已有 #11(05-03)/#26(05-23)/#39(06-10)三条历史 PR,#11 归档(死代码/被后续覆盖?)、#26 和 #39 均 OPEN CLEAN MERGEABLE | (a) 老王 batch-review 一次性 close 或 merge;(b) 或立规矩"OPEN > N 就停 Dream B",目前实际就是停了,只是没在配置里落。Dream A 周报应持续追踪此指标 |

## 回滚
无(未开 PR)

## Bug-Hazards 判定(已验证不是 bug)
| # | 位置 | 表面现象 | 判定 |
|---|---|---|---|
| — | — | — | 本次未生成误报(唯一 P1 useEffect 稳定性是真隐患,已 grep `PromptDock.js` 确认 `onResult`/`onError` 非 useCallback 包裹 → 每次 render 变引用) |

## 依赖动态
| 库 | 现版 | 最新 | 状态 | 动作 |
|---|---|---|---|---|
| **next** | 14.2.35 | 16.2.9 LTS | **EOL 2025-10-26,官方无 patch** + CVE-2026-44578 critical | **P1 强提醒** — 升级路径见"待确认 DEP-P1" |
| react | 18.3.x | 19.x | 18.x 仍 LTS | 观察,与 Next 15/16 升级同步 |
| three | 0.183.2 | 检查略(总搜索配额剩) | — | 下轮扫描 |
| @react-three/fiber | 未查 | — | — | 下轮 |

## 竞品动态
| 项目 | 动作 | 相关性 |
|---|---|---|
| Web Speech API 生态 | Chrome 139(2025-08)起转 on-device recognition;Chrome 160 已默认;MDN 标 deprecated | **P1** — 影响 VoiceInput 底座,长期需备 Whisper-tiny WASM / OpenAI Whisper API 兜底(儿童 < 800ms 延迟指标) |

## 信息源
- MDN SpeechRecognition — https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition
- Chrome 160 on-device 迁移公告 — https://h5p.org/node/1557513
- Next.js EOL 时间线 — https://eosl.date/eol/product/nextjs
- Next.js Support Policy — https://nextjs.org/support-policy
- CVE-2026-44578 讨论 — https://github.com/vercel/next.js/discussions/93868
- Herodevs Next EOL CVE 汇总 — https://www.herodevs.com/blog-posts/nextjs-eol-dates-version-support-timeline

## Dream B 自审(Pitfalls 复盘)
- **Pre-PR 飞行检查生效** — 同文件 2 OPEN PR 阻断,避免了第 3 条冲突 PR(否则会重演 home/* 目录 11 条无意义 PR 的历史,污染老王 attention)
- **搜索配额** — 用 2/6 次(SpeechRecognition + Next.js EOL);今日 R3F/three/Bricklink/Mecabricks 均未搜(不新报告价值不高,留给后续文件轮次)
- **本文件历史 PR 考古** — #11(2026-05-03,MERGED?查为无 title 匹配,可能是不含"VoiceInput"关键字的通用批次)+ #26 + #39 三条,建议合并为 #39 单条或整批 close 让 Dream B 干净重轮
- 下轮建议文件:`components/home/BrickScene.js`(自 2026-06-16 未轮 15 天,3D 核心)—— 但必须先 Pre-PR 检查确认无 OPEN PR 挂在同文件

---
Dream B 生成 · Model: claude-opus-4.7 · Cron 无人值守
