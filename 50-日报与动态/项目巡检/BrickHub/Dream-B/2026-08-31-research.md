# Dream B 日审 — 2026-08-31

## 结论

- 审查：`pages/model/[id].js`
- 自动修复：0；未建分支、未开 PR。
- 新发现：0。代码层问题均为既有项延续；`eslint` 通过，`modelDetail.test.js` 2/2 通过。
- Pre-PR 冲突：同文件已有 OPEN PR #46、#47，均 `CLEAN`；依规则不重复修改。
- 队列：Dream B OPEN 6，最近一次合并 #39（2026-07-02），流程梗死延续。

## 已修复

| ID | 位置 | 修复 | 验证 |
|---|---|---|---|
| — | — | 今日无自动修复 | — |

## 待确认（P0/P1）

| 级别 | ID | 位置 | 结论 / 建议 |
|---|---|---|---|
| P0 | DEP-NEXT | `package.json:33` | 当前 `next@14.2.35`，官方 Active LTS 为 16.3.3，已落后两大版本且 14.x 无安全补丁线；排期迁移到 16.3.3，或明确 defer 到具体 Phase。 |
| P1 | BUG-FETCH | `pages/model/[id].js:90-96,177-199` | 两处 fetch 未检查 `response.ok`；模型请求无 Abort/代次守卫，非 2xx 可伪成功，快速切换模型时旧响应可覆盖新响应。既有项延续。 |
| P1 | BUG-MPD | `pages/model/[id].js:201-235` | 步骤清单未处理 Packed MPD 的 `0 FILE/0 NOFILE` 边界，可能把内嵌子模型/primitive 定义计入主步骤。既有项延续。 |
| P1 | A11Y-HIDDEN | `pages/model/[id].js:300-361` | 清单关闭仅 `opacity/scale/pointer-events`，内部关闭按钮与零件交互项未从辅助技术/焦点语义移除；应拍板 `inert` + `aria-hidden` 或条件渲染。 |
| P1 | LIFE-TIMER | `pages/model/[id].js:55-58` | `useCallback` 内 timer 无 unmount cleanup；不能用 callback return 伪装 cleanup，应以 `useRef` 保存 timer 并由 `useEffect` 清理。 |
| P1 | TEST-GAP | `__tests__/modelDetail.test.js` | 现有 2 个测试均通过，但未覆盖 Packed MPD、非 2xx、乱序响应、关闭清单后的不可聚焦语义。 |
| P1 | PROC-QUEUE | GitHub PR 队列 | Dream B 6 条 OPEN 且近 60 天零合并；#43-#48 均 CLEAN，建议 batch review + merge/close。 |

## P2/P3 与 Pre-PR 拦截

| ID | 位置 | 可修项 | 拦截 |
|---|---|---|---|
| A11Y-TOUCH | `pages/model/[id].js:263-268,363-429` | 旧分支已覆盖 44/48→56px 与 aria 补齐 | OPEN PR #46 已修改同文件，禁止重复开 PR |
| A11Y-LINK | `pages/model/[id].js:263-268` | `Link` 内嵌 `button` 应改为单一交互元素 | OPEN PR #46/#47 与同页链路冲突，留待队列收口后统一处理 |

## 回滚 / 未能自动修复

| 项目 | 结果 |
|---|---|
| 回滚 | 0；今日未改项目代码 |
| 工作树保护 | BrickHub 当前位于 `dreamB/auto-fix-20260827`，含老王未跟踪文件；全程只读，未 checkout/清理 |

## 依赖动态

| 依赖 | 项目版本 | 上游状态 | 判定 |
|---|---:|---|---|
| Next.js | 14.2.35 | 16.3.3 Active LTS；15.5.24 Maintenance LTS | DEP-P0 延续 |
| `@next/swc-linux-x64-gnu` | 14.2.33 | 与 Next 同为 14.x | 主版本对齐，无 SWC 撕裂 |
| R3F | 8.18.0 | v9.7.0；v9 面向 React 19，含 Strict Mode、texture sRGB、类型导出等 breaking | 需随 React 19 / Next 16 一并迁移评估，不单独升级 |
| Three.js | 0.183.2 | 未发现今日新增 breaking 公告 | 无新增 |
| LDraw | 2026-07 | 2026-07 官方更新含 294 个新零件；Next Release 持续积累 | 无 breaking |

### Next.js August 2026 Critical 命中面

- `GHSA-2xp9-vwfh-vxw4`：版本命中（14.2.35）+ 功能命中（`next/image` 3 处）；`next.config.js` 未启用 AVIF，remotePatterns 仅固定 Rebrickable/BrickLink CDN，攻击者可控 AVIF 可达性未证实。若部署在 Vercel/Netlify，其托管侧声明已绕开/防护受影响优化路径；self-hosted 仍应升级。
- `CVE-2026-75604`：Linux + 仅 Pages Router、无 `app/`，不适用。
- 其余探查：middleware/proxy、Cache Components、Server Actions、RSC、rewrites/redirects、`fetch(new Request(...))` 均 0 命中。

## 竞品动态

| 项目 | 动态 | 判定 |
|---|---|---|
| BrickLink Studio | 官方更新页搜索结果有 2026-08-27 时间戳，但正文抽取仍只返回服务器时间，无法验证具体版本/变更 | 不上报 breaking |
| Mecabricks | 未检索到可核实的产品级 breaking / 重大功能发布 | 无新增 |
| GitHub LDraw 生态 | 搜索结果以既有导入/说明书工具为主，未发现会改变 BrickHub 路线的新 release | 无新增 |

## 信息源

- Next.js 官方 August 2026 Security Release：https://nextjs.org/blog/august-2026-security-release
- R3F v9 Migration Guide：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- R3F Releases：https://github.com/pmndrs/react-three-fiber/releases
- LDraw Official Library Updates：https://library.ldraw.org/updates
- LDraw Parts In Next Release：https://library.ldraw.org/tracker/next-release
- BrickLink Studio Update：https://www.bricklink.com/v3/announcement/studio_update.page

## 拍板清单

1. 为 Next 16.3.3 + React 19 + R3F 9 设迁移周，或明确 defer 到具体 Phase。
2. 清理 #43-#48：优先 batch review/merge CLEAN PR；不再需要的直接 close。
