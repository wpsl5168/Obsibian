# Dream B 2026-06-22 — BrickRenderer.js 全量复审 (1110 行)

> 轮换：上次轮 `BrickRenderer.js` 是 2026-06-04（PR #34），但**该 PR 至今仍 OPEN/未合**。
> 距上次成功 review 已 18 天，文件期间又有 6 次架构层改动 (AutoFit useFrame 包围球稳定算法 / PerfProbe overlay / 移动端性能回滚) → 必须复审。

## TL;DR
- **0 项自动修复**（跳过分支创建）
- **1 真 bug P1**：4 处 `<Edges color="#00000055" />` 8 位 hex,THREE.Color r183 不接受 alpha 通道,渲染为白色边线 — 已 verify
- **1 ARCH-P1**：流程梗死 — Dream B 累积 30 OPEN PR、近 30 天 MERGED=0
- **1 ARCH-P1**：同文件 OPEN PR #34 (BrickRenderer 微清理) 未合并,飞行检查触发不开新 PR
- **1 P3 死代码**：`SCENE_BG = '#FFF5E6'` 0 引用 (line 108)
- **依赖动态**：three r184 已发布（本项目 r183 落后 1 版），@react-three/fiber v9 已 stable 含 strict-mode breaking；**今日仅信息记录**

## 待确认 / 仅报告（含 P0/P1/Pre-PR 阻断）

| ID | 等级 | 位置 | 现象 | 根因/影响 | 建议 |
|---|---|---|---|---|---|
| EDG-1 | P1 真 bug | BrickRenderer.js:520/527/538/546 | `<Edges color="#00000055" lineWidth={1.2} />` 等 4 处使用 8 位 hex | THREE.Color r183 `Invalid hex color #00000055`,自动回落白色 (r=1,g=1,b=1)。本意 35% 透明黑勾边,实际所有 procedural 砖块边线为**亮白**,违反 LEGO 品牌色 + 暖色调红线 | 拆为 `<Edges color="#000000" opacity={0.35} transparent />`,但 LineBasicMaterial 在 r3f/drei 9.x 需用 `<lineBasicMaterial color="#000000" opacity={0.35} transparent />` 子节点替代 `color=` 简写。需要老王在 UI 设计语境下拍方案（深度黑 vs 灰 vs 透明度数值 vs 直接去掉 procedural 边线一致用 LDrawLoader 路径） |
| EDG-2 | P1 真 bug | BrickRenderer.js:546 | `color="#00000033"` 同上,studs 边线本意 20% 透明黑 | 同 EDG-1 | 同 EDG-1,合并修复 |
| PR-COLL-1 | ARCH-P1 (Pre-PR) | 仓库 | 同文件 OPEN PR #34 (2026-06-04 BrickRenderer 微清理) 仍 OPEN/mergeStateStatus=CLEAN 18 天 | 触发 skill 飞行检查 step 5b,**禁开新 PR**,否则与 #34 冲突 + 重复劳动 | 老王清账 #34 后再启动 BrickRenderer 的自动修分支 |
| PROC-P1 | ARCH-P1 (Pre-PR) | dreamB/* | OPEN 总数 ≥30 (#13~#42),近 30 天 MERGED=0,队列梗死 | Dream B 单写不读流水线,所有 PR 堆积 review 队列,机器既污染老王 attention 又撑爆队列,老王手工合并能力供给不足 | 拍板:(a) 暂停 Dream B 自动 PR(改为 issue 备忘);(b) 批量 `gh pr close` 历史 PR 留代码摘要在 Obsidian;(c) 引入门控(merge backlog>N 时跳过修复) |
| DEAD-1 | P3 死代码 | BrickRenderer.js:108 | `const SCENE_BG = '#FFF5E6'` | grep 全仓 0 引用 (除自身定义),应该早期 Canvas 背景常量但实际背景已改为 `'transparent'` | 删 1 行,小到没必要单开 PR,等飞行检查放行后随下次任意 BrickRenderer PR 顺手 |

## 已修复
（无 — 受 PR-COLL-1 + PROC-P1 双重飞行检查阻断,且 EDG-1/2 涉 UI 设计判断不在 P2/P3 自动修范围）

## 回滚
（无）

## 4 维度扫描笔记
**Bug 隐患**
- EDG-1/2 已列
- AutoFit (line 11-55) useFrame 实现 3 帧稳定后 fit,signature 用包围球字符串。**已 verify 无 bug** — 浮点 toFixed(2) 防抖逻辑正确,deps 数组变化后 ref 清零正确,无 race。比旧 Bounds 方案更稳。
- LDraw fileLoader 全局 monkey-patch (line 298-371) **可疑** — `THREE.FileLoader.prototype.__brickhubIdbPatched` 全局 flag 防双 patch,但跨 hot-reload/MPA 场景仍可能重复加载本文件,导致 `originalLoad` 引用旧 patch 后的版本。**未 verify**,需要老王本地热重载实测;暂列**观察项**
- `cacheBustedUrl = fetchUrl + '?cb=' + Date.now()` (line 324) 每次都加 cache bust → CDN/浏览器 HTTP 缓存彻底失效,完全靠 IDB 缓存,IDB miss 时每次都打全网。是有意为之(避免 Three 内部 FileLoader cache 命中陈旧),但**性能债**,可考虑 `cb=<contentHash>` 替换。属 P2-观察

**架构**
- PR-COLL-1 / PROC-P1 已列
- 单文件 1110 行已超 BrickRenderer.js 合理边界(职责:loader 注册/材质 patch/IDB cache/AutoFit/PerfProbe/SandboxAnimator/Brick procedural/默认模型/主组件)。属技术债观察项,**不建议**主动重构(项目早期阶段稳定第一,拆分 ROI 低)

**UI 规范**
- EDG-1/2 已列(品牌色违规)
- Canvas `background: 'transparent'` (line 1010) ✓ 暖色调由父容器承载
- ContactShadows opacity 0.42 / 中性地面 #d4dae1 ✓ 符合 DESIGN.md 中性投影规范

**测试覆盖**
- `__tests__/LDrawRenderer.{test,extended,report}.test.js` 三个测试文件均只 import `parseLDraw, PART_DEFS` (procedural 路径),Loader 路径 / AutoFit / PerfProbe / SandboxAnimator 等本文件核心 3D 逻辑**完全无 coverage**
- 但 3D + R3F + Canvas 单测 ROI 极低(需 jsdom + Canvas mock),**不列 P1**,符合启发式例外:不是"纯函数模块"

## 依赖动态
| 名称 | 本项目 | 上游最新 | breaking | 建议 |
|---|---|---|---|---|
| three | ^0.183.2 | r184 | 无 known breaking 关键(r184 主要 WebGPU 增强) | 可等 r185 LTS 节点再升 |
| @react-three/fiber | ^8.18.0 | v9 stable | **有** Strict Mode 行为改变 + useLoader API 调整 | **不建议**轻易升,要做 strict mode 回归。本仓库目前没用 StrictMode,迁移成本中等,但回报小,**搁置** |
| @react-three/drei | ^9.122.0 | (跟 R3F v8 配对) | 受 R3F v9 牵动 | 同上 |
| next | ^14.2.0 | 15.x / 16.x | 14 EOL ≤ 4 月 + 多个 CVE(前期报告已记) | 已在 06-15 / 06-18 报告 P1,本日不重复 |

## 竞品动态
（今日跳过 — 累计本周已 2 次报告动作,无新动作可省 attention）

## 信息源
- Three.js r184 发布:threejs.org/changelog/?r184 (本地 search hit)
- R3F v9 migration:r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- 实测 verify:`node -e "new THREE.Color('#00000055')"` → `Invalid hex color` warning + 回落白色

## Pre-PR 飞行检查记录
| 检查项 | 命中 | 决策 |
|---|---|---|
| 死代码 grep (`SCENE_BG`) | DEAD-1 命中 | 不开 PR(P3 太小),记录待清账 |
| 死代码姐妹扩张 (`components/home/*`) | 未跑(本次目标在 `components/BrickRenderer.js`,不在 home/ 下) | N/A — 历史 home/* 死代码问题已在 06-21 报告升级 ARCH-P1 |
| 同文件 OPEN PR (`gh pr list head:dreamB --json files`) | **PR #34 命中** | **不开 PR**,报 PR-COLL-P1 |
| PR 队列健康 (OPEN 总数/近 30 天 MERGED) | **30 OPEN, 0 MERGED 30d** | **PROC-P1 强报警**,简报必报 |

## 下次 Dream B 启动门控
当前流程梗死,**建议老王手动**:
1. 决定 PR #34 / 22~33 / 35~42 处置(merge / close / 拆批合)
2. 若 close > 50%,Dream B 再启动新自动 PR
3. EDG-1/2 修复方案拍板后由老王(或人类协作)单开 PR 实施,**Dream B 不接此 P1**
