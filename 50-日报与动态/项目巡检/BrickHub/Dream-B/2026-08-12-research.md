# Dream B — BrickHub 日审 2026-08-12

## 结论

- 审查：`lib/pipeline/index.js`
- 自动修复：0；未创建分支/PR（新增项为 P1，按红线仅报告）
- 新增：`maxRetries=0` 时 DSL 首轮失败会让 pipeline 返回 `undefined`，API 随后访问 `result.pipeline` 崩溃。
- 队列：Dream B OPEN 5（总 OPEN 6），未触发 ≥8 熔断；5 条均 CLEAN，但距最近合并已 41 天，流程持续梗死。
- 依赖：Next.js `14.2.35` 仍落后最新 `16.3.0` 两个大版本；July Security Release 只给 15.5/16.2 补丁，升级仍是 P0 排期项。

## 已修复

| ID | 级别 | 位置 | 结果 |
|---|---|---|---|
| — | — | — | 0 项；P0/P1 禁自动改 |

## 待确认（P0/P1）

| ID | 级别 | 位置 | 发现与证据 | 建议 |
|---|---|---|---|---|
| BUG-01 | P1 新增 | `lib/pipeline/index.js:126-147,230`、`pages/api/generate.js:39,85-90` | 当 `maxRetries=0` 且首轮无 `<LEGO>`/DSL 编译失败时，代码 `continue` 后跳出 `while`，函数走到末尾返回 `undefined`；API 紧接着读取 `result.pipeline.mode`。临时 Jest probe 实测输出 `DREAMB_PROBE_RESULT undefined`。环境变量 `GENERATE_MAX_RETRIES=-1` 经现有解析得到 0，可真实触发。 | 业务拍板后修：将“无剩余重试”显式返回 offline fallback，或把配置下限钳到 1；补 `maxRetries=0 + missing DSL tag/compile failure` 回归测试。 |
| TEST-01 | P1 延续 | `__tests__/generateIntegration.test.js:15` | 6/6 通过，但整模块 mock `generateLDrawPipeline`，只守 API 协议，不执行 pipeline；BUG-01 因而长期漏检。 | 新增 `pipeline/index.test.js`，真实执行 fallback、stalled validation、maxRetries 边界。 |
| DEP-01 | P0 延续 | `package.json` | 实装 Next `14.2.35`，npm latest `16.3.0`；主版本差 2，Next 16 已稳定超过 30 天。 | 明确迁移周；优先独立分支跑 Pages Router、React/R3F 兼容、`next/image` 与生产 build。 |
| CVE-01 | P1 延续 | Next/Image Optimization | 7 类命中面复扫：middleware/App Router/cache/server actions/RSC/危险 fetch/rewrites 均 0；`next/image` 3 处。自托管 + remotePatterns 命中，但 URL 固定可信 CDN `.png`，CVE-2026-64644 仅“条件部分命中”，未证实恶意 SVG 可达。 | 不夸大为已可利用；通过升 Next 15.5.21+/16.2.11+消除 14.x 无补丁窗口。 |
| PROC-01 | P1 延续 | GitHub PR 队列 | Dream B OPEN 5：#43–#47 全 CLEAN；最近合并 #39 为 2026-07-02，已 41 天。 | batch review 后 merge/close，解除同文件冲突与自动修停滞。 |

## 回滚 / 验证

| 项目 | 结果 |
|---|---|
| ESLint | `npx eslint lib/pipeline/index.js` 通过 |
| 现有测试 | `npx jest __tests__/generateIntegration.test.js --runInBand`：6/6 通过 |
| 边界 probe | 临时 Jest：`maxRetries=0 + missing DSL tag` 返回 `undefined`，1/1 验证通过；临时文件已删除 |
| SWC | Next `14.2.35` / SWC `14.2.33`，主版本一致，无 binding 撕裂 |
| 项目工作树 | 未修改 BrickHub；原有未跟踪目录/文件保持不动 |

## 四维扫描

- Bug：新增 BUG-01；其余 fallback/retry 路径未发现经工具链证实的新问题。
- 架构：`generateLDrawPipeline` 与 telemetry exports 有生产 caller；`dslTelemetry`、`getValidationSignature` 均非死符号。
- UI：纯 pipeline 模块，不适用触控/色彩/3D Visual Check。
- 测试：现有 integration test mock 整个 pipeline，真实核心仍零覆盖，TEST-01 延续。

## 依赖动态

| 依赖 | 项目 | 最新 | 判定 |
|---|---:|---:|---|
| Next.js | 14.2.35 | 16.3.0 | P0：落后 2 大版本；16.3 于 2026-08-03 stable，July 9 个漏洞补丁仅覆盖 15.5.21/16.2.11+ |
| Three.js | ^0.183.2 | 0.185.1 | 小版本落后；未发现需立即处置的 breaking |
| R3F | ^8.18.0 | 9.7.0 | v9 面向 React 19 且有 migration breaking；应与 Next/React 迁移统一评估，不单独升级 |
| Drei | ^9.122.0 | 10.7.8 | 主版本升级，随 R3F 迁移统一评估 |
| LDraw | 2026-07 | 2026-07 | 官方新增 414 files / 294 parts / 5 primitives；无 breaking |

## 竞品动态

- BrickLink Studio 最新 `2.26.7_1`（2026-07-30）仍为 bug fixes，无新 breaking/需跟进功能。
- Mecabricks 本轮未发现可信的新版本级动作。

## PR 队列

| PR | 状态 | 文件 |
|---|---|---|
| #43 | CLEAN | BrickRenderer |
| #44 | CLEAN | BrickRenderer、FreeBuildTray |
| #45 | CLEAN | sandbox |
| #46 | CLEAN | model/[id]、sandbox |
| #47 | CLEAN | PartsTray、model/[id]、sandbox |

## 信息源

- Next.js 16.3（2026-08-03）：https://nextjs.org/blog/next-16-3
- Next.js July 2026 Security Release：https://nextjs.org/blog/july-2026-security-release
- LDraw 2026-07 update：https://library.ldraw.org/updates?latest=
- BrickLink Studio download：https://www.bricklink.com/v3/studio/download.page
- R3F v9 migration：https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- 本地实测：ESLint、Jest、npm registry、GitHub CLI、CVE 7 类 grep 探查
