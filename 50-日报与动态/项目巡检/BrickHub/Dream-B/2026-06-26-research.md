# Dream B 2026-06-26 — lib/llmPrompt.js

## TL;DR
| 维度 | 结果 |
|---|---|
| 审查文件 | `lib/llmPrompt.js` (898 LOC, 4 exports, 42.8KB) |
| 自动修复 | **0 项** (无机械可修；所有问题涉及业务/语义判断) |
| 开 PR | 否 |
| P0 | 0 |
| P1 | 3 (ARCH 自相矛盾 prompt / TEST 0 真实覆盖 / PROC 队列梗死) |
| P2 / P3 | 0 / 0 |
| 依赖/竞品 | 无新 breaking（Next.js 14 EOL 状态延续 2026-06-18 固化警告） |

---

## 待确认 (P0/P1, 仅报告)

| ID | 等级 | 位置 | 描述 |
|---|---|---|---|
| ARCH-001 | **P1** | `lib/llmPrompt.js:51-54` vs `L127-281` | `buildSystemPrompt()` 头部 L52 强制 “Wrap ALL code between `<DSL>` and `</DSL>` tags ... Output MUST be valid JSON. DO NOT output raw LDraw text lines. Use the DSL JSON format ONLY.” 但正文 L127-281 提供 4 段 `<LDRAW>` 包裹的**纯 LDraw 文本示例**（Red Sports Car / Simple Red House / Red Double-Decker Bus / Blue Knight 等），并配 “PRE-OUTPUT VERIFICATION” L394 要求“Each line has exactly 15 tokens”（典型 LDraw 行格式约束，不是 JSON）。此函数在 `lib/pipeline/index.js:100` 是 DSL 模式失败后的 **LDraw 降级路径**（L145 错误码即 `未检测到 <LEGO> 标签，降级为 LDraw 模式`）— 但 prompt 头部却命令 LLM 输出 JSON。**自相矛盾的 prompt → LLM 不知道该输出什么 → 高概率再次失败 → 重试链路浪费 token + 体验受损。** 需老王业务拍板：① 把 `buildSystemPrompt` 改回纯 LDraw 模式（删 L51-54 的 “JSON only / DO NOT output raw LDraw”，恢复 LDraw 头）；② 或彻底废弃这条降级路径，DSL 失败直接报错；不要让一个函数同时挂两套互斥规范。 |
| TEST-001 | **P1** | `lib/llmPrompt.js` 全文件 | 4 个 export (`buildSystemPrompt` / `buildUserMessage` / `buildDSLSystemPrompt` / `buildDSLUserMessage`) `grep -rn "build(DSL)?SystemPrompt\|build(DSL)?UserMessage" __tests__/` **0 真实匹配**，0 测试。这是纯字符串拼接 + 错误分类逻辑（`errors.filter(e => e.type === 'collision'\|'float'\|...)` 走分支），ROI 极高：① snapshot 测 prompt 关键 token（部件库/颜色码/坐标系说明全在/格式标签 `<DSL>` `<LEGO>` `<LDRAW>` 正确）即可锁住 LLM 行为契约不被静默改坏；② `buildUserMessage` / `buildDSLUserMessage` 的错误分类（unsupported parts / format / matrix / collision / float）有 5 个独立 if 分支，逐项验证可防止 prompt 重试链路退化。强烈建议老王给一个 issue 排期补测试。 |
| PROC-001 | **P1** | 全仓 PR 队列 | `gh pr list --state open --json state` → **36 OPEN**；`gh pr list --state merged --jq '[.[] \| select(.mergedAt > "2026-05-26")] \| length'` → **过去 30 天 MERGED = 0**。Dream B 自 04-30 累计 35 条 OPEN PR (#8–#42) 全部 `mergeStateStatus=CLEAN` 但**无一合并**。**流程梗死。** 其中 #22-#33 是死代码 `components/home/*` 上的 a11y 修复（在 2026-06-21 Dream B 已固化确认该目录 0 import / 0 page 拉起），不应该 merge — 老王应该 batch close 或一次性回潮 `components/home/` 归档。若不处理 a) 后续 Dream B 还会继续往死目录写无意义 PR（已点名但 Dream B 选文件时仍可能轮到），b) 队列体积阻塞老王 review attention。**建议动作**：① `gh pr close #22 #23 #24 #25 #27 #28 #29 #30 #31 #32 #33` (11 条死代码 PR) ② 评估 `git mv components/home components/_archive/home-deprecated-20260626/` ③ 剩余 25 条按 ROI 排序，每周 merge ≥5 条或主动 close。 |

---

## 已修复 (自动修复)

| ID | 位置 | 描述 | commit |
|---|---|---|---|
| — | — | 无 P2/P3 | — |

理由：本文件 898 LOC 全为 prompt 模板字符串 + 错误分类逻辑，所有可改之处都直接影响 LLM 行为，**不属于自动修可碰范围**（命名/未用变量/触控/aria/动画/颜色规范全部不适用）。Lint 通过、import 通过（4 exports 全部返回非空字符串 ≥17 chars，最长 18873 chars），无机械 bug。

---

## 回滚 (本次无)

| ID | 文件 | 失败原因 |
|---|---|---|
| — | — | — |

---

## Bug-Hazards 速查（防止下次轮到同文件 reviewer 重复验证）

| ID | 位置 | 表面现象 | 判定 |
|---|---|---|---|
| BH-001 | `lib/llmPrompt.js:123` `` \`; `` 后跟 markdown 标题 | 看似模板字符串提前关闭，下方一大段 markdown 裸落在 JS 外 | **无 bug**。`\` 是反斜杠转义符，反引号被转义，模板字符串实际继续到 L399 `\`}`。`node -e "import().then(m => m.buildSystemPrompt())"` 返回 **18873 字符**，包含 L127-399 全部内容；eslint 全过；4 exports 均正常。Dream A/B 文档中已固化此 false-positive 验证套路。 |

---

## 依赖/竞品动态

| 类别 | 状态 |
|---|---|
| Next.js 14 EOL + High/Critical CVEs | **状态延续**（2026-06-18 已固化为 P1）。Vercel 明示 14.x 不再发布补丁，2026 累积多个 High CVE（auth bypass / SSRF / DoS / XSS）— 升级到 15 / 16 是迟早动作。本次不重复占用 search 配额，待老王在 dependency window 拍板。 |
| Three.js / @react-three/fiber | 无 breaking |
| LDraw parts library | 无变动 |
| Bricklink Studio / Mecabricks | 无竞品新动作 |

---

## 信息源

- 源文件: `lib/llmPrompt.js` (898 LOC, 42844 bytes)
- 调用方: `lib/pipeline/index.js:1,96,100,145`
- 验证命令:
  - `node -e "import('./lib/llmPrompt.js').then(m => ...)"` — 4 exports 全返回非空 string
  - `npx eslint lib/llmPrompt.js` — clean
  - `grep -rn "build(DSL)?SystemPrompt\|build(DSL)?UserMessage" __tests__/` — 0 命中
  - `gh pr list --state open --json state` — 36 OPEN
  - `gh pr list --state merged --jq '[.[] \| select(.mergedAt > "2026-05-26")]'` — 0 近 30 天
- Pre-PR Pre-Flight (skill `dreaming.references/dream-b-pre-pr-pre-flight.md`):
  - 死代码: ✗ 不适用（被 pipeline 主动 import，活）
  - 同文件 OPEN PR 冲突: ✗ 无（`headRefName:dreamB` 查询不命中 llmPrompt.js）
  - **PR 队列健康: ✓ PROC-P1 触发**

---

## 历史轮换

最近本文件被轮换：2026-06-12（建议下一轮跳过 llmPrompt.js，本次无机械可修，2 周内不会再有新可挖角度）。

---

## 老王 next-action 优先级（建议）

1. **PROC-001（最优先）**: `gh pr close #22-#33` 11 条死代码 PR，节省 review attention
2. **ARCH-001**: 拍板 `buildSystemPrompt` 走 LDraw 还是 DSL，二选一
3. **TEST-001**: 开 issue 给 `lib/llmPrompt.js` 加 snapshot 测试 + 错误分类分支测试
