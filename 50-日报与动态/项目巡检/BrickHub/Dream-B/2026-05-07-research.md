---
title: "BrickHub Dream B 2026-05-07"
created: 2026-05-07
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-05-07
---

# BrickHub 每日 Dream B

## 今日审查：lib/instancingPipeline.js (192 LOC)

模块职责：把 brick 列表按材质/几何特征聚合成 InstancedMesh 分组、生成入场动画时间线、以 ease-in-out 采样位置。

### ✅ 已自动修复
无 — 代码质量已经很高，没有 P2/P3 可改项。

### 🔴 待人工确认（P0/P1）
无 P0/P1 发现。模块整体属性：
- 输入全部经 `normalizeNum / safeArray3 / safeQuat` 兜底，无 NaN 风险
- 无副作用、无外部 IO、无 useEffect 清理需求（纯函数）
- 测试覆盖完整：`__tests__/instancingPipeline.test.js` 覆盖 `groupBricksForInstancing / sampleInstanceTransform / summarizeInstancing`
- mulberry32 PRNG 由 seed 控制，动画可复现，便于做 Visual Check

### ⚠️ 自动修复失败回滚
无（未触发自动修复）

### 📝 观察记录（非 issue，仅备忘）
- `groupsBySig.values()` 后用 `groups.push(...list)` 展平，再按 `instances.length` 倒序——若未来 bricks 量级到 10K+，可考虑直接在循环里维护排序堆，但当前 LEGO 中型 MOC 量级（<2K）无必要
- `sampleInstanceTransform` 里 `done: progress >= 1` 与 `progress` 已被 clamp 到 [0,1]，逻辑一致，无需改

## 依赖动态

| 依赖 | 当前 | 最新 | 变更 |
|------|------|------|------|
| three | ^0.183.2 | r170 / 0.170 系列 | 当前实际跑较新版本，无 breaking |
| @react-three/fiber | ^8.18.0 | 9.5.0 | **9.x 支持 React 19.2 + Activity feature**；升级需先升 React |
| @react-three/drei | ^9.122.0 | 与 R3F v8 配套 | 升 R3F v9 时同步升 drei |
| next | ^14.2.0 | **16.2** | 落后 2 个大版本；15 stable 已出，16.2 含 Turbopack 启动加速、新错误页、AI 增强 |

**升级建议（仅供老王评估，不自动执行）**：
- 短期：跟进 Next.js 14.x patch，关注安全公告即可
- 中期：评估 Next.js 15 升级路径（App Router 已稳定 + 缓存策略变更）
- 长期：React 19.2 + R3F v9 + Activity feature 对场景级 mount/unmount 性能有利，但需要整链路重测，建议放到 v2 milestone

## 竞品动态
- **BrickLink Studio** 2026-04-27 发布更新：修复 POV 渲染下柔性部件缩小的 bug；BrickLink 整体在做 LEGO Group 的安全合规升级。对我们影响：可继续作为对比参考的桌面级 MOC 工具，POV 渲染质量持续提升。
- **BrickLink Designer Program 2026–2027 Series 8–11** 已开放，社区设计师生态活跃，对 BrickHub 而言是潜在用户/素材源池。

## 信息来源
- https://newreleases.io/project/github/mrdoob/three.js/release/r170
- https://newreleases.io/project/npm/@react-three/fiber/release/9.5.0
- https://nextjs.org/blog/next-15
- https://releasebot.io/updates/vercel/next-js
- https://www.bricklink.com/v3/studio/download.page
