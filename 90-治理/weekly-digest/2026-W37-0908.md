---
title: Dream A 周度维护 2026-W37 · 09-08增量审计
date: 2026-09-08
week: 2026-W37
tags: [治理, Dream-A, 记忆, Skills, Cron]
---

# Dream A｜2026-W37 · 09-08增量审计

审计始于 2026-09-08 19:00，Asia/Shanghai。观察窗口为 09-01 至本轮审计时点；最近会话为有界抽样，不是全周全量。已有 [[2026-W37]]，本次另存日期后缀，不覆盖昨日报告。

## 结论

**部分完成，不能判全绿。** 记忆审阅、Skills 描述及静态检查、近期决策提取已完成；Cron 管理工具在本定时会话不可用，完整任务集、执行分类、调度合理性和投递核验未完成。未新验证出 P0/P1 业务故障，不沿用昨日故障作为今日告警。

本轮记忆 **0 条 demote / 0 条 remove**；仅补充一处 dreaming 审计方法。未改项目代码、调度或服务，未重启、发布或合并 PR。

## 关键决策表

<table>
<tr><th>主题</th><th>本周决策／偏好</th><th>证据与边界</th></tr>
<tr><td>BrickHub 发布隔离</td><td>用户明确要求不再在生产目录构建，必须有真实公网、静态资源验收及回滚路径。</td><td>09-07 16:08，用户消息 93992。历史修复授权不延续到本轮；本轮不执行部署。</td></tr>
<tr><td>亲子旅行页面</td><td>内容要包含各地景点介绍、必去点、酒店价格、逐段里程／时间及地图点位卡片，不只改页面形式。</td><td>09-08 14:09，消息 100150 的真实用户追加正文。仅提取需求，不接管仍在迭代的旅行项目。</td></tr>
<tr><td>北京出发机场</td><td>本次赣东北行程优先考虑北京大兴 PKX；不得把已核的首都 PEK 航季线索替代成大兴航班。</td><td>09-08 14:21，用户消息 100215：“飞机优先考虑北京大兴机场”。属于本次旅行偏好，不扩写为永久旅行规则。</td></tr>
</table>

相关历史会话可从 @session:default/20260907_160805_1b183d 回看发布要求；本次旅行需求见 @session:default/20260908_092043_0c3a275a 。

## 新发现与未闭环项

- **Skills 静态审计覆盖扩大**：本轮直接使用获准的 terminal 做纯静态解析，没有调用昨日被 cron 策略拦截过的 execute_code，也没有执行技能业务脚本。144 个非隐藏 SKILL.md 的 YAML/name/description 检查无错误；scripts 目录内 92 个 Python 文件 AST 解析无错误。语法通过不等于依赖可用或业务可运行。
- **隐藏归档不是活跃重复**：首次物理扫描包含 `.archive`，同名候选均涉及归档；排除隐藏目录后没有同名主入口。没有删除归档。已把此排假阳性步骤补入 dreaming 审计附件。
- **旅行航空偏好仍有交付缺口线索**：历史消息 100270 的笔记更新明确“PKX 优先尚未重排，不能声称大兴航班已核”。这是当时记录，未做本轮公网验收或补查票价；留给旅行任务继续收尾，不在维护任务顺手改页面。
- **历史工具记录异常待验证**：09-08 会话出现同一 tool-call ID 同时带有效参数与空对象，伴随 `Invalid command: expected string, got NoneType`、`path required`。例如消息 100264–100271。当前只能证明会话保存了这些记录，不能确定是模型生成、解析、持久化还是重放问题；本轮工具调用未复现，不定为已证实生产故障。最便宜的后续核验是对同一 ID 比较原始 provider 事件、执行日志与数据库记录，禁止用真实外部写来试重复执行。

## 记忆审计与变更记录

- MEMORY：系统注入 **1,997 / 2,200 Unicode chars**，余量 **203**。
- USER：系统注入 **1,367 / 1,375 Unicode chars**，余量 **8**。采用系统 current/limit，不混算 UTF-8 bytes。
- 本轮未调用 memory 写入：**0 demote / 0 remove / 0 replace**。没有找到足以确认已过时的新条目；昨日已清除 OpenHippo 必依赖 ollama 的断言，本轮不重复计算。
- 模型权限、服务拓扑及家庭／客户信息保留，历史实测不冒充今天实时结果。USER 的“缺 read:org 时……”是条件式排障提示，不因当前 gh 已有该 scope 就判整条过时。
- 官方当前 Cron 文档给出的模型解析顺序为 per-job pin → cron.model → global default，并有未固定模型的漂移守卫。注入记忆中的继承描述仍过简，但本机 fork 实现未核，暂不删除或改配置。
- 本轮提取的旅行日期、机场偏好及项目交付进度留在业务笔记／周报，不追加到已接近上限的跨会话记忆。程序性经验进入对应 skill。

## Skills 健康报告

### 枚举与描述

- `skills_list` 返回 **143 个运行时可见技能**；逐项审阅描述。较昨日报告的 139 个多 4 个，这是可见数量变化，不宣称同名、安装来源或版本全部完成对账。
- 非隐藏物理实体为 **144 个**；额外的 `sdlc-review` frontmatter 明确 `environments: [kanban]`、需要 kanban toolset，不在当前 cron 可见列表有合理门控解释，不报废弃或加载故障。
- 描述边界总体清楚：训练类标参考用途；营销设计与应用界面分流；飞书按业务域分流。OpenHippo 旧名入口和另一 Obsidian 入口均说明兼容／转路由，不按相近名称自动删除。
- 未确认需删除的过时技能；未做各外部项目当前版本、上游维护活跃度或全部依赖安装验收。

### 静态证据

- 144 个非隐藏 SKILL.md：YAML 可解析，name／description 字段检查无错误；主入口同名数 0。
- 92 个 `scripts/**/*.py`：只做 AST，不 import、不安装、不运行；语法错误 0。
- 主入口 Markdown 链接扫描得到 **6 个疑似缺失目标，逐项排除**：SAM 两项实际是 `sam_model_registry[...](checkpoint=...)` 代码；幼升小四项是引用模板的 URL 占位符。本扫描内确认断链 0，不外推为所有附件、裸路径、跨 skill 路由全部无断链。
- 初次按技能名猜 SAM 目录导致 `Path not found`；实际目录是 `mlops/models/segment-anything/`，已经重新定位并读到代码语境，不把名称与目录不一致算坏技能。
- 详细枚举与原始候选：`/home/wpsl5168/work/dream-a-audit/2026-09-08/skills-static.json`。该文件为本机证据，不作为已推送附件。

### 本轮 skill 变更

`dreaming/references/dream-a-audit-cheatsheet.md` 新增：隐藏归档单列、活跃重名排假阳性、环境门控查看 frontmatter、归档不自动删除。`skill_manage` 已确认一处 patch 成功；未改其他 skill。

## Cron 健康报告

### 阻塞及分类

- 工具检索没有提供 `cronjob`；官方文档明确 cron-run sessions 禁用 cron management。本轮无法执行用户要求的 `cronjob(action="list")`，没有改用 CLI、jobs.json、导入调度器或其他通道绕过。
- 未得到完整 job 集合，因此 **DISABLED / FAILED / NEVER_RUN / OVERDUE / OK / UNKNOWN 各组数量均未统计**，不能填 0；fleet 状态为 UNKNOWN。
- 未核 schedule、timezone、实际应触发时刻及宽限期，未以未来 next_run_at 或固定天数阈值洗成健康。
- **投递独立为 UNKNOWN**：未取得逐 job ledger 与精确回执；有会话、正文或执行记录不能证明已送达。
- 本轮无已执行 cron 列表调用的 error 文本，错误类型是“管理入口在当前会话不可用”，不是虚构某 job 执行失败。没有业务重跑、补投或调度变更。

### 频率疑点与修法建议

本轮无 query 浏览样本包含 09-07 19:00 的 Dream A，会话环境及实时时钟证明本轮为 09-08 19:00，连续日期均触发“周度”任务。只能记录频率疑点，不能由两次会话反推出当前 cron 表达式。

建议在有权限的交互会话做一次完整只读清单审计，并决定是否改为每周一次。若要长期无人值守审计，需要单独设计并授权只读、脱敏的调度与投递快照入口；不放开通用 cron 管理或 arbitrary Python 权限来满足审计。

## 下周关注／待拍板（最多三项）

1. 补齐 Cron 全量只读清单与投递核验，核实后决定 Dream A 是否降为每周一次；本轮不改调度。
2. 如继续出现重复空参数工具记录，单独授权只读链路诊断；先判明产生环节，再谈代码修复或升级。
3. 旅行任务收尾：按 PKX 优先补齐航空比较，指定日期酒店价格未核时继续留空；维护任务不代替预订或发布。

## Git 与变更范围

- 本轮开始于 Obsidian `main`；fetch 后 HEAD 与 origin/main 提交差为 **0 / 0**，暂存区为空。
- 原有 **9 个脏路径**（证券研究、旅行笔记／HTML及索引）留存；已在本机 `vault-before.json` 保存路径、状态与 SHA256。只 stage 本轮周报，不执行宽泛 `git add -A`。
- 为避免 `pull --rebase` 触碰已有未暂存工作，发布验证使用本轮干净 detached worktree；不 stash、不 reset、不覆盖原工作区。若发生远端冲突则停并保留材料。
- 提交消息为 `dreaming: weekly digest`；本文件的真实 commit SHA、push 与远端读回结果见最终简报和 Git 历史。正文不伪造自引用 SHA。

## 信源与验收边界

- 系统本轮注入的 MEMORY／USER current/limit；实时时钟与 Git 状态输出。
- 本轮 `skills_list`、非隐藏物理扫描、YAML／AST 结果、疑似链接语境回读及 skill_manage 写入回执。
- `session_search` 无 query 最近 10 个样本；两段大 session 为首尾截断视图，并按 anchor 补读关键决策。关键词搜索返回窗外旧结果或空结果，不把它当近期会话不存在；决策归属以消息 timestamp 为准。
- 昨日报告 [[2026-W37]] 仅作增量对照，不继承其实时健康结论或授权。
- [Hermes 官方 Scheduled Tasks 文档](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron/)，本轮重新提取；本地 fork 行为未做实现级对齐。

**未完成的明确范围：Cron 全量执行／投递／调度验收、skills 全部业务依赖与运行验收、历史工具重复现象根因验证。上述项目不得记为健康。**
