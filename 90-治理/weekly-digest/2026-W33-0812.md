# 2026-W33 周报增量（Dream A）

生成时间：2026-08-12 19:01 CST

## 总体健康度

🟡 13 个 Hermes Cron 本体与业务投递字段全部正常；但独立微信投递 watchdog 仍持续失败，治理监督面失效。BrickHub 已连续 41 天零合并。记忆容量安全，本轮无保守清理项。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 微信主动推送 | `context_token` 是用户入站后约 24h 回复窗口，Bot 无法自行续期；业务 Cron 继续走微信，20h 提醒、24h 飞书告警 | @session:default/20260807_184607_411c9d96 |
| 投递健康口径 | `last_status=ok` 与 `last_delivery_error=null` 仍不能替代独立监督面；watchdog service 必须成功 | jobs.json + systemd 实测 |
| 模型链 | Copilot Enterprise 未授权 Claude；主模型/Cron 使用 `gpt-5.6-sol`，Responses API 模式 | @session:default/20260805_113439_ca629f85 · @session:default/20260805_121538_8f20df |
| 儿童天赋评估 | 不采信“万能天赋题”；正规路径是中国常模标准化量表 + 访谈/行为观察/作品评价，多来源持续评估 | @session:default/20260811_103520_d05f932c |

## 新发现

- 13 个 Job 当前全部 `last_status=ok`，且 `last_delivery_error=null`；昨日“小贝周度知识库整理”的历史投递错误已在新一轮执行后清零。
- watchdog timer 虽每 5 分钟正常触发，但 service 截至 18:58 仍为 `Result=exit-code / ExecMainStatus=1`，前日报告的问题未修复。
- `wiki_lint_daily` 继续调用不存在的 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py`；`kb-maintenance` 参考文档反而明确记录该脚本不存在，Cron 提示词与现行 Skill 工作流冲突。
- Dream A 名义为“每周”，实际 schedule 仍是 `0 19 * * *`（每日），本周已连续生成 3 份报告。
- 8 月 11 日微信图片附件连续多次未进入会话，符合既有“微信发图常丢”记忆，无需新增重复条目。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| replace/compress | 0 | 当前内容高信号且无重复冲突 |

容量：USER.md 1350 字符（软上限 2750）；MEMORY.md 2186 字符（安全线 3500）。

## Skills 健康

- `skills_list` 共 115 个技能；描述可解析，未发现 `[SKILL_PRUNED]` 或明显失效描述。
- 新增/现有 `wp-psychometric-assessment-guidance` 与本周“标准化测评、官方背书、网络测试区分”需求匹配，描述准确。
- `kb-maintenance` / `obsidian-kb-maintenance`、`wp-openhippo-ops` / `wp-openhippo-ollama-ops` 职责有交叠，但触发范围不同，本轮不删不并。
- 问题不在 Skill 本体，而在 `wiki_lint_daily` 的过时调用入口。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无启用且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK | 13 | 本体成功、投递字段均清零 |

### P0：微信投递监督面持续失效

- watchdog timer：active，每 5 分钟触发。
- watchdog service：failed，`ExecMainStatus=1`，截至 18:58 仍复发。
- 已知根因：systemd PATH 不含 Node 所在的 `~/.local/bin`，`lark-cli` 经 `/usr/bin/env node` 启动失败。
- 修法：给 unit/runner 配显式 PATH 或 Node 绝对路径；随后手动触发，验证 `Result=success` 并完成飞书实发。

### P1：治理调度与自动修入口

- Dream A 应从每日 `0 19 * * *` 改为周日 `0 19 * * 0`，避免日更周报和重复审计。
- `wiki_lint_daily` 应删除不存在的 `wiki_fix.py` 调用，改为遵循 `kb-maintenance/references/daily-lint-workflow.md` 的现行修复流程。
- 本轮按 Dream A 边界仅报告，未改 jobs.json、未创建 Cron、未改项目代码。

## BrickHub PR 队列

| 指标 | 当前 | 判定 |
|---|---:|---|
| OPEN PR | 6 | #43–#47 Dream B；#5 单独处理 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 41 天零合并 |
| 近 7 天 commit | 0 | 主仓停滞 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 31 天未新增 |

#43–#47 当前均 `CLEAN`；#5 为 `DIRTY` 大型正式页复刻 PR。命中“OPEN ≥3 且 14 天零合并”流程梗死条件，需批量 review/merge/close；#5 禁止代合。

## 本周 Pitfalls

- Cron `last_status=ok`、投递字段清零、独立 watchdog 成功是三层不同健康信号，不能互相替代。
- Skill 文档可能已经迁移到新流程，但 Cron prompt 仍引用旧脚本；健康检查必须同时核真实路径和现行参考文档。
- 周报存在同周主版本时必须写日期增量版，禁止覆盖。

## 下周关注

1. 修复 watchdog Node PATH，完成 service + 飞书实发双验证。
2. 将 Dream A 调度收敛为周日 19:00。
3. 更新 `wiki_lint_daily` 的失效修复入口。
4. 批量清理 BrickHub #43–#47，恢复合并链。