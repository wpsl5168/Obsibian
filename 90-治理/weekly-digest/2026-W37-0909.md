---
title: Dream A 周度维护 2026-W37 · 09-09增量审计
date: 2026-09-09
week: 2026-W37
tags: [治理, Dream-A, 记忆, Skills, Cron]
---

# Dream A｜2026-W37 · 09-09增量审计

审计起点：2026-09-09 19:00，Asia/Shanghai。回顾窗口为09-02至本轮时点，使用有界会话样本，不代表全周穷尽。已有同周主报告及0908增量，本文件不覆盖它们。

## 结论

**部分完成，不能判全绿。** 记忆审阅、Skills描述与静态检查、近期关键决策提取完成；Cron管理入口在定时会话禁用，全量执行、调度和投递核验未完成。没有本轮验证出的P0/P1业务故障。

- 记忆：**0条 demote / 0条 remove / 0条 replace**。
- Skills：143个运行时可见；144个非隐藏主入口。92个Python脚本AST检查无错误。
- 修复：GitHub skill同一附件的3处文档问题，含失效旧路径、自动挖取凭据示例和宽泛暂存示例。未改业务代码或运行配置。
- 本轮不建cron、不重跑业务、不补投、不重启、不发布服务、不合并PR。

## 关键决策表

<table>
<tr><th>主题</th><th>决策／本周结果</th><th>信源与边界</th></tr>
<tr><td>持仓提醒</td><td>09-08用户明确要求继续盯盘，需要操作时提醒。提醒需求不是自动交易授权；不因只有同花顺标的列表而重建数量、成本台账。</td><td>用户消息98245，09-08 09:24:02；相邻回复98244说明截图缺少仓位资料。本轮不访问券商、不修改持仓。</td></tr>
<tr><td>知识库目录重组</td><td>目录迁移、导航及引用修复与正文拆章／内容事实重审分开验收；定时任务下一次完整业务执行和手机端同步仍需单独验证。</td><td>库内迁移记录当前正文；Git提交9b2ee71783749280085e365fe0d9865e71fa37b7，09-09 08:17:21。该记录的“Git不在本次执行中”描述初始迁移批次，后续已有独立提交，不当作迁移未提交。</td></tr>
<tr><td>Hermes安装／升级CI</td><td>本周修复保留TLS校验，纠正Node/npm CA及宿主Node头文件误用；指定E2E验收与全仓CI分开，不能把缺runner的检查说成通过。</td><td>09-09会话消息102260；本轮GitHub回读PR #3为MERGED，merge SHA 60c2fa5aa60f358712a90edc7db68e9599e882ba。测试通过及缺runner项为该历史任务记录，本轮没有重跑或重新审计整套CI。</td></tr>
</table>

持仓及CI原始会话可从 @session:default/20260908_092043_0c3a275a 回看。目录迁移证据见 [[90-治理/迁移记录/2026-09-09-目录重组/README]]。

## 新发现

### 1. 已合并技能仍残留旧附件指引

`github/references/pr-workflow.md`仍引用已不存在的`github-auth`独立skill及`skills/github/github-auth/scripts/git-credential-token.py`。本轮实查旧脚本路径不存在，现路径`software-development/github/scripts/git-credential-token.py`存在；不是仅凭名称相近判定过时。

同时该附件的认证探测示例会自动读取`.env`或凭据文件，另有`git add .`后直接push的循环示例，与主入口的安全边界及精确暂存要求不一致。已定向修正，未执行这些旧示例。

### 2. 会话role与标题都不能单独当证据

关键词检索即使指定`sort=newest`仍返回窗外旧会话；当前会话的检索元信息也出现继承旧标题／起始日期的情况。整读返回`truncated=true`，含压缩摘要和首尾窗口，不当作完整阅读。

`role_filter=user`还可命中后台子代理完成通知，并不必然是老王亲自下的新指令。本报告的持仓偏好已回到真实用户消息98245核验；子代理评审只当历史评审材料，不追加为用户偏好。

### 3. “周度”维护连续日期运行的疑点仍在

本轮实际时间为09-09 19:00；最近样本有09-08 19:00的Dream A，同周已有09-07主报告及09-08增量。足以提示频率与任务称谓可能不一致，但不足以反推出当前schedule，更不能据此修改调度。

## 记忆审计与清理记录

- MEMORY：系统注入**1,997 / 2,200 Unicode chars**，余203。
- USER：系统注入**1,367 / 1,375 Unicode chars**，余8。预算以系统current/limit为准，不混算UTF-8 bytes。
- 未找到明确失效且可安全删除的条目，未调用memory写入。**0 demote / 0 remove / 0 replace**；不为腾空间删除可疑事实。
- 已有描述中的临时运行状态、历史模型权限及服务拓扑只作带日期背景，不据此报告今天的实时健康。
- “Cron若未单独指定模型会继承主模型”概括过简：本轮官方文档含per-job pin → cron.model → global default及漂移守卫。未核本地fork实现与实际pin，不将它强改为当前配置事实；后续按Cron审计范围核实。
- 本周项目进度、持仓提醒、CI修复及目录重组留在本报告／领域笔记，不向近满的跨任务记忆继续堆积。
- 相近的记忆条目有重复风险，但均包含不同边界；本轮不做无证据压缩。不存在“删除了几条所以一定更好”的计数目标。

## Skills健康报告

### 覆盖与静态检查

- `skills_list`返回143个运行时可见技能，逐项审阅描述。训练类已注明参考／本机不执行；设计、飞书、Obsidian、OpenHippo入口有明确分流，没有确认需要删除的过时skill。
- 非隐藏物理主入口144个；隐藏目录另有143个SKILL.md，单列而非混入活跃冲突计数，未删除归档。
- 活跃主入口名称重复0；name／description字段存在性检查无缺失。本轮是静态字段扫描，**没有重新做完整YAML语义或所有业务依赖验收**。
- 额外的`sdlc-review`明确`environments: [kanban]`及`requires_toolsets: [kanban]`，解释当前Cron上下文不可见，不报加载故障。
- 主入口内以references／scripts／assets／templates开头的Markdown相对链接，共检查514条，缺失0。范围不含所有裸路径、代码参数及附件内部引用；GitHub旧路径恰是附件内部的独立人工发现，不能用主入口扫描“零问题”掩盖它。
- scripts目录内92个非隐藏Python文件：只读AST检查错误0；不import、不安装、不执行业务脚本。未检查全部Shell／JS脚本，也未做上游版本、外部服务或运行性能验收。

### 实际修复与验证

仅修改`~/.hermes/skills/software-development/github/references/pr-workflow.md`：

1. 独立`github-auth`指引改为本skill的`references/auth.md`。
2. 删除自动读取凭据的fallback示例，改为先`gh auth status`、失败后加载认证附件，并明确拒绝不能换通道；记录现有helper的真实相对位置，路径存在不等于执行授权。
3. 自动修复循环改为只stage本轮精确路径，核NUL分隔索引，获得本轮发布授权后才push。

`skill_manage`确认3处patch成功；备份17个文件后，按逐文件SHA256对比确认仅该附件变化。新引用目标存在，旧可执行路径及宽泛暂存示例已不在正文中。没有运行认证helper、读取凭据或改GitHub项目代码。

证据：`/home/wpsl5168/work/dream-a-audit/2026-09-09/skills-static.json`、`github-before/`、`github-before-hashes.json`。这些为本机证据，不宣称随Obsidian报告推送。

## Cron健康报告

### 本轮阻塞

- 工具目录没有`cronjob`；检索也未提供该工具。本轮官方文档明确：“Hermes disables cron management tools inside cron executions”。因此无法执行任务要求的`cronjob(action="list")`。
- 未改用CLI、jobs.json、Python导入调度器等方式绕过限制；未更改安全策略。
- 这不是某个业务job的执行错误。本轮没有伪造job error、连续失败次数或成功数量。

### 执行、调度、投递分别结论

- **执行fleet：UNKNOWN。** 未获得完整job集合，DISABLED／FAILED／NEVER_RUN／OVERDUE／OK／UNKNOWN各组数量均为未统计，不能填0。
- **调度合理性：未完成。** 缺当前schedule、timezone、实际应到期时刻及宽限期；不能用最近会话、累计次数或未来next_run_at替代。
- **投递：UNKNOWN。** 没有逐job精确回执、ledger及发送日志核验。会话出现、报告存在、执行ok均不证明收到。
- 历史`shutdown_interrupted`、模型失败或微信错误没有本轮时序证据，不作为今日P0/P1重新广播。

### 修法建议

在允许Cron管理的交互环境做一次完整只读审计；长期无人值守方案需另行授权固定字段、只读、脱敏快照，明确快照生成时间与执行／投递证据来源，不放开通用Cron管理或任意代码权限。

## 下周关注／待拍板

1. **Cron只读审计入口与频率**：补全清单和投递证据，核实后决定Dream A是否真正降为每周一次；本轮不改schedule。
2. **迁移后验收**：下一轮业务任务是否写入新目录、手机同步是否完成，按独立验收记录核对，不能把目录迁移或配置读回当作全部自动化已验收。

USER余量8字符属于维护压力，不是业务P1。没有新增需要老王批准的生产修复。

## Git范围及发布核验

- 起始为Obsidian `main`；本轮fetch后HEAD与origin/main差异0／0，暂存区为空。
- 原有两处脏路径保留：`40-调研报告/AI技术与产品/README.md`、`40-调研报告/AI技术与产品/2026-09-09-SGLang项目分析.md`。已记录状态和SHA256，不夹带入本轮提交。
- 本轮仅提交本报告，消息`dreaming: weekly digest`。不用`git add -A`，不stash／reset／clean其他工作。
- 为避免在原脏工作区执行rebase，发布步骤使用干净detached worktree拉取／rebase并push；若冲突则停止，不强推。实际执行结果与远端读回SHA见最终简报及Git历史，正文不伪造自引用SHA。

## 信源与验收边界

- 系统注入记忆预算；本轮实时时钟、Git NUL路径、哈希及远端查询结果。
- 本轮skills_list、静态扫描、sdlc-review frontmatter、GitHub附件及skill_manage回执。
- 最近10个会话无query浏览；有限关键词检索和精确anchor补读。窗外结果、压缩摘要和后台通知不作为新的用户决策。
- [Hermes官方Cron文档](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron/)，09-09重新提取；本地fork行为未做实现级对齐。
- [Hermes fork PR #3](https://github.com/wpsl5168/hermes-agent/pull/3)，本轮回读MERGED与精确merge SHA；并未重验整仓CI。
- 同周旧报告仅作对照，不继承实时健康结论或历史授权。

**未完成范围：Cron全量执行／调度／投递核验；skills全部依赖与业务运行；迁移后所有自动化及手机同步验收。上述不能记作健康。**
