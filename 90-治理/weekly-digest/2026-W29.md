# 2026-W29 周报 (2026-07-06 → 2026-07-12)

> 🟡 总体健康:cron 全绿 · BrickHub 队列已清账(36→6) · USER.md 轻度超容 175 字符

## 健康报告

### Cron (13/13)
- **全部 OK**,无 FAILED / 无 ZOMBIE / 无 DISABLED
- 上周末在跑重点:daily-brief 持仓三件套、AI圈简报、Dream A/B、小贝周度、wiki_lint_daily、Hermes/Obsidian 备份

### 记忆
| 文件 | 字符 | 上限 | 状态 |
|---|---|---|---|
| USER.md | 2925 | 2750 软 | 🟡 超 175 字符(容忍范围,但连续超容需拍板) |
| MEMORY.md | 3281 | 3500 软 | 🟢 |

**本周记忆清理**:0 demote / 0 remove。所有条目本周仍被业务引用(BrickHub 触控/CVE/dead-code、Dream A/B pitfalls、OpenHippo copilot 迁移、持仓纪律),无明确过时条目,保守不动。

### Skills
系统提示注入正常,无 broken refs 发现。

### BrickHub PR 队列快照
| 指标 | 本周 | 上周 | 趋势 |
|---|---|---|---|
| OPEN | **6** | 19 | 🟢 -13(core_logic 整合 + 批量 close 生效) |
| 30d MERGED | 5(2026-07-02 一次性合入) | 0 | 🟢 恢复流转 |
| 7d 仓库 commit | **0** | — | 🟡 无推进 |
| Dream B 最近开 PR | 未新开(队列熔断解除但无自动修) | — | — |

**判定**:PR 梗死已解,但主仓库 7d 零 commit — 老王本周精力在 OpenHippo 迁移与 Hermes agent 相关,BrickHub 主动开发暂停。**非红灯**(队列健康),下周若 7d 仍 0 commit 且 Dream B 继续零新 PR,列冷启动待拍板。

## 关键决策(近 10 天,session 抽取)

| 日期 | 议题 | 决策 | 影响 |
|---|---|---|---|
| 07-08 | Hermes agent 版本对比 v0.17.0 vs upstream | 已 up-to-date,无升级动作 | — |
| 07-08 | OpenHippo embedding 全量迁移到 GitHub Copilot | ✅ 23,863 条重嵌 text-embedding-3-small(45/s,~9 min),向量维度 768,fail=0 | 语义检索恢复,新旧同源 |
| 07-08 | sqlite-vec UNIQUE constraint bug + config.yaml `~` 未展开 | 已修 `vec_store` 先 DELETE 再 INSERT + db_path 改绝对路径;备份 328M 无损 | 检索链路打通 |
| 07-07 | AI圈每日简报去 AI 腔 | 重写 prompt(禁 emoji 分段/八股填表/空心连接词/意义拔高),job `13045d2d46c8` 每日 21:00 生效 | 输出与老王偏好对齐 |
| 07-03 | 磁盘 83% + 内存无 swap 体检 | 清 4 月旧 state 备份 70M,pip cache 452M,journal vacuum;docker `hermes-agent:latest` 5.29G 未走容器可清 | 磁盘缓 |
| 07-03 | Copilot Fable-5 可用性测试 | ❌ 账户权限不放(HTTP 400 model_not_supported),opus-4.8 ✅ 直连正常 | 建议不切,继续 opus |
| 07-02 | Dream B core_logic 去重整合入 main | 13 条杂乱 PR → 2 commit(ad44ca00 非 3D 修复 + fe0ae5c3 3D BrickRenderer 已 Visual Check),队列 36→19→6 | BrickHub 队列健康恢复 |

## 新发现 / Pitfalls(周内固化)

1. **`~/.hermes/.env` 是 gateway 真实 env 源** — auth.json 仅存 `secret_fingerprint`,真 token 在 `.env::COPILOT_GITHUB_TOKEN`,systemd Environment 里查不到(load_dotenv 在运行时注入)
2. **sqlite-vec 虚拟表不认 `INSERT OR REPLACE`** — 必须先 DELETE 再 INSERT,否则 `UNIQUE constraint failed`
3. **config.yaml 里 `~` 不会自动展开** — OpenHippo 早期一路创了幻影 DB(`OpenHippo/~/.hippocampus/`),配置里 db_path 必写绝对路径
4. **Fable-5 Copilot 门槛** — 计划等级(Pro+/Max/Business/Ent) + 管理员 policy + ZDR 数据保留同意,三缺一 400,非技术问题

## 待老王拍板(≤3)

1. **USER.md 连续超容(175 字符)** — 下周仍超考虑砍旧条目(候选:早期"触控 56/44 判定"这类已固化到 skill 的可 demote)
2. **BrickHub 主开发是否暂停** — 7d 零 commit,若下周同样,是否显式标 "Phase 冷启动" 让 Dream B 停自动扫,腾出 opus 配额
3. **docker `hermes-agent:latest` 5.29G** — 07-03 体检确认未使用(hermes 走 systemd venv 非容器),可安全 `docker rmi` 回收

## 下周关注
- BrickHub 是否恢复 commit / Dream B 是否重新开 PR
- OpenHippo copilot embedding 长期稳定性(gho_ token 轮换风险 / 每日调用配额)
- USER.md 是否继续膨胀

---
*Dream A W29 收口 · 2026-07-13 · cron 无人值守*
