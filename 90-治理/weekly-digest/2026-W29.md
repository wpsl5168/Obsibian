# 2026-W29 周报(Dream A)

生成:2026-07-17 Fri · 覆盖 W28 后半 + W29 上半(07-11 → 07-17)

## 一句健康度
🟡 基础设施全绿 · BrickHub PR 队列继续梗死(6 OPEN / 15 天零合并) · USER.md 超容 175 字符待收缩。

## 关键决策(本周)

- **07-08 Hermes fork 版本盘查**:本地 v0.17.0(2026.6.19),upstream `3509be71`,CLI 自报 up-to-date;辅助产物「学习/英语时态速查表」PNG + md 已入库。
- **07-08 OpenHippo copilot embedding 迁移全部落地**:CopilotProvider(gho_ 直连 + urllib + dims=768) + 修 sqlite-vec `INSERT OR REPLACE` 报 UNIQUE 的历史 bug(DELETE→INSERT + model 列),bulk_reembed 23,863 条完成,45/s,fail=0。DB 有 328M `.bak` 备份。
- **07-07 AI 日报去 AI 腔**:job `13045d2d46c8` prompt 重写,加了铁律段(禁 emoji 分段/填表八股/意义拔高/形容词通胀/pipe 表),已按新规产出 07-16 日报。
- **07-06 SQL review 沉淀**:UPDATE...FROM 派生表的\"多行匹配静默取值\"/NOLOCK 写入侧/排序无唯一兜底 三坑,统一收敛到 CROSS APPLY 模板。
- **07-03 系统体检收口**:磁盘 51G/83%(11G 余),清 3 个 4 月旧 state.db 备份;state.db 803M = 5894 会话 + 双份 FTS 索引,不动核心库。

## 记忆变更
无(本周 USER.md 无需 remove/demote;超容问题走「待拍板」不做保守裁剪)。

## Skills & Cron 健康

Cron:13 job **全 OK**,无 FAILED/ZOMBIE/DISABLED。全 `run_count=0`(cron 系统重置计数,不代表未跑;`last_run_at` 均为今日或昨日,产出正常)。
- 无 `last_status=error` 连挂项(W27/W28 记录的 `critical-skills-audit-sync` 07-17 显示 ok,已恢复,持续观察)。
- Dream B 昨晚(07-16)按新 skill Step 0 熔断闸后续跑完,产出 llm.js 报告。

Skills:系统提示已列出全量清单,本周无破损/描述过期,无 patch 需求。

## BrickHub PR 队列(周度快照)

- **OPEN=6**(#5 巨型首页复刻 + #43~#47 Dream B 自动修 5 条)
- **最近 merged**:#39 · 2026-07-02(**距今 15 天零合并**)
- **7 天 commit**:0
- **趋势**:与 W28 末快照(OPEN=5,零合并 13 天)相比,新增 0,老堆积继续延长 → **PROC-P1 队列梗死持续第 15 天**。skill 已在 07-15 W28 收编「低 OPEN + 长零合并」阈值(≥14 天且 OPEN≥3),本周就命中。
- Dream B 熔断阈值 OPEN≥8 未触发,但新一轮轮换起点已进入「同文件冲突高发窗口」(07-14 W28 已踩过 BrickRenderer 拦截)。

## 下周关注(优先级排序)

1. **BrickHub PR 队列 batch review + merge**(#43~#47 均低危 a11y/死代码清理,skill `references/dream-b-pr-queue-cleanup.md` 有配套 `scripts/batch_merge_recompute.sh`)。不合 = Dream B 下一轮持续 0 修复,PROC-P1 会一直挂在报告里。
2. **USER.md 超容 175 字符**:软上限 2750,当前 2925。W28→W29 连续两周超容(skill Pitfall #17 触发),需人手压 2-3 条陈旧偏好或合并同义条。**保守不自动删,等老王一句话**。
3. **Next 14→16 迁移周排期**(DEP-P0 已挂第 9 天):Next 16.3 stable > 30 天,brickhub 仍 14.2.35。老王要么给 timeline,要么显式 defer 到 Phase 5,别让它继续以 P0 姿态每天噪音。

## 待拍板(编号)

1. **合掉/关掉 BrickHub 6 条 OPEN PR**(尤其 #43~#47,均 Dream B 自动 a11y/死代码清理)。
2. **USER.md 压 175 字符**:老王亲自挑 remove 目标(memory 工具改),或授权我保守压 1-2 条最陈旧的。
3. **Next.js 主版本迁移**:排期周 or defer 到 Phase 5?一个决定,否则 DEP-P0 每天占报告首行。

## 本周 Pitfalls / 观察

- Dream B 07-14 触发的「新一轮 restart 首日同文件冲突」(BrickRenderer #43 阻塞)已进 skill,本次 Dream A 快照如实反映为「PR 队列梗死 = Dream B 下一轮 0 修复」。
- OpenHippo bulk_reembed 完成后应有 `.bak.20260708_094405`(328M)可考虑清理,占体检黄灯磁盘的 3%(11G 中 328M ≈ 3%),下周体检时决定。
