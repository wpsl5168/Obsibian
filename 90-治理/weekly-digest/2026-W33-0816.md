# Dream A 周度治理收口 · 2026-W33（08-16）

> 执行时间：2026-08-16 19:00 CST  
> 范围：Memory / User Profile / Skills / Cron / Sessions / BrickHub PR 队列  
> 本文件为 W33 第 7 个版本，增量收口，不覆盖前序报告。

## 结论

13 个 Cron 执行态全绿，无 failed / zombie-enabled；但治理面无实质改善：7 个微信任务保留投递失败、Dream A 仍误设为每日、wiki 自动修复脚本路径仍失效、BrickHub 仍 6 个 OPEN PR 且 45 天零合并。项目侧 Next 14→16 迁移继续维持 P0。记忆仅压缩 1 条失效引用，无删除。

## 关键决策与近期发现

| 日期 | 决策 / 发现 | 沉淀状态 |
|---|---|---|
| 08-15 | BrickHub `knowledgeBase.js` 英文分类器把单字母 token 当关键词子串，实测 `race car` 可误判为 `ship` | Dream B BUG-P1，待定英文 exact/boundary 策略 |
| 08-15 | 非法 `topK='abc'` 经 `NaN` 传播后静默返回空结果且 HTTP 仍 200 | Dream B API-P1，待定返回 400 或回退默认 4 |
| 08-15 | `knowledgeBase.js` 唯一集成测试整体 mock 模块，7 个 exports 零真实覆盖 | Dream B TEST-P1 |
| 08-16 | 只读仓库问答优先主 Agent 直接拿 evidence bundle；多 Agent 交接必须保留文件、行号、原文，避免摘要静默失真 | 研究启发，不写入长期 memory |
| 本次 | 最近 sessions 无新的稳定用户偏好或用户拍板 | 无新增 memory |

## 新发现

- `critical-skills-audit-sync` 已恢复并持续成功，本次 `last_status=ok`、累计 completed=97。
- 微信投递失败仍覆盖 7 个任务；独立 `hermes-weixin-delivery-watchdog.timer` 为 active，最近 service `Result=success`，监督面正常但不能替代用户入站续期。
- Dream A schedule 仍为 `0 19 * * *`，W33 已连续生成主报告和 6 份增量，偏离“每周治理”定位。
- `wiki_lint_daily` 今日健康静默，但 prompt 仍引用不存在的 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py`；warn/critical 出现时自动修复会断路。
- BrickHub 仍 6 OPEN：#43–#47 均 CLEAN，#5 DIRTY；最近 merge 为 2026-07-02 #39，距今约 45 天；7 天 commit 为 0。

## 记忆变更

| 类型 | 数量 | 说明 |
|---|---:|---|
| demote / 压缩 | 1 | 删除 MEMORY 中不存在的 `wp-importing-external-skills` 引用，保留外部 skill 管理原则 |
| remove | 0 | 未发现可安全删除的明确过时事实 |
| add | 0 | 新发现仍属项目待办或研究启发，不进入长期 memory |

容量：USER 1281 chars / 2718 bytes；MEMORY 2143 chars / 3190 bytes。均未越治理阈值；USER 接近软上限但尚不触发强清。

## Skills 健康

- registry 共 116 个 skills；描述整体可检索，无 `[SKILL_PRUNED]`。
- 静态扫描的“疑似 broken links”多数是命令参数、glob、跨 skill 或源码仓库相对路径；复核后未确认运行级 P0/P1 broken ref。
- `china-a-share-quotes` 明示 `references/cninfo-announcements.md（待补）`，属于文档债，不影响实时行情主链；公告抓取时需现场实现或补 reference。
- 重叠候选 `kb-maintenance` / `obsidian-kb-maintenance`、`design-taste-frontend` / `frontend-design` 仍有潜在调用方，本次不自动删。

## Cron 健康

| 分组 | 数量 | 结论 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error/failed` |
| ZOMBIE-ENABLED | 0 | 无 enabled 且超过 30 天未运行 |
| DISABLED | 0 | 无 |
| OK | 13 | 调度执行均正常 |

### P1

1. **微信投递面失效**：7 个 job 最近一次运行成功但未送达，错误为 iLink `rate limited` 误分类链；只能靠用户微信入站刷新约 24h 窗口。
2. **Dream A 调度错位**：`0 19 * * *` 应改为周日 `0 19 * * 0`；按红线本次只报告不改 Cron。
3. **wiki 自动修复断路**：失效 `wiki_fix.py` 路径尚未修；应改为 lint JSON targeted repair，或补受版本控制的 fixer。
4. **BrickHub 流程梗死**：6 OPEN、45 天零 merge、7 天零 commit；需要 batch review + merge/close。

## BrickHub PR 队列

| 指标 | 本周值 | 趋势 / 判定 |
|---|---:|---|
| OPEN | 6 | 与昨日持平；Dream B 5 CLEAN，#5 DIRTY |
| 30d MERGED | 0 | PROC-P1 |
| 7d commits | 0 | 停滞 |
| 最近 merged | 07-02 #39 | 约 45 天前 |
| 最近 Dream B PR | 07-12 #47 | 约 35 天前 |
| 最老 Dream B OPEN | 07-04 #43 | 约 43 天 |
| 最近 Dream B 报告 | 08-15 | 新增分类器 / topK / 测试 P1 |

## 下周关注

1. 为 Next 14→15/16 指定迁移周；同步拍板分类器边界策略、非法 `topK` 契约和真实单测。
2. 将 Dream A 改为周日运行，并修正 `wiki_lint_daily` 的失效 fixer 路径。
3. 清理 BrickHub #43–#47；#5 为 DIRTY 巨型正式页复刻，继续单独处理。

## 本周 Pitfalls

- Cron 健康必须同时看 `last_status`、`last_delivery_error` 与 prompt 内条件分支引用；执行绿不等于投递和修复链绿。
- Skill broken-ref 扫描不能直接信正则：命令参数、glob、跨 skill 链接、源码 repo 相对路径都会产生假阳性，必须逐条核实。
- `read_file` 曾把 UTF-8 Markdown 误判 binary；用字节级 NUL/UTF-8 验证确认文件正常，禁止据工具误判改文件。
- 同周主文件已存在，使用 `2026-W33-0816.md` 增量版，不覆盖历史周报。

## 信源

- `~/.hermes/cron/jobs.json` 实时快照，2026-08-16 19:00 CST
- `skills_list` registry：116 skills；本地 SKILL.md 引用复核
- Sessions：@session:default/cron_39a5573af875_20260815_200048、@session:default/cron_13045d2d46c8_20260815_210049、@session:default/cron_004e8d32135c_20260816_080005
- `04-项目开发/dreaming/2026-08-15-research.md`
- BrickHub GitHub PR / git log 实时快照，2026-08-16 19:00 CST
