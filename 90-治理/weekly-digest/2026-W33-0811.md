# 2026-W33 周报增量（Dream A）

生成时间：2026-08-11 19:00 CST

## 总体健康度

🟡 13 个 Hermes Cron 本体全部成功，微信任务投递错误已由昨日 7 个降至 1 个历史遗留；但独立投递 watchdog 仍每 5 分钟失败。BrickHub 连续 40 天零合并，治理项未收口。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 微信主动推送 | `context_token` 是用户入站后约 24h 回复窗口，Bot 无法自行续期；业务 Cron 继续走微信，20h 提醒、24h 飞书告警 | @session:default/20260807_184607_411c9d96 |
| 投递健康口径 | `last_status=ok` 只证明任务执行；消息可达需另核 `last_delivery_error` 与 watchdog | 同上 + 本次 jobs.json/systemd 实测 |
| 模型链 | Copilot Enterprise 未授权 Claude；主模型/Cron 使用 `gpt-5.6-sol`，Responses API 模式 | @session:default/20260805_113439_ca629f85 · @session:default/20260805_121538_8f20df |
| BrickHub | Dream B 停止扩张队列；#43–#47 需批量 review/merge/close，#5 大型正式页复刻 PR 禁止代合 | Dream B rotation + GitHub 实测 |

## 新发现

- 微信业务投递明显恢复：当前 13 个 Job 均 `last_status=ok`，仅“小贝周度知识库整理”保留 2026-08-04 的历史 `last_delivery_error`；它下次运行前不能判定已恢复。
- watchdog timer 虽 active，但 oneshot service 仍为 `Result=exit-code / ExecMainStatus=1`，18:58 最近一次继续失败。systemd PATH 不含 `~/.local/bin`，而 Node 位于 `~/.local/bin/node`；runner 调 `~/.hermes/node/bin/lark-cli` 时命中 `/usr/bin/env node` 失败。
- `wiki_lint_daily` 仍引用不存在的 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py`；全 skills 目录未找到 `wiki_fix.py`。
- Dream A 提示词定义“每周”，实际 schedule 为 `0 19 * * *`（每日），导致同一周反复生成增量报告。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| replace/compress | 0 | USER 1350 字符、MEMORY 2186 字符，均在安全区 |

## Skills 健康

- `skills_list` 共 114 个技能；描述可解析，未发现 `[SKILL_PRUNED]` 或明显失效描述。
- P1：`wiki_lint_daily` 的 supporting script 引用失效；Skill 本体存在不代表自动修入口存在。
- `kb-maintenance` / `obsidian-kb-maintenance`、`wp-openhippo-ops` / `wp-openhippo-ollama-ops` 有职责交叠，但仍有独立触发场景，本轮不删不并。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无启用且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK | 13 | 任务本体均成功 |
| DELIVERY ERROR | 1 | 小贝周度整理，历史错误待下次周二运行验证 |

### P0：微信投递监督面持续失效

- watchdog timer：active，每 5 分钟触发。
- watchdog service：failed，`ExecMainStatus=1`。
- 根因：systemd PATH 未覆盖 Node 所在的 `~/.local/bin`，`lark-cli` 无法启动。
- 修法：给 unit/runner 配显式 PATH 或用 Node 绝对路径；随后手动触发并验证 `Result=success` + 飞书实发。

### P1：治理调度与修复入口

- Dream A 应从每日 `0 19 * * *` 改为周日 `0 19 * * 0`。
- `wiki_lint_daily` 应恢复 `wiki_fix.py` 或更新为现存修复入口。
- Dream A 红线禁止自行改 Cron，本轮未改 jobs.json。

## BrickHub PR 队列

| 指标 | 当前 | 判定 |
|---|---:|---|
| OPEN PR | 6 | #43–#47 Dream B；#5 单独处理 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 40 天零合并 |
| 近 7 天 commit | 0 | 主仓停滞 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 30 天未新增 |

#43–#47 当前均 `CLEAN`；#5 为 `DIRTY`。命中“OPEN ≥3 且 14 天零合并”的流程梗死条件。

## 本周 Pitfalls

- `wc -c` 是 UTF-8 bytes，不可用于中文记忆字符上限；应使用 `wc -m`。
- timer active 不等于 oneshot service 成功，必须核 `Result/ExecMainStatus`。
- Cron 执行成功与消息投递成功必须分列。
- jobs.json 的 `state` 当前是字符串，解析器不能按 dict 调 `.get()`。

## 下周关注

1. 修复 watchdog Node PATH，并完成 service + 飞书实发双验证。
2. 将 Dream A 调度收敛为周日 19:00。
3. 修复 `wiki_lint_daily` 失效入口。
4. 批量清理 BrickHub #43–#47，恢复合并链。