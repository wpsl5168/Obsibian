# Dream A 周度治理收口 · 2026-W33（08-15）

> 执行时间：2026-08-15 19:01 CST  
> 范围：Memory / User Profile / Skills / Cron / Sessions / BrickHub PR 队列

## 结论

执行态全绿，但治理面仍有 4 个 P1：7 个微信任务执行成功但上次投递失败；Dream A 名义周任务实际每日运行；`wiki_lint_daily` 自动修复路径失效；BrickHub 6 个 OPEN PR、44 天零合并。项目侧另有 Next 14→16 P0 排期项。无 failed job、无 zombie-enabled。

## 关键决策

| 日期 | 决策 / 发现 | 沉淀状态 |
|---|---|---|
| 08-07 | 微信 iLink `context_token` 是用户入站后约 24h 回复窗口，Bot 不能续期；业务 Cron 保持微信，独立 systemd timer 做 20h 提醒、24h 飞书告警 | 已进 MEMORY + `wp-hermes-gateway-delivery-triage` |
| 08-05 | Copilot Enterprise 未授权 Claude；主模型统一 `gpt-5.6-sol`，Cron 继承主模型；Responses-only 模型使用 `codex_responses` | 已进 MEMORY |
| 08-14 | BrickHub `lib/pipeline/utils.js` 用 `parseInt` 校验矩阵 token，`1.5`/`1x` 会被截断后误判合法；需严格 `Number` + integer 校验并补测试 | Dream B P1，待业务拍板 |
| 本次 | 最近 sessions 未出现新的稳定用户偏好；研究侧新增“Reviewer 应把期望结构外置为可寻址 checklist”的启发，不写入长期 memory | 无变更 |

## 新发现

- 13 个 Cron job 的 `last_status` 均为 `ok`；7 个保留微信 `last_delivery_error`：AI 圈简报、Dream A、Dream B、每日深度文章、9:15 持仓分析、8:40 盘前计划、15:05 收盘日志。错误仍为 iLink `rate limited` 误分类链。
- Dream A schedule 仍为 `0 19 * * *`，每天执行；W33 已连续生成主报告及多个增量文件，偏离周度治理定位。
- `wiki_lint_daily` prompt 仍引用不存在的 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py`；健康日不会触发，出现 warn/critical 时才会爆。
- BrickHub 总 OPEN 6，其中 Dream B #43–#47 均 `CLEAN`，#5 `DIRTY`；最近 merge 为 2026-07-02 #39，距今 44 天；7 天 commit 为 0。
- Dream B 08-14 新增矩阵 token 严格解析缺陷；8 个 pipeline utils exports 零真实测试。Next 仍为 `14.2.35`，对比 16.x 落后两大版本，维持 P0 排期。

## 记忆变更

| 类型 | 数量 | 说明 |
|---|---:|---|
| demote / 压缩 | 0 | USER 2718 bytes，低于 2750 软上限 |
| remove | 0 | 未发现可安全删除的明确过时事实 |
| add | 0 | 稳定决策已存在，避免重复写入 |

容量：USER 2718 bytes；MEMORY 3251 bytes，均在治理阈值内。保守策略下不为腾空间而删有效事实。

## Skills 健康

- registry 共 116 个 skills；描述整体可检索，未发现 `[SKILL_PRUNED]` 或明确过时 skill。
- `dreaming` 的 7 个 reference、1 个 script 均存在，无 broken ref。
- 重叠候选仍有 `kb-maintenance` / `obsidian-kb-maintenance`、`design-taste-frontend` / `frontend-design`；均有潜在调用方，本次不自动删。
- `wiki_lint_daily` 的坏路径属于 Cron prompt，不是现有 skill 文件缺失；本次未修改 skill。

## Cron 健康

| 分组 | 数量 | 结论 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无 enabled 且超过 30 天未运行 |
| DISABLED | 0 | 无 |
| OK | 13 | 调度均按期运行 |

### P1

1. **微信投递面失效**：7 个 job 上次运行成功但未送达；恢复依赖用户微信入站刷新窗口，禁止 Bot heartbeat。
2. **Dream A 调度错位**：`0 19 * * *` 应改为周日，例如 `0 19 * * 0`；按红线本次未改 Cron。
3. **wiki 自动修复断路**：旧 `wiki_fix.py` 不存在。应更新 job prompt，改为按 lint JSON targeted repair，或补受版本控制的 fixer 后再引用。
4. **BrickHub 流程梗死**：6 OPEN、44 天零 merge、7 天零 commit；需 batch review + merge/close。

## BrickHub PR 队列

| 指标 | 本周值 | 趋势 / 判定 |
|---|---:|---|
| OPEN | 6 | 与昨日持平；Dream B 5 个 `CLEAN`，#5 `DIRTY` |
| 30d MERGED | 0 | PROC-P1 |
| 7d commits | 0 | 停滞 |
| 最近 merged | 07-02 #39 | 44 天前 |
| 最近 Dream B PR | 07-12 #47 | 33 天前 |
| 最近 Dream B 报告 | 08-14 | 审查继续，新增矩阵解析 P1 |

## 下周关注

1. 为 Next 14→16 指定迁移周；同时处理 `pipeline/utils.js` 严格矩阵解析与测试缺口。
2. 拍板 Dream A 改为周日运行，并修正 `wiki_lint_daily` 的失效 fixer 路径。
3. 清理 BrickHub #43–#47；#5 为巨型正式页复刻且 `DIRTY`，不得混入低危批量合并。

## 本周 Pitfalls

- Cron 健康必须同时检查 `last_status` 与 `last_delivery_error`；本次是执行全绿、投递 7 红。
- 条件分支中的不存在脚本不会在健康日暴露，必须静态核验 prompt 引用。
- `gh pr list` 必须在目标 repo 工作目录运行；在 Obsidian 目录会查询错误仓库并得到假 0。
- 同周主文件已存在，使用 `2026-W33-0815.md` 增量版，未覆盖历史周报。
- Memory/User 均为 Markdown，本次未直接写入。

## 信源

- `~/.hermes/cron/jobs.json`，更新时间 2026-08-15 19:00:45+08:00
- `skills_list` registry：116 skills；`~/.hermes/skills/devops/dreaming/` linked files
- Sessions：@session:default/20260807_184607_411c9d96、@session:default/20260805_113439_ca629f85、@session:default/cron_004e8d32135c_20260815_080029
- `04-项目开发/dreaming/2026-08-14-research.md`
- BrickHub GitHub PR / git log 实时快照，2026-08-15 19:01 CST
