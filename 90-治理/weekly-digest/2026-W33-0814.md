# Dream A 周度治理收口 · 2026-W33（08-14）

> 执行时间：2026-08-14 19:00 CST  
> 范围：Memory / User Profile / Skills / Cron / Sessions / BrickHub PR 队列

## 结论

调度执行态全绿，但治理面有 4 个 P1：7 个微信任务“执行成功、投递失败”；Dream A 名义周任务实际每日运行；`wiki_lint_daily` 自动修复路径已失效；BrickHub 6 个 OPEN PR、43 天零合并。无 P0、无失败 job、无 zombie-enabled。

## 关键决策

| 日期 | 决策 / 发现 | 沉淀状态 |
|---|---|---|
| 08-07 | 微信 iLink `context_token` 是用户入站后约 24h 回复窗口，Bot 不能续期；业务 Cron 保持微信，独立 systemd timer 做 20h 提醒、24h 飞书告警 | 已进 MEMORY + `wp-hermes-gateway-delivery-triage` |
| 08-05 | Copilot Enterprise 未授权 Claude；主模型统一 `gpt-5.6-sol`，Cron 继承主模型；Responses-only 模型使用 `codex_responses` | 已进 MEMORY |
| 08-13 | BrickHub 未知 LLM provider 会被静默误路由为 Anthropic，应改显式白名单并 fail-fast；Next 14→16 已升为必须排期项 | Dream B 报告，待业务拍板 |
| 本次 | 最近 sessions 未出现新的稳定用户偏好；不重复写入 memory | 无变更 |

## 新发现

- 13 个 Cron job 的 `last_status` 均为 `ok`，但 7 个任务保留微信 `last_delivery_error`：AI 圈简报、Dream A、Dream B、每日深度文章、9:15 持仓分析、8:40 盘前计划、15:05 收盘日志。错误均为 iLink `rate limited` 误分类链，说明 24h 主动回复窗口仍未恢复。
- `wiki_lint_daily` prompt 硬编码 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py --apply`；全盘检索确认 `wiki_fix.py` 不存在。当前 job 之所以为 `ok`，是健康日未触发修复分支；下一次出现 critical/warn 时会进入坏路径。
- Dream A schedule 仍是 `0 19 * * *`，每天执行；W33 已连续生成主报告和 4 个增量文件，已偏离周度治理定位。
- BrickHub OPEN 6，其中 Dream B 5 个均 `CLEAN`，另有 #5 `DIRTY`；最近 merge 为 2026-07-02 #39，距今 43 天；近 30 天项目 commit 为 0。

## 记忆变更

| 类型 | 数量 | 说明 |
|---|---:|---|
| demote / 压缩 | 0 | USER 2718 bytes，低于 2750 软上限 |
| remove | 0 | 未发现可安全删除的明确过时事实 |
| add | 0 | 本周稳定决策已存在，无重复写入 |

容量：USER 2718 bytes；MEMORY 3251 bytes，均在治理阈值内。保守策略下不为腾空间而删有效事实。

## Skills 健康

- registry 共 116 个 skills；描述整体可检索，未发现 `[SKILL_PRUNED]`、broken linked file 或必须立即 patch 的描述。
- `kb-maintenance` 本体健康，已明确“fixer 缺失则 targeted manual repair”；问题在 `wiki_lint_daily` Cron 未加载该 skill，且 prompt 仍写死不存在的旧路径。
- 重叠候选仍有 `kb-maintenance` / `obsidian-kb-maintenance`、`design-taste-frontend` / `frontend-design`；均有潜在调用方，本次不自动删。
- 本次未修改任何 skill。

## Cron 健康

| 分组 | 数量 | 结论 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无 enabled 且超过 30 天未运行 |
| DISABLED | 0 | 无 |
| OK | 13 | 调度均按期运行 |

### P1

1. **微信投递面失效**：7 个 job 运行成功但未送达；恢复只能依赖用户微信入站刷新窗口，禁止再尝试 Bot heartbeat。
2. **Dream A 调度错位**：`0 19 * * *` 应改为周日，例如 `0 19 * * 0`；按红线本次未改 Cron。
3. **wiki 自动修复断路**：`wiki_fix.py` 不存在。建议更新 Cron prompt：移除旧路径，critical/warn 时直接按 lint JSON 做 targeted manual repair，或先补受版本控制的 fixer 再引用。
4. **BrickHub 流程梗死**：6 OPEN、43 天零 merge、30 天零 commit；需 batch review + merge/close。

## BrickHub PR 队列

| 指标 | 本周值 | 趋势 / 判定 |
|---|---:|---|
| OPEN | 6 | 与昨日持平；Dream B 5 个 `CLEAN`，#5 `DIRTY` |
| 30d MERGED | 0 | PROC-P1 |
| 7d commits | 0 | 停滞 |
| 最近 merged | 07-02 #39 | 43 天前 |
| 最近 Dream B PR | 07-12 #47 | 33 天前 |
| 最近 Dream B 报告 | 08-13 | 审查继续，但自动修复产物被队列阻塞 |

## 下周关注

1. 微信入站恢复后，以真实业务发送验证 7 个 job 的 `last_delivery_error=null`。
2. 拍板 Dream A 改为周日运行，并修正 `wiki_lint_daily` 的失效 fixer 路径。
3. 清理 BrickHub #43–#47；#5 属巨型正式页复刻且 `DIRTY`，不得混入低危批量合并。

## 本周 Pitfalls

- Cron 健康必须同时检查 `last_status` 和 `last_delivery_error`；本次是 13 个执行全绿、7 个投递失败。
- “当前未报错”不等于修复链健康：条件分支中的不存在脚本只有等 warn 出现才会爆，必须静态核验 prompt 引用。
- 同周主文件已存在，使用 `2026-W33-0814.md` 增量版，未覆盖历史周报。
- Memory/User 均为 Markdown，本次未直接写入。

## 信源

- `~/.hermes/cron/jobs.json`，更新时间 2026-08-14 19:00:10+08:00
- `skills_list` registry：116 skills；`skill_view(kb-maintenance)`
- Sessions：@session:default/20260807_184607_411c9d96、@session:default/20260805_113439_ca629f85、@session:default/cron_39a5573af875_20260813_200032
- BrickHub GitHub PR / git log 实时快照，2026-08-14 19:00 CST
