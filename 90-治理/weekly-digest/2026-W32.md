# 2026-W32 周报 (Dream A)

生成时间: 2026-08-03 (周一)

## 总体健康度
🟡 一处 P1（持仓行情源全线挂了，两条 cron 同日报错/空转）+ USER.md 超软上限需老王拍板收敛。其余 12/13 job 状态 OK，skills/记忆无明显腐化。

## 关键决策与新发现（近周）

| 项 | 内容 | 出处 |
|---|---|---|
| 模型切换 | 主对话切 `claude-opus-5`（裸名，`anthropic/` 前缀会 400 `model_not_supported`），实测 opus-4.8/5 均 200 | @session:default/20260708_071745_dde7bc27 |
| 输出偏好 | 结构化数据默认 markdown 表格（2-3 列、单元格短词、长内容另起段落）—— 已入 USER.md | @session:default/20260728_100111_f10752cf |
| 新产品规划 | 「知识库 + AI 辅助词汇学习」四层架构（采集/知识库/学习/洞察）；形态 Web 主 + WPF 桌面探针；技术栈 FastAPI + SQLite + sqlite-vec + ollama nomic 复用 | 04-项目开发/词汇学习产品规划.md |
| 代码抢救 | english.brickhub.cc（原仅 Caddy volume）→ 拆 4 文件入 `wpsl5168/english-trainer` 仓，线上验证正常 | 同上 session |
| 客户培训 | 医药临床数据分析 Mixed Model 大纲已成稿（合规/职责/协作/QA/退出五段） | @session:default/20260731_095230_9d3a6073 |
| 持仓扩容 | 持仓从 8 只扩到 11 只（cron 描述里"8只"字样待同步） | 今晨盘前 cron |

## 健康报告

### Cron（13 个 job）

| 组 | 数 | 备注 |
|---|---|---|
| OK | 12 | 略 |
| FAILED | **1** | 「持仓盘中异动扫描」 |
| ZOMBIE-ENABLED | 0 | — |
| DISABLED | 0 | — |

**FAILED 详情**：
- Job：`持仓盘中异动扫描`（工作日 10/11/13/14 点）
- 最近报错：2026-08-03 14:01，`OSError: [Errno 101] Network is unreachable` → 请求 `hq.sinajs.cn` 直接连不上
- 手动 `curl` 该 URL 从本机返回 200 → 说明**不是纯粹的接口挂**，可能是 cron 执行环境的出网瞬时抖动，或 UA/Referer 差异（脚本已带 `Referer: finance.sina.com.cn`）
- **交叉参考**：今晨 08-03 9:15「持仓盘前」cron 报「全部 11 只现价=0」，也是 `brief portfolio` 行情源没拉到 → 说明**行情 provider 已出现结构性异常**，不止一次性抖动
- 建议：老王排查 `~/workspace/daily-brief/daily_brief/sources/portfolio.py` 的 sina fetch 逻辑，或考虑 fallback 到东财/腾讯行情源

### Skills
系统提示注入的技能列表清爽，无 broken ref。本周无新建/删除。

### 记忆
- USER.md：**2885 字符**，超软上限 2750（**+135**），已连续超容一次 → 老王拍板收敛（候选：合并"高考+成人学历+落户咨询"三行到一行、简化"沟通极简"节）
- MEMORY.md：3254 字符，安全区

本周未执行 demote/remove（保守原则，无明确过时项）。

## 待老王拍板

1. **[P1] 行情源修复**：`持仓盘中异动扫描` + `9:15 盘前` 双双失效，行情 provider 疑似结构性问题；恢复前每日盘前简报空转。修复方向：sina/东财/腾讯任一 fallback。
2. **[P1] USER.md 超容 2885/2750**：授权我下轮周治理中合并"咨询业务"三行 + 精简"沟通极简"节到 ~2700。
3. **[P0-软] 持仓 8→11 同步**：`9:15 盘前` cron prompt 里硬编码"8只"，需改为"11只"（触及 cron 描述编辑，需老王点头）。

## 下周关注
- Dream B（BrickHub 日审）本周产出情况的横向对比（若队列健康）
- 行情源修复后回归 `持仓盘中异动扫描` 稳定性
- 词汇学习产品是否进入 P1（wordbook schema 落地）
