---
title: "Dream A 周报 2026-W23 (0606)"
date: 2026-06-06
tags: [dreaming, governance, dream-a, memory-audit]
type: dreaming
status: draft
---

# Dream A 维护报告 — 2026-06-06 (W23)

## 1. 总体健康度
- HOT memory **从 17 条全清 → 0/4400**（清完后等下次 session 注入 + OpenHippo dream consolidate 重新填充）
- HOT user **2099/2750（76%）** — 健康
- USER.md 文件 **2789 / 2750 = 101%**（轻微超容,Pitfall #17）
- MEMORY.md 文件 3416 / 4400 = 78% — 健康
- Cold 14365 条;dream_runs 最近一次 `completed` 但 consolidated=0/forgotten=0(算法识别不出对话碎片)
- Cron 17 个 active,**最近一轮全 OK**(无 last_status=error)

## 2. 记忆变更
| 操作 | 数量 | 内容摘要 |
|------|------|----------|
| archive (demote→cold) | 17 | HOT memory 全部 17 条镜像入冷;其中 14 条为 `[YYYY-MM-DD]` 开头的**对话原文碎片**(非可复用事实),3 条与 MEMORY.md 重复或彼此重复(AI Agents 文章 x2、学习科学 x2) |

**根因**:OpenHippo post-llm-call 规则层把对话首句**带时间戳**直接当 memory 写入 HOT,但 dream consolidate 算法对这类"时间戳+对话片段"识别不出可合并 cluster(看到 `clusters_count=0`)。Pitfall #34 延伸:cron 里 memory() 不可用时,正常 session 直接走 OpenHippo HTTP API 也能做 hot→cold 搬运。

**残留问题**:
- USER.md 文件 101% 超容 — 老王需在正常 session 里删/合 1-2 条。重复嫌疑(同一段相似度高):
  - id=`a017c4ae` & `fb0a7644` 两条**几乎逐字相同**:"老王在微信端阅读偏好:报告嫌字大占屏,要求紧凑纯文本版..."
  - id=`7250e524` & `859011bb` 两条**语义重复**:都是"Cron 输出极简,只要关键信息+待优化项,9:00 盘前只要持仓"

→ **建议**:正常 session 用 memory(action='remove') 删掉 id `a017c4ae` 和 `859011bb`(各砍 1 条留 1 条),USER.md 立即降到 ~95%。

## 3. Skills 健康检查
- 总 skill 数 **225**(`~/.hermes/skills/**/SKILL.md`)
- 无重名 skill
- **broken `related_skills` 引用 20 条**(指向已不存在的 skill),其中 `github-pr-workflow` 一个 skill 就引了 6 个不存在的 sibling(`github-auth`/`github-code-review`/`github-issues`/`github-repo-management`/`github-repo-vetting`/`github-copilot-coding-agent`/`fork-upstream-sync`)— 上游 hermes 升级把这些 skill 合到 `github-pr-workflow` 自身了,本地 frontmatter 没同步
- 其他散点:`llm-wiki -> agentic-research-ideas`、`research-paper-writing -> ml-paper-writing`、`codebase-inspection -> github-repo-management`

→ **建议**:`hermes skill update github-pr-workflow` 重拉一次上游版本(若该 skill 是外部导入的),或老王在正常 session 里手动清 `related_skills:` 字段。低优先,不影响 skill 加载,只是导航失效。

## 4. Cron Jobs 健康
- **17 个 active job 全绿**(最近一次 last_status=ok)
- 1 个 paused: `DeepLearning.AI每日摘要(小贝)` (id `ce283ced880e`) — 历史已知,无需处理
- 已知 delivery 抖动:Weixin "rate limited"(`AI圈每日简报` / `Dream B质检`/`每日深度文章`)— iLink 网关侧限频,不是 cron 故障,无需处理

## 5. 近期 Session 关键决策提取(过去 ~7 天,主对话)
1. **2026-06-06 08:00 深度文章评论新增"全局逻辑校核"维度** — 卡兹克"飞书 Markdown"那篇被点破"段落对、全局回避对手最尖论点(Thariq 的 in-the-loop 论)";同时收纳 Karpathy 的 verify > specify 双轴(可验证性 + 实验室训过没)作为 agent 流水线设计的第一刀
2. **2026-06-05 客户访谈调研表** — 21V→Global 摸排表(AAD/Teams/Exchange/SP/OneDrive/邮箱/用户数/数据量)入库 `30-调研报告/迁移方案/`,wiki_lint 自动修复链路验证 OK
3. **学区 cron 已收紧** — 看花校区 8 校监控,全未发布静默,科丰发布单独高亮
4. **Weixin 通道是底层 rate-limit 而非 Cron 故障** — 已多次出现,但 cron 本身全绿;长期需考虑 iLink 之外的备份投递通道

无新增技术决策需进 MEMORY.md/USER.md。

## 6. 下周关注
- [ ] (老王手动)USER.md 删 `a017c4ae` 和 `859011bb` 各 1 条,降容到 95% 以下
- [ ] (老王手动)`github-pr-workflow` skill 重拉或清 related_skills 字段,消 6 条 broken 引用
- [ ] OpenHippo dream consolidate 算法对"时间戳对话碎片"的识别 — 看是否需 plugin 侧加 source 过滤(post-llm-call 阶段就不写这种条目)。这是 OpenHippo 项目本身的优化项,非 Dream A 范畴
- [ ] iLink 微信 rate-limit 长期方案 — 短期靠重试,长期可能要双投递通道
