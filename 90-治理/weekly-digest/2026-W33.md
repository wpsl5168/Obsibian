# 2026-W33 周报（Dream A）

生成时间：2026-08-10 19:00 CST

## 总体健康度

🔴 13 个 Hermes Cron 本体均成功，但 7 个微信任务保留投递失败，独立 watchdog 每 5 分钟持续失败；BrickHub 39 天零合并。记忆容量正常，本轮无保守清理项。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 微信主动推送 | `context_token` 是用户入站后约 24h 回复窗口，Bot 无法自行续期；业务 Cron 继续走微信，20h 提醒、24h 飞书告警 | @session:default/20260807_184607_411c9d96 |
| 投递健康口径 | `last_status=ok` 只证明任务执行成功；消息可达必须另核 `last_delivery_error` 与 watchdog | 同上 + 本次 jobs.json/systemd 实测 |
| 模型链 | Copilot Enterprise 未授权 Claude；主模型与 Cron 继承 `gpt-5.6-sol`，API mode 用 `codex_responses` | @session:default/20260805_113439_ca629f85 · @session:default/20260805_121538_8f20df |
| BrickHub | Dream B 不再扩张 PR 队列；#43–#47 需 batch review/merge/close，#5 大型正式页复刻 PR 禁止代合 | @session:default/cron_39a5573af875_20260809_200009 |

## 新发现

- USER.md 当前 1350 字符，低于 2750 字符软上限；此前按 UTF-8 bytes（2861）误报超容，已纠正口径。
- 微信 watchdog timer 为 active，但 oneshot service 持续 `Result=exit-code / ExecMainStatus=1`；日志明确为 `/usr/bin/env: ‘node’: No such file or directory`。
- `wiki_lint_daily` 的修复命令仍指向 `~/.hermes/skills/note-taking/kb-maintenance/scripts/wiki_fix.py`，全局未找到 `wiki_fix.py`。健康日不触发，出现 warn/critical 时自动修链会断。
- 近期未出现需要新增的稳定用户偏好；SQL Server 2016 兼容约束已在 USER profile 中。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| replace/compress | 0 | 容量正常，无需为压缩牺牲高价值事实 |

容量：USER.md 1350 字符；MEMORY.md 2186 字符。均在安全区。

## Skills 健康

- `skills_list`：114 个技能；描述均可解析，未发现 `[SKILL_PRUNED]`。
- `kb-maintenance` / `obsidian-kb-maintenance` 与 `wp-openhippo-ops` / `wp-openhippo-ollama-ops` 职责部分重叠，但仍有独立触发场景或 Cron 引用，不删。
- P1：`wiki_lint_daily` 引用的 `kb-maintenance/scripts/wiki_fix.py` 已不存在；应更新 Cron 修复入口或恢复脚本。Dream A 禁改 Cron，本轮仅报告。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无启用且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK | 13 | 任务本体均成功 |
| DELIVERY ERROR | 7 | AI 简报、Dream A/B、小贝周整理、9:15 持仓、深度文章、8:40 持仓 |

### P0：微信投递监督失效

- watchdog timer：active，每 5 分钟触发。
- watchdog service：failed，`ExecMainStatus=1`。
- 根因：systemd runner PATH 中无 Node，`/usr/bin/env node` 失败。
- 修法：给 unit/runner 写入包含 Node 的显式 PATH，或改用 Node 绝对路径；手动触发后验证 `Result=success`，并实发一次飞书告警。

### P1：治理调度与修复入口

- `Dream A` 调度仍为 `0 19 * * *`（每日），与“每周周日晚”不符；建议改为 `0 19 * * 0`。
- `wiki_lint_daily` 自动修脚本路径失效；出现告警时只能发现、不能自动修。
- Dream A 红线禁止自行改 Cron，本轮未动 jobs.json。

## BrickHub PR 队列

| 指标 | 当前 | 判定 |
|---|---:|---|
| OPEN PR | 6 | #43–#47 为 Dream B；#5 单独处理 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 39 天零合并 |
| 近 7 天 commit | 0 | 主仓停滞 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 29 天未新增 |

命中“OPEN ≥3 且 14 天零合并”的流程梗死条件。#43–#47 均为 CLEAN，需 batch review/merge/close；#5 为 DIRTY 大型正式页复刻 PR，禁止代合。

## 本周 Pitfalls

- 字符上限要用字符数（`wc -m`），不能拿 UTF-8 bytes（`wc -c`）误判中文超容。
- timer active 不代表 oneshot service 成功，必须核 `Result/ExecMainStatus`。
- Cron 执行成功与投递成功必须分列；`last_delivery_error` 不能被 `last_status=ok` 掩盖。
- Skill 本体存在不代表 Cron 引用的 supporting script 仍存在，需验证真实路径。

## 下周关注

1. 修复 watchdog Node PATH，并完成 service + 飞书实发双验证。
2. 将 Dream A 调度改为周日 19:00。
3. 修复 `wiki_lint_daily` 的失效脚本入口。
4. 批量清理 BrickHub #43–#47，恢复合并链。
