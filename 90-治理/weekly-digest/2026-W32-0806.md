# 2026-W32 周报增量（Dream A · 08-06）

生成时间：2026-08-06 19:00 CST

## 总体健康度

🟡 Hermes 核心服务已恢复且 12/13 cron 为 OK；治理面仍有三处 P1：Dream A 名为周度却每天运行、USER.md 连续超软上限、BrickHub 6 个 PR 已 35 天零合并。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 主模型链路 | 主模型改为 `gpt-5.6-sol`，API mode 使用 `codex_responses`；architect/developer 同步使用该模型，auxiliary 为 `gpt-5.4-mini` | @session:default/20260805_113439_ca629f85 |
| Copilot Claude | 当前 Enterprise seat 的 `/models` 不再暴露 Claude，Claude 请求均 400；只能由 Enterprise owner 在 AI controls 开模型权限 | @session:default/20260805_113439_ca629f85 |
| Hermes 修复 | Hermes 已同步 upstream，SQLite 升至 3.53.1，npm 漏洞清零，微信定制保留，doctor 全通过 | @session:default/20260805_115334_b4a18e |
| 微信投递 | `ret=-2 + rate limited` 实为 stale context token；fork 补丁按带 token 的 `ret=-2` 刷新并重试，后续 cron 投递已恢复 | @session:default/20260804_110527_d0334f9d |

## 新发现

- OpenHippo 体检正常：24,694 条向量，`embed_fail_count=0`；Feishu WebSocket 已恢复连接。
- Dream B 08-05 在 `lib/ldrawParser.js` 发现坐标容错路径的 P0 与测试缺口；项目仍受 Next 14 停维、`next/image` 命中面和 R3F 8→9 breaking 约束。
- Dream A 的 job 配置与 skill 定义冲突：skill 明确为“每周/周日晚”，实际 schedule 是 `0 19 * * *`，每天执行；本周已产生多个收口文件并重复消耗。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| add/replace | 0 | 本周关键模型、微信、Gateway 事实已在现有 memory 中 |

容量：USER.md 2885 bytes（软上限 2750，连续超容）；MEMORY.md 3154 bytes（安全区）。

## Skills 健康

- `skills_list` 可见 113 个技能；底层共 255 个 `SKILL.md`。
- 未发现 `[SKILL_PRUNED]`、描述占位或明显 broken ref。
- 低优先级重叠：`wp-openhippo-ops` / `wp-openhippo-ollama-ops`、`kb-maintenance` / `obsidian-kb-maintenance` 职责有交叉；暂不自动合并，避免破坏现有 cron 引用。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 1 | Dream A 上次运行因旧 Claude 模型 `HTTP 400 model not supported` 失败；当前本轮已由主模型 `gpt-5.6-sol` 执行，属待本轮成功后自动消除的单次状态 |
| ZOMBIE-ENABLED | 0 | 无 enabled 且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK | 12 | 其余 job 最近运行正常 |

Schedule 审计：
- **P1** Dream A 实际每日 19:00，和“每周治理”定义冲突。建议改为 `0 19 * * 0`（周日 19:00）；本次遵守红线，未修改 cron。
- 持仓类 cron 本周已恢复 OK；上次新浪行情不可达未持续复发。
- `critical-skills-audit-sync` 已恢复 OK，不再是长期失败项。

## BrickHub PR 队列

| 指标 | 当前 | 趋势/判定 |
|---|---:|---|
| OPEN PR | 6 | 低于 8 条熔断线，但仍有积压 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 已 35 天零合并 |
| 近 7 天 commit | 0 | 主仓无提交 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 已 25 天未新增 PR |

结论：命中“OPEN ≥3 且 14 天零合并”的低 OPEN 梗死条件。Dream B 仍在产出审查报告，但修复合并链断开；需批量 review/merge/close 清账。

## 本周 Pitfalls

- Cron `model: null` 会继承主模型；企业策略撤掉 Claude 后，旧会话/旧运行仍可能报 400。模型权限变化后需用实际 cron run 验证，不靠历史 `/models` 结论。
- 周报同周已存在主版本时必须写日期增量版，本次使用 `2026-W32-0806.md`，未覆盖既有内容。
- `skills_list` 的可见技能数与磁盘 `SKILL.md` 数不同，不能仅凭文件数判断重复或失效。

## 下周关注

1. 将 Dream A schedule 从每日改为周日，终止重复治理与重复周报。
2. 收敛 USER.md 至 2750 bytes 内，优先合并咨询业务与沟通偏好条目，不丢事实。
3. BrickHub 批量处理 6 个 OPEN PR，并给 Next 14→16 / R3F 8→9 联动迁移明确排期或 defer 决策。
