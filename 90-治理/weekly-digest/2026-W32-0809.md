# 2026-W32 周报收口（Dream A · 08-09）

生成时间：2026-08-09 19:00 CST

## 总体健康度

🔴 13 个 Hermes Cron 本体均为 OK，但微信投递 watchdog 连续处于 failed，3 个低频/工作日任务仍保留投递错误；BrickHub 已 38 天零合并。USER.md 连续超软上限，Dream A 仍被误设为每日执行。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 微信主动推送 | `context_token` 是用户入站后约 24h 回复窗口，Bot 无法自行续期；20h 微信提醒、24h 飞书告警 | @session:default/20260807_184607_411c9d96 |
| 投递健康口径 | `last_status=ok` 不等于消息可达，必须同时核 `last_delivery_error` 与独立 watchdog | 同上 + 本次实测 |
| 模型链 | Copilot Enterprise 未授权 Claude；主模型/Cron 用 `gpt-5.6-sol`，API mode 为 `codex_responses` | @session:default/20260805_113439_ca629f85 |
| Context 治理 | 班子下一步优先定义 context 隔离、交接 lossless 字段和 memory 召回权限，不优先继续加 Agent | @session:default/cron_004e8d32135c_20260809_080053 |
| BrickHub | 自动修 PR 在合并链恢复前不扩张；#43–#47 需批量 review/merge/close | 本次 PR 快照 |

## 新发现

- watchdog timer 仍 active、每 5 分钟触发，但 oneshot service 仍为 `failed`：`Result=exit-code`、`ExecMainStatus=1`。昨日 P0 未恢复。
- Hermes cron-mode `terminal` 的 lifecycle guard 仍会因命令文本触发 `ValueError: embedded null byte`；本次通过拆分命令绕过。该问题已在 08-04 升级会话首次发现，尚未修复/上游化。
- 近期用户未新增稳定偏好；现有 memory 已覆盖模型、微信窗口、watchdog 和 OpenHippo 等关键事实。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| replace/compress | 0 | 本轮不为压字数牺牲高价值事实 |

容量：USER.md 2861 bytes（连续高于 2750 软上限）；MEMORY.md 3213 bytes（安全区）。

## Skills 健康

- `skills_list` 共 113 个技能；描述均可解析，未发现 `[SKILL_PRUNED]`。
- `wp-openhippo-ops` / `wp-openhippo-ollama-ops`、`kb-maintenance` / `obsidian-kb-maintenance` 有职责重叠，但均仍有实际用途或 Cron 引用，本轮不删。
- `kb-maintenance` 的部分 references 仍写 `.archive/kb-maintenance` 旧路径；当前主 skill 已在活跃目录。属于文档路径腐烂风险，暂未改业务流程。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无启用且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK | 13 | 任务本体均成功 |
| DELIVERY ERROR | 3 | 小贝周整理、9:15 持仓、8:40 持仓仍留旧错误；需等下次运行验证清除 |

### P0：微信投递监督失效

- Timer：active。
- Service：failed，`ExecMainStatus=1`。
- 前次已定位根因为 systemd runner 找不到 Node：`/usr/bin/env: ‘node’: No such file or directory`。
- 修法：给 unit/runner 显式 PATH 或 Node 绝对路径，手动触发后必须验证 `Result=success` 与飞书告警实发。

### P1：治理任务调度错误

- `Dream A` 仍为 `0 19 * * *`（每日），与“每周周日晚”语义不符。
- 建议改为 `0 19 * * 0`；Dream A 红线禁止自行改 cron，本轮仅上报。

## BrickHub PR 队列

| 指标 | 当前 | 判定 |
|---|---:|---|
| OPEN PR | 6 | #43–#47 为 Dream B；#5 单独处理 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 已 38 天零合并 |
| 近 7 天 commit | 0 | 主仓停滞 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 28 天未新增 |

命中“OPEN ≥3 且 14 天零合并”的流程梗死条件。#43–#47 应批量 review/merge/close；#5 是大型正式页复刻 PR，禁止代合。

## 本周 Pitfalls

- timer active 不代表 oneshot service 成功，必须检查 `Result/ExecMainStatus`。
- Cron 本体成功与投递成功必须分列；旧 `last_delivery_error` 需等任务下次运行才可判恢复。
- cron-mode lifecycle guard 会在解析复杂命令文本时误读出含 NUL 的“脚本路径”，导致 terminal 在命令执行前崩溃；拆分短命令可绕过。
- 同周已有主报告和多份增量，本次写 `2026-W32-0809.md`，不覆盖历史。

## 下周关注

1. 修复 watchdog Node PATH 并实跑告警链。
2. 将 Dream A 调度改为周日 19:00。
3. USER.md 压至 2750 bytes 内；处理 `kb-maintenance` reference 旧路径。
4. 批量清理 BrickHub #43–#47，恢复合并链。
