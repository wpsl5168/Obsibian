# 2026-W32 Dream A 收口(08-04)

## 健康度
🟡 一般 — 持仓 cron 3 个 job 今日集中报错(Network unreachable,ret code 触发首日),需盘中核实是否瞬时;微信投递链已在上周主动修好(cherry-pick 上游 PR #62386)。

## 关键决策(近周)
| 日期 | 主题 | 结论 |
|---|---|---|
| 08-04 | 微信推送 rate_limited 死循环 | fork cherry-pick 上游 open PR #62386(`fix/weixin-stale-token`,commit `7779e3131f`),`_is_stale_session_ret` 带 context_token 的 `ret=-2` 一律判 stale,tokenless 长轮询不误伤,30/30 pytest 过,gateway 已重启 |
| 07-28 | 英语学习产品路线 | 抢救 english.brickhub.cc 单文件 HTML 入库 `wpsl5168/english-trainer`(commit `522a5fd`),规划文 `04-项目开发/词汇学习产品规划.md`(commit `46a3731`) — 形态 B「Web 主 + 桌面探针」,PWA + FastAPI + sqlite-vec,复用 ollama nomic embed |
| 07-27 | 主对话切 opus-5 | `config.yaml` `model.default: claude-opus-5`,copilot 实测 200 裸名可用(记忆已更新) |

## 记忆变更
- 无 demote / 无 remove(近周新增偏好已在既有条目内)
- USER 2885 字符(⚠ 略超 2750 软上限,需下轮收紧);MEMORY 3254 字符(在 3500 内)

## Skills & Cron 健康

### Cron 状态(全 13 job)
| 分组 | 计数 | 备注 |
|---|---|---|
| FAILED (last_status=error) | 3 | 持仓盘前 8:40 / 盘中扫描 / 收盘 15:05 |
| ZOMBIE-ENABLED (>30d 未跑) | 0 | — |
| DISABLED | 0 | — |
| OK | 10 | — |

**持仓 3 挂详情**:全部 `urllib.error.URLError: [Errno 101] Network is unreachable`,`fetch_sina_quotes` 撞死。本机手动 `curl https://hq.sinajs.cn/` 返回 200 + DNS 正常 → **不是接口挂/token 失效,是 cron 执行时刻的瞬时网络不可达**(days=0,首日;last 一致命中 `sina.com.cn`)。若明日复发升 P1 深挖(cron sandbox 出口/egress ACL);首日归入观察项。

### Skills
- 系统提示已注入完整列表,扫查无 broken ref
- 上周固化的 `wp-hermes-fork-upgrade` skill 在 08-04 fork upgrade + weixin 补丁场景实操验证通过

## 下周关注
1. 持仓 cron 8-05 首个盘前(8:40)复现观察 → 挂第二天升 P1 诊断 cron 出口
2. USER.md 略超软上限,择机压缩(可合并"微信推送"两处描述)
3. weixin 补丁真实验证:8-05 首个 cron 主动推送若不再触发 `context_token restored`,即算实锤

## 待老王拍板
1. 持仓 cron 若明日复现 → 是否允许把 `fetch_sina_quotes` 加 3 次重试 + 备源(东财 push2 现返 502,腾讯 qt.gtimg.cn 待验)
2. 英语学习产品是否进入 P0 备份 → P1 schema 定义
3. USER.md 压缩谁动手(我直接精简 or 老王指定条目)

## 信源
- `~/.hermes/cron/jobs.json`(13 job 全扫)
- session_search sort=newest 3 次
- fork:`~/.hermes/hermes-agent` `fix/weixin-stale-token`
