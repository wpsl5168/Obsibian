# 2026-W32 周报增量（Dream A · 08-07）

生成时间：2026-08-07 19:00 CST

## 总体健康度

🟡 16 个 Cron 均无执行失败，但微信主动投递存在独立 P1：7 个任务的最近运行仍保留 `last_delivery_error`，`last_status=ok` 不能代表消息送达。今天已补投递监督和隔夜验收，保活效果尚未证实。USER.md 连续超软上限，BrickHub 合并链仍停滞。

## 关键决策

| 决策 | 当前结论 | 信源 |
|---|---|---|
| 微信仍为主通道 | 9 个业务 Cron 保持微信；飞书只承接故障告警，不以改投飞书代替修复微信 | @session:default/20260807_184607_411c9d96 |
| 投递健康判定 | 必须检查 `last_delivery_error`，不能只看 `last_status=ok`；新增每 5 分钟投递监督，报告新失败和恢复 | 同上 |
| iLink 保活 | `getconfig + sendtyping` 只是待验证实验，不等于 token refresh；08-08 08:20 跨失效窗口验收，失败则自动暂停 | 同上 |
| 模型链路 | Copilot Enterprise 当前不暴露 Claude；主模型 `gpt-5.6-sol` 使用 `codex_responses`，Cron 继承主模型 | @session:default/20260805_113439_ca629f85 |

## 新发现

- iLink 没有已证实的 `context_token refresh` API；新 token 依赖用户入站。即时心跳成功不能证明主动推送可跨夜存活。
- 微信投递故障此前长期藏在 `last_delivery_error`，因此任务执行成功、调度面全绿，用户仍收不到消息。治理检查需将“执行状态”和“投递状态”拆开。
- 本周 Hermes 已同步 upstream，SQLite 升至 3.53.1，npm 漏洞清零，`hermes doctor` 全通过；OpenHippo 24,694 条向量且 `embed_fail_count=0`。

## 记忆变更

| 动作 | 数量 | 说明 |
|---|---:|---|
| demote | 0 | 无明确应降级项 |
| remove | 0 | 无明确过时项，按保守原则不删 |
| add/replace | 0 | 本周稳定事实已在现有 MEMORY/USER；投递保活仍处实验期，不写入长期记忆 |

容量：USER.md 2885 bytes（软上限 2750，已连续至少 3 周超容）；MEMORY.md 3154 bytes（安全区）。

## Skills 健康

- `skills_list` 可见 113 个技能，描述均可解析，未见 `[SKILL_PRUNED]`、占位描述或明显失效引用。
- 低优先级职责重叠仍在：`wp-openhippo-ops` / `wp-openhippo-ollama-ops`，`kb-maintenance` / `obsidian-kb-maintenance`。现有 Cron 有引用，未擅自合并或删除。
- `wp-hermes-gateway-delivery-triage` 已补微信保活实验边界与投递监督思路；当前无须再改。

## Cron 健康

| 分组 | 数量 | 详情 |
|---|---:|---|
| FAILED | 0 | 无 `last_status=error` |
| ZOMBIE-ENABLED | 0 | 无启用且超过 30 天未运行项 |
| DISABLED | 0 | 无 |
| OK / 待首次运行 | 16 | 15 个已有运行记录；1 个隔夜验收待 08-08 08:20 首跑 |

### P1：执行绿不等于投递绿

7 个微信任务最近一次运行仍有 `iLink sendmessage rate limited` 投递错误：AI 圈日报、Dream A、Dream B、小贝周整理、9:15 持仓、每日文章、8:40 持仓。错误发生于 08-04 至 08-07，任务本体均显示 `ok`。

今天新增的闭环：
- `微信 Cron 投递监督`：每 5 分钟扫描 `last_delivery_error`，新失败/恢复才飞书告警。
- `微信 iLink 保活实验（待隔夜验证）`：每 15 分钟心跳，当前仅证明 API 可调用。
- `微信 iLink 隔夜保活验收`：08-08 08:20 检查 08:00 文章任务；失败自动暂停无效心跳。

### Schedule 审计

- **P1** `Dream A` 名义为每周治理，实际 `0 19 * * *` 每天运行。本周已生成多个同周增量报告并重复耗费；建议改为 `0 19 * * 0`。
- 其余日/周/工作日 schedule 与任务语义一致；隔夜验收为一次性任务，执行后应确认不再残留 enabled zombie。

## BrickHub PR 队列

| 指标 | 当前 | 判定 |
|---|---:|---|
| OPEN PR | 6 | 未到 8 条熔断线 |
| 近 30 天 MERGED | 0 | PROC-P1 |
| 最近 merged | 2026-07-02 · #39 | 已 36 天零合并 |
| 近 7 天 commit | 0 | 主仓停滞 |
| Dream B 最近开 PR | 2026-07-12 · #47 | 已 26 天未新增 |

命中“OPEN ≥3 且 14 天零合并”流程梗死条件。Dream B 仍可审查，但修复进入不了主干；需批量 review/merge/close。

## 本周 Pitfalls

- Cron 审计必须并列 `last_status` 与 `last_delivery_error`；只按 FAILED/OK 二分会漏掉“执行成功、投递失败”。
- 微信 token 相关方案必须跨过失效窗口验收；即时 API 200 不能宣称修复。
- 同周主报告已存在，继续写日期增量版，禁止覆盖。

## 下周关注

1. 读取 08-08 隔夜验收结果：有效则继续观察 7 天；无效则保持暂停并接受 iLink 的入站刷新边界。
2. 将 Dream A 改为周日 19:00，终止每日重复治理。
3. 压缩 USER.md 至 2750 bytes 内；批量处理 BrickHub 6 个 OPEN PR。
