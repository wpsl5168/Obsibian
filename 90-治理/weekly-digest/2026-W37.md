---
title: Dream A 周度维护 2026-W37
date: 2026-09-07
week: 2026-W37
tags: [治理, Dream-A, 记忆, Skills, Cron]
---

# Dream A 周度维护｜2026-W37

审计时间：2026-09-07 19:00–19:03，Asia/Shanghai。近期决策观察窗口：09-01 至本轮审计时点；历史检索是有界抽样，不声称覆盖本周全部 sessions。

## 结论

**部分完成，不能判全绿。** 已审阅注入记忆、核对 skills 可见清单、提取近期决策，并清除一条明确过时的 OpenHippo 依赖断言。Cron 工具在本次定时会话中不可用；批量静态审计的 execute_code 被 cron 安全策略明确拒绝，两项均停止，不换通道绕过。

没有本轮新验证的 P0/P1 业务故障。历史告警保留为待复核，不把昨天的状态、版本差距或服务修复报告当今天的实时巡检结果。

## 关键决策表

<table>
<tr><th>主题</th><th>决策与边界</th><th>证据</th></tr>
<tr><td>BrickHub 发布</td><td>采用独立 release 构建、暂存验收后切换；已激活 release 不再 build，源码工作区与生产运行目录分离。</td><td>09-07 session 用户消息 93992、交付消息 94362；本轮已读回主站修复 REPORT.md。</td></tr>
<tr><td>OpenHippo 数据安全</td><td>一致性备份、隔离候选、真实恢复和代码回滚；回滚不以旧 DB 覆盖新增记忆。空间门禁保留 model/space 来源，不能仅凭同维度混用向量。</td><td>09-07 session 消息 96259、96267；本轮读回 summary.json、REPORT.md 与 wp-openhippo-ops。</td></tr>
<tr><td>Skill 安全维护</td><td>当前 dreaming、hermes-agent、OpenHippo 总入口已明确：历史授权不延续；拒绝后不换工具重试；迁移不自动清库。</td><td>本轮 skill_view 实际加载内容；09-07 session 消息 94250–94251 记录过相关修订。</td></tr>
</table>

上述项目修复不是本轮 Dream A 执行；本轮没有部署、重启、修改项目代码或合并 PR。新增跨场景用户偏好证据不足，USER 保留不变。

## 新发现

- OpenHippo 09-07 修复报告明确有效 embedding 后端为 Copilot / text-embedding-3-small / 768；原全局记忆仍说“靠 ollama nomic 供 embedding”。这条确定过时的依赖断言已移除，不再据此启动或保留 ollama。本轮没有调用 embedding 或检索 API。
- 官方当前 Cron 文档描述模型解析顺序为 per-job pin → cron.model → global default，并说明未显式固定配置时的模型漂移守卫。旧记忆“未单独指定模型就继承主模型”过于简化；本机 fork 是否实现同版守卫未核，不据文档直接改本地配置或删除其余历史实测。下次配置核查需先验证版本及有效解析链。
- session_search 的 newest 检索仍返回了窗外旧会话；本轮按实际消息日期排除。部分延续会话标题日期为 08 月，但命中消息实际为 09-07，不能按标题日期漏掉本周工作。
- 本轮已将“有界浏览、实际消息时间、整读截断不等于全量、历史结果不替代实时验收”补入 dreaming 审计附件。

## 记忆变更记录

- **0 条 demote / 1 条 remove（过时事实片段）；实际 memory 工具为 1 次条目 replace。** 未删除整段有效历史。
- 删除：OpenHippo 当前必依赖 ollama/nomic 的断言。
- 同条收敛：Copilot embedding 改为“曾实测”；不兼容空间迁移保留原数据、在独立新索引全量重嵌。保留 voice-device、OpenHippo 与持仓 skill 路由。
- MEMORY：注入时 2,032 / 2,200 chars；memory 工具写后确认 **1,997 / 2,200 chars，13 个条目**。
- USER：注入时 **1,367 / 1,375 chars**，本轮未写；容量接近上限，但没有明确过时证据，不为腾空间强删。
- 其余模型权限、服务拓扑、客户与家庭信息只有历史线索，不做臆测清理。项目完成日志没有再塞入全局记忆。

## Skills 健康

- skills_list：**139 个运行时可见技能**；逐项审阅描述。训练类明确参考用途，设计类区分营销页与应用界面，飞书按业务域分流，未确认新的过时技能应删除。
- 文件搜索返回 **139 个 SKILL.md 路径**，但不等于与运行时清单逐项一致：物理清单含 sdlc-review，其 frontmatter 明确 environments: kanban；当前 cron 可见清单不含它，属于有解释的门控，不能误报废弃。hermes-agent 能实际加载，但该次文件搜索未列出，枚举机制差异未进一步定因。
- 相近能力保留：wp-openhippo-ollama-ops 为旧名兼容路由；obsidian-kb-maintenance 为其他库/旧名入口；主入口边界清楚，不按名字相近删除。
- **覆盖限制**：本轮未完成全技能 frontmatter、重复名、附件路径和依赖执行验证；不能写“139 个全部健康”。尝试批量静态处理时 execute_code 被拒，未生成该批次 skills.json，也未改用 terminal/Python 复现同一操作。
- 本轮唯一 skill 改动：dreaming/references/dream-a-audit-cheatsheet.md 增加 Session 浏览与证据日期小节；skill_manage 已确认写入。未删除或重写其他 skill。

## Cron 健康：UNKNOWN，不报虚构数量

### 本轮阻塞

- 工具发现未提供 cronjob。官方文档明确 cron-run sessions 禁用 cron management；本轮没有执行 cronjob(action="list")，也没有绕读 jobs.json、调用 CLI、导入调度器或运行任务。
- execute_code 返回：`BLOCKED: execute_code runs arbitrary local Python ... Cron jobs run without a user present to approve it.` 已停止该批量静态审计动作；未修改 approvals.cron_mode 或任何安全配置。
- 未取得当前完整 job 集合，因此 DISABLED / FAILED / NEVER_RUN / OVERDUE / OK / UNKNOWN 的各组数量均**未统计**，不是 0；没有使用 next_run_at、累计运行数或任意天数阈值假判健康。
- 执行维度：UNKNOWN。投递维度：UNKNOWN，未取得逐 job delivery ledger/精确消息回执。session 存在、正文存在或历史执行完成均不能证明已送达。

### 历史问题与建议

- 上周报告记录 Dream A 实际为每日 19:00；本轮文件枚举看到 W36 主报告与 09-01～09-06 共七份，最近浏览亦有 09-06 的 Dream A session。频率疑点仍值得处理，但未把旧表达式当本次现配。
- 上周报告的 shutdown interruption、微信投递失败、持仓 prompt 数量硬编码均待重新核实；本轮不沿用旧失败计数、不由 shutdown 标签推断真实 SIGTERM。
- 上周以“落后两个大版本”定 Next.js P0、用 ZOMBIE/固定天数描述逾期，不符合当前 Dream A 契约；保留原文作为历史档案，不复用其分级。版本过期与可利用漏洞需分别取证，本次未做依赖或 PR 队列审计。
- 修法建议：在有权限的交互会话中，以只读 cronjob list 获取完整快照，按实际 schedule/timezone/宽限期逐项分类，再独立核验投递。需要无人值守长期审计时另行明确授权安全的只读快照入口，不放开通用 cron 管理或 arbitrary Python 权限。

## 已完成项目的遗留风险（非本轮实时探针）

- OpenHippo 18:58 修复报告：本机测试及召回验收通过，但公网 Cloudflare 403 未通过；没有证据说明它是错误规则还是有意访问控制。只保留待核，不擅自放通 WAF。PR #1 的 OPEN/无云端 CI 为该报告当时记录，本轮未重新查询 GitHub。
- BrickHub 修复报告明确未验完整 3D 画质、未做锁文件冷安装复现；独立 release 目前是流程隔离，不是不同 Unix 用户权限隔离。不能把其 HTTP/静态资源通过扩写成全业务验收。

## 下周关注／待拍板

1. 在允许的交互会话补全 Cron 只读审计；核实后再决定是否把 Dream A 改成每周日 19:00，本轮不改调度。
2. 如需处理 OpenHippo 公网 403，先明确访问对象与认证策略，再授权单独诊断；不把直接开放公网当修复。
3. 记忆容量继续保守治理：新流程进对应 skill，临时配置状态留报告；不新增全局项目进度条目。

## Git 与变更范围

- 同周 YYYY-WXX 文件搜索为空，故新建本文件，不覆盖历史周报；目录已 mkdir -p。
- 开始时 Obsidian main 与 origin/main 在 fetch 后没有提交分歧；暂存区为空。
- 原有未跟踪文件 `20-项目/海马体/2026-09-07-代码与运行审查.md` 保留，不夹带提交；只 stage 本周报。pull/rebase 不使用 autostash 或强制覆盖。
- 本文件提交 SHA 与远端读回结果见最终简报及 Git 历史；不在提交正文内伪造自引用 SHA。

## 信源与覆盖边界

- 本轮系统注入 MEMORY/USER；memory 工具写后回执。
- 本轮 skills_list、search_files 与已加载 skill 正文；sdlc-review frontmatter。
- session_search 无 query 最近 10 个会话样本，及带 query 的发现／anchor 浏览。主要历史会话：20260907_103311_4b10f843、20260907_160805_1b183d。超出观察窗口的搜索结果不纳入本周决策。
- `/home/wpsl5168/work/system-remediation/2026-09-07/brickhub/REPORT.md`。
- `/home/wpsl5168/work/openhippo-fix/2026-09-07/summary.json`、`REPORT.md`。
- `90-治理/weekly-digest/2026-W36-0906.md`：仅历史对照，不作当前状态证明。
- [Hermes 官方 Cron 文档](https://hermes-agent.nousresearch.com/docs/user-guide/features/cron/)，本轮已获取；本地 fork 与上游新行为可能有差异。
