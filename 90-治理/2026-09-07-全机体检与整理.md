---
title: 全机体检与工作区整理
created: 2026-09-07
updated: 2026-09-07
type: research
tags: [hermes, architecture, best-practices]
status: stable
---

# 2026-09-07 全机体检与整理

## 结论

**服务器资源健康，但业务健康不是全绿。优先恢复BrickHub主站和海马体异地备份，再治理cron，最后做磁盘回收。** 本次完成只读审计、安全目录索引、5个skill格式修复及海马体本地热备；没有删除旧文件、停站、改DNS、修改cron/模型或升级Hermes。

采样约14:59—15:17 CST。范围为myAI2和当前Cloudflare凭据可见brickhub.cc，不包括新加坡代理机、外部旧源站和其他账户。一个Hermes审计子任务超时，主Agent从已保存证据完成复核；不能将子任务超时说成全部检查完成无异常。

## 1. 服务器 / 容器 / 内存 / 磁盘

- Azure myAI2：Standard_D2s_v3，centralus，VM running，运行约18天。
- 内存7.8GiB，采样可用约4.7—4.9GiB；无swap，当前无资源紧急告警。
- 根盘61GiB，初始71%，补本地备份后72%；仍余约18GiB。inode使用10%。不是清盘危机。
- 5个容器running，未见OOMKilled或重启计数；系统与用户failed services均0。
- 容器未设显式内存/CPU预算、healthcheck和日志限额；建议排期治理，不立即重建。
- journal约628MiB，容器日志规模不构成当前磁盘问题。本次不清事故证据。
- Docker显示镜像100%可回收，但逐个image ID验证全部被容器引用，**不计入可释放空间**。
- `.hermes/.git`约2.6GiB，`~/OpenClaw`约2.15GiB（主要Git历史）、`~/ollama`约4.8GiB是大项，但不能当无用缓存删。
- 系统级ollama正常，User=ollama，ExecStart=/usr/local/bin/ollama；停用的是旧用户级unit，二者不混淆。

## 2. 网站、域名和TLS

Cloudflare现有授权范围：1个zone、15条DNS、15个主机名；11个本机路由、4个仅DNS记录。API记录总数与ID二次核对一致；所有公网证书及本机有路由源站证书当前有效。

### 必须修复，不是废站

- **brickhub.cc：HTTP500。** 公网、源站、127.0.0.1:3005三层一致。运行目录缺`.next/BUILD_ID`、`.next/server/pages/index.html`及`404.html`，日志ENOENT/MissingStaticPage。
- 生产checkout当前分支`dreamB/auto-fix-20260827`，且有未跟踪文件；显示生产与自动修复环境未隔离。尚未建立损坏因果链，不能武断归因于某次cron。
- 恢复方案：保全改动，隔离worktree构建、验证产物后切换发布，最后HTTP/静态资源验证。不要在当前生产目录清.next或强制切main。

### 需要决定恢复或退役

- **pets-app.brickhub.cc：502**，3015无监听；PM2持久快照为stopped。Android配置仍引用，不宜直接删。
- **safety.brickhub.cc：502**，3010无监听，service disabled。近7天日志仍出现请求；扫描器流量不等于真实用户。

### DNS退役候选

- **mm2.brickhub.cc：525**，指本机，无Caddy路由、无8065，历史文档明确为Mattermost遗留；最强候选。
- **news / openclaw / www.brickhub.cc：522**，均指外部旧源站20.109.160.206，不是myAI2。应确认是否恢复/迁移/退役，不能把域名和本机文件一起盲删。
- 本次未修改任何DNS记录或Caddy配置；宿主、容器挂载和运行配置一致。

### 其余站点与注意

- english、gaokao、rss、wewe根路径200；webui、hippo正常重定向；kb未登录401为预期。
- pets是Hermes API，根路径404不等于接口宕机。
- english线上volume内4个文件与宿主源码hash一致，但仅本地一致不能证明异机备份。
- **kb/WebDAV日志最后成功同步约41天前（7月28日）**。需要确认手机Remotely Save是否还在使用；Git推送成功不等于手机同步成功。
- webui源站证书剩约30天，关注自动续期即可，不需要立刻手工换证。

## 3. 备份与数据安全（额外发现，优先级高）

### 海马体异地备份实际停了113天

- 服务health和真实search均成功；hot36 / cold26180 / vec26154 / embed_fail_count0。
- OS cron每日03:30备份日志：5月18日至9月7日共113次启动、113次schema错误。
- GitHub私有仓`wpsl5168/openhippo-backups`最新release仍为5月17日。
- 原脚本使用PATH的Python；系统SQLite3.45.1不支持生产触发器中的动态RAISE表达式，兼容的服务venv为3.50.4。不是凭报错认定数据库损坏。
- **已完成本地止损**：在线一致性热备到`~/backups/health-audit-20260907/openhippo-150358.db`，权限0600；副本integrity_check=ok，hot/cold数量与服务一致。校验见同目录verification.json。
- **异地备份任务仍未修复。** 后续固定解释器、加失败/新鲜度监控、上传并下载恢复验证；本地副本不替代异地副本。

### Hermes状态备份可用，但范围偏宽

- `~/.hermes`关联私有仓`wpsl5168/hermes`；父Agent远端回读最新SHA为`f9addfdf88a1e884945e0448712b0665c109b028`，对应本地9月7日04:40备份。
- tracked包含lsp依赖、cache、sessions、profiles及auth/config/memories；应区分运行依赖、机密状态和必要备份，设计加密/保留策略。私有仓不等于可随意提交凭据，也不是已确认公开泄露。
- 凭证文件权限600；主库644但home750限制其他用户遍历。权限收紧需核服务调用，不盲改。

## 4. Hermes / cron

- Gateway、WebUI正常；主模型保持gpt-6-astra@copilot，auxiliary与其他profiles未改。
- Doctor提示：config schema v38→v40；state.db约1.4GB；agent-browser/web workspace依赖有安全提示。未执行doctor --fix、npm自动修复或在线压缩DB。
- **14项cron，13启用、1完成停用**；启用的9个Agent任务继承GPT6，4个no_agent。
- 最新5项error：AI简报、Dream A、Dream B、9:15持仓分析、每日深度文章，均标shutdown_interrupted。
- 8:40盘前计划最新delivery_failed；今日15:05收盘日志已真实执行ok。没有发现采集时启用却长期未运行的僵尸任务。
- 7天execution ledger有82 completed、10 failed；错误标签不能单独证明都是gateway重启所致，需关联取消异常和投递回执。
- wiki_lint_daily依赖隐藏归档脚本；异常分支可能返回成功退出码，prompt还引用缺失wiki_fix.py。**归档skills不能整删，job显示ok也可能有假绿。**
- Dream A文档写每周，实际每日19:00；应明确频率。确定性备份/lint可改成no_agent，但必须先完成退出码/幂等/恢复验证。
- 近7天insights的94sessions价格数据均Unknown；总token口径未分解缓存，不能拿它计算实际费用。

## 5. Skills优化

- 初始非隐藏139个、隐藏/归档143个；静态全量清点，重点语义复核，不宣称逐篇精读。
- 发现6个YAML描述错误，**已修5个**：axolotl、grpo-rl-training、peft、trl-fine-tuning、unsloth。解析通过且正文hash未改。
- 剩余pytorch-fsdp约160KB，超skill_manage单文件上限；格式问题仍在，需先拆references，未绕过限制或截掉正文。
- 初始9个SKILL入口超过30KB，优先拆hermes-agent、design-taste-frontend、dreaming等，减少无关上下文，而不是盲删训练/外部skills。
- 已修wp-system-healthcheck和wp-hermes-fork-upgrade中的危险旧指导：不把删日志说成零风险，不用脚本绕过审批；同步纠正对应记忆。
- 新增wp-readonly-site-audit保存全量DNS/路由/TLS/引用核验流程。最终非隐藏140个、隐藏143个，同名0，YAML剩1个错误。
- 其他确认的失效引用、旧文档/危险示例见Hermes分报告；仍有待处理项，未把静态模式命中数当作真实缺陷数。

## 6. 工作区整理

- 审计home第一层275项，散落169个可见文件、27个脚本；workspace当时12项目目录＋2交付包。
- 21个Git工作目录，2个有未提交内容：OpenHippo、BrickHub。未提交代码原样保留。
- daily-brief、hermespet、旧Obsidian备份存在本地remote refs未覆盖的提交；没fetch，不直接判定远端丢失。
- BrickHub README仍写PM2、ecosystem配置指向不存在旧路径；应先修部署文档与隔离工作区，不是强行搬目录。
- **已落地**：`~/workspace/README.md`＋`PROJECTS.json`，12项目与7个固定路径例外已登记；所有登记目录核存在。
- 新任务落`~/work/<task>/<date>/`，交付落`~/exports/<topic>/<date>/`，生产路径保持不动。
- 本次新审计资料已从home根移入`~/work/system-audit/2026-09-07/`，有relocation.json；未移动用户既有项目。
- 3个重复归档候选约27.05MiB：hn_2026-08-28_latest.json、v2rayng_aa.dat、v2rayng_ab.dat。hash已核、保留副本存在，但依赖扫描仍有边界；**待确认后归档，不删除，同盘归档不释放空间**。
- 旧OpenHippo副本、worktree、Ollama目录、OpenClaw Git历史、旧gaokao-ah与交付包均保留，不能靠“名字旧”判定废弃。

## 7. 待确认的执行批次

1. **推荐先做可靠性修复**：BrickHub隔离构建与恢复、海马体异地备份修复、cron取消/投递问题定位。涉及生产发布、远端写入和定时配置，先明确范围后执行。
2. **站点去留**：pets-app与safety恢复还是退役；mm2删除DNS是否确认；news/openclaw/www是否仍需要外部源站。
3. **可逆整理**：批准3个重复文件归档；旧目录与大库另行确认保留期，不做通配清理。
4. **安全补查**：后台3005/8200/8642/8765/8787全网卡监听，UFW inactive；NSG/SSH补查被审批机制拦下，未执行、不绕过。不能据此断言公网暴露，也不能直接改loopback切断Caddy回源。

## 证据入口

本机完整证据：`~/work/system-audit/2026-09-07/`。
- `host-summary.md`、`host.json`、`backup-freshness.json`。
- `sites/REPORT.md`、`site-ledger.json`与HTTP/TLS/DNS证据。
- `workspace/REPORT.md`、git/路径/重复文件台账。
- `hermes/REPORT.md`、doctor、cron、skills及修复前后hash。

工作区子报告的“Ollama inactive”指旧用户级unit；系统级已由父Agent复核正常，以本总报告的区分为准。子报告保留原始采集路径，资料迁移映射见relocation.json。

未做：第二台VM远程体检、所有业务登录/浏览器E2E、外部旧源站文件审计、完整应用恢复演练、费用账单核对。没有声称这些已完成。
