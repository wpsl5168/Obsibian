---
title: "BrickHub Dream B 2026-05-12"
created: 2026-05-12
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-05-12
---
# BrickHub 每日 Dream B

## 今日审查：lib/ldrawParser.js (618 LOC)

模块职责：LDraw 颜色表 + 零件别名表 + `parseLDraw()` 字符串→bricks + `validateGeometry()` 碰撞/悬空检测。
近 7 天无 git 改动；`__tests__/parseLDraw.test.js` 31/31 通过；`eslint` clean。

### ✅ 已自动修复
无。文件已 lint clean、覆盖测试，可改的 P2/P3 仅剩"风格喜好"级（如 `tok = line.split(/\s+/)` 拆名、行 523 局部变量 `ii` 命名），相对回归风险（31 个测试快照、矩阵展开符号约定）收益太小，按 skill pitfall #6/#19 的保守原则**不动手**。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | 长度<15 的 type-1 行被静默"半解析"：仍会 push brick（用 `tok[2..4]` 拼 position 但**不乘 LDU**也无 quat），与正常分支坐标系不一致，几何校验/渲染会出错位 | L410-429 | error 已记录但又 push 了一个坐标错误的 brick | 选其一：(a) 仅记录 error 不 push；(b) 若坚持 push，position 也要 `* LDU` 并补 `quat: [0,0,0,1]` | P1 |
| 2 | 直接颜色 `0xRRGGBB` 解析丢 alpha 通道：LDraw 规范支持 `0x2RRGGBB` 透明色（高位编码 alpha），当前 `raw.slice(2)` 直接当 hex 用 | L149-156 | 规范覆盖不全 | 检测高位 `2` 时拆出 alpha=0.5；或显式注释"暂不支持 0x2 透明前缀" | P1 |
| 3 | `validateGeometry` 复杂度 O(N²)，>1k 砖时主线程会卡。`parseLDraw` 已有"避免 65K spread"的优化注释说明确实存在大模型场景 | L562-583 | 双层 for 全量遍历 | 对碰撞检测加空间分桶（按 yMax/20 LDU 分层 + XZ grid），或在 N>500 时 worker 化 | P1 |
| 4 | 未知 `.dat` 零件**默默替换为 1x1 box**（L393），用户得不到任何警告，3D 视图会出现"幽灵 1x1" | L386-395 | 全库兼容兜底过于激进 | 至少 `console.warn` 一次，或在返回 errors 里追加一条 `unsupported-fallback` 信息让上层 UI 显示 | P1 |
| 5 | LDraw 颜色 21（Glow in Dark）和 294（Glow In Dark）拼写差异（"in" vs "In"）：`isGlow` 检测靠 `.includes('glow')` 大小写敏感，而 `name.toLowerCase()` 已统一过 → 实际无 bug，但代码注释/常量易误导后续维护 | L106 | 命名易混淆 | 加注释说明两个 code 的差异（trans alpha=0.8） | P3 (信息) |
| 6 | `mkSlope` 未旋转/未生成斜面几何，仅当 box 处理 → 斜面砖与方块视觉同形 | L188-190 | 简化模型 | 若已知刻意为之可忽略；否则在 `BrickRenderer` 侧针对 `shape:'slope'` 做切角 | P2 (待业务确认) |

### ⚠️ 自动修复失败回滚
无（未尝试自动修复）。

## 依赖动态

| 依赖 | 当前 | 最新 | 重要变更 |
|------|------|------|---------|
| three | ^0.183.2 | r184 | 多为 examples/gi 系更新，常规使用无需迁移 |
| @react-three/fiber | ^8.18.0 | 9.3.0 | **major 跳版**：v9 在 React StrictMode 下行为变化、`useLoader` 接受新参数；升级前需通读 v9 migration guide |
| next | ^14.2.0 | 16.1 | **跨两个大版本**：Cache Components、MCP 集成、Node.js 20.9+ 要求；累积 breaking change 较多 |
| @react-three/drei | ^9.122.0 | 9.x | 与 fiber v9 配套版本待评估 |

## 竞品动态
- **Bricklink Studio 2.0** 5 月仍在迭代，社区在讨论 May 后的稳定性/缓存问题（论坛帖 2026-05-10），可作为竞品稳定性观察点。
- 无新开源 LDraw 渲染项目浮现。

## 建议行动（供老王确认）
- [ ] 评估上方 P1 #1 (半解析 brick)、#2 (0x2 alpha)、#4 (未知零件兜底)：都属"小改动+提升正确性/可观测性"，可下次正式 session 一并修。
- [ ] R3F v9 + Next 16 升级是大动作，建议单独立项做 spike，不混入 Dream B 自动修。

## 信息来源
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- https://www.wisp.blog/blog/nextjs-16-1-upgrade-guide
- https://github.com/mrdoob/three.js/milestone/97
- https://www.bricklink.com/messageList.asp?ID=1
