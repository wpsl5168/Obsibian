---
title: "BrickHub Dream B 2026-04-27"
created: 2026-04-27
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-04-27
---

# BrickHub 每日 Dream B — 2026-04-27

## 今日审查：lib/dslCompiler.js（722 行 / 27.8 KB）

DSL → LDraw 编译器，把 LLM 输出的结构化 JSON 转为 LDraw 文本。包含组件展开器（RECT_FILL / LINE / STACK / WALL_FRAME / WHEEL_SET / TREE_BASIC / WING_PAIR）、双层巴士专用宏 buildDoubleDeckerBusBricks、相对坐标解析、旋转矩阵字典。

### ✅ 已自动修复（PR [#3](https://github.com/wpsl5168/brickhub/pull/3) — dreamB/auto-fix-20260427）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 尾部空白字符 | L666 | 去除行尾空格 | P3 |

守卫：`npx eslint lib/dslCompiler.js` ✅；该文件无对应 jest 测试，已记录为后续待补。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | `matStr` 为 undefined 时仍写入 LDraw 行（fallback 到 'I'），但未在每行回退；当 ROT_PRESETS 命中失败时 warning 推入了，最后 `matStr \|\| ROT_PRESETS['I']` 兜底，逻辑虽对但易读性差，且大量未知 rot 会写出全 identity 矩阵导致零件重合而无 hard error | L706–L713 | 静默回退 + 仅 warning | 当未知 rot 出现 ≥1 次时建议 throw 或在 brickCount 上做硬阈值 | P1 |
| 2 | `collectBusSpec` 默认硬编码 6×12 双层巴士、frontPair=1/rearPairs=2/floors=2，任何非默认配置都被 warning 后忽略；这与 PROJECT_VISION 中"AI 生成多样化模型"的目标冲突 | L360–L416 / L441–L449 | 临时把"巴士宏"特化进通用编译器 | 把 BUS_COMPONENTS 整套从 dslCompiler 拆出，作为独立模板/示例素材；编译器只保留泛型展开器 | P1 |
| 3 | `expandAssemblyToBricks` 对同一 entry 既走 bus macro 又走 expandGenericComponent，而 BUS_COMPONENTS 在 expandGenericComponent 里又被显式 return []，逻辑分散，新增 component 时极易踩坑 | L574–L594 / L343–L358 | 双分支调度耦合 | 抽 dispatcher 单点：先 normalize → 单一 switch 路由到 (bus / generic / unknown) | P1 |
| 4 | 相对坐标 `b.relative_to` 无环检测；若 LLM 生成自引用或循环引用链，brickStateMap 解析的是"上一帧"的状态，不会死循环但会输出错误位置且 0 warning | L668–L685 | 缺校验 | 解析前做 in-degree 检查或显式拒绝自引用 + 警告未解析的引用 | P1 |
| 5 | 多处 `Number(b.color ?? b.colour ?? 15)` / `readNum(...)` 静默回退到默认色 15（白色）；LDraw 色码错误对儿童视觉冲击极大，建议命中 fallback 时强 warning | L692 / L180 / L217 等 | 静默默认 | 未传 color 时 warning，区分"显式 0"和"缺失" | P2（业务影响升 P1） |

> 自动修复仅放最低风险 P3；上述 P1 全部涉及编译语义/架构，需老王确认。

### ⚠️ 自动修复失败回滚

无。

## 依赖动态

| 依赖 | 当前版本 | 最新版本 | 重要变更 |
|------|---------|---------|---------|
| three | ^0.183.2 | r178 (~0.178)+ 已发布；2025-2026 主线在 WebGPU 全浏览器可用 | WebGPU 后端在所有主流浏览器可用；R3F v9 依赖 three 路径无 breaking，但 WebGPURenderer API 持续演进 |
| @react-three/fiber | ^8.18.0 | v9.5.0 | **major** — v9 适配 React 19/19.2 含 Activity；升级需同步升级 React 版本，BrickHub 当前在 React 18 链路，**不建议立即升级** |
| @react-three/drei | ^9.122.0 | 与 R3F v8 兼容线仍维护 | 跟随 R3F v9 时需对应升级；当前可保持 |
| next | ^14.2.0 | 15.x | Node ≥18.17、部分实验 API 变化、配合 React 19；和 R3F v9 升级是同一个决策包 |

**Breaking 风险**：R3F v9 + Next 15 + React 19 是一个绑定升级，单独跳任一版本都会引发依赖冲突。建议作为独立 spike，**不在 Dream B 自动修内进行**。

## 竞品动态

- **BrickLink Studio**：2026-03-25 发布最新版（修复 1×2 砖 replace 无结果、Viewport cube 等）；2026-03-03 释放 Series 11 新调色板。Studio 仍是 LDraw 生态官方主力 GUI。
- **Mecabricks**：2026 年新增 LEGO 2026 元素（含 Bugatti Vision 同款），保持月度增量。Web 端 3D 体验仍是 BrickHub 直接竞品。
- **Three.js 生态**：社区在 Reddit r/threejs 讨论 2026 走向（WebGPU、AI/vibe coding），与 BrickHub "AI + 3D" 定位高度重合，可关注 Utsubo 等长文。

## 建议行动（供老王确认）

- [ ] 评审 PR [#3](https://github.com/wpsl5168/brickhub/pull/3)（仅 1 行空白清理，安全）
- [ ] 决策 P1#2/#3：把 bus macro 从 dslCompiler 拆出，让编译器回归通用职责
- [ ] 决策 P1#1/#5：把"未知 rot/缺色码"的静默 fallback 升级为硬警告或抛错
- [ ] 安排独立 spike：R3F v8 → v9 + Next 14 → 15 + React 18 → 19 联动升级评估
- [ ] 给 dslCompiler.js 补 jest 用例（rot fallback、relative_to 引用、bus spec 边界）

## 信息来源

- [Three.js r178 release - 80.lv](https://80.lv/articles/three-js-178-is-now-available)
- [What's New in Three.js (2026) - utsubo.com](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [R3F v9.5.0 release notes - SourceForge mirror](https://sourceforge.net/projects/react-three-fiber.mirror/files/v9.5.0/)
- [What's New in Next.js 15 - apidog](https://apidog.com/blog/next-js-15-what-is-new/)
- [BrickLink Studio download (2026-03-25)](https://www.bricklink.com/v3/studio/download.page)
- [Mecabricks on X — 2026 elements](https://x.com/Mecabricks)
