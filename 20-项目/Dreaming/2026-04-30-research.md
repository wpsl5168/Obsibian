---
title: "BrickHub Dream B 2026-04-30"
created: 2026-04-30
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-04-30
---

# BrickHub 每日 Dream B — 2026-04-30

## 今日审查：pages/model/[id].js（495 行）

详情/截屏模式两用的模型详情页：动态加载 LDraw 源码 → 解析零件清单 / 逐步指南 → 切换"清单/放映/沙盒"三种交互。

### ✅ 已自动修复 — PR [#8](https://github.com/wpsl5168/brickhub/pull/8)

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | "返回主页"按钮无 aria-label，仅 `&larr;` 字符 + md+ 隐藏文字 | line 264 | 加 `aria-label="返回主页"` | P2 a11y |
| 2 | 关闭面板的 ✕ 按钮无任何 accessible name | line 316 | 加 `aria-label="关闭面板"` | P2 a11y |
| 3 | 步骤导航 ←/→ 按钮无 aria-label，仅 unicode 箭头 | line 367, 378 | 加 `aria-label="上一步"`/`"下一步"` | P2 a11y |
| 4 | 清单/放映/沙盒三个 toggle 按钮缺 `aria-pressed` | line 391, 403, 414 | 加 `aria-pressed={state}` | P2 a11y |

**守卫**：`npx eslint pages/model/[id].js` 通过；`npx jest __tests__/modelDetail.test.js` 2/2 通过（与 baseline 一致）。

> 修复迭代说明：初版给"清单"按钮加 `aria-label="显示积木清单"` 时，`getByRole('button', { name: /清单/i })` 测试匹配到多个按钮（顶部 toggle + 面板标题 ✕），按守卫规则回滚该 label 改用纯 `aria-pressed`，可见文字"清单"已能作为 accessible name。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | `setTimeout(700ms)` 无 cleanup | line 55-58 `handle3dRendered` | 组件在 700ms 内卸载（如快速切换模型）会触发 `setIs3dReady` on unmounted → React 警告 + 潜在内存泄漏 | 改为 `useEffect` 内 `setTimeout` + cleanup，或在外层加 mounted ref 守卫 | P1 |
| 2 | 步骤导航按钮 36×36 px，关闭 ✕ 按钮 24×24 px | line 316, 368, 378 | 违反触控最低 44px 标准（DESIGN.md），儿童 56px 更失守 | 步骤箭头 → `w-11 h-11`；关闭 ✕ → 至少 `w-11 h-11` 或加 padding hit-area | P1 (UX) |
| 3 | `handleImageFallback` 未 `useCallback` 包裹 | line 76-87 | 作为 prop 传给 `<PartsTray>`（line 466），每次 render 都新建引用，触发子树无谓重渲染 | 用 `useCallback(..., [])` 包裹 | P2 性能（涉及 PartsTray 接口语义，未自动改） |
| 4 | `handlePlacePart` / `handleHintRequest` 同样未 `useCallback` | line 60-70 | 同上，作为 PartsTray prop 触发重渲 | 同上 useCallback | P2 性能 |

### ⚠️ 自动修复失败回滚

| # | 问题 | 失败原因 |
|---|------|---------|
| 1 | "清单"按钮 `aria-label="显示积木清单"` | 与 modelDetail.test.js `getByRole({ name: /清单/i })` 匹配多个元素，违反测试守卫，回滚后改用纯 `aria-pressed` |

## 依赖动态

| 依赖 | 当前 BrickHub | 最新 | 重要变更 |
|------|--------------|------|---------|
| Three.js | ~r170 (待核) | r183+ | **RenderPipeline**（节点化后处理）替换旧 EffectComposer 心智模型；WebGPU 渐成默认（仍兼容 WebGL） |
| @react-three/fiber | ~v8 (待核) | v9 stable + alpha | 持续 React 19 适配；alpha 通道有 v10 进展 |
| Next.js | 14 | 15 stable / 16.2 | App Router + Turbopack 默认稳定；async Request APIs；React 19；近期 16.2 安全补丁 CVE-2026-27979（maxPostponedStateSize 强制） |
| LDraw | — | **2026-04 Parts Update** | 新增官方零件 + Bricklink Render-Only 颜色（LDConfig 更新） |

**重点提示**：Next.js 16.2 的 `CVE-2026-27979` 涉及 maxPostponedStateSize 强制，建议确认 BrickHub 升级路径；LDraw 2026-04 更新有新零件颜色，可考虑同步官方素材库。

## 竞品动态

- **Bricklink Studio**：跟进 LDraw 2026-04 颜色更新，Render-Only 颜色机制走 LDConfig 注入。Studio 2.0 第三方零件（如 6801/44556）需手动放 Unofficial 目录。
- 暂无 Mecabricks / GitHub LDraw 突出热门项目（本次 search 配额已节约用于核心 4 项）。

## 信息来源

- [Three.js Post-Processing 2026 (r183 RenderPipeline)](https://threejsroadmap.com/blog/the-complete-guide-to-threejs-post-processing-in-2026)
- [WebGPU Three.js Migration Guide 2026](https://www.utsubo.com/blog/webgpu-threejs-migration-guide)
- [pmndrs/react-three-fiber Releases](https://github.com/pmndrs/react-three-fiber/releases)
- [Next.js Release Notes April 2026 (含 CVE)](https://releasebot.io/updates/vercel/next-js)
- [LDraw 2026-04 Parts Update](https://forums.ldraw.org/thread-29497.html)

## 建议行动（供老王确认）

- [ ] PR #8 review + merge
- [ ] 处理 P1：`handle3dRendered` setTimeout cleanup（高频切换模型场景下会复现警告）
- [ ] 评估触控目标尺寸：导航/关闭按钮放大到 44px+
- [ ] 跟踪 Next.js 16.2 升级路径（含安全补丁）
- [ ] 同步 LDraw 2026-04 官方素材
