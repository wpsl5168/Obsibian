---
title: "BrickHub Dream B 2026-05-03"
date: 2026-05-03
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-03

## 今日审查：components/VoiceInput.js (90 行, 2.7 KB)

Web Speech API toggle 按钮组件，被 `components/home/PromptDock.js` 使用。

### ✅ 已自动修复（PR [#11](https://github.com/wpsl5168/brickhub/pull/11)）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 文件首字节 UTF-8 BOM (U+FEFF)，污染 git diff 与 ESM import | L1 | 移除 BOM | P3 |
| 2 | toggle 按钮缺无障碍属性，可访问名仅为 emoji "🎤"，屏幕阅读器无法识别按下状态 | L68-71 | 加 `aria-pressed={listening}` + 状态化 `aria-label`("开始/停止语音输入") | P2 |

**守卫**：`npx eslint` 通过；jest 无对应测试文件（grep `__tests__/` 无 VoiceInput 用例）；按 pitfall #20 已确认仓库无 `getByRole({name:/语音|录音/})` 类查询，aria-label 不会撞断现有测试。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | useEffect 依赖数组含 `[onError, onResult]`，但父组件 `PromptDock.js` 每次 render 传入的 `onVoiceResult` 是内联函数，会触发 effect 频繁重建 SpeechRecognition 实例 | L51 | 父组件未 useCallback 包裹 callback | (a) `PromptDock.js` 用 `useCallback` 稳定 `onVoiceResult`/`onError`；或 (b) `VoiceInput.js` 用 `useRef` 持有最新 callback，effect 依赖改为空数组 — 涉及组件契约变更，请老王定 | P1 |
| 2 | iOS Safari 上 `webkitSpeechRecognition` 行为不一致：连续调 `start()` 在已 listening 状态下会抛 `InvalidStateError`，组件未防御 | L61 | 缺 try/catch；缺 listening guard 重检 | 包 try/catch，或在 `start()` 前再判 `listening` | P1 |
| 3 | recognition `r.abort()` 清理时若 listening=true，`onend` 不会触发 → 父组件可能仍持有旧 listening 假象（但本组件 unmount 故影响小） | L50 | 卸载语义 | 卸载前 `setListening(false)` 显式重置（仅本地状态，无害） | P1 |

### ⚠️ 自动修复失败回滚

无。

## 依赖动态

| 依赖 | 当前 | 最新 | 变更 |
|------|------|------|------|
| three | ^0.183.2 | r171 (~0.171) | 当前已较新；r171 起 WebGPURenderer 零配置可用，未来若做高端渲染可评估迁移（非紧急） |
| @react-three/fiber | ^8.18.0 | 9.5.0 | **v9 有 breaking changes**（StrictMode 行为、useLoader 改动、负 renderpriority 修复）。当前 R3F v8 仍受支持，无需仓促升级；老王评估升级时机时再读 [v9 migration guide](https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide) |
| @react-three/drei | ^9.122.0 | 9.x | 与 R3F v8 配套，跟随 fiber 升级节奏 |
| next | ^14.2.0 | 15.x stable | Next 15 的 async Request APIs/Turbopack stable/React 19 支持均为 breaking。当前 14.2 LTS 稳定，无安全更新缺口前不建议跟 |
| react | ^18.3.0 | 19 | 与 Next 升级绑定 |
| jest | ^30.3.0 | 30.x | 已最新 |
| LDraw 部件库 | (运行时下载) | 2026-04 update | 库新增 188 文件 / 99 新部件 / 8 新 primitives，总计 16739 件。**建议**：若项目内打包了 LDraw 子集，刷新到 2026-04 |

## 竞品动态

- **Bricklink Studio**：2026-04-27 发新版，配套 Series 11 调色板 (3 月发布)。BrickLink 在做安全合规升级（LEGO 集团网络安全政策对齐）。
- **GitHub LDraw 生态**：未观察到本周新热门项目，主要活跃仓库仍是 `trevorsandy/lpub3d`、`leocad`、`ad-si/awesome-lego` 索引仓。

## 建议行动（供老王确认）

- [ ] PR #11 review & merge
- [ ] P1-1 决定 callback 稳定性方案（影响 PromptDock 接口）
- [ ] P1-2/3 防御性补丁（小改，但 iOS Safari 是真实风险）
- [ ] 评估 LDraw 部件库刷新到 2026-04（新部件 99 件）

## 信息来源

- [Three.js r171 + WebGPU 现状](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [R3F v9 migration guide](https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide)
- [Next.js 15 vs 14 changelog](https://www.abhs.in/blog/nextjs-15-vs-nextjs-14-what-changed-should-you-upgrade)
- [LDraw 2026-04 Parts Update](https://forums.ldraw.org/thread-29495.html)
- [Bricklink Studio download (2026-04-27)](https://www.bricklink.com/v3/studio/download.page)
- [awesome-lego 索引](https://github.com/ad-si/awesome-lego)
