---
title: "BrickHub Dream B 2026-04-25"
created: 2026-04-25
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-04-25
---

# BrickHub 每日 Dream B — 2026-04-25

## 今日审查：lib/ldrawParser.js（614 行 / 26.6KB）

LDraw 解析器 + 几何约束校验（碰撞/悬空检测），是 AI 生成内容落地到 3D 渲染前的最后闸门。

### ✅ 已自动修复（PR #2）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | `Math.min(...bricks.map(b=>b.position[1]))` 在大数组上风险栈溢出/超过 V8 参数上限（~65K） | `parseLDraw` L467 | 改为 `for...of` 循环求最小值 | P2 |
| 2 | `Math.max(...brickInfos.map(b=>b.yMax))` 同上风险 | `validateGeometry` L585 | 改为 `for...of` 循环求最大值 | P2 |

守卫：✅ ESLint 通过 / ✅ `parseLDraw.test.js` + `partLibrary.test.js` 共 54 用例全过。

PR：https://github.com/wpsl5168/brickhub/pull/2

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|----------|------|
| A | `validateGeometry` 零测试覆盖（搜索 `__tests__/` 仅 import 路径，无任何用例） | lib/ldrawParser.js L486-614 | 该函数是 AI 生成 LDraw 后的几何安全门（碰撞 + 悬空），但未被任何测试守护。一旦 epsilon/坐标系误改即静默放过非法模型 | 补 6-8 个用例：单砖/正常叠/重叠/单砖悬空/边缘相切/旋转后 AABB/全空数组/含未知零件 | **P1** |
| B | type-1 字段不足时的 fallback（L410-429）记录的 `position` 直接用原始 LDraw 数值，未乘 `LDU = 1/20`，与主路径（L432-434）不一致；同时也没有翻转 Y（主路径 `[x, -y, z]`） | parseLDraw L420-425 | 错误恢复路径产生的砖块坐标系与正常路径不同，会让下游渲染/校验把它误置在外太空 | 要么遵循主路径单位换算，要么在 fallback 不 push brick（仅记 error），二选一 | **P1** |
| C | `validateGeometry` 中 LDU 几何使用魔法数 `20`（L513-515），与文件顶部的 `LDU = 1/20`（L176）语义重复但未复用常量 | L513-515 | 重复定义易漂移，今后一处改了另一处会忘 | 提取 `LDU_PER_UNIT = 20` 或 `Math.round(def.h / LDU)` | **P2 (但语义改动有微小风险，留待人工 review，未自动修)** |

> 注：另有"零件 alias 表 ~80 行硬编码"，属设计选择（保持显式映射 vs 自动推导），不计为缺陷。

### ⚠️ 自动修复失败回滚

无。

## 依赖动态

| 依赖 | 当前 | 最新 | 变更 |
|------|------|------|------|
| three | ^0.183.2 | r183+ 系列在用，2026 主线推 RenderPipeline 替代 EffectComposer，更多 WebGPU 路径 | 暂无 breaking，但若上 post-processing 需评估 |
| @react-three/fiber | ^8.18.0 | 9.6.0（11 天前）；v10-alpha 支持 WebGLRenderer + WebGPURenderer 双轨；v9 需 React 19 | **跨大版本升级**：v8→v9 需先升 React 18→19，本仓 react ^18.3，暂不动 |
| next | ^14.2.0 | 16（GA）— Turbopack 默认、Cache Components、路由系统重构 | **两个大版本落后**，建议规划 14→15→16 升级窗口 |
| LDraw 库 | — | 2026-03 更新已发布（+134 文件 / 76 新零件 / 1 新基元）；2026-02、2026-01 也已更新 | **alias 表未跟进**，新零件渲染会落到 `1x1` 兜底（见 resolvePartId L393） |

## 竞品动态

- **BrickLink Studio**：Designer Program Series 11 推进中（社区设计众筹），渲染引擎层面无公开升级
- **GitHub LDraw 生态**：未观察到本周明显热门项目变动

## 建议行动（供老王确认）

- [ ] **P1-A** 给 `validateGeometry` 补单元测试（建议下一次 Dream B 直接做这件事，因为是规范化补测试 = P2/P3 范畴的"测试覆盖"，可自动化）
- [ ] **P1-B** 决定 fallback 分支策略（推荐：fallback 不 push brick，只记 error）
- [ ] **架构窗口**：Next 14→16 升级要排期（影响构建系统 + 路由）；R3F v8→v9 绑定 React 19 升级
- [ ] **LDraw 资产同步**：2026-01/02/03 三轮共 ≥134 个新零件，跑 `npm run sync:ldraw:minimal` 后审视 alias 表

## 信息来源

- [Three.js Post-Processing 2026 / r183 RenderPipeline](https://threejsroadmap.com/blog/the-complete-guide-to-threejs-post-processing-in-2026)
- [@react-three/fiber 9.6.0 npm](https://www.npmjs.com/package/@react-three/fiber)
- [R3F v10-alpha discussion](https://github.com/pmndrs/react-three-fiber/discussions/3665)
- [Next.js 16 release notes](https://nextjs.org/blog/next-16)
- [LDraw 2026-03 update](https://library.ldraw.org/updates?latest)
- [PR #2 — Dream B 自动修复](https://github.com/wpsl5168/brickhub/pull/2)
