---
title: "BrickHub Dream B 2026-05-21"
date: 2026-05-21
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-21

## 今日审查：`lib/dslCompiler.js`（722 LOC，least-recently-audited，上次 5-11 P3 trim）

按 pitfall #6 / #24 准则，此文件 lint 全过、自 5-11 无 git 改动、上次已做 P3 风格统一。强行造 P2/P3 修复属"为业绩硬改"，跳过本轮自动修。仅记录核心发现。

### ✅ 已自动修复
本轮 0 项（理由见上）。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | **整个 `components/home/` 目录 11 个文件 + `hooks/useBrickHubHome.js` 全部 orphan**（pitfall #29b 触发：5-17/18/19/20 连续 4 天每天审一个 home/ 组件都报 dead/unwired，证明根因是目录级未接线，不是单组件孤立问题） | `components/home/*.js`（11 个）+ `hooks/useBrickHubHome.js` | grep 全仓 `from.*['"].*home/` 在 pages/components 外**零引用**；`useBrickHubHome` 仅自身定义处出现。整套首页 v2 组件群从未挂到任何 page | **一次性决策**：(a) 若 v2 首页方案推进 → 接到 `pages/index.js` 或新 `pages/home-v2.js` 并补 e2e；(b) 若已废弃 → `git rm` 整个 `components/home/` + `hooks/useBrickHubHome.js`（约 11+1 文件，预估 ≥1500 LOC）。继续按周扫单组件无意义，建议本周内拍板 | **P1** |
| 2 | `compileDSL` 中 `y_ldu` 走 `Number(b.y_ldu)` 后未做 `Number.isFinite` 校验，行 695 只校验 `col/row/layer`。若 LLM 输出 `y_ldu: "abc"` 或 `Infinity`，最终 `Math.round(NaN)` → `y` 写入 LDraw 行为 `NaN`，渲染器会拒载或显示在原点 | `lib/dslCompiler.js:665, 700-704` | y_ldu 解析路径与 col/row 不对称 | 把 `y_ldu` 加入 finite 校验：若 `y_ldu !== null && !Number.isFinite(y_ldu)` → warning + 回退到 `-layer*24`。这条改 LDraw 输出语义、影响生成流水线，留 P1 让老王在 PR 里评估 | **P1** |

### ⚠️ 自动修复失败回滚
无（本轮未尝试自动修）。

## 依赖动态

| 依赖 | 当前 | 最新 | 变更 |
|------|------|------|------|
| three.js | r155（r3f 8.x peer 范围） | r172 | 累计 17 个 minor，2026 主线在推 WebGPU 全浏览器支持；非 breaking，但跟上 r172 可获 WebGPU 路径性能提升。**暂不升级**（r3f v8 peer 锁 ≤r158） |
| @react-three/fiber | ^8.18.0 | v9.x stable | **v9 重大版本**，依赖 React 19 + Next.js 15；StrictMode 行为变更可能暴露隐藏 bug。Issue #3471 显示 Next 15.1.7 + r3f 9.0.2 早期组合有问题。**建议跟随 Next 15 升级一并评估**，单独升 r3f 收益小风险大 |
| @react-three/drei | ^9.122.0 | 9.x 持续小版本 | 跟随 r3f 大版本，暂稳定 |
| next.js | ^14.2.0 | 15.x stable / 16 beta | App Router 主路径无 breaking；React 19 / 异步 `cookies()`/`headers()`/`params` 是主要迁移点。BrickHub 用 Pages Router（保留 React 18 兼容），升级压力低，**Q3 内再评估**即可 |

无即时 breaking 影响。

## 竞品动态

- **Bricklink Studio**：2026-01 起对所有"既存 IP 模型"启用"revamped display"模式，普通用户只能看 3D 预览不能下载零件清单 / LDraw → BrickLink 在收紧 UGC IP 边界。BrickHub 的"LDraw 官方零件 + AI 生成"路线**反而是利好**：用户无法再用 Studio 自由制作 IP 衍生模型时，"自然语言 → 自制乐高"赛道空间扩大。已记入项目笔记，无代码动作。
- **Mecabricks**：本期无显著动态。
- **GitHub LDraw 热门项目**：无新涌现项目。

## 信息来源

- https://newreleases.io/project/github/mrdoob/three.js/release/r172
- https://r3f.docs.pmnd.rs/tutorials/v9-migration-guide
- https://github.com/pmndrs/react-three-fiber/issues/3471
- https://nextjs.org/blog/next-15
- https://www.bricklink.com/v3/studio/design.page?idModel=514748

## 备忘

- 今日触发 pitfall #29b（目录级 orphan 升级报告），已用于替代单组件 dead component 重复报告
- 触发 pitfall #6（"0 auto-fix 是合法结果"），未为凑业绩硬造 P2/P3
- rotation.json 追加 `2026-05-21:lib/dslCompiler.js`
