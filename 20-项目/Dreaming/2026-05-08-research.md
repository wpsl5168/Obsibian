---
title: "BrickHub Dream B 2026-05-08"
created: 2026-05-08
updated: 2026-05-27
type: entity
tags: [brickhub]
status: draft
date: 2026-05-08
---
# BrickHub 每日 Dream B — 2026-05-08

## 今日审查：lib/knowledgeBase.js（301 行，知识库检索/RAG 提示拼接）

### ✅ 已自动修复（PR #14）
| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | `CATEGORY_KEYWORDS.building` 关键词数组里 `'house'` 出现两次（拼写复制粘贴遗留） | line 22 | 去重 | P3 |
| 2 | `formatKnowledgeForPrompt` 中 `theme.category_path.join(' > ')` 缺空安全；同文件 `rankThemes` 用 `(theme.category_path \|\| [])`，前后不一致；taxonomy 数据缺字段时此处会抛 TypeError | line 264 | 加 `(\|\| [])` 兜底 | P2 |

PR：https://github.com/wpsl5168/brickhub/pull/14
守卫：`npx eslint lib/knowledgeBase.js` ✅ ；`npx jest __tests__/generateIntegration.test.js` ✅ （文件无直接单测）

### 🔴 待人工确认（P0/P1）
- **无 P0/P1**。文件整体质量良好，模块边界清晰（taxonomy/omr/corpus 三套缓存独立、合并逻辑用 mtime token 防陈旧）。

### ⚠️ 自动修复失败回滚
- 无

### 📝 P2/P3 之外的观察（仅记录，不动手）
1. `examplesByLdrawId` 是模块级可变 Map，跨调用复用没问题，但 `mergedExamples()` 在每次 token 命中时不重建该 Map → 如果 `mergedExamples` 缓存命中走 early-return（line 93-95），`examplesByLdrawId` 仍为旧值。当前 happy path 因为 token 命中时缓存有效，逻辑成立；但若未来引入 hot-reload 或部分失效，需要重新审视。**P3 提示**，不改。
2. `getKnowledgePreviewLDraw` 在 `example.generic_category` 不在 `PREVIEW_FALLBACKS` 五类（vehicle/f1/tank/building，缺 ship/aircraft/tree/playground）时回落到 `vehicle`。属于业务取舍，建议**老王确认**是否需要为 ship/aircraft/tree/playground 也补 fallback LDraw 模板（涉及 LDraw 编写工作量，红线"素材必须 LDraw 官方"需走人工挑选，不自动）。

## 依赖动态
| 依赖 | 当前 | 最新（2026-05） | 重要变更 |
|------|------|----------------|---------|
| three | ^0.183.2 | r183（即 0.183.x，2025 年 9 月起 RenderPipeline 取代 EffectComposer，r171 起 WebGPU 渲染器 production-ready） | 已是较新；**RenderPipeline 是非破坏性新 API**，EffectComposer 仍可用。**无紧急升级压力** |
| @react-three/fiber | ^8.18.0 | 9.5.0（兼容 React 19） | **R3F v9 要求 React 19**；BrickHub 现在是 React 18.3，**升级要双跳**（react→19 + r3f→9）。当前不阻塞 |
| @react-three/drei | ^9.122.0 | 与 r3f v8 兼容线 | 跟随 r3f；不动 |
| next | ^14.2.0 | 15.x（已稳定） | **breaking**：async Request APIs（`cookies()`/`headers()`/`params` 变 async）、缓存默认值变更（fetch 不再默认缓存）、Image 组件更新、Turbopack stable。**升级前建议老王确认时间窗** |
| react | ^18.3.0 | 19.x | 依赖 next 15 升级路径；目前不动 |
| LDraw 库 | （未在 npm，使用 LDraw.org 官方 parts） | **2026-04 parts update 已发布**（+188 文件 / +99 parts / +8 primitives）；2026-03 也是新一批 | 老王同步素材时可以拉一次官方更新。注意 `~/workspace/brickhub/predev`/`prebuild` 脚本含 `sync:ldraw:minimal`，确认是否拉到最新 |

## 竞品动态
- **Bricklink Studio**：2026-04-27 发版，bug fix（柔性件 POV 渲染缩放问题）。LEGO 已**正式停用 LDD**，全面切到 Studio。竞品上下游在收敛，BrickHub 的"AI 直接生成 LDraw + 浏览器内 R3F 渲染"路线在工具链层面差异化更明显。
- **GitHub LDraw 生态**：pybricks/ldraw 镜像持续维护（带版本控制），适合作为 BrickHub 拉 parts 的 upstream 候选。当前未发现可直接借鉴的"AI + LDraw"开源项目，差异化窗口仍开放。

## 建议行动（供老王确认）
- [ ] **拉一次 LDraw 2026-03/04 parts 更新**到本地 minimal 库（`npm run sync:ldraw:minimal`），看 `data/ldraw/` 是否同步
- [ ] Next.js 15 升级**先不要做**，等本月儿童 UI 主线收尾后再开窗口；做时按官方 codemod 走（`npx @next/codemod@canary upgrade latest`）
- [ ] R3F v9 + React 19 升级**依赖 Next 升级链**，同窗口处理

## 信息来源
- Three.js / WebGPU 现状：https://altersquare.io/three-js-vs-webgpu-2026-large-scale-construction-viewers/
- Three.js r183 RenderPipeline：https://threejsroadmap.com/blog/the-complete-guide-to-threejs-post-processing-in-2026
- R3F v9 / React 19：https://grokipedia.com/page/React_Three_Fiber
- Next.js 15 迁移指南：https://jishulabs.com/blog/nextjs-15-16-features-migration-guide-2026
- LDraw 2026-04 update：https://forums.ldraw.org/thread-29495.html
- LDraw 2026-03 update：https://forums.ldraw.org/thread-29466.html
- Bricklink Studio 2026-04-27 release：https://www.bricklink.com/v3/studio/download.page
