---
title: "BrickHub Dream B 2026-05-22"
created: 2026-05-22
updated: 2026-05-25
type: entity
tags: [brickhub]
status: draft
date: 2026-05-22
---
# BrickHub 每日 Dream B — 2026-05-22

## 今日审查：`lib/pipeline/index.js`（230 LOC，LDraw 生成主管线）

该文件是 LLM 输出关键路径（DSL → LDraw → 校验 → 兜底），按 Pitfall #19 列入"仅报告不可动手"白名单。ESLint clean，无 P2/P3 自动修。

### ✅ 已自动修复
本日无（pipeline 文件白名单 + 文件本身 lint clean，符合 Pitfall #6 "0 auto-fix 是合法结果"）。

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | **整个 `components/home/` 目录 + `hooks/useBrickHubHome.js` 是 orphan，没有任何 page/component 引用** | `components/home/*.js` (11 文件 ~660 LOC) + `hooks/useBrickHubHome.js` (259 LOC) | grep 全仓 `from.*home/` / `useBrickHubHome` 均零命中；`pages/index.js` 174 行 imports 只有 react/next/Image，未引入 home 子目录任何组件 | 二选一：**(a) 接线**：在 `pages/index.js` 引入 `useBrickHubHome` + home 子组件，恢复设计稿首页；**(b) 删除**：`git rm -r components/home/ hooks/useBrickHubHome.js`，减 ~900 LOC 维护负担。**触发 Pitfall #29b**：5-17 StatusBanner、5-18 ProcessDebugPanel、5-19 LDrawModal、5-20 PromptDock 连续 4 天报"dead component"——根因是整个目录从未挂载，不是单组件孤立 | **P1** |
| 2 | `dslTelemetry` 是模块级全局可变状态，进程级累加，Next.js 多实例/SSR 重启会归零，且无并发保护 | `lib/pipeline/index.js:7-17` | `let` + 进程内单例，多 lambda/edge worker 不共享；无锁，但 `bumpTelemetry` 是同步原子操作所以 race 影响小 | 中期迁移到外部存储（Redis/DB）；近期可接受现状但需在 `/api/admin/telemetry` 等暴露接口的文档中标注"per-instance approximation" | P1 |
| 3 | `validateGeometry` 仅在 `formatErrors.length === 0` 时调用，但 `errors = [...formatErrors, ...geoErrors]` 后续 retry 路径用 `previousValidationSignature` 比对——format 错误 signature 与 geo 错误 signature 可能误判为"stalled"（同一类 formatError 反复出现时跳出循环并触发 offline-fallback） | `lib/pipeline/index.js:159-173` | `getValidationSignature` 只按 `e.type` 计数，未区分阶段 | 在 signature 中加阶段前缀，如 `format:matrix=2,geo:overlap=1` | P1 |
| 4 | `useDSLPath = false` 后下次循环仍会走 `extractDSL`/`compileDSL` 之外的 `extractLDraw`，但若上游又返回 `<LEGO>` 标签（DSL JSON）会被当作纯 LDraw 文本，触发解析失败 | `lib/pipeline/index.js:149-150` | DSL 路径与 LDraw 路径切换是单向（DSL→LDraw），但上游 prompt 已切换为 `buildSystemPrompt()`，正常 LLM 不会再返回 DSL，所以这是低概率边界 | 在 `extractLDraw` 前检测是否仍是 DSL JSON，若是则归类为 `upstream_misbehavior` 退出 | P1 |

### ⚠️ 自动修复失败回滚
无（未启动自动修分支）。

## 依赖动态

| 依赖 | 当前 | 最新（稳定） | 重要变更 |
|------|------|-----------|---------|
| next | ^14.2.0 | 15.x（2026-03 稳定） | async Request APIs、Turbopack stable、React 19 支持。**升级需配合 react 18→19**，BrickHub 当前 react ^18.3.0 锁死，整套升级才有意义。无紧迫性 |
| three | ^0.183.2 (r183) | r172（注：项目当前已超出 r3f v8 peer 上限 r158）| 与 r3f v8 peer 范围冲突，**当前已存在隐式 peer 警告**。建议保持现状或整套升 r3f v9 + three 跟随 |
| @react-three/fiber | ^8.18.0 | v9 已稳定，需 react 19 | 升 v9 需联动 react→19、next→15 整套迁移，单包升级会破坏 peer |
| @react-three/drei | ^9.122.0 | v10 系列对应 r3f v9 | 与 r3f 锁定升级 |
| react | ^18.3.0 | 19.x | 联动项 |

**结论**：依赖栈处于"全锁死"状态，无单包安全升级窗口；整套（next 15 + react 19 + r3f v9 + drei v10 + three 跟随）迁移是一个独立 epic，今日无紧急 breaking。

## 竞品动态
- Bricklink Studio / Mecabricks：无重要新版本搜索命中
- GitHub LDraw 生态：无新热门项目跳出

## 信息来源
- https://nextjs.org/docs/app/guides/upgrading/version-15
- https://r3f.docs.pmnd.rs/getting-started/installation
- https://www.abhs.in/blog/nextjs-15-vs-nextjs-14-what-changed-should-you-upgrade
