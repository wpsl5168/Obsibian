---
title: "BrickHub Dream B 2026-05-20"
created: 2026-05-20
updated: 2026-05-25
type: entity
tags: [brickhub]
status: draft
date: 2026-05-20
---
# BrickHub 每日 Dream B — 2026-05-20

## 今日审查：`components/home/PromptDock.js` (57 LOC)

AI 生成提示输入栏 + 语音入口 + 提交按钮组合组件。VoiceInput / Submit / 文本输入三件套布局。

### ✅ 已自动修复（PR #25）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 提交按钮 loading 状态下只剩 spinner，screen reader 听不到 "AI 生成"，且无 busy 状态信号 | `PromptDock.js:43-54` | 加 `aria-label`（随 loading 切换）+ `aria-busy={loading}` | P2 |
| 2 | spinner span 是装饰元素但未标 `aria-hidden` | `PromptDock.js:50` | 加 `aria-hidden="true"` | P2 |
| 3 | 输入框只有 placeholder，placeholder 不是有效的 accessible name | `PromptDock.js:32-40` | 加 `aria-label="乐高模型描述"` | P2 |

守卫：`npx eslint components/home/PromptDock.js` 全过；无对应测试文件。

PR: https://github.com/wpsl5168/brickhub/pull/25

### 🔴 待人工确认（P0/P1）

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | **整个 `components/home/` 目录 + `hooks/useBrickHubHome.js` 是 dead/unwired code** | `components/home/*.js` (11 文件) + `hooks/useBrickHubHome.js` | grep `from.*home/` / `useBrickHubHome` 全仓零调用；`pages/index.js` (174 LOC) 不 import 任何 home 组件，也不用 hook | (a) 在 `pages/index.js` 接线 home flow，让用户能用到这套；或 (b) 删除 `components/home/` + hook，统一只走 sandbox/model 路径。同 pitfall 在 5-17 / 5-18 / 5-19 已陆续命中单个组件，**今日确认是整个目录** | P1 |
| 2 | 组件 props 无 PropTypes/TS 校验 | `PromptDock.js:3-12` | 全仓未启 PropTypes 也非 TS，契约全靠人脑维护 | 长期迁 TS（大改不在 Dream B 自动修范围） | P1 |

### ⚠️ 自动修复失败回滚

无（所有 P2 修复一次过 lint 守卫）。

## 依赖动态

本次跳过——把"dead-code 全目录确认"这件结构性发现作为主信号。`Three.js / R3F / Next.js / LDraw` 上周 5-13 已有完整记录。

## 竞品动态

本次跳过。

## 信息来源

- 本仓代码全文检索（grep + find）
- 同类 pitfall 历史：2026-05-17 (StatusBanner)、2026-05-18 (ProcessDebugPanel)、2026-05-19 (LDrawModal)
