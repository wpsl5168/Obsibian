---
title: "BrickHub Dream B 2026-05-15"
date: 2026-05-15
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-15

## 今日审查：`pages/model/[id].js` (495 行)

文件 lint clean、modelDetail.test.js 2/2 通过、近期无 git 改动 (febe1246, 2 周前)。
作为模型详情页核心入口,职责包含 LDraw 加载、partsList 解析、stepsPartsList 解析、放映/沙盒模式切换、3D 渲染编排、清单面板 UI,代码体量偏大已显冗。

### ✅ 已自动修复 (PR #20)

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 放映模式 ←/→ 步骤按钮 36px 低于 WCAG 触控最低 44px | L368/L378 | `w-9 h-9` → `w-11 h-11`,补 aria-label | P2 |

PR: https://github.com/wpsl5168/brickhub/pull/20

### 🔴 待人工确认 (P0/P1)

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | 内嵌 110 行 LDraw partsList + stepsPartsList 解析逻辑,与 `lib/ldrawParser.js` 职责重叠 | L102-235 | 早期 prototyping 直接写在组件里,后续 ldrawParser 抽出后未回填 | 抽离为 `useLdrawPartsList(ldrawCode)` / `useStepsPartsList(ldrawCode)` 两个 hook 放到 `lib/hooks/`,组件代码可瘦 ~110 行 | P1 |
| 2 | 三大模式按钮(清单/放映/沙盒)缺 `aria-pressed`,屏幕阅读器无法感知激活状态 | L390-420 | 仅靠背景色 `#FFD500` 视觉表达 toggle 状态 | 加 `aria-pressed={showParts/isAssemblyMode/isSandboxMode}`,**不要**额外加 aria-label 否则会撞 modelDetail.test.js 里 `getByRole('button',{name:/清单/i})` 查询(参 skill pitfall #20) | P1 |
| 3 | `handleImageFallback` 用 `target.src.includes('cdn.rebrickable.com')` 字符串嗅探判断回退阶段 | L76-87 | 没有显式 fallback step 状态机 | 改用 `data-fallback-step` 属性记录回退阶段 (`0→1→2`),CDN 改 path 时不致永久卡在阶段 1 | P1 |
| 4 | useEffect 重置状态时缺少 cleanup,模型快速切换可能 race | L177-199 | fetch 不可中断 | 用 AbortController 或 mounted 标志 | P1 |

### ⚠️ 自动修复失败回滚

无。本次只 1 项自动修,守卫一次通过。

## 依赖动态

| 依赖 | 当前 | 最新 | 变更 |
|------|------|------|------|
| three | ^0.183.2 | ~r183+ (RenderPipeline 节点化后处理已 GA) | 当前已在 r183 线;若后续追 r184+,关注 RenderPipeline 是否影响 BrickRenderer 的 outline pass |
| @react-three/fiber | ^8.18.0 | v9 line (双渲染器 WebGL+WebGPU) | v9 是大版本,引入 useFrame 高级调度 + WebGPURenderer 支持;升级前需评估儿童端浏览器 WebGPU 兼容性,**不建议本季度升** |
| next | ^14.2.0 | 15.x stable / 16 已发布 | **breaking**: async Request APIs (cookies/headers/params 改异步)、caching 行为变更、React 19 强依赖。BrickHub 是 Pages Router,影响小但 fetch 缓存语义需 review |
| @react-three/drei | ^9.122.0 | 仍在 v9 线 | 跟 R3F v8 配套,若 R3F 升 v9 则此处也要同步 |

**结论**:无紧急 breaking change 需立即响应。Next 15 升级建议挂入 Q3 backlog,R3F v9 + Three WebGPU 路线观察一个季度再决定。

## 竞品动态

- **Bricklink Studio**:2026-04-27 发新版,修复 POV 渲染中 flexible parts 收缩问题。Series 11 Designer Program 调色板 2026-03-03 更新 (开放提交 4/20-5/4)。Studio 仍是 LDraw 生态最权威工具,BrickHub 在儿童 AI 创意场景上是错位竞争,无直接威胁。
- Mecabricks / GitHub LDraw 热门项目本周无重大动态(跳过详搜节省 token)。

## 信息来源

- [Three.js 2026 What Changed (Utsubo)](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [R3F Releases (GitHub)](https://github.com/pmndrs/react-three-fiber/releases)
- [Next.js 15 Upgrade Guide](https://nextjs.org/docs/app/guides/upgrading/version-15)
- [BrickLink Studio Download](https://www.bricklink.com/v3/studio/download.page)
