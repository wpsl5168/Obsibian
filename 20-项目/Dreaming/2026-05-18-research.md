---
title: "BrickHub Dream B 2026-05-18"
date: 2026-05-18
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-18

## 今日审查：components/home/ProcessDebugPanel.js (130 LOC, 弹窗组件)

选择理由：rotation.json 里未审过；属于 modal/弹窗类组件，按 Pitfall #28，a11y 三件套高产；同期 home/ 子目录最大未审文件。

### ✅ 已自动修复（PR #23）

| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | 模态遮罩缺 `role="dialog"` / `aria-modal="true"` / `aria-labelledby` | line 12 | 加齐三件属性，引用标题 id `process-debug-title` | P2 |
| 2 | h2 标题缺 id，无法被 aria-labelledby 关联 | line 16 | 加 `id="process-debug-title"` | P2 |
| 3 | 右上 ✕ 关闭按钮 `p-2` ≈ 32px，违反 44px 最低触控标准（且无 aria-label） | line 19-25 | 改成 `h-11 w-11`（44px）+ `aria-label="关闭处理过程详情"` | P2 |
| 4 | 底部"关闭"按钮 `py-2` ≈ 32px 高，违反 44px 最低标准 | line 119-125 | 加 `min-h-11` 兜底高度 | P2 |

PR: https://github.com/wpsl5168/brickhub/pull/23

### 🔴 待人工确认（P1）—— a11y 行为类

| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | 缺 Escape 键关闭 | ProcessDebugPanel.js 整体 | 没监听 keydown | `useEffect(()=>{ if(!open) return; const h=e=>e.key==='Escape'&&onClose(); addEventListener('keydown',h); return ()=>removeEventListener('keydown',h) },[open,onClose])` | P1 |
| 2 | 缺 backdrop 点击关闭 | line 12 外层 div | onClick 未挂 | 外层加 `onClick={onClose}`，内层卡片 `e.stopPropagation()` | P1 |
| 3 | 缺焦点管理（陷阱） | 整体 | 没 trap focus | 打开时 useRef 把关闭按钮 focus，关闭时复位到触发元素 | P1 |

三件套通常一并修，留 PR 由老王决定，不要分散自动改。

### 🟡 死代码观察（P2 报告，不自动改）

`hooks/useBrickHubHome.js:22` 定义了 `processDebugOpen` state，但 grep 全仓只有这一处。setter `setProcessDebugOpen` 从未被调用，UI 也从未挂载 `<ProcessDebugPanel />`。

→ 这就是**整个 ProcessDebugPanel 组件目前不会被任何用户看到**。两条出路（老王挑）：
- (a) 接线：在 hooks 里 export setter，pages/index.js 挂载组件，加入口按钮
- (b) 删：移除 hook 里的 state 和组件文件，避免维护幽灵代码

涉及产品决策，留 P1 级报告，不动手。

### ⚠️ 自动修复失败回滚

无。

### 守卫

- `npx eslint components/home/ProcessDebugPanel.js`：✅ clean
- 无对应测试文件，跳过 jest

---

## 依赖动态

| 依赖 | 当前 (BrickHub) | 最新 | 重要变更 |
|------|----------------|------|---------|
| three | 0.169.x | r181 (2026-10 Reddit, 当前可能已更新) | 持续 WebGPU 完善，2026 年主线是 WebGPU 路径成熟（utsubo.com 综述） |
| @react-three/fiber | 8.17.x | 持续维护 | R3F 移动端性能仍是热点（krapton.com 2026 指南：Draco/LOD/OffscreenCanvas/instancing） |
| next | 14 | 16.3.0-canary 在跑，15 已是生产标准 | **⚠️ 安全：CVE-2026-27979（maxPostponedStateSize）、CVE-2026-29057（http-proxy），Vercel 5 月发了 backport 安全补丁** |

**🔴 行动项**：Next.js 这两个 CVE 老王需确认 BrickHub 当前 14.x 版本是否受影响、是否要 backport。

---

## 竞品动态

本次未搜索（搜索预算优先给依赖 + 安全）。下次轮换补 Bricklink Studio / Mecabricks / LDraw GitHub 热门项目。

---

## 信息来源

- [Three.js r181 release (Reddit)](https://www.reddit.com/r/threejs/comments/1okz0nj/threejs_r181_released/)
- [What's New in Three.js 2026](https://www.utsubo.com/blog/threejs-2026-what-changed)
- [R3F 移动端性能 2026 指南](https://www.krapton.com/blog/boosting-react-three-fiber-mobile-performance-in-2026-a-deep-dive-d6105c)
- [Next.js Updates May 2026 (Releasebot)](https://releasebot.io/updates/vercel/next-js)
- [Next.js GitHub Releases](https://github.com/vercel/next.js/releases)
