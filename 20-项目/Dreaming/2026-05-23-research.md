---
title: "BrickHub Dream B 2026-05-23"
date: 2026-05-23
tags: [BrickHub, dreaming, qa]
---

# BrickHub 每日 Dream B — 2026-05-23

## 今日审查：`components/VoiceInput.js` (90 LOC)

选取依据：least-recently-audited，上次 2026-05-03 (20 天前)。文件 30 天内无 git 改动，但属于核心输入路径（语音→PromptDock）。

### ✅ 已自动修复 (PR [#26](https://github.com/wpsl5168/brickhub/pull/26))
| # | 问题 | 位置 | 修法 | 级别 |
|---|------|------|------|------|
| 1 | mic toggle 按钮无 `aria-pressed`，SR 听不到开/关状态 | VoiceInput.js:68-72 | 加 `aria-pressed={listening}` | P2 |
| 2 | accessible name 只靠 `title`（部分 SR 不暴露） | VoiceInput.js:72 | 加 `aria-label="停止/开始语音输入"` | P2 |
| 3 | interim 实时识别浮层无 live region | VoiceInput.js:83-86 | div 加 `role="status" aria-live="polite"` | P2 |

ESLint clean；VoiceInput 无对应测试文件（已 grep `__tests__/`），PromptDock 也未按 name regex 查 mic 按钮，无回归风险。

### 🔴 待人工确认 (P0/P1)
| # | 问题 | 位置 | 根因 | 建议方案 | 级别 |
|---|------|------|------|---------|------|
| 1 | `useEffect` 依赖 `[onError, onResult]` — 父传非 memoized 回调时每次 render 都会重建 SpeechRecognition 实例 | VoiceInput.js:16-51 | 闭包陷阱 vs 引用稳定性的经典取舍 | 用 ref 存最新回调（useEvent 模式），useEffect 依赖收窄到 `[]`；或要求 PromptDock 用 `useCallback` 包 `onVoiceResult` | P1 |
| 2 | 0 测试覆盖 | __tests__/ 无 VoiceInput.* | 核心输入路径零保护 | mock `window.SpeechRecognition`，断言 toggle → onResult 调用链 | P1 |

P0：无。

### ⚠️ 自动修复失败回滚
无。3 项 P2 全部一次通过。

### Dead code 备注
P1#1 的运行时风险在当下被 `components/home/` orphan 这件事自然遮蔽（PromptDock 整个目录无 page 引用，5-20 已报告）。两种结局都安全：(a) 老王接线 home/ → 修 PromptDock 时顺手 useCallback；(b) 删 home/ → P1 一并消失。本次仍按 a11y prep 处理，不因 dead 而跳过 P2。

---

## 依赖动态

| 依赖 | 当前 | 最新 | 重要变更 / 行动建议 |
|------|------|------|---------|
| three.js | 0.183.2 (r183) | r180 系列稳定，r183 已发布 | **项目领先主流**。R3F v8 peer 范围历史上 ≤r158，r183 跑通是运气，peer 警告可能存在。维持现状即可。 |
| @react-three/fiber | ^8.18.0 (v8) | v9 已 stable | v9 才正式支持 r172+，但升 v9 是 breaking（API 调整）。**建议**：等到下一个 R3F major 周期或 three.js 出问题时再整套升 (R3F+drei+three)，**不要单包升**。 |
| @react-three/drei | ^9.122.0 | 与 R3F v8 配套 | 跟随 R3F 升级节奏，单升无意义。 |
| Next.js | ^14.2.0 | 15.x stable, 16 beta | 15 默认 fetch caching 行为变了（默认不缓存），App Router 用户基本兼容。**非紧急**，等积累 3-4 个 must-have 特性再升。 |

行业风向（轻量观察，无需立即行动）：
- WebGPU + three.js 迁移路径在 2026 年明显成熟（utsubo.com 长文 + youngju.dev deep-dive 都在推），但 BrickHub 的 LDraw 渲染量级远没到 WebGL 瓶颈，WebGPU 是 1+ 年后的事。
- Gaussian Splatting 在 3D web 圈仍是热词，与积木几何渲染场景不沾边。

## 竞品动态
- **BrickLink Studio**：2026-04-27 仍在持续更新（修了 flexible parts shrinking on POV renders 等 bug）。竞品健康，没有停滞迹象。
- **Mecabricks / GitHub LDraw 项目**：本日搜索预算用完，无新动态。

## 信息来源
- [Next.js 15 release notes](https://nextjs.org/blog/next-15)
- [three.js r180 release - reddit](https://www.reddit.com/r/threejs/comments/1n7hhfs/threejs_r180_released)
- [BrickLink Studio download](https://www.bricklink.com/v3/studio/download.page)
- [3D Web 2026 deep dive (youngju.dev)](https://www.youngju.dev/blog/culture/2026-05-14-3d-development-for-web-three-js-react-three-fiber-webgpu-gaussian-splatting-deep-dive-2026.en)
