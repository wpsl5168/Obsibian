---
title: SWE-Agent 生产化与 CI/CD 闭环
created: 2026-04-21
updated: 2026-09-09
type: concept
tags: [agent, agentic-coding, workflow, security, evaluation, best-practices]
status: stable
oversized_ok: true
---

# SWE-Agent 生产化与 CI/CD 闭环

> 📚 上级页面：[[10-知识库/AI模型与Agent/06-工程落地与Vibe_Coding实战/6.3-SWE-Agent端到端闭环开发.md]]

## 一、结论：目标不是“自动改代码”，而是“受控地产生可审查变更”

生产环境中的 Coding Agent 不应直接拥有从 Issue 到 Production 的全链路权限。正确目标是把 Agent 放进一个**权限分层、证据驱动、失败默认安全**的交付系统：Agent 负责提出候选 patch，CI 负责在无特权环境中验证，人类或策略引擎负责批准，部署工作流只消费已合并、可追溯的提交。

最危险的设计不是模型偶尔写错代码，而是把以下能力同时交给同一个执行上下文：

- 读取不可信 Issue、PR 描述或仓库内容；
- 使用模型 API、云账号、包仓库等高价值 secrets；
- 修改默认分支、创建 Release 或部署生产；
- 执行 Agent 刚生成的任意代码；
- 在失败后无限重试并继续扩大修改范围。

生产化的核心原则可以压缩为一句话：**生成、验证、发布必须分权；不可信代码永远不能在持有写权限或生产凭据的 Job 中执行。**

## 二、威胁模型：先定义“谁能污染输入，谁持有什么权限”

### 2.1 四类不可信输入

1. **外部文本**：Issue 标题、正文、评论、PR 描述、commit message 都可能包含命令注入或 prompt injection。
2. **仓库代码**：fork PR 可以修改测试脚本、构建脚本、依赖安装钩子和 GitHub Actions workflow。
3. **依赖与工具输出**：包安装、测试日志、MCP Server、网页内容都可能向 Agent 注入恶意指令。
4. **Agent 产物**：patch、命令、配置和测试不能因为“由内部模型生成”就被视为可信。

### 2.2 关键资产

- `GITHUB_TOKEN` 的 `contents: write`、`pull-requests: write` 权限；
- 模型 API key、制品库凭据、代码签名证书；
- OIDC token 签发能力；
- 生产 Environment、数据库和云资源；
- 私有源码、客户数据及日志中的敏感信息。

### 2.3 必须阻断的攻击链

```text
攻击者提交恶意 Issue/PR
  → Agent 被诱导读取或外传 secret
  → 生成修改 workflow / 构建脚本的 patch
  → 特权 Job checkout 并执行该代码
  → 获取 GITHUB_TOKEN / OIDC / 云权限
  → 篡改默认分支、制品或生产环境
```

阻断点至少要有三处：Agent Job 不持有发布权限；验证 Job 不持有 secrets；部署 Job 不接触未合并的外部代码。

## 三、架构选择：三种模式不要混用

| 模式 | Agent 输入 | Agent 输出 | 可执行权限 | 适用范围 | 主要风险 |
|---|---|---|---|---|---|
| 本地辅助 | 开发者当前工作区 | 本地 diff | 开发者账号范围 | 探索、重构、交互调试 | 人为误提交、环境不可复现 |
| PR 候选生成 | 已筛选 Issue + 默认分支 | patch / 候选分支 / PR | 只读源码；发布阶段单独授权 | 团队日常修复 | Issue 注入、过度修改、成本失控 |
| 自主合并与部署 | Issue 到生产全链路 | 已部署版本 | 写库、签名、云权限 | 原则上仅限极低风险封闭系统 | 单点越权、供应链污染、审计失真 |

推荐默认采用第二种：**Agent 只能创建候选变更，不能批准自己的工作，不能绕过分支保护，不能直接部署。**

## 四、推荐闭环：候选生成与可信发布解耦

```text
Issue / 人工派单
  ↓ 入口过滤：仓库、标签、作者、路径、预算
非特权 Agent Job
  - checkout 默认分支
  - 无 contents:write / 无 OIDC
  - 模型 key 仅在该 Job 可见
  - 在隔离容器中生成 patch
  ↓
静态策略检查
  - 限制路径、文件数、diff 行数
  - 禁止改 workflow/CODEOWNERS/发布脚本
  - secret scan、依赖策略、许可证策略
  ↓
无 secret 验证 Job
  - 编译、单测、集成测试、SAST、SBOM
  - 失败即终止，不自动扩大权限
  ↓
发布候选 PR
  - 独立 Job 只应用已验证 patch，不执行代码
  - 最小 contents/pull-requests 写权限
  ↓
CODEOWNERS + Required Checks + 人工 Review
  ↓ merge
主分支部署工作流
  - 只 checkout 已合并 SHA
  - Environment 审批
  - OIDC 短期凭据
  - 可回滚发布
```

### 4.1 为什么不让 Agent 直接 push main

分支保护不仅是流程要求，也是安全边界。Agent 的判断来自概率模型，无法替代确定性的权限策略、审批记录和可重复测试。即使某模型在公开 benchmark 上得分很高，也不能证明它理解本仓库的业务规则、合规边界、部署拓扑和历史兼容债务。

### 4.2 为什么 `pull_request_target` 特别危险

GitHub 官方明确警告：`pull_request_target` 在目标仓库默认分支上下文中运行，可能拥有写权限和 secrets。若工作流再 checkout fork PR 的代码并执行，攻击者就能让不可信代码进入特权上下文。`workflow_run` 同样属于特权触发器，且可能与前序工作流共享 artifact/cache；不能因为“前一个 workflow 成功”就盲目信任其产物。

安全默认：

- fork PR 的测试用 `pull_request`，`permissions: contents: read`，不注入 secrets；
- 不在 `pull_request_target` 中 checkout、构建或执行 PR head；
- 特权工作流只处理结构化、签名或经过严格验证的元数据；
- artifact 必须绑定 `run_id`、仓库、commit SHA，校验摘要和大小；
- cache key 不允许由不可信输入单独决定，特权与非特权 Job 不共享可写 cache。

## 五、生产示例一：无特权 PR 验证工作流

下面的工作流可以执行 fork PR 代码，但它没有写权限、云权限和仓库 secrets。对于需要外部服务的集成测试，使用临时本地容器或 mock，不把生产凭据降级塞入 PR Job。

```yaml
# .github/workflows/pr-verify.yml
name: PR Verify

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: mcr.microsoft.com/dotnet/sdk:8.0
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Reject protected-path changes
        shell: bash
        run: |
          git diff --name-only \
            "${{ github.event.pull_request.base.sha }}" \
            "${{ github.event.pull_request.head.sha }}" > changed-files.txt
          python3 scripts/ci/check_changed_paths.py changed-files.txt

      - name: Restore locked dependencies
        run: dotnet restore --locked-mode

      - name: Build
        run: dotnet build --no-restore -warnaserror

      - name: Test
        run: >-
          dotnet test --no-build
          --logger "trx;LogFileName=test.trx"
          --collect "XPlat Code Coverage"

      - name: Dependency audit
        run: dotnet list package --vulnerable --include-transitive

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-${{ github.event.pull_request.number }}-evidence
          path: |
            **/TestResults/**
            changed-files.txt
          retention-days: 14
```

路径策略脚本必须由默认分支维护，不能由 PR 自己放宽：

```python
# scripts/ci/check_changed_paths.py
from pathlib import Path
import sys

DENY_PREFIXES = (
    ".github/workflows/",
    "CODEOWNERS",
    "scripts/release/",
    "infra/prod/",
)
MAX_FILES = 40

paths = [line.strip() for line in Path(sys.argv[1]).read_text().splitlines() if line.strip()]
violations = [p for p in paths if p.startswith(DENY_PREFIXES)]

if len(paths) > MAX_FILES:
    raise SystemExit(f"change set too large: {len(paths)} > {MAX_FILES}")
if violations:
    raise SystemExit("protected paths changed:\n" + "\n".join(violations))
```

注意：PR 可以修改这个脚本在工作树中的副本。因此高风险路径检查应同时由组织级规则、CODEOWNERS、Ruleset 或外部策略服务执行；仓库内脚本只是第一层，不是唯一边界。

## 六、生产示例二：合并后用 OIDC 部署，而不是长期云密钥

部署只由 `main` 的已合并提交触发，不读取 Issue/PR 文本，不 checkout fork SHA。GitHub OIDC 的价值是用短期、带 claims 的令牌换取云凭据，避免仓库保存长期 secret；云端信任策略仍必须限制 repository、branch/ref、workflow 和 environment。

```yaml
# .github/workflows/deploy.yml
name: Deploy Production

on:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

concurrency:
  group: production
  cancel-in-progress: false

jobs:
  deploy:
    environment: production
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Verify immutable commit
        run: test "$(git rev-parse HEAD)" = "${{ github.sha }}"

      - name: Login Azure with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy immutable artifact
        run: ./scripts/release/deploy.sh "${{ github.sha }}"
```

生产 Environment 应配置 required reviewers、部署分支限制和等待规则。云端 federated credential 只信任该仓库的 production environment subject；不能把 `repo:org/*` 或所有 branch 配成通配信任。

## 七、生产示例三：Agent 候选 patch 的策略门禁

Agent 生成 patch 后，先做确定性检查，再决定是否进入 CI。以下策略不是完整安全沙箱，但能快速砍掉高风险候选：

```python
from dataclasses import dataclass
from pathlib import Path
import json

@dataclass(frozen=True)
class PatchPolicy:
    max_files: int = 20
    max_added_lines: int = 800
    denied_prefixes: tuple[str, ...] = (
        ".github/", "infra/prod/", "scripts/release/", "CODEOWNERS"
    )


def validate_manifest(path: str, policy: PatchPolicy = PatchPolicy()) -> None:
    manifest = json.loads(Path(path).read_text(encoding="utf-8"))
    files = manifest.get("files")
    if not isinstance(files, list):
        raise ValueError("manifest.files must be a list")
    if len(files) > policy.max_files:
        raise ValueError(f"too many files: {len(files)}")

    total_added = 0
    for item in files:
        name = item.get("path", "")
        if not name or name.startswith("/") or ".." in Path(name).parts:
            raise ValueError(f"unsafe path: {name!r}")
        if name.startswith(policy.denied_prefixes):
            raise ValueError(f"protected path: {name}")
        total_added += int(item.get("added", 0))

    if total_added > policy.max_added_lines:
        raise ValueError(f"patch too large: +{total_added} lines")


if __name__ == "__main__":
    validate_manifest("agent-output/manifest.json")
```

更严格的实现还要验证 patch 可干净应用、禁止 symlink/path traversal、限制二进制文件、扫描 secret、检查生成文件与源文件关系，并在新的临时工作树中执行测试。验证失败时应丢弃候选，不允许 Agent 自行关闭策略或请求更高权限。

## 八、预算、重试与错误恢复

Agent 的重试不是普通网络重试。模型可能在每次失败后扩大搜索范围、修改更多文件或调用更多工具，因此需要显式状态机：

```text
候选生成
  ├─ 语法/策略失败 → 最多修正 1 次，仅允许改候选 patch
  ├─ 单测失败 → 最多修正 2 次，保留失败证据
  ├─ 安全扫描失败 → 直接停止并转人工，不自动解释为误报
  ├─ 修改受保护路径 → 直接拒绝
  └─ 达到时间/Token/费用/文件数任一上限 → 终止，不继续调用工具
```

建议每个任务至少设置：

- wall-clock timeout；
- 模型调用次数和 token/费用上限；
- 命令执行次数、单命令超时、输出字节上限；
- 可读目录与可写目录 allowlist；
- 最大修改文件数、最大 diff 行数；
- 最大修复轮数和相同失败指纹次数；
- 网络 egress allowlist；
- 日志脱敏和 artifact 保留期。

相同错误连续出现两次时，应触发 circuit breaker，而不是继续“换一种说法”重试。数据库迁移、权限、支付、认证、加密和 CI 配置变更默认转人工。

## 九、从 Benchmark 到生产评测集

公开 SWE benchmark 主要衡量给定仓库快照与问题描述下能否产生通过测试的 patch。生产系统还必须评估权限、成本、稳定性和长期维护性。不能把某个公开榜单分数直接换算成“自动合并率”。

### 9.1 企业评测样本

从本组织历史 Issue→PR 中构建时间切分数据集，至少保留：

- 修复前 commit、Issue 原文及必要附件；
- 人类最终 patch 和 Review 讨论；
- 可离线运行的测试与依赖快照；
- 受保护路径、数据分类和风险等级；
- 是否需要外部系统、数据库或人工业务判断；
- 真实失败样本，而不是只挑已解决的清晰 Issue。

防泄漏要求：评测任务的最终 patch 不能出现在 Agent 可检索的知识库、训练缓存或 prompt 中；使用时间切分，并记录模型、工具、镜像和依赖版本。

### 9.2 指标矩阵

| 维度 | 指标 | 不能只看什么 |
|---|---|---|
| 正确性 | 首次通过率、最终通过率、回归失败率 | 测试是否变绿 |
| 范围控制 | 无关文件率、diff 放大率、受保护路径触碰率 | 修改文件总数 |
| 安全 | secret 暴露、越权调用、策略拒绝率、SAST 增量 | 是否“看起来安全” |
| 成本 | 每个有效 PR 的 token、时间、CI 分钟 | 单次模型价格 |
| 可维护性 | Review 修改量、撤回率、30 天回滚/缺陷率 | PR 是否合并 |
| 稳定性 | 同题多次运行方差、工具失败恢复率 | 最好的一次结果 |

### 9.3 最小评测聚合脚本

```python
import json
from pathlib import Path
from statistics import mean

rows = [json.loads(x) for x in Path("eval/results.jsonl").read_text().splitlines() if x]
if not rows:
    raise SystemExit("no evaluation results")

required = {"passed", "policy_violation", "cost_usd", "review_changed_lines", "retries"}
for i, row in enumerate(rows, 1):
    missing = required - row.keys()
    if missing:
        raise ValueError(f"row {i} missing: {sorted(missing)}")

summary = {
    "cases": len(rows),
    "pass_rate": sum(bool(r["passed"]) for r in rows) / len(rows),
    "policy_violation_rate": sum(bool(r["policy_violation"]) for r in rows) / len(rows),
    "avg_cost_usd": mean(float(r["cost_usd"]) for r in rows),
    "avg_review_changed_lines": mean(int(r["review_changed_lines"]) for r in rows),
    "avg_retries": mean(int(r["retries"]) for r in rows),
}
print(json.dumps(summary, indent=2, sort_keys=True))
```

上线门槛应由风险等级决定。例如文档修复可自动开 PR；业务代码需要 Reviewer；认证、支付、数据库迁移和生产 workflow 必须指定 CODEOWNER。指标门槛用本组织基线校准，不照搬供应商宣传数字。

## 十、可观测性与审计

每次 Agent 任务生成不可变 task ID，并关联：

- 触发者、Issue/PR、基线 SHA、候选 SHA；
- 模型/provider、提示模板版本、工具与镜像摘要；
- 每次工具调用的名称、参数摘要、结果状态和耗时；
- token、费用、CI 分钟、重试原因；
- 修改文件、diff 摘要、策略决策和审批人；
- 测试、SAST、SBOM、许可证和制品签名证据；
- 最终状态：拒绝、PR、合并、部署、回滚。

日志不能保存原始 token、Authorization header、完整客户数据或未脱敏模型上下文。审计事件与普通 debug log 分开存储，限制访问并设置保留策略。

建议建立四类告警：

1. Agent 尝试访问受保护路径或未知网络目的地；
2. 单任务成本、重试、文件数或执行时间异常；
3. 特权 workflow 下载来源不明的 artifact 或 checkout 非默认分支 SHA；
4. Agent PR 合并后出现回滚、安全告警或事故关联。

## 十一、落地成熟度

### L0：观察

- 仅在本地或沙箱运行；
- 不自动创建 PR；
- 建立历史任务评测集和失败分类。

### L1：候选 PR

- Agent 自动生成 patch；
- 无特权 CI 验证；
- 人工创建或批准 PR；
- 限制低风险目录与任务类型。

### L2：受控自动开 PR

- 入口标签/派单 allowlist；
- 独立发布 Job 创建候选分支；
- Ruleset、CODEOWNERS、Required Checks 生效；
- 成本、策略和安全证据完整。

### L3：有限自动合并

仅对文档、格式化、确定性依赖更新等低风险类型开放；必须满足稳定评测门槛、双重策略检查、回滚能力和异常自动熔断。任何生产部署仍由合并后工作流和 Environment 审批控制。

## 十二、上线检查清单

### 权限

- [ ] workflow 顶层或每个 Job 显式声明 `permissions`
- [ ] PR 验证仅 `contents: read`
- [ ] Agent Job 无 `contents: write`、无 `id-token: write`
- [ ] 部署用 OIDC，云端 claims 精确限制仓库/ref/environment
- [ ] 分支保护、Ruleset、CODEOWNERS 和 Required Checks 不可被 Agent 绕过

### 执行隔离

- [ ] 不可信代码只在无 secret、无写权限 runner 中执行
- [ ] 禁止在 `pull_request_target` 特权上下文 checkout fork code
- [ ] 特权 workflow 不盲目信任 artifact/cache
- [ ] runner 使用一次性环境，限制网络、CPU、内存、磁盘和时间
- [ ] patch 限制路径、文件数、diff 大小和文件类型

### 质量与安全

- [ ] 编译、单测、集成测试、SAST、依赖审计和 secret scan
- [ ] 测试新增不能替代旧测试，禁止只删除失败断言
- [ ] 生成 SBOM，制品绑定 commit SHA 并可验证来源
- [ ] 高风险目录必须人工 Review
- [ ] 失败默认停止，不允许 Agent 修改治理策略自救

### 运营

- [ ] 每任务成本、重试、耗时、修改范围可观测
- [ ] 有历史任务离线评测和 shadow rollout
- [ ] 有熔断开关、任务队列暂停和制品回滚路径
- [ ] 事故可从部署追溯到 PR、Agent 轨迹和模型配置

## 关联知识

- [[10-知识库/AI模型与Agent/06-工程落地与Vibe_Coding实战/6.3-SWE-Agent端到端闭环开发.md]]
- [[10-知识库/AI模型与Agent/05-评测监控与安全防护/5.1-模型评测基准与Evals驱动开发.md]]
- [[10-知识库/AI模型与Agent/05-评测监控与安全防护/5.3-AI安全护栏与防御机制.md]]
- [[10-知识库/AI模型与Agent/04-智能体架构与工作流设计/4.4-Human-in-the-loop交接机制.md]]
- [[10-知识库/AI模型与Agent/04-智能体架构与工作流设计/4.2-工作流编排模式.md]]

## 更新日志

| 日期 | 更新内容 |
|---|---|
| 2026-04-21 | 初始版本：CI/CD 闭环、Repo Map、上下文管理与风险清单 |
| 2026-08-25 | 按生产安全边界重写：删除未验证榜单与固定模型推荐；新增生成/验证/发布分权架构、`pull_request_target` 风险、最小权限 PR CI、OIDC 部署、patch 策略门禁、企业评测指标、审计与成熟度清单 |

## Sources

1. SWE-agent 官方仓库：https://github.com/SWE-agent/SWE-agent
2. SWE-agent 官方文档：https://docs.swe-agent.com/latest/
3. GitHub Actions Secure use reference：https://docs.github.com/en/actions/reference/security/secure-use
4. GitHub `pull_request_target` 安全指南：https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target
5. GitHub OpenID Connect 概览：https://docs.github.com/en/actions/concepts/security/openid-connect
6. GitHub 在云提供商中配置 OIDC：https://docs.github.com/actions/security-for-github-actions/security-hardening-your-deployments/configuring-openid-connect-in-cloud-providers
